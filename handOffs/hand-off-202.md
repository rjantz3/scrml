# scrmlTS — Session 197 (WRAPPED + PUSHED; trucking engine-spine scoped + teed up)

**Date:** 2026-06-15.
**Previous:** `handOffs/hand-off-201.md` (S196 CLOSE, rotated at this session's OPEN).
**Next-session pickup:** rotate THIS file → `handOffs/hand-off-202.md` at next OPEN.
**Profile:** A — FULL (default; *"read pa.md and start session"*, no profile signal). **Effort: ultracode.**

**WRAP+PUSH DONE (user: "wrap and push, then trucking").** scrmlTS `23fbca78` (wrap(s197); full suite **24,338 pass / 0 fail / 225 skip** + TodoMVC PASS; pre-push within-node rebump for 29/30/31 folded in) + scrml-support `20d4855` — both pushed, coherence **0/0**, trees clean. **NEXT = trucking engine-spine slice** (scoped below; a fresh focused run — NOT rushed at session-tail).

## Session-open state (verified at OPEN)
- **HEAD `441e1645`** (wrap(s196)). Working tree CLEAN. Sync **0/0** vs origin (scrmlTS + scrml-support both clean + in-sync). Inbox EMPTY. Worktrees: ONLY main.
- **Board (live `bun scripts/state.ts` @ 441e1645):** **HIGH 1 · MED 8 · LOW 20 · Nominal 9** (171 @gap tokens). Tests: pre-commit subset **17,088 / 90 skip / 0 fail**. Version v0.7.0.
- **Inventory:** 1007 test files · 877 samples · 65 examples · SPEC.md 32,699 lines.
- **Maps:** 1 commit behind HEAD (watermark `471cbb34`, HEAD `441e1645` — the gap IS the wrap commit itself; not load-bearing-stale). Refresh at this session's wrap (6c) if any source lands.
- **Hooks:** config B (`.git/hooks` with pre-commit + post-commit + pre-push) — correct, leave it.
- Session-start reads done (Profile A): pa.md + PRIMER + SPEC-INDEX + master-list §0 + S196 hand-off + user-voice S191–S196.

## S197 PROGRESS (live — `/effort ultracode`)
**Arc: RemoteData / no-generics scoping → user opened Approach C (loading-sugar) as a deep-dive.**

1. **RemoteData scope-lock DONE.** Doc: `../scrml-support/docs/deep-dives/remotedata-no-generics-scope-2026-06-15.md` (status:current; UNCOMMITTED on disk). Authored by workflow `wf_90dba067-7d8` (11 agents, 5 evidence readers + synthesis + 5 adversarial verifiers) + PA-independent compile probes (/tmp/rd-probe). **Resolution:** the fork mostly dissolves — §13.5 specs a per-screen-enum PATTERN not a built-in; `Loaded(T)` is structurally impossible (no generics); Nominal-7 is factually wrong on 3 axes (retire). Recommendation = **A+D** (per-screen enum canonical + the 4 shipped composition surfaces) · **B vetoed** (god-ify) · **C gated** · retire Nominal-7. One gating discovery item: are the 21 boolean-flag corpus files backlog or standing-signal.
2. **CORRECTION banked (verify-before-claim):** my interim "§13.5 examples are doc-stale" was OVERSTATED — adversarial verifier #5 refuted it against the defining sections (`transitions{}`-in-type = §51.2 Site-1-of-3; qualified variants = legal-everywhere; `on mount` = §6.7.1a canonical). Only Tier-0 `for/lift` is an older (non-deprecating) tier. Any §13.5 refresh is verdict-coupled docs-polish, not a fix-broken-docs imperative.
3. **gap-184 diagnostic-quality FINDING (candidate dispatch, `feedback_dont_soft_classify_bugs`):** the kickstarter §11.1 engine recipe STILL fails verbatim (5 errors — gap accurate), but gap-184's stated root ("type-checker can't see auto-cell from fn scope") is likely a MIS-ATTRIBUTION — the failure leads with `E-STRUCTURAL-ELEMENT-MISPLACED` on a top-level engine (block-splitter mis-tracks the `${}` close → auto-cell never registers → cascade). A hand-written engine with the same sibling-fn writes compiled CLEAN. **Trigger ISOLATED (7 probes):** NOT the Tier-0 for/lift, NOT backtick-SQL, NOT parenthesized payload tags, NOT the bare-assign `onclick=@x = .V` (all ruled out). The **after-`>` `:`-shorthand placement IS implicated** — removing it (→ bare-body) makes the E-STRUCTURAL-ELEMENT-MISPLACED cascade VANISH (replaced by a distinct 4× E-TYPE-063 = a SECOND issue: rule=/bare-variant resolution in the engine-body). The after-`>` form is S160-DEPRECATED (should only warn `W-COLON-SHORTHAND-LEGACY-PLACEMENT`, not break) → the deprecated-`:`-shorthand parser path is broken when combined with engine state-children. **Dispatch = (a) fix/migrate the after-`>` `:`-shorthand path + (b) the residual rule=/bare-variant E-TYPE-063 + (c) modernize the kickstarter §11.1 recipe to inside-opener/bare-body + opener `effect=` (the form that compiles clean, my Probe E) + correct gap-184's mis-attributed root.** Probes: /tmp/rd-probe/ks-*.scrml. The canonical teaching recipe being broken = a real currency bug worth its own dispatch, independent of the RemoteData verdict.
4. **C deep-dive DONE — verdict: DON'T BUILD C** (workflow `wf_a9c65a97-58d`, 11 agents; doc `../scrml-support/docs/deep-dives/remotedata-loading-sugar-2026-06-15.md`, status:current, UNCOMMITTED). **Synonym gate FAILS** (the make-or-break — a sugar generating the per-screen enum = the same artifact §13.5.6 SHALL mandates = textbook §53.14.4 synonym; serialize-STASH-S103 dispositive; verifier sharpened "plausibly"→"cleanly fails"). **Sequencing NOT-READY** (variant-name-canon UNMET + distinct from §51.0.C; fluency 0/17 stale; measure-step unrun). **Axiom BORDERLINE** (bare `loading` keyword = god-ify per S67 CLI cost; only no-keyword transparent form survives, still trips synonym). Deep-dive honored Rule 3 explicitly ("easy answer = ship-the-keyword; rejected"). **Re-open ONLY if** fresh-fluency confirms a standing gap AND variant-name-canon ratifies → then only Candidate-1 (attr-modifier) / Candidate-3 (engine-opener), no new keyword, desugared-enum-visible-in-errors. **No debate needed** (gates converge). **Cite correction (verdict #3):** the isValid-replaces-RemoteData-wrapper statement is **§55.16** (SPEC.md:31641), NOT §52.11 (Error-Codes table) — fixed in BOTH docs. **Fluency twist (verdict #4):** the boolean-flag corpus files POST-DATE the §13.5 SHALL (since 2026-04-10) → genuine standing discoverability signal, NOT pre-SHALL backlog (but synonym gate blocks the sugar regardless) → the right response is the **corpus-rewrite arc + a fresh gauntlet measurement** (§404 measure-step), not the sugar.

5. **RATIFICATION LANDED (user: "Ratify A+D + retire Nominal-7").** Three edits: (a) design-insight S197 RESOLUTION in `../scrml-support/design-insights.md` (A+D canonical · B veto · C don't-build · the 4th same-axis LIMIT ruling); (b) **Nominal-7 retired** in `docs/known-gaps.md` (status nominal→resolved + 3-axis-wrong rationale) → board **Nominal 9→8**; (c) §13.5.6 sugar-door SPEC note (the same-landing ouroboros-closer — annotates the MAY-clause with the don't-build verdict + re-open gates). 6d state-regen ran (`state.ts --write` + `--check` PASS; gap-counts + recent-sessions regenerated). SPEC line-count unchanged (one-bullet→one-longer-bullet) → no SPEC-INDEX shift. **RemoteData arc CLOSED.**

**The fluency-gap → next:** the standing discoverability signal (boolean corpus post-dates the §13.5 SHALL) routes to the **corpus-rewrite arc (priority #2) + a fresh gauntlet measurement** (the §404 measure-step = also the C re-open gate), NOT a sugar. Plus the **gap-184 / kickstarter §11.1 currency-bug dispatch** (isolated this session).

## S197 CORPUS ARC — wave 3 (NEW examples) + trucking (sequenced)
**User picked: corpus arc → "NEW gap-filling examples (G1/G4/G6/G3)" + (added) trucking rewrite "Exemplar + preserve friction findings".** All 8 REWRITE offenders (03/04/05/06/08/09/16/25) are DONE (waves 1+2). Audit: `../scrml-support/docs/deep-dives/example-corpus-idiomatic-audit-2026-06-14.md`.

**Wave 3 — NEW examples DONE (workflow `wf_01e89628-024`; 8 agents draft+adversarial-review → PA compiled+R26+landed). 3 of 4 LANDED, 1 DEFERRED:**
- **`29-engine-vs-flags` (G1) — LANDED** (PA-R26 clean, exit 0; bare-body engine, gap-184-safe by construction; reviewer SHIP). The flags→engine "make impossible states impossible" lesson.
- **`30-validated-form` (G4) — LANDED (PA-fixed)** — reviewer REVISE: bare compound-field render-by-tag → PA nested to `<signup><name/></>` (×5, per 05 + §6.3.5); dead `if=@signup.submitted` confirmation → PA removed `reset(@signup)` so submitted persists + the confirmation works; dropped the contested I-FN-PROMOTABLE comment. Recompiled clean (info-only I-FN-PROMOTABLE on the persist stub).
- **`31-reach-discipline` (G3) — LANDED (PA-fixed, renamed from 32)** — reviewer REVISE: brace-wrapped `onclick={...}` → PA changed to canonical bare assignment `onclick=@scanMode = .V` (§5.2.3, ×4). Recompiled clean. (Renamed 32→31 so the landed set is contiguous 29/30/31.)
- **`31-markup-as-value` (G6) → DEFERRED as future `32`** — BLOCKED on a real codegen gap PA isolated: markup-in-ternary + markup-as-fn-return → `E-CODEGEN-INVALID-JS` (filed **HIGH `g-markup-value-ternary-fnreturn-codegen`**, §S197). The example's whole lesson (branch/return markup) is exactly the broken forms. README/VERIFIED carry 29/30/31 only.

**The codegen gap (NEW HIGH this session, board HIGH 1→2):** PRIMER §6.4(2)/§6.6.17 + kickstarter §6.4 document `${ cond ? <a/> : <b/> }`, `const <x> = cond ? <a/> : <b/>`, and `fn f() -> markup { return <m/> }` as VALID — all three hard-error `E-CODEGEN-INVALID-JS` (isolated via 4 probes /tmp/cw3/p{A,B,C,D}; plain non-ternary markup-typed derived cells D work). Pillar-1 spec-vs-impl drift. Codegen fix needed (markup-value emit for ternary consequent/alternate + fn-return). Also flagged: free-standing `snippet name(){}` + file-scope `${render name()}` emits undefined-fn ReferenceErrors (exit-0-but-broken) — separate check.

**Trucking arc — SCOPED, sequenced AFTER the examples (they're the reference shapes), INCREMENTAL not big-bang:**
- **Precondition SATISFIED:** `examples/23-trucking-dispatch/FRICTION.md` is a standalone P0/P1/P2 friction log — leave it intact (+ a "files rewritten S197+" addendum). The friction data is NOT inline in the .scrml, so the rewrite preserves it automatically.
- **KEY de-risk:** the canonical enums ALREADY EXIST — `schema.scrml` (pure-type file) exports `LoadStatus:enum` / `DriverStatus:enum` / `UserRole` / `AccountStatus` / `TrailerType` / `PaymentTerms` / `LogEntryType`. The bug is they're stored/compared as STRINGS (`status == "in_transit"` if-chains) + the HOS engine is declared-then-bypassed. The rewrite = WIRE the existing enums into engines/matches, not invent types.
- **5 slices, highest-leverage first. Slice 1 SCOPED S197 (read the files; the audit's flat "load-status engine" framing CORRECTED via the S194 engine-2C ruling):**
  - **(1) "Enum-wiring spine" (NOT a flat engine) — wire the EXISTING `schema.scrml` enums (`LoadStatus`/`DriverStatus`/`AccountStatus`/etc., already declared; the bug is they're used as STRINGS):**
    - **load-status = per-load = MULTI-INSTANCE → `match` over `LoadStatus` + a transition-validation `fn`, NOT a singleton `<engine>`** (S194 2C: per-entity state = components+match, not engine). Fire-sites: `components/load-status-badge.scrml` (`statusBadgeClasses`/`statusLabel` string if-chains → `match`), `components/status-picker.scrml` (`validNextStates` — the transition state-machine AS string if-chains; comment literally says "`<engine>` deferred, rules live as a pure helper" → `match`-based `validNextStates(current: LoadStatus) -> LoadStatus[]`), `components/invoice-card.scrml` (invoice-status if-chains → `match`), `pages/customer/loads.scrml` (the `matchesFilter` filter).
    - **driver HOS = per-session-driver = SINGLETON → a genuine `<engine for=DriverStatus>`** in `pages/driver/hos.scrml` (fix the declared-then-bypassed engine; it OWNS transitions). The ONE real engine in the slice.
    - **OPEN string↔enum DB-boundary question:** `loads.status` is a `text` column (§14.8 row type → `string`); using `LoadStatus` in `match` needs the string→enum at the query boundary (cast / typed projection — interacts with the S175 g-sql-row-type arc). Resolve in the slice-1 scoping (cast-at-boundary vs match-on-string-with-variant-names).
    - **gap-184 caution:** the HOS engine MUST use the clean form (inside-opener/bare-body + opener `effect=${}`; NO after-`>` `:`-shorthand).
  - (2) decl-coupled validators on the forms (login/register/quote/load-new/profile) + `@form.isValid` + `<errors>`; (3) `<each>` sweep (17 `lift` files); (4) errors-as-states (17 `@errorMessage` files → failable fn + `!{}` → Phase enum); (5) typed component props (8 `asIs` files → schema enums/structs). Each slice PA-R26-landed, sequenced after the wave-3 examples (the landed reference shapes).

**Uncommitted on disk (no commit/push authorization given this session):** scrml-support — `remotedata-no-generics-scope-2026-06-15.md`, `remotedata-loading-sugar-2026-06-15.md`, `design-insights.md`, `user-voice-scrmlTS.md`; scrmlTS — `compiler/SPEC.md` (§13.5.6 note), `docs/known-gaps.md` + `master-list.md` (state-regen), `hand-off.md`, `handOffs/hand-off-201.md`. Probes /tmp-only. Repo otherwise clean. Workflow scripts at `/tmp/wf-remotedata-scope.js` + `/tmp/wf-loading-sugar-dd.js`; deep-dive raw outputs in the task transcript dir.

## THE NEXT-SESSION PICKUP — priority order (carried from S196 CLOSE)
**1. RemoteData (H3, §13.5) — scope the no-generics tension (the deferred design fork).** scrml has NO generics so `RemoteData<T>` can't exist; the audit + 16-rewrite use a per-screen enum. Real design Q: a built-in `Loading/Loaded(T)/Failed(Error)` (Nominal-7) vs per-screen-enum-is-the-idiom (PRIMER §10 explicitly says per-domain enums beat `AsyncPhase<T>`). Composes with the new render-expr (`Failed(Error)` is a held error variant `<render of=>` displays). Surface as a scoping question — NOT batch-ratify; possible deep-dive per `feedback_no_batch_ratify_foundational_axioms`. A FRESH design question, not part of the (now-complete) render-expr arc.
**2. Corpus wave 3 + the audit's NEW gap-filling examples** (G1–G6) + 23-trucking wholesale — the corpus-rewrite arc continues (waves 1+2 done S195/S196). Audit: `scrml-support/docs/deep-dives/example-corpus-idiomatic-audit-2026-06-14.md`.
**3. Open gaps (non-blocking):** `g-each-body-bare-variant-arg` (HIGH, emit-each.ts — wave-2 used the per-direction-handler workaround; the actual fix is a separate dispatch) · `g-shorthand-interp-engine-element-loci` (MED, render-expr-prereq fallout) · `g-render-not-enum-asis-miss` (LOW).

## Open questions to surface immediately (next session)
- **RemoteData scoping** (above) — the one substantive design fork queued.
- The render-expr `<render of=X/>` is BUILT but only PA-R26-verified + unit-tested — consider a dog-food in a real example (a wave-3 file could adopt it to display a held `.Failed(err)`) for human-visible validation (`feedback_show_visual_work_before_push`).

## PRE-EXISTING observation (flagged, not this session's)
stdlib `http`/`random`/`fs`/`math`/`auth` index.scrml emit "statement boundary not detected — trailing content silently dropped" warnings during the test run (library-mode meta-block). Pre-existing; a future S138-class "silent drop" investigate candidate.

## pa.md directives in force
R1–R5 · `---` delimiter · Profile A (default) · S88 isolation-explicit · F4/S90/S99/S126 path-discipline · S136 BRIEF.md archival · S138 R26 dual-verify (every landing) · S147 coherence check · S164 bg-commit-race · S112 worktree-base-staleness · S180 waiting-time 3-tier · wrap 8-step (incl. 6b/6c/6d).

## Tags
#session-197 #open #remotedata-scoping-queued #corpus-wave3-queued #render-expr-built-s196
