---
from: scrmlTS-PA-machine-A (S99 OPEN)
to: scrmlTS-PA-machine-B (next pickup)
date: 2026-05-17
subject: state-vs-logic scaffold updated on Machine A — pull scrml-support before any voice work
needs: fyi
status: unread
---

# state-vs-logic scaffold — user authored bridge prose on Machine A

User updated `scrml-support/voice/articles/state-vs-logic-axiom-evolution-arc-scaffold-2026-05-17.md` on Machine A this session. Pushed at scrml-support `98e28ce`.

## What changed

+16 lines:

1. **Bridge Q1 — first-person narrative arc** (between Quote 1 and Quote 2). User-authored prose covering: the "v0.2 stable-ish chewing phase" → realization that "the State System should be just that, a SYSTEM. Orthoganally equitable to the logic and type systems" → secondary role for logic → typed the prompt to claude → continued with human tasks (running business, driving trucks, family) → started noticing scrml snippets degrading on screen → "I had to know what was going on" → "turns out the latter [prompt was mis-leading]" → unfinished quote insert tag.
2. **Bridge Q3 — lead-in fragment** "I would " (mid-thought, preserved as-is per voice-author preserve-voice-texture rule).

## Status

User-authored content. Voice-author rules preserve grammar/punctuation/voice-texture; agent does not edit the user's prose. Two `<insert qt>` placeholders are user's own — they'll select the canonical quotes when they finalize the prose.

## Action required from Machine B

None mandatory. FYI so the next voice-author corpus-refresh pass doesn't accidentally collide with the new user prose.

If Machine B's voice-author runs in bio-mode against the corpus next session, the new bridge-Q1 narrative is a candidate verbatim source — but it's user-authored prose, NOT a quote (scaffold is private; user authors final article).

## Tags

#cross-machine #s99 #voice-scaffold-update #state-vs-logic-axiom #user-authored-prose #fyi-only
