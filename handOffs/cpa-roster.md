# cPA roster — who the concierge fronts for

**Created S209 (2026-06-19).** The cPA's client list. cPA-owned (write-before-act). Per the cPA
contract (`../../scrml-support/cpa-scrml.md` Fork D): the roster is **permission-gated** — the cPA
NEVER adds a client PA without explicit user authorization, and routing is a no-op until N>1.

## Clients (project PAs the cPA fronts for)

| PA | repo | boot phrase | status | authorized |
|----|------|-------------|--------|------------|
| scrml-PA | `scrml` | `read pa.md and start session` | active | (founding — S209) |

> **N=1 today** → routing is inert; the cPA is a stow-and-relay buffer for the single scrml-PA.
> When a second project PA is proposed, the user authorizes the add HERE (a new row + the date +
> the authorizing message), and only then does the cPA route between them.

## Add-permission log (append-only)

- _(none yet — the founding scrml-PA row above predates any add)_
