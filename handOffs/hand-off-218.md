# scrml — Session 213 (CLOSE)

**Date:** 2026-06-22. **Profile:** A — FULL ("read pa.md and start session"). **Boot:** cold. A big `<api>`/render-bridge + PongAI-adopter-bugs session — heavy execution.

> **Thinned (S205).** Mechanical state → `bun scripts/state.ts` + `handOffs/digest.md` · `handOffs/delta-log.md` · `handOffs/deputy-state.md`. Irreducible + open below.

## Board / sync (@ close)
- Board after this session: **HIGH 1 · MED ~11 · LOW ~14** (HIGH 1 = the lift-path render gap; render-bridge canonical-forms RESOLVED). Version **0.7.0**. Full suite green at every landing (24851-24857/0). **See the digest / `bun scripts/state.ts` for exact counts** (the deputy regens §0 — a NEW HIGH `g-request-lift-nested-interp-mangle` was added; the main render-bridge gap is partially-resolved).
- **DEPUTY ACTIVE** all session (ticks 168-175+); it batch-deferred maps "fire on W5 land" — **W5 landed this session → fire the maps refresh at/after wrap.** deputy-maint merged at the mid-session push (S205 gate held: 0/0 + deputy^main==0).
- **PUSH:** mid-session push landed up to `9a4950f3` (origin). The render-bridge merge + C1/C2 fixes + the W5-items/banner commit are the wrap-push (run merge-before-push gate again — the deputy ticked since).

## ✅ DONE this session — `<api>` arc completed + 2 PongAI bugs (all the big stuff landed)
1. **g-block-match-in-lift** → NEW targeted §34 `E-MATCH-BLOCK-IN-LIFT` (replaces the misleading E-COMPONENT-035 cascade; detect-at-VP-2 on a residual `markup`-tag-`match` node). Pushed.
2. **`<api>` in-`<program>` DEEP-WALK (HIGH silent ship-blocker)** — found by PA `<api>` dog-food AND independently by the A2-W5 agent (cross-validated). Both `checkApiDeclarations` (typer) + `buildApiEndpointRegistry` (codegen) scanned top-level-only; `<api>` nested in `<program>` (every real app) silently wired NOTHING (no E-API-* checks, no fetch, no parseVariant — compiled green). Fixed: both deep-walk into `<program>`. W5's form (`apiDeclNodes` via classifyMarkupNodes) landed. Pushed.
3. **PongAI C1** — `animationFrame` builtin missing from `LOGIC_SCOPE_GLOBAL_ALLOWLIST` (type-system.ts) → spurious E-SCOPE-001; the compiler's OWN fixture was red. Added it. Pushed.
4. **PongAI C2** — `new URL(import.meta.url).pathname` → Windows drive-doubling `C:\C:\` → `scrml:` stdlib unresolvable. Fixed all **3 sites** (module-resolver.js + commands/compile.js + validators/lint-async-user-source.ts) → `fileURLToPath`. Linux-verified. Pushed.
5. **`<request>` `<#id>` render-bridge (HIGH)** — THE big one. `<request>` (url= AND api=) fetched+decoded into `_scrml_request_<id>` but `<#id>` markup refs read an unpopulated `_scrml_input_state_registry` → undefined/silent-static-shell; `if=<#id>.X`→E-SCOPE-001; `<match on=<#id>.data>`→E-CODEGEN-INVALID-JS. Found via PA `<api>` dog-food + W5 deferred-#1. Verified, filed, dispatched, FIXED (agent a7ebad43, 13 source files): `_scrml_request_<id>` now `_scrml_deep_reactive`; `<#id>` refs route to it across interp/if=/match-on=; bindings `_scrml_effect`-wrapped. ALSO killed A2 W4's dead `_scrml_notify` (emitted 4× but never in the runtime — latent ReferenceError on every fetch). Landed via **3-way merge** onto deep-walk+C1 (disjoint; R26 dual-verified; one stale W5 test-assertion reconciled). +20 tests. **4 canonical render forms work end-to-end** (both modes).
6. **A2 §60 Nominal→Implemented + examples/32-external-api.scrml + B-docs byob guide** — landed at wrap (user "banner now"; render bridge wired). §60 banner notes the render bridge as a separate `<request>`-surface item.

## ⚠️ OPEN / READY — next session (fully scoped)
- **PongAI C3/C4/C5 — ALL ROOT-CAUSED (3 real codegen/type-system fixes; execute on fresh context, PA-direct or one cluster dispatch):**
  - **C3 (HIGH, silent):** `emit-expr.ts:1726` unconditionally hijacks `render(...)` → the `_scrml_render` builtin, so a user `function render` called from a fn body mis-encodes (def `_scrml_render_4` vs call `_scrml_render` → ReferenceError). **Fix:** mirror the **`log` shadowing** precedent (emit-expr.ts:1747-1758 + `_logShadowedInFile`/`setLogShadowing` + `checkLogShadowing` in type-system.ts + log-loc.ts): yield to a user `function render`, emit its §47-encoded call, fire NEW info-lint `W-RENDER-SHADOWED` (§34, Rule 4). (`reset` is the hard-reserved alternative — `log` is the right model.)
  - **C4 (MED, silent):** `@cell == Type::Variant` (payload variant) emits `_scrml_structural_eq(@cell, P.B)` where `P.B` is the constructor FUNCTION → always-false, NO lint (confirmed). **Fix:** type-system/lint — fire on `==`/`!=` vs a payload-variant constructor → steer to `is .Variant` / `match`.
  - **C5 (MED):** `Outer::Variant(.Inner)` types `.Inner` against the OUTER enum → E-TYPE-063 (confirmed). **Fix:** contextual typing — the ctor-arg position supplies the param's type (same rule that makes `<x>: T = .V` work). Affects match-bound payloads too.
- **giti + 6nz PA modernization (USER ADD S213):** make giti and 6nz PAs **totally up to date** — at least **6nz is still on kickstarter V1** (→ V2, `docs/articles/llm-kickstarter-v2-2026-05-04.md`); both sibling pa.md to current practices (wrap/coherence/R26/deputy/etc.). Cross-repo (PA writes via the inbox dropbox, OR the S209-style "update their pa.md" with explicit user authorization). The S209 giti/6nz pa.md modernization was partial (LOCAL+UNPUSHED in the siblings per the S212 carry) — verify + complete + the kickstarter V1→V2.
- **render-bridge lift-path forms (agent-deferred, file as gaps):** **D1 (MED) `g-request-lift-nested-interp-mangle`** — `lift <h1>${<#id>.data}</h1>` nested in a markup-lift block mangles the inner interp (emit-lift requestIds-threading gap + content-split corruption). **D2 (LOW)** bare `${ if (<#id>.loading) { lift } }` reads the §36 registry not `_scrml_request_<id>` (same emit-lift gap). Separate lift-path dispatch.
- **SPEC §6.7.7 Worked Example 1 STALE** (agent-flagged) — uses bare `if(){lift}` directly in `<div>`, now invalid (`E-CONTROL-FLOW-IN-MARKUP`, S204). Migrate to `${}`-wrapped when D1/D2 land.
- **scrml-bug-report.md** — PongAI consolidated report, dropped by the user in the **project ROOT** (unusual). C1+C2 fixed; C3-C5 scoped above. Decide its placement (handOffs/ or docs/) — leave for now.

- **NEW flogence bug (UNREAD in `handOffs/incoming/`, arrived 2026-06-22 — triage next session):** `g-on-mount-bare-call-render-slot` — a bare CALL statement in `on mount { f() }` is mis-collected as a render interpolation → emits `<span data-scrml-logic>` + `_scrml_render_value(el, f())`, printing the call's return as a text node; an ASYNC fn → visible **"[object Promise]"** at page top. Repro: `on mount { val() }` (any bare call; pure-assignment body emits 0 slots; a trailing assignment does NOT suppress it; both default-logic + `${}` modes). Root (flogence guess): the on-mount block's expression-statement hits the "render the block's value" path; lifecycle-hook bodies should run as effects, never be render-collected. Common pattern (load-on-mount) → MED-leaning-HIGH. flogence workaround: sync fire-and-forget wrapper. R26-triage + fix next session (the collector — likely `emit-html`/`emit-event-wiring` on-mount statement collection).

## OPEN — carried from prior sessions (unchanged)
- A4/stdlib Phase 3 ruling · flogence raw-route (dpa-002) · external-backend follow-ons · g-tier1-ssr-prerender · 6nz AA (match-value-discard) · ss12 (selfhost-mirror-parity, LOW post-v1.0).

## Anomalies recovered (reasoning)
- **Deep-walk bug found TWICE independently** (PA dog-food + W5 agent) — strong cross-validation; W5's form landed, mine reverted.
- **W5 + render-bridge agents BOTH based at session-start `ca712295` (S112 worktree-base-staleness), NOT the dispatch HEAD** — so both diverged from this session's landed commits on shared files (emit-reactive-wiring.ts, type-system.ts). Both landed via 3-way (W5 via the deep-walk file-delta; render-bridge via `git merge --no-ff` — clean, disjoint regions). The render-bridge merge surfaced **one stale W5 test-assertion** (api-decl-codegen.test.js:132 asserted the pre-render-bridge non-reactive shape) — updated to the new `_scrml_deep_reactive({...})` form (not a regression; the test predated the fix). **Lesson for next dispatch: brief a `git merge origin/main` startup step (S112) so agents work off current HEAD.**
- **zsh does NOT word-split unquoted `$VAR`** — a multi-file `git checkout $FILES` failed (whole string as one pathspec). Use explicit file lists or `${=VAR}`. (Bit me once on the render-bridge file-delta.)
- **Deputy partition held** — I filed the @gap source tokens; the deputy regen'd §0/digest on its branch; merged clean at the push (S205 gate). The deputy batched maps for "W5 land."

## pa.md directives in force
R1–R5 · `---` delimiter · Profile A · digest-first · S88 isolation · S99/S126 path-discipline · **S112 worktree-base-staleness (brief `git merge origin/main` startup — bit us 2× this session)** · S136 BRIEF.md · S138 R26 (forward+reverse, used heavily) · S147 coherence · S164 bg-commit-race · S199/S205 deputy + merge-before-push · S119 explicit-pathspec · S140 file-delta-vs-cherry-pick + **3-way merge for stale-base agent landings (S211/S212)** · wrap 8-step.

## Tags
#session-213 #close #profile-a #api-arc-complete #render-bridge-landed #deep-walk-fix #pongai-c1-c2-fixed #pongai-c3-c4-c5-rootcaused #banner-implemented #giti-6nz-modernize-next #s112-worktree-staleness-2x
