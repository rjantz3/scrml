# domain.map.md
# project: scrmlts
# updated: 2026-06-21  commit: 8569f774

The domain is the scrml COMPILER pipeline. scrml is a single-file, full-stack reactive web
language compiled by this TypeScript/JS toolchain running on Bun. The compiler converts `.scrml`
source files into `*.server.js` + `*.client.js` + `*.html` + `*.css` outputs.

## Core Concepts

| Concept | Definition |
|---------|-----------|
| `.scrml` file | Single-file source combining markup, logic, styles, SQL, auth, types, and tests |
| Pipeline | 12 ordered stages: BS → TAB → NR → MOD → CE → PA → RI → TS → META → VSS → DG → CG |
| BS (Block Splitter) | Stage 1: tokenizes `.scrml` into typed blocks (markup/logic/sql/css/etc.) — `block-splitter.js` |
| TAB (Tokenizer+AST Builder) | Stage 2: builds FileAST from block stream — `tokenizer.ts` + `ast-builder.js`; the `each-block`/`match-block` structural transform lives HERE (in `buildAST`) for the live pipeline — AND, as of S162, is now MIRRORED in the native parser (`synthEachBlockNode`/`synthMatchBlockNode` in parse-file.js) so `--parser=scrml-native` promotes both too |
| NR (Name Resolver) | Stage 3: resolves reactive decls, engine vars, component refs — `name-resolver.ts` |
| MOD (Module Resolver) | Stage 3.1: builds import graph, detects circular imports, produces export registry — `module-resolver.js`; S173: `E-EXPORT-001` (reactive state-cell export reject) fires here in `buildImportGraph`. (Protect-analyzer PA stage S175: `F-SCHEMA-001` adds `<schema>` DDL as a 3rd ColumnDef source via `extractSchemaCreateTableStatements`.) |
| CE (Component Expander) | Stage 3.2: expands component references via same-file + cross-file registries — `component-expander.ts`; re-parses synthesized component bodies, falling back to legacy `splitBlocks`+`buildAST` when the body contains `<each>`/`<match>` (S153); **S201 `normalizeTokenizedRaw` (~:560) collapses tokenized member-access spacing `obj . field`→`obj.field` so a component-body nested-component prop member-arg `<Badge s=row.name/>` survives re-tokenization (g-nested-component-member-arg-misparse)** |
| PA (Pre-Analysis) | Stage 4: structural validation, attribute allowlists — `attribute-registry.js` + validators/ |
| RI (Route Inference) | Stage 5: infers server routes from page structure — `route-inference.ts`; supplies the server/client side classification (§12.4) that the `log()` builtin reads for its origin tag |
| TS (Type System) | Stage 6: type checking, validity surface synthesis, engine type verification — `type-system.ts` (~19200L; +990 in S175); hosts S173 W-TYPE-FN-FIELD (S175: ESCALATED to `E-STRUCT-FUNCTION-FIELD` hard reject) + S174 `W-LOG-SHADOWED`/`E-TYPE-ANY-FORBIDDEN` + **S175 typed-SQL-row flow: `resolveSqlRowType`/`resolveTableView` (read-site typing), `checkSqlRowWidthSubtype`/`checkPropContract`/`checkSqlRowAgainstCellContract` (width-subtyping → `E-SQL-ROW-CONTRACT-MISMATCH`), `resolveIterableRowElement`/`checkRowFieldAccessInExpr` (typed loop-var, E-TYPE-004), `inferReturnTypeFromBody` (fn-return over-approx)** decl-site/read-site scans |
| META (Meta Check+Eval) | Stage 6.5: validates phase separation, evaluates `^{}` compile-time blocks — `meta-checker.ts` + `meta-eval.ts` |
| VSS (Validity Surface Synthesis) | Stage 6.7: synthesizes `@x.isValid` / `@x.errors` / `@x.touched` / `@x.submitted` accessor cells |
| DG (Dependency Graph) | Stage 7: builds reactive dependency DAG, detects cycles — `dependency-graph.ts` |
| CG (Code Generator) | Stage 8: emits server.js + client.js + html + css from IR — `code-generator.js` + codegen/ |
| FileAST | Compiler's internal AST representation for one .scrml file — `types/ast.ts` (**2073L** at S201; NEW `MarkupValueExpr` in the ExprNode union) |
| CGError | Structured diagnostic: code + message + span + severity — `codegen/errors.ts` |
| V5-strict | Access model: `@x` is read, `@x = v` is write; compiler tracks every read/write site |
| reactive-decl | A V5-strict reactive variable (`@name`): server-side cell with compile-time dependency tracking |
| engine | State machine declared in scrml (`<engine>`/`EngineDeclNode`); Tier 2 abstraction over reactive cells |
| engine state-child | A state arm of an engine declared as a markup child; closer-pairing handled by the custom raw-text parser `engine-statechild-parser.ts` (NOT block-splitter) |
| `:`-shorthand child (§4.14) | `<tag : expr>` form (e.g. `<li : @.name>`); correctly excluded from engine-arm closer-depth via `isColonShorthandOpener` (S153) — a `:`-shorthand opener has no closer, so it must not be pushed onto `lowerDepth` (else it absorbs the state-child `</>` → E-ENGINE-STATE-CHILD-MISSING) |
| `:`-shorthand HTML content-model (§4.14 / S159) | For a NON-VOID lowercase HTML element, `<span : @label>` renders as `<span>${@label}</span>` — byte-identical to the bare-body form. ast-builder.js synthesizes the body child via re-parse of `<tag>BODY</tag>`. For a VOID HTML element (`<input>`, `<br>`, SVG geometry), `:`-shorthand is REJECTED with `E-COLON-SHORTHAND-ON-VOID`. Component `:`-shorthand and `@.`-sigil per-item `:`-shorthand are EXCLUDED from synthesis (separate owners). Resolves the prior empty-`<span></span>` emission gap + `E-DG-002` false-fire for cells consumed via `:`-shorthand. |
| `log()` builtin — location-transparent logging (§20.6, S174) | (`916b8bb3`): a location-transparent debugging builtin. `log(...args)` lowers to `_scrml_log(side, loc, ...args)` where `side` ("server"/"client") is the COMPILER-CERTAIN call-site classification (from `ctx.mode`, per-statement-accurate including inside CPS-split functions) and `loc` ("basename:line") is resolved at COMPILE time from the call node's byte offset against the file source (`log-loc.ts resolveLogLoc`; the node's `span.line` is unreliable post-re-parse, the byte offset is). Origin tag format `[server|client] <args> (file:line)` (§20.6.2). Args are rendered **value-faithfully** via `_scrml_log_render` (§20.6.4 — NOT host `JSON.stringify`; value cycles are forbidden by construction §59.5 so the render terminates). **Dev unified view (§20.6.3, F2=B):** server `log()` prints to the `scrml dev` terminal; client `log()` POSTs its tagged payload to a `POST /_scrml/log` endpoint on the dev server (dev.js ~L459) AND keeps the browser console — terminal-as-the-single-view. **Production strip (§20.6.5, F4=A):** the `--production`/`--prod` compile flag lowers `log(...)` → `(void 0)` (0 bytes of log infrastructure, no `_scrml_log` reference, no arg-eval residue; mirrors the test-bind clean-strip precedent §19.12.7). The `'log'` runtime chunk (`SERVER_LOG_HELPER` prepended in emit-server.ts; client chunk in runtime-chunks.ts) is gated on the emitted JS ACTUALLY containing a `_scrml_log(` call — a shadowed or stripped build omits the chunk entirely. v1 is `log()` only (no `.info`/`.warn`/`.error` levels — §20.6.6). |
| `log()` shadowing — `W-LOG-SHADOWED` (§20.6.7, S174) | A user-declared in-scope binding named `log` (the canonical no-op debugging stub `function log(...)`, or any local/import named `log`) WINS over the builtin: the builtin steps aside, `log(...)` is emitted as an ordinary call to the user function (no origin tag, no dev forwarding). `log` is NOT a reserved identifier (declaring `function log` is legal — this is a lint, not `E-RESERVED-IDENTIFIER`). Info-level `W-LOG-SHADOWED` fires at the shadowing DECLARATION (`checkLogShadowing` in type-system.ts:3620, wired type-pass:15920) — codegen's `EmitExprContext.errors` is not reliably populated, so the lint lives in the type pass; emit-expr.ts only suppresses the builtin lowering (`_logShadowedInFile` set via `fileDeclaresLog(fileAST)` at index.ts:587/760). Partitions into `result.warnings` (non-fatal). Reserved for promotion to `E-LOG-SHADOWED` end-of-window. |
| `any`-reject — `E-TYPE-ANY-FORBIDDEN` (§14.1.1, S174 hard line) | (`f0b3cb04`): the literal type-token `any` is rejected in EVERY type-annotation position (struct/error field type, state-cell `typeAnnotation`, `fn`/`function` param + return-type). `any` is not a scrml type — "there is no any" (S174 user hard line; TypeScript's type-checking opt-out has no scrml equivalent). The sanctioned untyped escape hatch is `asIs` — a deliberate, named, greppable opt-out (analogous to TS `unknown`, NOT `any`). Today the `any` token falls through `resolveTypeExpr`'s unresolvable path to `asIs`/`unknown` with NO diagnostic, silently masquerading as `asIs`; this check catches the LITERAL `any` atom BEFORE that collapse. `any`-token-SPECIFIC: `typeTextMentionsAnyToken` (type-system.ts:3678) splits the raw type string on non-identifier chars and tests for a bare `any` atom — catches `any`/`any[]`/`any | not`/`[string: any]`/`{ payload: any }`, does NOT mis-fire on a NAME merely containing the substring (`Company`, `manyThings`, a param literally named `any`). An arbitrary undefined type name (`Frobnicate`) that ALSO resolves silently to `asIs` is a SEPARATE, broader leak (deferred follow-on). `checkAnyTypeForbidden` (type-system.ts:3720, wired type-pass:15924), span-bearing decl-site scan; mirrors `checkFunctionTypedStructFields`/`checkMapKeyComparability` shape. Fatal. |
| engine opener `effect=` | §51.0.H Form 3 boot-only opener effect; fires once at engine init; emitted by `emitEngineOpenerEffect()` in emit-engine.ts (S148 C1) |
| `accepts=MsgType` (§51.0.S.2.2) | Engine-opener attribute naming the message-vocabulary `:enum`; parsed batch 1 (engine-statechild-parser.ts, S154); resolved batch 2 (symbol-table.ts SYM PASS 11, S155); codegen batch 3 (emit-engine.ts `emitEngineMessageArmTable`, S155) |
| message-arm (§51.0.S) | Per-state `| .Variant(bindings) :> body` arms inside an engine state-child; parsed to `MessageArmEntry[]` by batch 1; two-plane dispatch: state-transitions via `_scrml_engine_advance`, message-dispatch via `_scrml_engine_dispatch_message` (runtime-template.js S155) |
| engine-graph sidecar | Static "what-comes-next" JSON artifact per engine; written to `<base>.engine-graph.json` via `--emit-engine-graph`; produced by `buildEngineGraphJson()` in `engine-graph.ts` (S149) |
| EngineGraph | Exported type from engine-graph.ts: `{ engines: EngineGraphEngine[] }`; honest-empty `{ engines: [] }` when no engines |
| errorBoundary | Markup-context error catch (§19.6): typed `!`-error path + host-JS try/catch backstop; implemented in `emit-error-boundary.ts`; the backstop routes caught diagnostics to scrml's logging surface (the adopter-callable backing for which is the `log()` builtin §20.6, §19.6.8 B5) |
| `lin` (linear type) | Value that must be consumed exactly once; enforced by compiler across all branches — `LinDeclNode` |
| `~` (tilde-decl) | Deferred-init mutable slot; must be initialized before read — `TildeDeclNode` |
| channel | Server-push WebSocket channel declared in markup; `<channel name="X">` — `ChannelDeclNode` |
| SSE (§37) | Server-Sent Events; client-stub wiring via `EventSource` — `emit-client.ts` GITI-026; the §38 SSE channel is the cheap path for the deferred server→browser log-mirror leg (§20.6.3 F2=C north-star) |
| `_scrml_modules` registry | §21.3 cross-file CLIENT module-loading (known-gaps #6, S152); idempotent global object in runtime-template.js; exporter appends `_scrml_modules[key] = { ... }` footer; importer reads via `const { x } = _scrml_modules[key]`; key derived by `moduleRegistryKey()` from absolute path + outputBaseDir |
| source-map provenance | Real per-line `.js.map` produced by build-source-map.ts + srcmap-provenance.ts; emit fns inject `#scrmlmap#` sentinel marks; buildSourceMap() resolves them to use-site spans and strips marks before output (S149 B2; S150 line-lie close) |
| Shape 4 typed-array default | §6.2 Shape 4 (S152): `<name>: T[]` with no RHS defaults to `[]`; non-array typed decl with no RHS → E-DECL-NEEDS-INITIALIZER. (Open design Q parked S153: scalar/struct zero-default — should `<x>: int`→`0`, `<x>: string`→`""`?) |
| COW-all bracket-write (cycles-prereq, S168) | (`8d9db4e1`): a bracket-index WRITE `@arr[i] = x` now routes through copy-on-write — `reactive-nested-assign` → `_scrml_deep_set(_scrml_reactive_get(target), [<path>], value)` — instead of a raw in-place index assignment. Bracket READS (`@y = @arr[i]`) stay verbatim. The path is a HETEROGENEOUS segment list: a literal index `[0]`/`["DAL"]` rides a STRING segment; a computed index `@arr[@sel]=x` becomes a COMPUTED segment `{ index: ExprNode }` whose index expression emit-logic emits INLINE into the JS array-literal path. Collected by the shared `collectAtPathSegments()` in ast-builder.js. The point: value cycles are forbidden and made-acyclic-true — `_scrml_deep_set`'s clone-then-set snapshots any self-reference into a stale (acyclic) value, so even `@arr[0] = @arr` produces no live cycle. This is the build prerequisite for value-native maps (§59). |
| structural-eq cycle guard (S168) | (`8d9db4e1`): `_scrml_structural_eq(a, b, seen)` in runtime-template.js (~L2491) carries a `WeakMap<a-object, WeakSet<b-object>>` `seen`-set that short-circuits a re-visited `(a,b)` object pair to `true`, so structural `==` on a (now made-acyclic) self-referential value terminates instead of stack-overflowing. |
| value-native map (§59, IMPLEMENTED S169) | (`fbb3c208`→`40679720`): a `[KeyT:ValT]` map is reassignment-canonical / COW (a write returns a new map: `@m = @m.insert(k, v)`), mirroring arrays. Reads are bracket (`@m[k]` → `V | not`); writes are method (`E-MAP-BRACKET-WRITE` on `@m[k] = v`). Keys identified by structural `==` (key type must be §45-comparable). Maps are order-independent-comparable; `@ordered` postfix affix opts into iteration order. **IMPLEMENTED end-to-end on the default pipeline + native-parser literal parity** (banner flipped Nominal→Implemented at `8963ae52`). Type recognition `MapType`/`tMap`/`resolveTypeExpr` `[K:V]` via `findMapEntryColon` + `@ordered` affix + key-comparability (`isComparableType`/`classifyMapKey`/`checkMapKeyComparability`) in type-system.ts. Literal `MapLitExpr`/`MapEntry` (types/ast.ts) parsed by legacy `preprocessMapLiterals` (expression-parser.ts) + native `parseArrayLiteral` map fork (native-parser/parse-expr.js + ast-expr.js + translate-expr.js). Runtime: tagged `{__scrml_map}` structure + 14-method surface + `_scrml_fnv1a`/`_scrml_value_canonical` (§59.5) hasher + §57-envelope codec + order-independent `map` case in `_scrml_structural_eq` (runtime-template.js; `'map'` chunk in runtime-chunks.ts). Codegen lowering (`collectMapVarNames`/`fileHasMapUsage` in reactive-deps.ts; `mapVarNames` threaded through `EmitExprContext`; map-lit/bracket-read/method/`.size` in emit-expr.ts). `<each in=@m.entries() as (k, v)>` destructure sugar (emit-each.ts/ast-builder.js/parse-file.js). The cycles-prereq above (`8d9db4e1`) is its enabling groundwork. |
| set-algebra helpers (`scrml:data`, S170 — type DEFERRED) | (`df08f282`): S170 ratified "defer the set TYPE, ship the helpers" (warrant confirmed THIN — zero adopter demand; deep-dive `set-warrant-and-shape-2026-06-06.md`). NO `set` type, NO new syntax — pure `scrml:data` transforms over plain arrays returning value-DISTINCT plain arrays: `union`/`intersection`/`difference` + value-safe `member`, each with an optional `keyOrFn` mirroring `unique`'s signature. **Value-correct** — they reuse the §59.5 value-canonical codec, replicated standalone as `_data_value_canonical` in the `scrml:data` runtime shim (`compiler/runtime/stdlib/data.js` ~L147 — bundles where the runtime-template global is absent; cross-checked byte-identical to `_scrml_value_canonical` over 21 value cases incl. reversed-field structs / enums / nested maps) so the helpers agree with `==` (§45) and the §59 map key — `Array.includes` + JS `Set` are reference-keyed (WRONG for structs/enums). **`unique` struct-unsafety FIX:** its no-key path was `[...new Set(array)]` (JS-reference dedup, value-broken for structs); now dedups by the value-canonical key (keyed path preserved). `.scrml` mirror `stdlib/data/transform.scrml` in S115 lockstep + extended self-test; `stdlib/data/index.scrml` exports +4. SPEC §59.12 set bullet reconciled (type deferred; B2 `set[K]`-over-map on the shelf). PRIMER §10 catalog + Set-idioms block. NO normative §41 home (plain transforms are PRIMER-only). |
| structural-compound deep-set retarget (Bug B, S170 — CLOSED) | (`72aa6836`): `@a.ref = v` on a Variant-C structural compound (`<a><ref>=""</>`, where `a` lowers to a `_scrml_derived_declare` composite reading the leaf `a.ref`) previously emitted `_scrml_reactive_set("a", _scrml_deep_set(get("a"), ["ref"], v))` — the derived recompute then silently clobbered the write (lost mutation, NO diagnostic, default pipeline, failed even for a SINGLE write). Distinct root from Bug A (S167 `75431e9e` multi-statement deep-set write-loss — that was a PARSER boundary bug; Bug B is a CODEGEN mistarget). **Fix:** `reactive-deps.ts:stampCompoundDeepSetTargets` (~L739, run once-per-file at `runCG` via index.ts:740, mirrors `collectSynthCellKeys`) stamps the `reactive-nested-assign` node with `_deepSetLeafKey` (deepest statically-resolvable backing leaf cell along the path, ~L764) + `_deepSetResidualPath` (segments past that leaf, ~L765); `emit-logic.ts` (~L3025-3035) honors the stamp — single-segment → `_scrml_reactive_set("a.ref", v)`; residual/computed → COW deep-set into the LEAF cell. FLAT object cells UNCHANGED. SPEC §6.3.2. Closes the last open HIGH (Bug A S167 + Bug B S170 → known-gaps HIGH 1→0). |
| reactive state-cell export reject (`E-EXPORT-001`, §21.2, S173) | (`642950a2`): exporting a plain (Shape-1) OR derived reactive STATE CELL is a fatal MOD-stage error; previously the export was swallowed SILENTLY (no export in emitted JS; cross-file import → garbage). Keyed on the `kind:"state-decl"` binding (NOT name-case), so `export const Greeting` (component-as-const), `export <channel>`, and exported engines stay legal. SHARED check — both pipelines feed `file.ast.exports`. `collectStateCellNames`/`exportedLocalNames`/`buildImportGraph` [module-resolver.js:97/136/295-313]. Fix-it: export a function returning the value, a `const` of its current value, or a wrapping component. |
| function-boundary rule — `E-STRUCT-FUNCTION-FIELD` (§14.3 / §15.11, S175 hard line; ESCALATED from S173 `W-TYPE-FN-FIELD`) | (`9e6156c4`): a struct field declared with a function type (`onClick: () -> void`, `cb: fn()`) is HARD-REJECTED at declaration. The limit-the-primitive axiom (§14.1.1): a function is NOT value data — no structural equality (§45.2), not serializable, not a map key (§59.4) — so it must never be STORED on a value-shaped collection. This is the STORED face of the named passed-vs-stored rule (§15.11.5.1): a function may be PASSED (a prop, `W-COMPONENT-001`) or CALLED (handler) but never STORED (struct field / state cell). The field type now resolves to a distinguishable `FunctionType` via `tFunction()` (NOT opaque `asIs`) so the reject fires precisely; lifecycle annotations `(A to B)`/`(A -> B)` are NOT function types (never reject). S173's `W-TYPE-FN-FIELD` Info-nudge is RETIRED. `checkFunctionTypedStructFields` [type-system.ts:3497] via `isFunctionTypeAnnotation` [type-system.ts:2087], wired at the type pass [type-system.ts:15917]. Fatal. |
| typed SQL projection rows — the flagship typed-data delivery (§14.8.7/§14.8.8, S175) | (Tranche 1 `45bea7c5` / Tranche 2 `1dbf67b4` / Tranche 3 `95c25b67`): end-to-end SQL-row type-flow. A `?{ SELECT ... }` query resolves to a typed projection-row struct (`resolveSqlRowType` joins `sql-projection.ts extractSelectProjection` against the generated table types). **Read-site (T2a):** a projection-row binding (loop var over a `Row[]`, or unwrapped `Row | not`) is typed as the row struct — `r.<projected-col>` resolves to the column type, `r.<unknown>` → E-TYPE-004 (`resolveIterableRowElement`/`checkRowFieldAccessInExpr`). **Prop-contract (T2b):** a row passed to a component's `:struct` prop is STRUCTURALLY width-subtyped INTO the contract (every contract field must be present + assignable; EXTRA columns OK; one-directional) — a miss is `E-SQL-ROW-CONTRACT-MISMATCH` (`checkPropContract`, fed by `__propContractChecks` stamped by component-expander.ts; delegates to `checkSqlRowWidthSubtype`). **Cell-contract (T3b):** same width-check INTO a `:struct`-typed state cell (`checkSqlRowAgainstCellContract`). **Fn-return (T3c):** a server-fn body returning a row over-approximates via `inferReturnTypeFromBody` stamping the `<fn-return>` sentinel — those inferred types are EXEMPT from the reject. Boundedness IS the design — width-subtyping applies ONLY projection-row → declared `:struct` (general struct assignment stays NOMINAL §14.8.1). The deferred long tail (`*` / CTE / UNION / subquery-in-FROM / unknown col) degrades to `asIs` + `W-SQL-ROW-UNTYPED` (per-column where possible). The generated table types remain WALLED inside the `<db>` block — this carves a single bounded exception (§14.8.8). |
| `<schema>` DDL as a 3rd ColumnDef source — `F-SCHEMA-001` (§39/§14.8, S175) | (`45bea7c5`): protect-analyzer's `extractSchemaCreateTableStatements` synthesizes CREATE TABLE DDL from `<schema>` blocks, feeding the typed-SQL-row generated-table resolution as a THIRD ColumnDef source after (1) the live DB file and (2) the schema-differ (`generateCreateTable` now exported from schema-differ.js). |
| unrecognized-type-name reject — `E-TYPE-UNKNOWN-NAME` (§14.1.2, S176) | (`46cffc83`): closes the BROAD unknown-type-name leak the S174 `any`-reject explicitly deferred. Every type-NAME leaf in EVERY type-annotation position must resolve to a built-in / a same-file decl (incl. forward ref) / an import / the `asIs` escape hatch; an unrecognized name (typo'd / undefined PascalCase like `Frobnicate` / `LaodCardRow`) now RED-fires instead of silently collapsing to `asIs`. SYMMETRIC to the `any`-reject — both drive `forEachTypeAnnotationLocus` (identical loci); the difference is the per-leaf predicate (position-aware `forEachTypeNameLeaf`, `emitMapKeys:false`). REGISTRY-DEPENDENT — wired AFTER the imported-types seed (vs the registry-free any-check). Exempt: import-specifier names (single-file-mode guard) + machine names (`@state: M`, in the machineRegistry). Out of v1 scope: db-block generated types, native `.scrml` mirrors, type-as-argument idents. `checkUnknownTypeNames`/`isUnrecognizedTypeNameAtom` [type-system.ts:4232/3726]. Fatal. |
| `pure`-modifier deprecation — `W-PURE-DEPRECATED` (§33, S176) | (`4a19a047`): the `pure` modifier is DEPRECATED language-wide; the canonical pure form is `fn`. Fires info-level on ANY `pure`-modified declaration (SUPERSEDES the narrower `W-PURE-REDUNDANT` `pure fn`-redundancy nudge), gated on `isPure` at the fn-decl walk [type-system.ts:7597]. Auto-fixed by `scrml migrate` **Migration 3** [commands/migrate.js:197]: `[server ]pure[ server ](function\|fn) NAME(` → `[server ]fn NAME(` (declaration-shape regex so prose is untouched; idempotent). Not yet a hard reject. |
| scalar-vocabulary stdlib + capability classes (`scrml:math` / `scrml:time.now()` / `scrml:random`, §41.18-41.20, S176) | (DD1 Fork 1, `beb8a115` math+time / `35172d78` random): closes the scalar-vocabulary gap. **`scrml:math`** (§41.18, PURE) — the centralized sanctioned `Math.*` touch: `round`/`floor`/`ceil`/`abs`/`min`/`max`/`clamp`/`parseInt`/`parseFloat`/`toNumber`/`isNaN`; usable inside a pure `fn` (deterministic only — `Math.random` is deliberately NOT here). **`scrml:time.now()`** (§41.19) + **`scrml:random` `random()`/`randomInt()`** (§41.20) are CAPABILITY-SCOPED NON-DETERMINISTIC (class-C IO) — each is the ONE sanctioned host touch point (`Date.now()` / `Math.random()` respectively). Calling any of them inside a pure `fn` body is **E-FN-004** (`function`/`server function` bodies are not pure → legal). E-FN-004 was GENERALIZED this session to fire on these IMPORTED bindings, not just hard-coded host member-expressions — registry-driven (`NONDET_STDLIB` [type-system.ts:6589], `collectNonDetStdlibBindings` :6598) so the next non-det primitive extends with one registry row. http's retry-jitter was de-leaked through `scrml:random` (the one place http reads host entropy); `bundleStdlibForRun` now recursively copies that transitive sibling-shim into `_scrml/` at runtime. Each `stdlib/<m>/index.scrml` mirrors a `compiler/runtime/stdlib/<m>.js` shim. |
| `<each>` cell-init order | (S152 HIGH): `<each>` body render fn runs synchronously at module-init BEFORE same-file cell `_scrml_reactive_set`; guard `if (!_items)` added; `_scrml_effect_static` re-runs after cell-init fires |
| `<each>` dep-first read | (S153): the render fn reads `_items` (the dependency) BEFORE the `if (!_mount) return;` early-return so `_scrml_effect_static`'s one-shot dep pass records the dep even when the mount is absent at module-init (the engine-gated case) |
| `_scrml_each_renderers` / `_scrml_remount_each` | (S153): runtime registry + remount helper in runtime-template.js. An `<each>` whose mount lives in a non-`initial=` engine arm is absent from the DOM at module-init; its render fn registers itself keyed `each_${id}`; when the arm later mounts, the variant-swap dispatcher (emit-variant-guard.ts) calls `_scrml_remount_each(armRoot)` which `querySelectorAll('[data-scrml-each-mount]')` and re-invokes each registered renderer directly (no new dep edge). Idempotent across engine re-entry |
| each-in-block-form-match | (S153): each-bearing `<match>` arms re-parse via `splitBlocks`+`buildAST` (native parser leaves a generic `markup tag="each"`); lifted each-blocks attach to `matchBlock.bodyChildren`; `restampEachBlockIds` namespaces ids; `__scrmlCachedArms` memoizes across passes — emit-match.ts |
| markup-expansion-in-arms (g-formfor, S177) | (`75f724af`): the markup-EXPANSION passes (CE `walkAndExpand`, type-system formFor/tableFor `walkAndSplice`) descended into `.children`/`.body` but NOT an engine-decl's `bodyChildren` (state-children) nor a match-block's per-arm body — so a `<formFor>`/`<tableFor>`/`<Component>` inside an `<engine>` state-child or `<match>` arm was emitted RAW (silent non-render; empty `onsubmit=${}` formFor → invalid JS). ast-builder builds a walkable `match-block.armBodyChildren` (re-parsed from `armsRaw`, `_matchArmBodyForm` tag) parallel to the raw `armsRaw` codegen path; `walkAndSplice`/`walkAndExpand` recurse into BOTH `bodyChildren` (r27-c6) + `armBodyChildren` (g-formfor-in-match-arm); emit-match `buildMatchArms` consumes the expanded wrapper for formFor/component arms. LIVE-only (within-node STRIP_KEYS += both keys). |
| each-over-enclosing-scope | (S153): nested `<each>` (the `as` pattern) emits inline in the outer factory via shared `emitEachReconcileLines` (was lifted to module-scope reading undefined enclosing var → ReferenceError); each-in-component-body fixed via the 3 component-expander roots |
| `<each>` engine-ctx threading (Bug 62) | (S156): `buildEachEngineCtx(fileAST)` in emit-each.ts collects per-file engine metadata once (engines with message arms, message-variant sets) and threads `EachEngineCtx` through every per-item template attr/child lowering so `@engine.advance(.X)` / `@engine = .X` inside `<each>` templates lower correctly to the state or message plane. Bug 65 (S157) is the SAME fix applied at the Tier-0 `${for…lift}` path in emit-lift.js — CLOSED (see below). |
| `${for…lift}` engine-ctx threading (Bug 65) | (S157 — CLOSED): `buildLiftEngineCtx(fileAST)` + `buildLiftEngineCtxFromExtras(extras)` in emit-lift.js delegate to `buildEachEngineCtx` (emit-each.ts) so both tiers share one implementation. Engine extras are threaded via emit-logic opts into `emitForStmt` → `emitConsolidatedLift` / `emitCreateElementFromMarkup`. Also: `tryLowerLiftEngineHandler` delegates to `emitEngineHandlerBody` (emit-each) — no duplicated `.advance` logic. |
| per-item content reactivity (Bug 64 / R28-1c) | (S158 — CLOSED): `_scrml_reconcile_list` reuses DOM nodes for same-key items; per-item TEXT + class: bindings that close over the create-time iter var showed STALE content on array-replace/reorder. Fix: `EachReconcileCtx` stack in emit-each.ts + `_scrml_lift_reconcile_ctx_stack` in emit-lift.js + control-flow.ts push of ctx inside `createFn`. `maybeWrapEachPerItemEffect`/`maybeWrapLiftPerItemEffect` wrap per-item bindings in a live-keyed `_scrml_effect` that calls `_scrml_resolve_item(mount, keyVar)` to get the CURRENT item for the node's create-time key. `_scrml_resolve_item` in runtime-template.js tracks `(container, "_scrml_items")` so `_scrml_trigger(container, "_scrml_items")` (fired by `_scrml_reconcile_list` on each pass after first) re-fires these effects; field reads through `_scrml_deep_reactive` proxy subscribe to field-level mutations. |
| per-item EVENT HANDLER live-keying (Bug 73) | (S159 — CLOSED): Sibling-gap #2 of Bug 64. Per-item event handlers in BOTH tiers closed over the CREATE-TIME iter var. On same-key reconcile the display binding (Bug 64) showed live data but the handler still fired against the stale create-time snapshot. Fix is DISTINCT from Bug 64: handlers do NOT subscribe (`_scrml_effect`); the fix prepends a fire-time re-resolution prelude inside the existing `function(event) { ... }` body. Tier-1 (emit-each.ts): `iterScopeReferencedInHandler` token-scan gates the wrap (string/regex literals blanked); `maybeWrapEachPerItemHandler` prepends `let <iterVar> = _scrml_resolve_item(<mount>, <keyVar>); if (<iterVar> === null) return;`. Tier-0 (emit-lift.js): `maybeWrapLiftPerItemHandler` (function-body shape) + `maybeWrapLiftCallableHandler` (callable-direct shape — inlines the arrow inside a wrapper whose `let` lexically shadows the arrow's free reference). Global handlers and literal-only bodies stay byte-identical. |
| nested `<each>` in Tier-0 lift (Bug 72) | (S158 — CLOSED): a `<each>` child of lifted markup arrives at codegen as a generic `markup` node (ast-builder's `parseLiftTag` never promotes to `each-block`). Pre-fix: rendered as literal `<each>` DOM tag + inner `@.` leaked raw → E-CODEGEN-INVALID-JS. Fix: `tryEmitNestedLiftEach` in emit-lift.js routes through `emitNestedEachFromMarkup` (emit-each.ts). Also: `_parseLiftAttrValue` in ast-builder.js now handles `PUNCT "@"` token by collecting the balanced `@...` token run as an `{kind:"expr"}` value, keeping the lift on the structured markup path. |
| g-each-component-body-invalid-js (component-with-helper inlining) | (S200 — STEP1 `60ace8b4` + STEP2 A+B `ecba9fee`): a consumer importing a component from module M inlines the component body, whose helper calls resolve to M's NON-component exports — but the consumer bound only the component name, so helper refs were unbound (hard E-SCOPE-001 on the `<each>` per-item path; silent runtime ReferenceError on the Tier-0 `${for…lift}` path — latent in every shipped component-with-helper incl. the trucking board). **STEP 1** (direct imports): CE's import-enrichment seed loop adds M's non-component (helper) exports to the existing import's bindings (`exportIsUserComponent` gate; `alreadyImported`-guarded) so the inlined body resolves in BOTH the TS symbol table AND the codegen `_scrml_modules[key]` destructure. **STEP 2-A** (transitive): a component whose body renders ANOTHER imported component-with-helper reaches the inner module TRANSITIVELY — CE's enrichment BFS synthesizes a consumer import + matching `importGraph` edge for the transitive module's non-component exports (injected into an existing import-bearing logic block for TS scope-walk AND `ast.imports`/`importGraph` for codegen `_scrml_modules`); transitive module is already runtime-loaded via the outer component's import. **STEP 2-B** (nested expression-prop): `buildPropExprMap`/`substitutePropsInExprNode` now substitute EXPRESSION-valued nested props (via `parseExprToNode` per arg + IdentExpr-replacement walk, not raw text) so a component body passing `prop=${expr}` to an inner component substitutes correctly. |
| `<each>` per-item §42 predicate + `if=` conditional (g-each-peritem-if-predicate) | (S200 C1+C2 `39bd061f` — RESOLVED): **C1** — a per-item expression carrying a §42 absence predicate (`is some`/`is not`/`not`) leaked as invalid JS (`String((x is some))`) because text-based `rewriteIterValueExpr` lowered iter-scope + `@cell` but NOT predicates. New `lowerEachExpr(text, iterVar)` helper (emit-each.ts:594) pre-rewrites iter-scope, then — only when a predicate token is present — routes the text through the STRUCTURED emitter (`parseExprToNode` → `emitExprField`, emit-predicates.ts path) which lowers `is some`→`(v !== null && v !== undefined)` etc.; falls back to the text path on parse failure / no-predicate (common case, no parse round-trip). Used by per-item `${…}` interp, attr interpolation, `if=` cond. **C2** — a per-item element `if=` attribute now gates `fragmentVar.appendChild(elVar)` behind the lowered predicate (renderTemplateChildToJs:314-351): `if` is consumed as a conditional (not a `setAttribute`), `ifCond = lowerEachExpr(raw, iterVar)`, and the append becomes `if (ifCond) frag.appendChild(el)`; the each render-fn re-runs on collection change so the conditional re-evaluates per render. |
| markup-as-value (Pillar 1, §1.4/§7.4) in EXPRESSION position (g-markup-value-ternary-fnreturn-codegen) | (S201 `268a27c5`→`2b4ea4d8`→`fa2edccf` — RESOLVED): markup appearing as a VALUE in expression position — a ternary arm (`${ @n > 0 ? <span>p</span> : <span>n</span> }`, PRIMER §6.4(2); or a derived-cell ternary `const <badge> = @n > 0 ? <span>p</span> : <span>n</span>`, PRIMER §6.6.17), or a `return <markup>` value (`fn name(...) -> markup { return <span>${n}</span> }`, PRIMER §6.4(4)). Pre-fix acorn couldn't parse `<span>` so a markup-bearing expr degraded to an escape-hatch emitted verbatim (raw `< span >`) or the legacy block-splitter split the ternary arms into separate blocks (arms DROPPED) → E-CODEGEN-INVALID-JS. **End-to-end**: NEW `MarkupValueExpr` ExprNode (types/ast.ts); ast-builder `parseExprWithMarkupValues` recovers markup spans to `markup-value` leaves (placeholder-skeleton + re-tokenize `lift <markup>`) + `sawTernaryAtRoot` ternary-arm-survival latch + `return <markup>` hook; block-splitter `scanShape12DeclEnd` full-RHS markup scan + value-terminator discriminator; emit-lift NEW `emitMarkupValueExpr` markup→DOM-node IIFE primitive, dispatched from emit-expr `case "markup-value"` + emit-logic `return <markup>`; emit-event-wiring routes `${}` display through the NEW node-aware `_scrml_render_value(el,v)` runtime helper; emit-client pulls the `derived` chunk for markup-typed derived cells (tree-shake gap). Runtime: `_scrml_render_value` (`instanceof Node`→`replaceChildren`, else `textContent`). |
| the each-inline Class-A arc — inline-component prop substitution + LAYER-2 lift + arm-payload `<each>` binding (g-each-inline-component-prop-member-unsubstituted + g-inlined-component-root-class-interp-raw + g-each-over-arm-payload-binding-unbound) | (S202 `d830ec59`→`60d547e1` — RESOLVED; completes the S193 trucking-board flagship, board HIGH 1→0): the codegen arc that made the board `<each>` / for-lift columns render. **(1) CE markup-attr prop substitution** (`d830ec59`, component-expander.ts): NEW `substituteInterpSegments` (~:1163) substitutes prop refs appearing as LEADING identifiers INSIDE a string-literal markup-attr's `${...}` segments (`${load.id}` member-base, `${cls(status)}` call-arg) via `substitutePropsInRawExpr`, leaving literal class text untouched; the inlined-component root `class=` merge runs AFTER substitution so an interpolated root class emits the substituted expr not raw `${...}` (g-inlined-component-root-class-interp-raw). **(2) LAYER-2 lift** (`d830ec59`, emit-lift.js ~:1081): the `val.kind === "string-literal"` markup-attr branch lowers a string-literal attr value carrying `${expr}` interpolation segments (distinct from the LAYER-1 bare-expression attr value). **(3) arm-payload `<each>` binding** (`60d547e1`, emit-each.ts): a TOP-LEVEL `<each in=BARE>` whose iterable is an enclosing match-/engine-arm's PAYLOAD binding (`.Loaded(rows)` → `<each in=rows>`) lost its arm association after arm-body flattening → the top-level no-arg render fn emitted `const _items = rows;` UNBOUND → ReferenceError at mount. NEW `armPayloadBinding {cellName, variantTag, fieldName}` field on EachBlockAstNode + exported `stampArmPayloadEaches(body, cellName, variantTag, payloadBindings, payloadFieldNames)` (:242) walk-stamps only a top-level bare-binding each (nested/`@cell`/`.filter()` left alone); emitEachBodyRenderForFile (:2022) then resolves the iterable from `_scrml_reactive_get(cell).data[field]` (gated on variant===variantTag, else `[]`). ONE shared mechanism for both match (`buildMatchArms`/emit-match.ts:920) + engine (`buildEngineArms`/emit-engine.ts:2459) arms — emitted shape identical. **Flagship** (`a0f93c92`): examples/23-trucking-dispatch/pages/dispatch/board.scrml converted from Tier-0 `for/lift` filter blocks to Tier-1 `<each>` over NEW derived filtered cells (`const <incomingLoads>/<activeLoads>/<closedLoads> = @loadRows.filter(...)`). |
| render-by-tag nested compound (Bug 60) | (S157 — CLOSED): `<signupForm><userName/></>` — self-tag `<userName/>` failed lookup because bare `lookupStateCell` can't see it; it lives under the compound parent namespace. Fix: `enclosingCompoundStack: string[]` in emit-html.ts tracks the active compound wrapper tag; fallback `lookupQualifiedStateCell(fileScope, [enclosing, tag])` resolves the nested field and emits the render-by-tag expansion. Dependency-graph.ts structural-read credit added for render-by-tag tag names (E-DG-002 false-positive cleared). |
| `given` guard | §42.2.3 presence guard: `given ident [, ident]* => { body }`; produces `kind: "given-guard"` AST node; standalone form `:>` ratified S148 as Insight-33 extension |
| formFor | Type-driven form generation from struct definition (§41.14) — `emit-form-for.ts`. **S177 g-formfor:** a `<formFor>` placed inside an `<engine>` state-child (`bodyChildren`) or a `<match>` arm (`armBodyChildren`) now EXPANDS — the type-system formFor `walkAndSplice` walker (type-system.ts:15159) + CE `walkAndExpand` recurse into both arrays (r27-c6 + g-formfor-in-match-arm); was emitted RAW = silent non-render. |
| schemaFor | Type-driven schema emission (§41 family) — `emit-schema-for.ts`; S156 (d)-A batch 3 adds enum-subset `CHECK IN` lowering (§41.15.6 + §41.15.8a): a `Role oneOf([.Admin,.Editor])` field column emits `CHECK (col IN ('Admin','Editor'))` in declaration order |
| tableFor | Type-driven table rendering (§41 family) — `emit-table-for.ts`. **S177 g-formfor:** the tableFor `walkAndSplice` walker (type-system.ts:16395) gained the SAME `bodyChildren`/`armBodyChildren` two-array recursion so a `<tableFor>` inside an engine state-child / match arm expands. |
| native-parser | scrml-native replacement for BS+TAB; `compiler/native-parser/` (38 `.js` files + FEATURE-stale `.scrml` mirrors); STRICTLY OPT-IN `--parser=scrml-native` (default output UNCHANGED, fully green / 0-fail). The ACTIVE strategic line: drive to default → delete legacy BS+Acorn at M6 (direction-a, ratified S161; v0.8 target). **S162: the S153 "does NOT promote each/match" HARD precondition is CLOSED.** **S164-S165: §51.0.S message-arm, exprNode population, F2-match, promote-each, R1 typed-`@cell`, server-fn-star LANDED → flip 674→451.** **S170 re-measured 605 native-only flip-failures on df08f282 + landed fix-wave-1 (`5a346faa`: `on mount`/`on dismount` desugar, `const @name` derived-decl [F5], deepset/array-mutation node-synth, destructured-param structuring, var-decl typeAnnotation thread, exprText-backfill walker) + fix-wave-2 (`cc69c62d`: BlockStub `verbatim` match-arm/lambda body recovery — the Mario fix, +17) → ~508 native-only flip-failures.** Remaining flip-failures are a reduced family set — see "Native-Parser Swap Orientation" below |
| library mode | Compile mode that emits ES module exports JS + server JS without HTML/runtime (SPEC §12.6); `emit-library.ts`; suppresses `.server.js` for body-content-escalated fns |
| arm separator `:>` | Canonical match / `!{}`-handler / `given`-guard arm separator (SPEC §18.2 / §34, S147-S148); `=>` and `->` are deprecated aliases; all three parse, build, and emit identically during the deprecation window |
| W-MATCH-ARROW-LEGACY | Info-level diagnostic emitted at every match arm, `!{}`-handler arm, OR `<engine ... derived=match @VAR { ... }>` engine-decl arm using a deprecated `=>` or `->` separator; suggests `bun scrml migrate --fix` for AST-driven rewrite. **S172 (ratified S171): the §51.0.J derived-engine `derived=match` locus joined the deprecation** — its body is captured as RAW TEXT (no structured arm nodes), so ast-builder.js `scanInlineMatchArmArrows()` (~L1726) stamps `engine-decl.inlineMatchArmArrows[]` (`{glyph, srcOffset}` per arm); the lint fires from type-system.ts (~L7931) and `migrate --fix` consumes the same stamp (migrate.js ~L268) |
| per-file watcher | `commands/dev.js` (S152): Bun `fs.watch` per-file (not recursive-dir) to avoid inotify exhaustion; degrades gracefully on ENOSPC limit. S174: hosts the `POST /_scrml/log` client-log forwarding endpoint (§20.6.3) for the dev unified-view |
| enum-subset refinement (§53.15.1) | `Role oneOf([.Admin,.Editor])` / `Role notIn([.Guest])` type annotation form (S156 (d)-A); parsed by shared `parseEnumSubsetAnnotation()` in `enum-subset-refinement.ts`; materialized as `PredicatedType` with `subsetVariants: Set<string>` (already complemented for `notIn`) in type-system.ts; three consumers: (1) match exhaustiveness narrowed to subset variants (§18.8.1 / §18.0.1); (2) `["A","B"].includes(v)` boundary check codegen in emit-predicates.ts; (3) `CHECK IN` subset in schemaFor emit-schema-for.ts. Range form `.A .. .B` is explicitly forbidden (reintroduces SPARK RPP02 union-evolution hazard) |
| `E-MATCH-SUBSET-DEAD-ARM` | Dead arm diagnostic (S156 (d)-A): a `<match>` arm names a variant outside the declared subset — it can never be reached. Emitted by both type-system.ts (full type-resolution pass) and symbol-table.ts PASS 20 (string-based exhaustiveness pass via shared recognizer) |
| `_scrml_engine_dispatch_message` | Runtime helper (S155, runtime-template.js): resolves `(state × message)` dispatch table; calls per-state arm fn (receives `stateData, msgData`); routes result to `_scrml_engine_advance`; implements §51.0.R handled-message idle-reset |
| `_scrml_reconcile_list` key→item map | (S158): `container._scrml_item_by_key: Map<key, item>` built on EVERY reconcile pass (including fast-path B2 same-order bail); `_scrml_trigger(container, "_scrml_items")` re-fires per-item effects after map rebuild. This is the runtime side of Bug 64 / R28-1c. |
| `_scrml_resolve_item(container, key)` | (S158, runtime-template.js): reads `container._scrml_item_by_key`, tracks `(container, "_scrml_items")` via `_scrml_track`, returns live item wrapped in `_scrml_deep_reactive` (null when key gone — canonical absence §42.5). Used by live-keyed per-item effects emitted by `maybeWrapEachPerItemEffect` / `maybeWrapLiftPerItemEffect`; AND by per-item event handler re-resolution (Bug 73) emitted by `maybeWrapEachPerItemHandler` / `maybeWrapLiftPerItemHandler`. |
| match-as-expression exhaustiveness (S157) | `const x = match @cell { ... }` (let/const/return positions) now hooks into the exhaustiveness pass: ast-builder.js dual-parse inserts a structural `matchExpr` side-field; type-system.ts `checkMatchDiagnostics` runs on it (E-TYPE-020). Also: derived `const <x> = match @cell { ... }` reactive cell (Bug 71). |
| E-SYNTAX-064 / redundant-CODEGEN suppression | (S157 + S159 R3): `@.` outside an `<each>` body scope now fires E-SYNTAX-064 (both the attr-walk site in the TS pass and the markup-attr-value walk site, S157; additionally `:`-shorthand body positions, S159 R3) instead of leaking to E-CODEGEN-INVALID-JS. api.js suppresses E-CODEGEN-INVALID-JS when compilation already has a prior fatal error (Bug 70). |

## Business Invariants (from SPEC + code)
- `null` and `undefined` do NOT exist in scrml source; both → `not` (SPEC §42; `W-ABSENCE-IN-SCRML-SOURCE`)
- `any` does NOT exist in scrml — the literal type-token `any` in ANY type position is `E-TYPE-ANY-FORBIDDEN` (§14.1.1, S174 hard line). The sanctioned untyped escape hatch is the named, greppable `asIs`. An arbitrary undefined type NAME that also resolves silently to `asIs` is now `E-TYPE-UNKNOWN-NAME` (§14.1.2, S176) — the symmetric companion check over the identical loci; the S174 deferred follow-on is CLOSED
- Client JS MUST NOT contain SQL execution calls, server env access, or other server-only constructs (E-CG-006)
- `<auth role="X">` gates JS-mount only, NOT served HTML content (W-AUTH-CONTENT-NOT-GATED, GITI-027A)
- Every reactive write site must be in a logic context (E-WRITE-NOT-IN-LOGIC-CONTEXT)
- A reactive STATE CELL (plain Shape-1 or derived) may NOT be exported — `E-EXPORT-001` (§21.2, S173); a cell holds per-instance runtime state with no cross-file meaning. Components-as-const, `export <channel>`, and exported engines stay legal (keyed on `kind:"state-decl"`)
- A function is NOT value data — it may be PASSED (a prop, `W-COMPONENT-001`) or CALLED (handler), but NEVER STORED on a value-shaped collection. Storing one on a struct field is `E-STRUCT-FUNCTION-FIELD` (§14.3/§15.11, S175 hard line; the STORED face of the named passed-vs-stored rule §15.11.5.1; limit-the-primitive axiom §14.1.1). Resolves to a distinguishable `FunctionType` (not opaque `asIs`) so the reject is precise; lifecycle annotations `(A to B)` are NOT function types
- Every TYPE-NAME in a type-annotation position must be DEFINED — a built-in, a same-file decl (incl. forward ref), an import, or the `asIs` escape hatch; an unrecognized name is `E-TYPE-UNKNOWN-NAME` (§14.1.2, S176). Symmetric to the `any`-reject (same loci, registry-dependent so wired after the imported-types seed)
- NON-DETERMINISM is capability-scoped: the wall clock (`scrml:time.now()`, §41.19) and the random source (`scrml:random` `random()`/`randomInt()`, §41.20) are class-C IO; calling either inside a pure `fn` body is `E-FN-004` (`function`/`server function` are not pure → legal). `scrml:math` (§41.18) is PURE (deterministic `Math.*` only — `Math.random` is NOT in it). The E-FN-004 gate is registry-driven over imported non-det bindings (`NONDET_STDLIB`), not just hard-coded host members
- The `pure` modifier is DEPRECATED language-wide (§33, S176, `W-PURE-DEPRECATED`); the canonical pure form is `fn`. `scrml migrate` Migration 3 auto-rewrites `pure`-modified declarations to `fn`
- Generated table types are NOMINAL and WALLED inside the `<db>` block (§14.8.1/§14.8.4). S175 carves ONE bounded exception (§14.8.8): a SQL projection row STRUCTURALLY width-subtypes into a developer-declared `:struct` prop/cell contract (one-directional: contract is a lower bound, EXTRA row columns OK). A miss is `E-SQL-ROW-CONTRACT-MISMATCH`. General struct-to-struct assignment stays nominal — a plain user struct coinciding with a row's columns is NOT width-subtype-assignable. Untyped long-tail SELECT shapes degrade to `asIs` + `W-SQL-ROW-UNTYPED`
- `log()` is a location-transparent builtin (§20.6, S174): the side ("server"/"client") is COMPILER-CERTAIN per call site (§12.4 placement), NOT runtime-detected; a user-declared `log` WINS (`W-LOG-SHADOWED` info-level nudge, `log` is not a reserved identifier); `--production`/`--prod` strips ALL `log()` to 0 bytes (no `_scrml_log` reference, no arg-eval residue)
- `lin`-typed values must be consumed exactly once across all code paths
- `async`/`await` are forbidden in scrml source (E-ASYNC-NOT-IN-SCRML, E-AWAIT-NOT-IN-SCRML); CPS is the canonical async surface
- `switch`/`try`/`throw` are forbidden scrml vocabulary (E-SWITCH-FORBIDDEN, E-THROW-NOT-IN-SCRML, E-TRY-NOT-IN-SCRML)
- Engine state-children are canonical state-machine representations; nested engines are permitted
- Match / `!{}`-handler / `given`-guard arm separator is `:>`; `=>` / `->` are deprecated aliases — new code SHALL use `:>` (SPEC §18.2 / §34)
- Typed-array decl with no RHS defaults to `[]` (§6.2 Shape 4); non-array typed decl with no RHS is E-DECL-NEEDS-INITIALIZER
- An `<each>`/`<match>` body render must remain reachable from the file's runtime-chunk walk even when the mount lives only inside an engine/match arm `bodyChildren` (S153 — else the reconcile/effect chunks are tree-shaken → ReferenceError)
- The each render's reactive dependency must be recorded at module-init regardless of mount presence (read `_items` before the `!_mount` early-return) so a later arm-mount re-render has a live subscription (S153)
- An `<each>` template's event handlers that contain `.advance(.X)` or `@engine = .X` MUST go through the engine-ctx lowering path (`emitEngineHandlerBody`) in emit-each.ts — direct lowering without ctx produces stale `undefined`-call JS (Bug 62 root cause; Bug 65 was the same gap in emit-lift.js — CLOSED S157)
- `oneOf`/`notIn` enum-subset refinements: range form `.A .. .B` is forbidden (§53.15.1); the argument must be an explicit enumerated set; `notIn` is complemented to positive IN-SET at resolution time (both loci read the same positive set)
- Per-item TEXT / class: / attr bindings in both Tier-1 `<each>` and Tier-0 `${for…lift}` MUST be wrapped in a live-keyed `_scrml_effect` via `maybeWrapEachPerItemEffect` / `maybeWrapLiftPerItemEffect` so same-key reconcile (array-replace, reorder) does not show stale content (Bug 64 / R28-1c — CLOSED S158)
- Per-item EVENT HANDLERS in both tiers that READ the iter var MUST go through the live-keyed prelude (`maybeWrapEachPerItemHandler` / `maybeWrapLiftPerItemHandler`) so same-key reconcile fires with live item data, not the create-time snapshot (Bug 73 — CLOSED S159). Handlers do NOT use `_scrml_effect`. The wrap is gated by `iterScopeReferencedInHandler` — global handlers (no iter-var read) stay byte-identical.
- `_scrml_resolve_item(container, key)` returns `null` (NOT `undefined`) when the key is gone — canonical compiled-output absence is `null` per SPEC §42.5; the W-CG-UNDEFINED-INTERPOLATION lint forbids `undefined` in emitted JS
- E-CODEGEN-INVALID-JS is SUPPRESSED when the compilation already has a prior fatal error (Bug 70); the gate's contract is "emits valid JS for VALID source" — firing it on top of an earlier error is misleading
- A void HTML element (`isVoid:true` in the §24 registry) REJECTS `:`-shorthand body with `E-COLON-SHORTHAND-ON-VOID` (§4.14 / S159); a non-void HTML element's `:`-shorthand body is synthesized as its single-expression body at ast-builder.js `buildBlock` time — byte-identical to `<tag>${expr}</tag>` (§4.14)

## Domain Events / Diagnostic Codes (key runtime lifecycle)
W-AUTH-CONTENT-NOT-GATED — emitted when `<auth role>` is used without content gating (GITI-027A)
W-MATCH-ARROW-LEGACY — emitted (info-level) at every match / `!{}`-handler arm AND `derived=match` engine-decl arm using deprecated `=>` or `->` separator (S147 + S172 derived-engine locus, SPEC §18.2 / §34 / §51.0.J)
W-EACH-PROMOTABLE — emitted (info-level) at `${ for (let x of @cell) { lift ... } }` sites eligible for `<each>` promotion (S130 HU-1, Stage 6.4c)
W-EACH-KEY-001 — emitted (info-level) at `<each in=@cell>` sites where items have no inferable `.id` key (S130 HU-1, Stage 6.4d)
W-LOG-SHADOWED — (S174, Info, §20.6.7) emitted at a user-declared `log` declaration that shadows the location-transparent `log()` builtin [type-system.ts `checkLogShadowing` :3620]
E-TYPE-ANY-FORBIDDEN — (S174, Error, §14.1.1) literal type-token `any` in a type-annotation position; `any` is not a scrml type [type-system.ts `checkAnyTypeForbidden` :3720]
E-EXPORT-001 — (S173, Error, §21.2) a plain/derived reactive state cell is named in an `export` clause [module-resolver.js `buildImportGraph` :301]
E-STRUCT-FUNCTION-FIELD — (S175, Error, §14.3/§15.11; ESCALATED from the retired S173 W-TYPE-FN-FIELD Info) a struct field is declared with a function type — HARD REJECT (a function is not value data) [type-system.ts `checkFunctionTypedStructFields` :3497]
E-SQL-ROW-CONTRACT-MISMATCH — (S175, Error, §14.8.8) a SQL projection row fails to width-subtype into a declared `:struct` prop/cell contract [type-system.ts `checkPropContract` :9491 / `checkSqlRowAgainstCellContract` :9629 → `checkSqlRowWidthSubtype` :704]
W-SQL-ROW-UNTYPED — (S175, Info, §14.8.7) a `?{ SELECT ... }` row (or column) degrades to `asIs` — deferred long tail (`*`/CTE/UNION/subquery/unknown col) [type-system.ts `resolveSqlRowType` :5676]
E-DECL-NEEDS-INITIALIZER — emitted at non-array typed-decl with no RHS (S152 §6.2 Shape 4)
E-ENGINE-STATE-CHILD-MISSING — engine state-child closer un-findable; S153 fixed the `:`-shorthand-child false-fire class
E-ENGINE-ACCEPTS-NOT-ENUM — (S155) `<engine for=T>` declares `accepts=MsgType` but `MsgType` is absent or non-`:enum` [symbol-table.ts SYM PASS 11]
E-ENGINE-MSG-WITHOUT-ACCEPTS — (S155) a state-child declares a message arm (`| .Variant :>`) but the engine has no `accepts=` [symbol-table.ts PASS 20]
E-ENGINE-MSG-ARM-NOT-EXHAUSTIVE — (S155) a state declares message arms but the set does not cover all `accepts=` enum variants and has no wildcard arm [symbol-table.ts PASS 20]
E-ENGINE-MSG-UNKNOWN — (S155) `.advance(.X)` targets a variant that is in NEITHER the state-transition plane NOR the message-dispatch plane [type-system.ts]
E-MATCH-SUBSET-DEAD-ARM — (S156 (d)-A batch 2+4) a match arm names a variant excluded by the matched cell's `oneOf`/`notIn` subset refinement — it is unreachable [type-system.ts + symbol-table.ts PASS 20]
E-COLON-SHORTHAND-ON-VOID — (S159 NEW) `:`-shorthand body on a void HTML element; void elements have no content model [type-system.ts:5004]. Fatal.
E-SYNTAX-064 — (S157 Bug 70/73; S159 R3 extended) `@.` contextual sigil used outside an `<each>` body scope; fired at the TS attr-walk site, the markup-attr-value walk site, AND (S159) `:`-shorthand body positions [type-system.ts:6643 + 7434 + 5016+]
I-MATCH-PROMOTABLE — info diagnostic suggesting match → engine promotion (§56)
I-FN-PROMOTABLE — info diagnostic suggesting function promotion
W-ENGINE-SERVER-DEFERRED — (S209 ss2, Warning/Info, §51.0.A) `<engine for=T server>` (bare flag, NO `=@source`) is recognized but non-operational; fires from symbol-table.ts `validateEngineStateChildrenAndRules` Step 2.5b; routes to result.warnings (severity:"warning"); steers adopter to the wired `server=@source` (§51.0.E) form. Non-fatal.

## Pipeline Source Files (stage → primary file)

| Stage | File |
|-------|------|
| BS | compiler/src/block-splitter.js |
| TAB | compiler/src/tokenizer.ts + compiler/src/ast-builder.js |
| NR | compiler/src/name-resolver.ts |
| MOD | compiler/src/module-resolver.js (S173: `E-EXPORT-001` reactive state-cell export reject in `buildImportGraph`) |
| PA | compiler/src/protect-analyzer.ts (S175 F-SCHEMA-001: `extractSchemaCreateTableStatements` :471 adds `<schema>` DDL as a 3rd ColumnDef source for typed-SQL-row table resolution) · compiler/src/sql-projection.ts (NEW S175, 419L: `extractSelectProjection` SELECT-projection extractor) · compiler/src/schema-differ.js (`generateCreateTable` :366 now exported) |
| CE | compiler/src/component-expander.ts |
| PA | compiler/src/gauntlet-phase1-checks.js + validators/ |
| RI | compiler/src/route-inference.ts (supplies the §12.4 server/client side classification that `log()` reads) |
| TS | compiler/src/type-system.ts (~19200L; PASS-level + decl-site/read-site scans: `checkFunctionTypedStructFields` S173/S175 [now `E-STRUCT-FUNCTION-FIELD`], `checkLogShadowing` + `checkAnyTypeForbidden` S174, `resolveSqlRowType`/`checkSqlRowWidthSubtype`/`checkPropContract`/`checkSqlRowAgainstCellContract`/`resolveIterableRowElement`/`checkRowFieldAccessInExpr`/`inferReturnTypeFromBody` S175 typed-SQL-row) |
| META | compiler/src/meta-checker.ts + compiler/src/meta-eval.ts |
| DG | compiler/src/dependency-graph.ts |
| CG | compiler/src/code-generator.js + compiler/src/codegen/ |
| Engine-arm parsing | compiler/src/engine-statechild-parser.ts (custom raw-text closer-pairing + message-arm lexer, S154) |
| SYM analysis | compiler/src/symbol-table.ts (11280L; PASS 11 `accepts=` resolution; PASS 20 match exhaustiveness incl. subset dead-arm, S155-S156) |
| Enum-subset shared recognizer | compiler/src/enum-subset-refinement.ts (143L NEW, S156) |
| log() location resolver | compiler/src/codegen/log-loc.ts (NEW S174, 224L; `resolveLogLoc`/`registerFileSource`/`fileDeclaresLog` + `SERVER_LOG_HELPER` runtime chunk) |
| Sidecar | compiler/src/engine-graph.ts (--emit-engine-graph, S149) |
| Tailwind class emit | compiler/src/tailwind-classes.js — class-token → CSS rule registry + the W-TAILWIND-* lints. **S191: composing-utility families (§26.7.x) — Approach C inline-`var()`-fallback model.** `registerRing()`/`ringShadowSetter()`/`BOX_SHADOW_COMPOSE` (ring/shadow §26.7) · `registerGradient()` (gradient §26.7.1) · `registerTransform()`/`TRANSFORM_COMPOSE` (transform §26.7.2) · `registerFilters()`/`registerBackdrop()` + `FILTER_COMPOSE`/`BACKDROP_COMPOSE` (filter/backdrop §26.7.3); all invoked in the registry build [:3008-:3012]. Each utility emits a shared `var()`-based composing shorthand + sets one `--tw-*` prop; **every `var()` carries an inline fallback so NO global `*, ::before, ::after` preflight block is emitted** (§26.1/§26.2 minimalism axiom). Also `findInterpolationRanges`/`tokenTouchesInterpolation` (S183 dynamic-class-prefix lint precision). |

## Codegen `<each>` / `<match>` Emit Map (S152-S159 — read before any each/match or engine codegen)

| File | Role |
|------|------|
| compiler/src/codegen/emit-each.ts (**2149L** at S202) | Tier-1 `<each>` render fns; `collectEachBlocks`, `emitEachBodyRenderForFile`, `emitEachMountHtml`, shared `emitEachReconcileLines`; dep-first read + `_scrml_each_renderers` registration (S153); Bug 62 (S156): `buildEachEngineCtx` + `EachEngineCtx` threading; Bug 64 (S158): `EachReconcileCtx` stack + `maybeWrapEachPerItemEffect` + push/pop/current for live-keyed per-item TEXT/class: bindings; Bug 73 (S159): `blankStringAndRegexLiterals` + `iterScopeReferencedInHandler` (exported, shared with emit-lift.js) + `maybeWrapEachPerItemHandler` for live-keyed Tier-1 per-item EVENT HANDLERS; `emitNestedEachFromMarkup` (exported, used by emit-lift.js Bug 72); **S200 C1+C2 (`39bd061f`, g-each-peritem-if-predicate): `lowerEachExpr(text, iterVar)` (:594) lowers §42 predicates in per-item exprs via `parseExprToNode`→`emitExprField` (used by per-item `${…}` interp / attr interp / `if=`); per-item element `if=` consumed as a conditional gating `frag.appendChild(el)` (renderTemplateChildToJs:314-351)**; **S201 g-each-body-bare-variant-arg (`17d2711a`): `lowerEachExpr` guard broadened to route bare `.Variant` enum literals through the structured emitter + NEW `serializeCallArgsLowered` lowers a bare-`.Variant` call-arg in the NON-engine per-item handler fallback (engine path keeps raw callText for `.advance(.X)` detection)**; **S202 g-each-over-arm-payload-binding-unbound (`60d547e1`): NEW `armPayloadBinding {cellName, variantTag, fieldName}` field on EachBlockAstNode (~:138) + exported `stampArmPayloadEaches(body, cellName, variantTag, payloadBindings, payloadFieldNames)` (:242) — stamps a TOP-LEVEL `<each in=BARE>` over an enclosing match-/engine-arm PAYLOAD binding; emitEachBodyRenderForFile (:2022) resolves the iterable from `_scrml_reactive_get(cell).data[field]` (gated on variant===variantTag, else `[]`) instead of the UNBOUND `const _items = rows;`** |
| compiler/src/codegen/emit-lift.js (**2349L** at S201) | Tier-0 `${for…lift}` path; Bug 65 (S157): engine-ctx threading via `buildLiftEngineCtx`/`buildLiftEngineCtxFromExtras`/`tryLowerLiftEngineHandler`; Bug 64 (S158): `pushLiftReconcileCtx`/`popLiftReconcileCtx` + `maybeWrapLiftPerItemEffect`; Bug 72 (S158): `tryEmitNestedLiftEach` routes nested `<each>` through `emitNestedEachFromMarkup`; Bug 73 (S159): `maybeWrapLiftPerItemHandler` (function-body shape) + `maybeWrapLiftCallableHandler` (callable-direct shadow shape) + `_liftIterScopeReferenced` (delegates to emit-each.ts `iterScopeReferencedInHandler` via `require()`); **S201 markup-value: NEW `emitMarkupValueExpr(node)` (~:1312) — the markup→DOM-node IIFE primitive shared by emit-expr `case "markup-value"`, emit-logic `return <markup>`, and the ternary-arm path (wraps `emitCreateElementFromMarkup`)**; **S202 LAYER-2 (`d830ec59`): the `val.kind === "string-literal"` markup-attr branch (~:1081) lowers a string-literal attr value carrying `${expr}` interpolation segments (LAYER-2, distinct from the LAYER-1 bare-expression attr value)** |
| compiler/src/codegen/emit-control-flow.ts (2013L) | Tier-0 for-loop `createFn` key capture + `pushLiftReconcileCtx`/`popLiftReconcileCtx` (Bug 64 S158); engine-ctx threading into `emitConsolidatedLift`/`emitLiftExpr`/`emitIfStmtWithContainer`/`emitForStmtWithContainer` |
| compiler/src/codegen/emit-match.ts | block-form `<match>` arm emission; each-bearing arms re-parse via splitBlocks+buildAST; `restampEachBlockIds`, `__scrmlCachedArms` (S153). **S177:** `buildMatchArms` (line 522) consumes the ast-builder-expanded `armBodyChildren` wrapper (lookup 676-694) for a bare-body arm whose body hosts a `<formFor>`/`<tableFor>`/PascalCase component — takes the EXPANDED body instead of re-parsing the raw tag (g-formfor); plain + each-bearing arms stay on `armsRaw`. **bug-48:** `resolveOnExpr` (line 272) complex-`on=` → Shape-B effect-mode lowering (paren/bracket-aware). **S202 g-each-over-arm-payload-binding-unbound (`60d547e1`):** `buildMatchArms` (:578) calls `stampArmPayloadEaches(body, _armCellName, tag, payloadBindings, payloadFieldNames)` (:920) per arm so a top-level `<each in=rows>` over the arm's `.Loaded(rows)` payload resolves from the reactive cell. |
| compiler/src/codegen/emit-engine.ts (4647L at S202) | Engine substrate codegen; S155: `emitEngineMessageArmTable()`, `engineMessageArmTableName()`, `engineHasMessageArms()`, `collectEnginesWithMessageArms()`, `collectEngineMessageVariants()` exported for threading into emit-each and emit-event-wiring; `parseEnumVariantFieldsForType()` resolves payload-binding field names at codegen time. **S202 g-each-over-arm-payload-binding-unbound (`60d547e1`):** `buildEngineArms` (:2142) calls `stampArmPayloadEaches(body, meta.varName, tag, payloadBindings, payloadFieldNames)` (:2459) — the engine-arm twin of the emit-match stamp (same shared emit-each mechanism). |
| compiler/src/codegen/emit-html.ts (2432L) | HTML emit; Bug 60 (S157): `enclosingCompoundStack` + `lookupQualifiedStateCell` fallback for render-by-tag inside compound wrappers. **S191 if=fn() condition routing:** the call-ref attr-value branch [:1773] (`g-attr-if-fn-call-misroute`) routes a bare-call `if=fn()`/`show=fn()` as a reactive conditional (emits `data-scrml-bind-if`/`-show` + `addLogicBinding`), parity with the paren form `if=(fn())`; the clean-subtree if= handler [:1413] (`g-attr-if-fn-display-not-mount`) gives the bare call the SAME mount/unmount controller as `if=(fn())`/`if=@var` (gate at [:1394] now accepts `call-ref` alongside `variable-ref`/`expr`). §5.1. |
| compiler/src/codegen/emit-variant-guard.ts | shared engine/match arm-swap dispatcher; calls `_scrml_remount_each` after arm innerHTML+wire (S153) |
| compiler/src/codegen/emit-event-wiring.ts | event-binding + chain-condition codegen. **S191 if=fn() chain routing:** `_update_chain_<id>()` [:1267] handles a `call-ref` chain-head / else-if condition [:1282] (`g-attr-if-fn-chain-head-call-misroute`) — a bare-CALL `if=isHigh()` is CALLED (build call string → `parseExprToNode` → `emitExprField`), not read as a cell (`_scrml_reactive_get("isHigh")` → branch never activates). Also s169-map-inline-insert [:480-506]: inline map-method assign routed via `emitExprField`→`emitAssign`. **S201 markup-value:** `${}` text-interpolation display (:1228 reactive / :1279 one-shot) routed through the NEW node-aware `_scrml_render_value(el,v)` runtime helper (was raw `el.textContent=` → stringified a DOM node to `[object HTMLSpanElement]`). |
| compiler/src/codegen/emit-client.ts (**2550L** at S201) | `detectRuntimeChunks` descends into engine + each-block `bodyChildren` so reconcile/effect/remount chunks survive tree-shaking (S153); S174: includes the `'log'` client chunk ONLY when an emitted line contains `_scrml_log(` (shadowed/stripped builds omit it); **S201 markup-value:** pulls the `derived` runtime chunk for a markup-typed derived cell (`_cellKind==="markup-typed"`) — emit-logic still emits `_scrml_derived_declare` for it, so without the gate the chunk was tree-shaken away → runtime `_scrml_derived_declare is not defined` (Bug-57 class) |
| compiler/src/codegen/emit-server.ts | server-batch emit; S174: prepends `SERVER_LOG_HELPER` (from log-loc.ts) to the server bundle ONLY when the final emitted JS contains a `_scrml_log(` call (production-stripped / all-client-log builds emit no helper) |
| compiler/src/codegen/emit-expr.ts (**1906L** at S201) | expr lowering; S174: `log(...)` call lowering (line ~1630) — `_scrml_log(side, loc, ...args)` for the builtin; plain call for a shadowed `log` (`_logShadowedInFile`); `(void 0)` no-op when `_logProductionStrip` is set; `setLogShadowedInFile`/`setLogProductionStrip` toggles + `resolveLogLoc` import; **S201 markup-value: NEW `case "markup-value"` (:336) → `emitMarkupValueExpr(node.node)` (import from emit-lift.js) — lowers a markup-as-value ExprNode leaf (ternary arm) to the DOM-node IIFE** |
| compiler/src/codegen/log-loc.ts (**224L, NEW S174**) | location resolver for `log()`: `resolveLogLoc(span)` → "basename:line" from byte offset against `registerFileSource`'d source; `resetLogLoc()` per-compile; `fileDeclaresLog(fileAST)` detects a user `log` decl (drives shadow toggle); `SERVER_LOG_HELPER` raw-string runtime chunk (`_scrml_log`/`_scrml_log_render`) |
| compiler/src/codegen/emit-predicates.ts (518L) | §53 predicate boundary-check codegen; S156 (d)-A: `kind: "variant-set"` case emits `(["A","B"].includes(v))` |
| compiler/src/codegen/emit-schema-for.ts (516L) | schemaFor SQL DDL emission; S156 (d)-A batch 3: enum-subset `predicated` type emits `CHECK IN (ordered subset names)` |
| compiler/src/codegen/emit-logic.ts (**4018L** at S201) | Bug 65 (S157): `for-stmt` case threads all engine extras into `emitForStmt`; S168 cycles-prereq piecewise bracket-write path; S170 Bug B `reactive-nested-assign` leaf-retarget (~L3025-3035); S174 `log()`-related arm distinction (user `log` decl vs bare builtin call ~L703); **S201 markup-value:** return-stmt handler (~:2391) lowers a `node.markupNode` (`return <markup>`) via `emitMarkupValueExpr`, bypassing `_wrapReturnWithCheck` (markup is not a refinement-predicated scalar) |
| compiler/src/runtime-template.js (**4667L** at S201) | `_scrml_each_renderers` registry + `_scrml_remount_each` (S153); `_scrml_engine_dispatch_message` (S155); `_scrml_reconcile_list` + `container._scrml_item_by_key` key→item map + `_scrml_trigger(container, "_scrml_items")` (Bug 64 S158); `_scrml_resolve_item(container, key)` (Bug 64 S158; Bug 73); S168 `_scrml_structural_eq(a,b,seen)` cycle guard + `_scrml_deep_set` COW; S169 §59 map runtime (`{__scrml_map}` + 14-method surface + `_scrml_fnv1a`/`_scrml_value_canonical`); `_scrml_effect_static`, `_scrml_modules`; **S201 markup-value: NEW core-chunk `_scrml_render_value(el,v)` (~:716)** — `v instanceof Node` → `el.replaceChildren(v)`, else `el.textContent=(v==null?"":String(v))` (byte-identical string/primitive path; `""` is a defined value) |
| compiler/src/dependency-graph.ts (3354L) | Bug 60 (S157): render-by-tag structural-read credit for E-DG-002 (markup tag name matches reactive var → `creditReader`) |
| compiler/src/component-expander.ts (**4196L** at S202) | each-in-component-body: legacy re-parse fallback + substituteProps each-block string fields + tokenized-sigil collapse (S153); **S200 g-each-component-body-invalid-js: STEP1 (`60ace8b4`) hoists a directly-imported component's non-component (helper) exports into the consumer's existing import bindings (`exportIsUserComponent` filter); STEP2-A (`ecba9fee`) synthesizes a consumer import + `importGraph` edge for TRANSITIVELY-inlined component modules' helper exports; STEP2-B `buildPropExprMap`/`substitutePropsInExprNode` (:1143/:1289) substitute EXPRESSION-valued nested props via `parseExprToNode`+IdentExpr-replacement walk**; **S201 g-nested-component-member-arg-misparse (`7d3855a6`): `normalizeTokenizedRaw` (~:560) collapses tokenized GENERAL member-access spacing `obj . field`→`obj.field` (the logic tokenizer space-pads the `.` operator, so a component-body nested-component prop arg `<Badge s=row.name/>` round-trips as `s=row . name` → ATTR_IDENT strands `.name`); pre-`.` class `[A-Za-z0-9_$)\]]`, post-`.` `[A-Za-z_$]` (excludes `3.14`/`.Idle`)**; **S202 g-each-inline-component-prop-member-unsubstituted + g-inlined-component-root-class-interp-raw (`d830ec59`): NEW `substituteInterpSegments` (~:1163) substitutes prop refs appearing as LEADING identifiers INSIDE a string-literal markup-attr's `${...}` segments (`${load.id}`/`${cls(status)}`) via `substitutePropsInRawExpr` (literal class text untouched); the inlined-component root `class=` merge runs AFTER substitution so an interpolated root class emits the substituted expr not raw `${...}`** |
| compiler/src/engine-statechild-parser.ts (2418L) | `:`-shorthand-child closer-pairing fix (S153); `accepts=` + message-arm lexer `parseMessageArms()` (S154) |
| compiler/src/enum-subset-refinement.ts (143L NEW) | Shared pure `parseEnumSubsetAnnotation()` recognizer; used by type-system.ts + symbol-table.ts to agree on the §53.15.1 grammar |
| compiler/src/engine-statechild-grammar.ts (**NEW S209 ss2 item 3, 81L**) | SSOT for two engine-state-child grammar sets formerly duplicated across type-system + codegen. Exports: `ENGINE_STATE_CHILD_RESERVED_ATTRS: ReadonlySet<string>` (rule/history/internal:rule/effect — §51.0.B.1 reserved attr names, take precedence over payload-binding barewords) + `STATE_CHILD_STRUCTURAL_TAGS: ReadonlySet<string>` (onTimeout/onTransition/onIdle/engine/machine — structural elements inside a state-child, not renderable markup). Placed at `compiler/src/` (NOT `codegen/`) to break the upstream→downstream import barrier: type-system.ts imports `./engine-statechild-grammar.ts` (line 81); codegen/emit-variant-guard.ts imports `../engine-statechild-grammar.ts`. ZERO behavior change — member-identical to the literals replaced. Residual inline copies remain at engine-statechild-parser.ts / native-walker/engine-statechild-walker.ts / symbol-table.ts (clean follow-on). |
| compiler/src/ast-builder.js (**16080L** at S201) | Bug 72 (S158): `_parseLiftAttrValue` bare-`@` branch collects `@.` token run as expr value; Bug 71 (S157): derived `const <x> = match` dual-parse hook; Bug 67 (S157): `return match` hook; S159 R1: `buildBlock()` body-child synthesis for non-void HTML `:`-shorthand bodies; S167 (HIGH Bug A): `parseLogicBody`/`collectExpr` depth-0 statement-boundary recognizer (~L2747) for dotted-path reactive statements; **S168 cycles-prereq: NEW shared `collectAtPathSegments()` (~L2523) — bracket-depth-aware path collector consumed by both AT_IDENT call sites + the S167 boundary recognizer; bracket-index WRITES now route through COW (`reactive-nested-assign`) with a heterogeneous string-or-`{index:ExprNode}` path; bracket READS stay verbatim**; **S201 markup-value: NEW `parseExprWithMarkupValues` (~:2944) recovers markup in EXPRESSION position to `markup-value` leaves (balanced-span scan → `__scrml_mv_N__` placeholder skeleton → acorn-clean parse → markup recovered by re-tokenizing `lift <markup>` → substitute); `safeParseExprToNode` markup-aware fork + `_inMarkupValueParse` guard; `sawTernaryAtRoot` latch suppresses the `markupRootClosed` break in a ternary RHS (alternate arm survives); `return <markup>` hook (~:7275) attaches `markupNode`** |
| compiler/src/block-splitter.js (**3268L** at S201) | S159 R4a: `shorthand && !selfClosing` branch placed BEFORE void/self-closing short-circuit so `<void : expr>` reaches the type-system guard; **S201 markup-value: `scanShape12DeclEnd` derived/Shape-1/3 RHS branch (~:1475) FULL-RHS balanced scan — a markup-bearing decl is gobbled as ONE text block (incl. ternary markup arms; pre-fix returned -1 → legacy per-char path split the arms into SEPARATE blocks → arms DROPPED); a VALUE-TERMINATOR discriminator cedes back to -1 for a sibling-markup decl (`<x>=1<div>`); NO-markup RHS still -1** |
| compiler/src/type-system.ts (**~19200L**, +990 S175) | Bug 63 (S157): markup event-handler attr `.advance` two-plane checking; E-SYNTAX-064 emitted for `@.` outside each body scope; Bug 67/71: match-as-expr exhaustiveness wired; S159 R4b: `E-COLON-SHORTHAND-ON-VOID` guard + R3 `@.`-shorthand-body E-SYNTAX-064 extension; S173 `checkFunctionTypedStructFields` (S175: now `E-STRUCT-FUNCTION-FIELD` hard reject); S174 `checkLogShadowing` (W-LOG-SHADOWED) + `checkAnyTypeForbidden`/`typeTextMentionsAnyToken` (E-TYPE-ANY-FORBIDDEN); S175 typed-SQL-row flow (`resolveSqlRowType`/`checkSqlRowWidthSubtype`/`checkPropContract`/`checkSqlRowAgainstCellContract`/`resolveIterableRowElement`/`checkRowFieldAccessInExpr`/`inferReturnTypeFromBody`) decl-site/read-site scans |

## Native-Parser Swap Orientation (S161 ratified / S170 re-measured at 605 → ~508) — READ FIRST for any native-parser flip-failure work

**Strategic line:** drive `--parser=scrml-native` to DEFAULT, then DELETE legacy block-splitter (BS) +
Acorn at M6 (direction-a, ratified S161; realistic v0.8 multi-session target). The native parser is
`compiler/native-parser/` — paired `.js` bootstrap + `.scrml` self-host mirror. Fix the `.js`; the `.scrml`
mirrors are FEATURE-stale (S162 — whole machinery missing, not mere predicate-drift; S115 lockstep moot until
a deliberate re-sync). **The Phase-A default-flip itself is a STANDING USER DECISION — PA ships parity-closers
feeding it, never "the flip."**

**each/match promotion — CLOSED (S162):** the former HARD M5-swap precondition (native "does NOT promote
`<each>`/`<match>` to structural nodes", S153) is RETIRED. Native now promotes BOTH: `<match>` → `match-block`
(`isMatchBlock`/`synthMatchBlockNode`) and `<each>` → `each-block` (`isEachBlock`/`synthEachBlockNode` in
parse-file.js). `@.` contextual sigil lexed; emit-each honors the `exprNode` contract; SPEC §4.15/§24.4
register `<each>` structural.

**F1 engine-substrate silent-miscompile — CLOSED (S163, the headline fix):** the ~168 "engine arm-body"
family's DOMINANT cause was NOT spurious `E-UNQUOTED` or arm-body classification (that was the S139-trap
framing at survey level — files compiled CLEAN but silently miscompiled). Native silently DROPPED the entire
§51.0 engine substrate (transition table, `_scrml_engine_direct_set` rule-validation, var-init, mount/body-
render) across ALL engine files — emitting `<engine>` as a dumb `_scrml_reactive_set` cell. **Root cause = a
`machineDecls` TWO-INSTANCE object-identity defect:** native synthesized two `engine-decl` objects (nodes copy
via `parse-file.js synthEngineNode` + a SEPARATE machineDecls copy via `collect-hoisted.js synthEngineDecl`);
SYM stamped `_record`/`engineMeta` on the nodes copy, codegen `collectC12EngineDecls` read the un-stamped
machineDecls copy FIRST → `isC12EngineDecl` false → substrate dropped. **Fix (a41df176, ~40L):** native derives
`machineDecls` from the mapped `nodes` instances (`collectMachineDeclsFromNodes` in parse-file.js) + maps
`bodyChildren` to AST nodes (nested engines); `collect-hoisted.js` no longer synthesizes engines. All 6 swept
engine sub-features (basic/hierarchy/onTimeout/onIdle/history/effects) now BYTE-IDENTICAL native==default.
**B1 (S163, 6ad8ca13):** native `reset(@cell)` → `reset-expr` (translate-expr.js intercepts bare-`reset` callee
→ live `reset-expr` node; 3 §6.8.2 shapes). **§4.18 ruling (S163):** native's `E-UNQUOTED-DISPLAY-TEXT` fire on
bare display text in an engine/match/`:`-shorthand arm body is SPEC-CORRECT (§4.18.7), NOT spurious — native is
the canonical enforcer, LIVE stays lenient (doomed at M6); corpus bare-text→`"..."` migration is deferred
swap-prep backlog.

**Flip re-measure: 451 flip-failures at S165 close** (674 S164-open → 509 S164-close → 451 S165; was ~790 S162,
1,150 S161). Default output UNCHANGED across S162-S165 (no version bump — every closer is shadow-only, gated on
`--parser=scrml-native`). Full suite at S165: 23,054 pass / 0 fail / 912 files; within-node parity **1005/0**.
**S164 closed (674→509):** §51.0.S message-arm (B2) + native attr-value `exprNode`/`argExprNodes` population +
lift `<markup>` close-tag + F2a chained `?{}.method()` + table-for struct-field-drop. **S165 closed (509→451,
−58):** F2-match string-literal arms `2c2e5bb2` · promote-each 3 §17.4 for-stmt gaps `785f24d1` · R1 typed-`@cell`
decl `89912bb9` · server-fn-star `function*`+yield `26a24b71`. **S166 closed two re-triage roots WITHOUT a full
flip re-measure (next dispatch re-runs the harness):** bare-`function name()! -> Err` failable recognition
`76059024` (`parse-stmt.js parseFunctionDecl` threads `{canFail,errorType}` as `makeFunctionDecl`'s 7th arg —
was silently dropped; `fn`/`server`/`pure` already worked; ~31 failable-via-`function` test files) + cross-file
`${...}`-wrapped `export const X = <markup>` raw-slice `9d12d980` (`collect-hoisted.js synthExportDecl` anchors
the raw slice to `block.bodyStart`, not `block.span.start`, fixing the over-shift that emptied `raw` → spurious
E-COMPONENT-020/035; `parse-markup.js` stamps `bodyStart` at 4 body-attach sites; `bodyStart` added to
within-node-classifier STRIP_KEYS). **S170 re-measured 605 native-only flip-failures on `df08f282` (default BS+Acorn pipeline 0-fail / fully green; native is opt-in `--parser=scrml-native`) + landed two file-disjoint waves:** **fix-wave-1 `5a346faa`** — GROUP P (parse-stmt.js): `on mount`/`on dismount` blocks (§6.7.1 desugar, mirrors ast-builder.js:7226-7248; ~52 incl. cross-file cascade) + `const @name` derived-state-decl (rerouted to `parseConstAtStateDecl` — F5 CLOSED; ~40; brief's `parseBindingIdent` was wrong, live treats `const @name` as a DERIVED state-decl); GROUP T (translate-stmt.js): deepset/array-mutation node-synth (native now emits live `reactive-nested-assign`/`reactive-array-mutation` → COW + trigger, routing through the S170 Bug-B-fixed emit-logic) + destructured-param structuring (closes E-SCOPE-001 on destructure names) + var-decl `typeAnnotation` thread (E-VARIANT-AMBIGUOUS → E-CONTRACT-001); GROUP W (NEW `src/native-walker/exprtext-backfill-walker.ts` + api.js): post-parse walker backfills `.expr`/`.init`/`.condition` from structured `exprNode`/`initExpr`/`condExpr` so the type-system's regex-over-text passes work under native (INERTNESS VERIFIED 295/297 byte-identical; 2 deltas a NET-POSITIVE latent `const token;` fix). **fix-wave-2 `cc69c62d`** — BlockStub `verbatim`-body recovery: native `reconstructArmBody` returned literal `"{}"` for block-bodied match arms (dropping statements; the Mario MUSHROOM click fired but never transitioned Small→Big) + same gap dropped lambda callback block bodies; fix = `parse-expr.js parseBlockStub` stamps `stub.verbatim` (balanced `{...}` source slice) + `translate-expr.js reconstructArmBody` returns it / `translateArrow`+`translateFunctionExpr` emit a full-lambda `EscapeHatchExpr` (mirrors live expression-parser.ts:2152-2192) — +17 flip closures, full suite 23405/0. FIX 3 (arrow-shorthand fn body) correctly NOT implemented (invalid scrml — live REJECTS); FIX 4 (`export <cell>`) deferred pending SPEC ruling. → **~508 native-only flip-failures**; next dispatch re-runs the harness for exact counts. ~8 environmental; remaining families below.

| Family | ~Count | Size | Locus (native file) | Status |
|--------|--------|------|---------------------|--------|
| ~~F1 engine substrate-drop~~ | ~~~168~~ | — | parse-file.js / collect-hoisted.js | **CLOSED S163** (machineDecls instance-share fix; 6 sub-features byte-identical) |
| ~~F1-narrow + B2 §51.0.S message-arm~~ | ~~~20~~ | — | `parse-markup.js` (arm-region recognition) + `native-walker/engine-statechild-walker.ts:516` + `collect-hoisted.js synthEngineDecl` | **LANDED S164 `7cbad5dd`** — parser-level: native `engineMeta` (acceptsType/messageArms/arm-counts) byte-identical; within-node 1005/0; E-UNQUOTED 2→0. NOTE: full-fixture emit-R26 PENDS the exprNode family below (the fixture's `@x.advance(...)` handler hits the exprNode gap, not a message-arm flaw). |
| ~~native attr-value `exprNode`+`argExprNodes` population~~ | LARGE | — | `compiler/src/native-walker/attrvalue-exprnode-walker.ts` (NEW) `populateNativeAttrValueExprNodes`, run from `api.js` native `_buildAST` | **LANDED S164 `c1566faa`** — placed in the NATIVE-WALKER (not tag-frame.js — native-parser/* cannot import the live acorn parser without inverting self-host layering); reuses live `safeParseExprToNodeGlobal` (exported from ast-builder.js). Covers BOTH `exprNode` (expr/variable-ref) AND `argExprNodes` (call-ref — `onclick=@x.advance(.Drop(...))` parses as call-ref; ast-builder.js:1831-1832). R26(A) isolated + R26(B) message-dispatch BOTH byte-identical. **§51.0.S message-dispatch family now FULLY native-parity end-to-end** (message-arm 7cbad5dd + this). within-node 1005/0, MISSING-FIELD −1221 convergence. Cross-cutting spot-check: 12 handler files → 0 native E-CODEGEN-INVALID-JS. |
| native attr-value span inside lift/each | benign | — | native attr-value `span.start` | **WATCH/FOLLOW-UP (S164)** — native markup attr-VALUE `span.start` is BLOCK-RELATIVE (not file-absolute) INSIDE lift/each markup-as-value subtrees (PRE-EXISTING; identical with/without the exprNode pass). The exprNode population PROPAGATES it into parsed spans there → +34 SPAN-COORD within-node residuals (rebumped benign; EMIT byte-identical; top-level exprNodes byte-identical to live). Follow-up: thread file-absolute offset for attr-values inside lift/each. |
| mario PowerUp payload-enum | ~few | — | native enum-body capture | NEW S163 — native captures only `["Mushroom"]` (drops payload variants), mis-emits `PowerUp.Flower(3)` as `"Flower"(3)`; payload-bearing-enum native gap (mario residual 133 diff-lines) |
| `effect=` opener (§51.0.H Form 3) | small | S | `synthEngineDecl` (no openerEffect read) | OPEN — small separate gap |
| nested-engine `acceptsType` pairing | benign | — | nested engine-decl synth | WATCH (S164) — native stamps `acceptsType:null` on PRE-EXISTING-mis-paired nested engine-decls (4 hierarchy fixtures +2 EXTRA-FIELD, benign within-node rebump); top-level matches live; possibly tied to S163 machineDecls/bodyChildren nesting |
| F2 SQL `?{}` in server-fn | ~58 | — | `parse-sql-body.js` / `translate-stmt.js` / `translate-expr.js` | PARTIALLY closed. **F2a chained `?{}.method()` LANDED S164 `7e54f321`** (`translate-stmt.js reconstructChainedSql`). **F2-generator `server function*` SQL LANDED S165** (see server-fn-star row). OPEN: top-level server-fn `?{}` body drop + assign-RHS `@x = ?{}.all()` (state-decl-routed). |
| ~~promote-each (3 §17.4 for-stmt gaps)~~ | ~25 | — | `translate-stmt.js` / `parse-stmt.js` / `ast-stmt.js` | **LANDED S165 `785f24d1`** — iterable-field synth (`makeForStmtCStyle`/`makeForStmtInOf`) + §17.4b `key <expr>` clause + §17.4a `else` empty-state block; `keyExpr`/`elseBody` threaded through `makeFor`/`makeForIn`/`makeForOf`. |
| ~~server-fn-star (`server function*` + yield)~~ | ~few (2 roots) | — | `parse-markup.js` (`BARE_DECL_RE`) / `translate-stmt.js` (Yield) | **LANDED S165 `26a24b71`** — `BARE_DECL_RE` synced VERBATIM with live (admits generator `[*\s]` form so `server function*` lifts); `Yield`-expr stmt unwraps to `makeYieldStmt`. Closes the F2-generator sub-root. |
| ~~table-for struct-field-drop~~ | ~21 | — | `parse-stmt.js` (`typeBodyText`/`joinWithNewlines`) | **LANDED S164 `66301357`** — struct/enum field-separator newlines preserved (was: `<tableFor>` emitted only the first struct `<th>`, silent miscompile). |
| ~~bare-`function` failable `function name()! -> Err`~~ | ~few (1 root; ~31 files) | — | `parse-stmt.js` (`parseFunctionDecl`) / `ast-stmt.js` (`makeFunctionDecl`) | **LANDED S166 `76059024`** — `parseFunctionDecl` consumes the trailing `!` failable marker + `! -> Err` arrow form (§19.4.1) / bare `! Err` form (§41.14, R25-Bug-36 continuation disambiguation), ported verbatim from `parseScrmlFunctionDecl`; threads `{canFail,errorType}` as `makeFunctionDecl`'s 7th arg (was 6 → metadata silently dropped). `fnKind` stays `"function"`. New native `E-STMT-FN-ERROR-TYPE`. BANKED: native empty `fail X::V(arg)` envelope + native `renders ${id}` interpolation break (separate families). |
| ~~cross-file `${...}`-wrapped `export` raw-slice~~ | ~few | — | `collect-hoisted.js` (`synthExportDecl`) / `parse-markup.js` (`bodyStart` stamp) / within-node-classifier.ts (STRIP_KEYS) | **LANDED S166 `9d12d980`** (ROOT-2) — native `synthExportDecl` anchors the raw slice to `block.bodyStart` (bodyText host-start = `frame.openSpan.end`, one byte past `${`) instead of `block.span.start` (the opener `$`); over-shifting LEFT by the opener length emptied `raw` → cross-file CE registry empty → spurious E-COMPONENT-020/035. Same off-by-opener class as M6.7-C1 `synthComponentDef`. `bodyStart` stamped at 4 body-attach sites + added to STRIP_KEYS (no allowlist rebump). within-node 1005/0; cross-file integration 48/0; +1 emitted-output regression. **ROOT-1 (exported-inner-decl reaching codegen) BANKED** — emit-fix surfaced a 58-fixture within-node divergence (2 prereqs: deep-shift spans by `bodyStart` + native FunctionDecl trailing-match-as-return/return-type drop, which reproduces on NON-exported fns too). |
| ~~F2-match string-literal arms~~ / F3 if-as-expr residual | small | — | `parse-expr.js` / `ast-expr.js` / `translate-expr.js` | **F2-match LANDED S165 `2c2e5bb2`** (`MatchArmPatternKind.Literal` + `makeLiteralPattern` + `StringLit` branch in `parseMatchArmPattern` + `reconstructArmPattern` Literal case — `match action { "add" => {...} }` now parses). Same-line arms DONE S162. **if-as-expr residual still OPEN (LOW).** |
| F4 formFor expansion | ~32 | — | native parse → bridge → form pass | OPEN — drops field-markup expansion (largest open family) |
| ~~F5 `const @name` derived-decl~~ | ~20 | — | `parse-stmt.js` (`parseConstAtStateDecl`) | **LANDED S170 `5a346faa`** (GROUP P) — `const @name` rerouted to `parseConstAtStateDecl` (live treats it as a DERIVED state-decl, NOT a plain const — the brief's `parseBindingIdent` approach was empirically wrong); byte-identical emit. Distinct from typed-`@cell` decl `@name: Type = e` (R1, S165 `89912bb9` `parseTypedAtStateDecl`). |
| ~~`on mount` / `on dismount` blocks~~ | ~52 | — | `parse-stmt.js` | **LANDED S170 `5a346faa`** (GROUP P) — §6.7.1 lifecycle-block desugar (mirrors ast-builder.js:7226-7248); incl. cross-file cascade. |
| ~~deepset / array-mutation node-synth~~ | — | — | `translate-stmt.js` | **LANDED S170 `5a346faa`** (GROUP T) — native emits the live `reactive-nested-assign` / `reactive-array-mutation` kinds → COW + trigger, routing through the S170 Bug-B-fixed emit-logic (byte-identical to live). + destructured-param structuring (E-SCOPE-001) + var-decl `typeAnnotation` thread (E-VARIANT-AMBIGUOUS → E-CONTRACT-001). |
| ~~native `.expr`/`.init`/`.condition` text backfill~~ | cross-cutting | — | NEW `src/native-walker/exprtext-backfill-walker.ts` (`backfillNativeExprText`, run from api.js:960) | **LANDED S170 `5a346faa`** (GROUP W) — post-parse walker stamps the legacy string fields from the structured `exprNode`/`initExpr`/`condExpr` siblings so the type-system's regex-over-text lifecycle/enum-subset passes work under native (mirrors the S164 attrvalue walker). INERTNESS VERIFIED: 295/297 samples byte-identical; 2 deltas a NET-POSITIVE latent `const token;` invalid-JS fix. |
| ~~BlockStub `verbatim`-body match-arm / lambda block-body drop (Mario)~~ | ~17 | — | `parse-expr.js` (`parseBlockStub`) / `translate-expr.js` (`reconstructArmBody`/`translateArrow`/`translateFunctionExpr`) | **LANDED S170 `cc69c62d`** (fix-wave-2) — native `reconstructArmBody` returned literal `"{}"` for block-bodied match arms (dropping statements; the dominant ~1463-file corpus form — Mario's MUSHROOM click fired but never transitioned Small→Big) + same gap dropped lambda callback block bodies. Fix: `parseBlockStub` stamps `stub.verbatim` (balanced `{...}` slice; render-body scope-guard skips lift/markup arm bodies); `reconstructArmBody` returns `body.verbatim`, `translateArrow`/`translateFunctionExpr` emit a full-lambda `EscapeHatchExpr` (mirrors live expression-parser.ts:2152-2192; reuses `emitEscapeHatch`). +17 closures, ZERO new failures. DEFERRED (pre-existing, NOT introduced): code-position match-BLOCK promotion for render arm bodies · engine-write-guard routing for match-as-statement over an engine cell · single-word bare-display-text silent-drop tokenizer bug (filed). |
| F6/F9 fn param / export-fn-body | ~16 | — | `parse-stmt.js` / `parse-expr.js` | OPEN |
| F7 missing diagnostics | ~15 | — | body-parser gates (`tag-frame.js` etc.) | OPEN — swallow `E-STRUCTURAL-ELEMENT-MISPLACED` etc. |
| F8 stdlib `await import()` | 13 | — | — (NOT a native-parser change) | RULED a stdlib-migration task; native stays strict no-`await` enforcer |

Other large flip-failure signatures in the remaining ~508 (native mis-parse → wrong/downstream diagnostic, spread across; counts below are S165 snapshots — re-run the harness for current
families): `E-CODEGEN-INVALID-JS` (18, downstream invalid JS), `E-TYPE-063` (15) + `E-VARIANT-AMBIGUOUS` (4)
(native bare-variant resolution gap), `E-TYPE-001` (14) + `E-TYPE-020` (14) (lifecycle / exhaustiveness),
`E-MATCH-NOT-EXHAUSTIVE` (7) + `E-MATCH-SUBSET-DEAD-ARM` (4) (match family).

**AUTONOMOUS SWAP-LOOP:** the dedicated triage `docs/changes/native-swap-triage-s164/TRIAGE.md` is the
authoritative next-pick family map (grouped by native parse-gap ROOT, each reproduced default-clean/native-fail).
**LANDED S164:** §51.0.S message-arm `7cbad5dd` + exprNode `c1566faa` (family FULLY closed) · lift `<markup>`
close-tag `649f4ef8` · F2a chained `?{}.method()` `7e54f321` · table-for struct-field-drop `66301357`.
**LANDED S165:** F2-match string-literal arms `2c2e5bb2` · promote-each 3 §17.4 for-stmt gaps `785f24d1` ·
R1 typed-`@cell` decl `89912bb9` · server-fn-star `function*`+yield `26a24b71`.
**LANDED S166 (re-triage roots, no full re-measure):** bare-`function` failable `76059024` · cross-file
`${...}`-export raw-slice `9d12d980` (ROOT-2). Triage: `docs/changes/native-swap-retriage-s166/TRIAGE.md`.
**LANDED S170 (re-measured 605 on `df08f282` → ~508):** fix-wave-1 `5a346faa` — `on mount`/`on dismount` (~52) ·
`const @name` derived-decl (F5 CLOSED, `parseConstAtStateDecl`) · deepset/array-mutation node-synth + destructured-param +
var-decl typeAnnotation thread (translate-stmt.js) · NEW exprText-backfill walker (GROUP W). fix-wave-2 `cc69c62d` —
BlockStub `verbatim`-body recovery (the Mario match-arm/lambda-block fix, +17). Triage workflow: `docs/changes/native-parser-*-2026-06-06/`.
**NEXT-PICK (re-triage before dispatch — re-run the flip harness for current counts):** F4 formFor expansion (~32, was largest open) ·
F2 top-level server-fn `?{}` body-drop + assign-RHS `@x = ?{}.all()` · F6/F9 fn-param / export-fn-body ·
`effect=` opener (§51.0.H Form 3, small) · mario PowerUp payload-enum · F7 missing-diagnostics (lower-leverage).
**S170 DEFERRED (pre-existing native divergences surfaced, NOT introduced):** code-position match-BLOCK promotion for
render arm bodies · engine-write-guard routing for match-as-statement over an engine cell · single-word bare-display-text
silent-drop tokenizer bug (filed) · FIX 4 `export <cell>` (pending SPEC ruling). **AVOID single-dispatch:** enum-subset
struct-ctor `Type { field: val }` (multi-stage) + r24-bug-31 if-as-expr/`<state>` block (multi-gap, decompose first).
**F8 await = stdlib-migration, NOT native.** A fix-dispatch agent MUST re-run the flip harness to re-rank before picking — the ~508 figure is an estimate (S170 closures applied to the 605 re-measure; no post-wave-2 full re-measure ran).

**S212 Source Changes (`a9c2108f` — 2026-06-21; flogence dogfood bugs; ZERO new domain concepts / AST shapes / diagnostic codes):**

Three compiler-internal codegen behavior fixes. No new pipeline concepts. Updated codegen-emit-map file rows below.

- **`codegen/collect-class-names.ts`** — Tailwind class-name AST walker gains `"match-block"` branch
  (walks `bodyChildren` + `armBodyChildren`) and `"each-block"` branch (walks `bodyChildren`). Mirrors
  the engine-decl arm-descent precedent. Closes g-tailwind-not-scanned-in-match-arms; ZERO new domain
  concepts (the Tailwind class-scan domain row and §26.1 contract are unchanged — this is coverage
  expansion, not a new concept).
- **`codegen/emit-each.ts`** — nested `<each>` reactive subscription fix (Approach C). Tier-1 and
  Tier-0 nested-each branches both wrap the inner source-read + reconcile in `_scrml_effect`; uses
  the same `_scrml_effect` reactive-subscription primitive documented in the per-item-reactivity
  concept row. No new concept; the nested-each-reactive-subscription fix is an implementation
  correctness fix for an existing concept (each-per-item reactivity, §17.7.3).
- **`codegen/scheduling.ts`** — lift batching transitive-dep closure + reassigned-`let` gate. Uses
  the existing body-DG (`BodyDG` / `buildBodyDG`) and lift-concurrent scheduler domain. No new
  concept; `collectReassignedNames` + `depClosure` are scheduling-internal helpers, not new
  pipeline concepts.
- **`body-dg-builder.ts`** — `collectLambdaBodyReads` NEW helper descends lambda bodies for free-read
  collection (`collectExprFacts` now calls it after `forEachIdentInExprNode`). Companion to the
  scheduling.ts transitive-dep fix. No new domain concept; reinforces the existing body-DG §19.9.9.1
  conservative-over-approximate edge doctrine.

**domain.map watermark advanced `6d8a47ab`→`a9c2108f`.**

**S212 A2 W4 + flogence Bug A/B (`a9c2108f`→`8569f774`): ZERO new domain CONCEPTS / AST shapes. The three
changes are codegen-internal behavior fixes and a new Info-lint:
- A2 W4 (`914029dc`): `emitRequestNode` api= mode + `ApiEndpointForEmit` registry + `emitApiUrlExpr`
  + automatic `parseVariant` decode for ENUM ResponseT + `W-API-RESPONSE-NOT-VARIANT` honesty lint.
  Codegen-internal. No FileAST shape changes; no new pipeline stage.
- flogence Bug A (`3d311fc9`): bare-ref event handler wiring (`bareRefHandler` flag in binding-registry +
  emit-html.ts routing + emit-event-wiring.ts direct-listener path). Codegen-internal.
- flogence Bug B (`93e02b35`): reactive class:/attr-template directives in `<match>` arm bodies now
  wire their `_scrml_effect` per-arm-mount. Root: collect.ts:88 `collectMarkupNodes` descends only
  `node.children` not arm bodies — the fix is in emit-html.ts arm-context registration +
  emit-bindings.ts shared helpers + emit-variant-guard.ts `emitArmWireFunction` directive loop.
  Codegen-internal. Note the root site (collect.ts:88) in the structure map if investigating similar
  arm-body reachability gaps.
domain.map watermark advanced `a9c2108f`→`8569f774`.**

## Tags
#scrmlts #map #domain #compiler #pipeline #reactive #state-machine #scrml #match-arrow #engine-graph #source-map #cross-file-modules #each #each-in-dynamic-context #engine-statechild #enum-subset #message-dispatch #bug60 #bug62 #bug63 #bug64 #bug65 #bug70 #bug71 #bug72 #bug73 #r28-1c #per-item-reactivity #live-keyed #colon-shorthand-html #s149 #s151 #s152 #s153 #s154 #s155 #s156 #s157 #s158 #s159 #native-parser #native-parser-swap #each-promotion #match-promotion #flip-failure-families #f1-engine-substrate-closed #engine-substrate-fix #machinedecls-instance-share #b2-message-arm-closed #native-exprnode-walker #f2-match #promote-each #typed-atcell #server-fn-star #bare-function-failable #cross-file-export-bodystart #flip-605 #flip-508 #deepset-write-loss #reactive-nested-assign #reactive-array-mutation #cycles-prereq #cow-all-bracket-write #structural-eq-cycle-guard #value-native-maps #spec-section-59 #s160 #s161 #s162 #s163 #s164 #s165 #s166 #s167 #s168 #s169 #value-native-maps-implemented #set-algebra #scrml-data #value-canonical #bug-b-structural-compound-deepset #stamp-compound-deepset-targets #on-mount-dismount #const-at-derived-decl #f5-closed #deepset-node-synth #exprtext-backfill-walker #blockstub-verbatim-body #mario-match-arm-fix #s170 #s173 #e-export-001 #w-type-fn-field #s174 #log-builtin #location-transparent-logging #w-log-shadowed #e-type-any-forbidden #no-any-hard-line #production-strip #log-loc #s175 #typed-sql-row #sql-projection #width-subtyping #e-sql-row-contract-mismatch #w-sql-row-untyped #e-struct-function-field #function-boundary #passed-vs-stored #f-schema-001 #fn-return-inference #flagship-typed-data #limit-the-primitive #s176 #e-type-unknown-name #unrecognized-type-name #w-pure-deprecated #pure-deprecation #migration-3 #scrml-math #scrml-random #scrml-time-now #capability-scoped #non-deterministic #e-fn-004 #scalar-vocabulary #dd1-fork1 #s177 #g-formfor #markup-expansion-in-arms #arm-body-children #match-arm-body #r27-c6 #formfor-in-arms #tablefor-in-arms #s191 #tailwind-composing-families #approach-c #inline-var-fallback #no-preflight-block #register-ring #register-gradient #register-transform #register-filters #register-backdrop #box-shadow-compose #transform-compose #filter-compose #backdrop-compose #ring-shadow-setter #section-26-7 #section-26-7-1 #section-26-7-2 #section-26-7-3 #if-fn-condition #call-ref-conditional #g-attr-if-fn-call-misroute #g-attr-if-fn-display-not-mount #g-attr-if-fn-chain-head-call-misroute #update-chain-call-ref #reactive-conditional-not-event #section-5-1 #s200 #g-each-component-body-invalid-js #g-each-peritem-if-predicate #lower-each-expr #serialize-call-args-lowered #s201 #markup-as-value #markup-value-expr #pillar-1 #section-1-4 #section-7-4 #emit-markup-value-expr #markup-to-dom-node-iife #scrml-render-value #node-aware-display #ternary-markup-arm #return-markup #parse-expr-with-markup-values #saw-ternary-at-root #scan-shape12-decl-end #value-terminator-discriminator #markup-typed-derived #derived-chunk-treeshake #g-markup-value-ternary-fnreturn-codegen #g-each-body-bare-variant-arg #bare-variant-call-arg #g-nested-component-member-arg-misparse #normalize-tokenized-raw #member-access-collapse #s202 #each-inline-class-a #g-each-over-arm-payload-binding-unbound #arm-payload-binding #stamp-arm-payload-eaches #g-each-inline-component-prop-member-unsubstituted #g-inlined-component-root-class-interp-raw #substitute-interp-segments #class-merge-post-substitution #layer-2-string-literal-attr #build-match-arms #build-engine-arms #trucking-board-flagship #board-each-conversion #derived-filtered-cells #board-high-0 #s209 #ss2 #engine-statechild-grammar #ssot-dedup #w-engine-server-deferred #server-flag-bare #engine-server-deferred #boot-effect-write-validation #engine-shorthand-body-render #section-51-0-i #section-51-0-b-1 #block-analysis-d6 #import-inlined-channel-fns #s212 #g-tailwind-not-scanned-in-match-arms #each-block-body-children #match-block-arm-body-children #g-nested-each-no-own-subscription #nested-each-reactive-subscription #approach-c-inner-each #g-lift-concurrent-transitive-exclusion-tdz #transitive-dep-closure #dep-closure #collect-reassigned-names #decl-is-reassigned-later #reassigned-let-gate #collect-lambda-body-reads #lambda-body-free-reads #lin-scope-boundary #conservative-over-approximate #zero-new-domain-concepts #a2-w4 #w-api-response-not-variant #api-endpoint-for-emit #emit-api-url-expr #seq-guard #refetch #response-enum #parse-variant-decode-iife #section-60-5 #section-60-6 #g-bare-ref-event-handler #bare-ref-handler-flag #g-match-arm-reactive-attr-effects #class-directive #attr-template #arm-tagged-directive #emit-arm-wire-function-directive-loop #collect-markup-nodes-arm-gap #collect-ts-88 #shared-lowering-helpers

## Links
- [primary.map.md](./primary.map.md)
- [master-list.md](../../master-list.md)
- [pa.md](../../pa.md)
- [schema.map.md](./schema.map.md)
- [error.map.md](./error.map.md)
