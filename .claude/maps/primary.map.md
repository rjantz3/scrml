# primary.map.md
# project: scrmlts
# updated: 2026-06-21  commit: 8569f774

## Project Fingerprint
Language:   TypeScript / JavaScript (mixed; Bun runtime)
Framework:  Custom compiler pipeline (no web framework)
Runtime:    Bun >=1.3.13
Type:       CLI compiler + language toolchain (single-file full-stack web language compiler)
Size:       ~1470 source files (992 test .test.js by find at 471cbb34; +5 S196 render-expr-primitive + 4 render-expr prereq-bug unit; +1 S195 match-arm-void-element-scanner; was 984 at 0cafe665 +8 S192 + ~150 compiler/src [116 .ts incl. sql-projection.ts + engine-varname.ts [NEW S192] + native-walker/exprtext-backfill-walker.ts + codegen/log-loc.ts + 34 .js] + 37 native-parser .js + stdlib [21 module dirs; +S176 `math` pure + `random` capability-scoped non-det] + lsp; +2 S198-S199 engine-hydration unit [initial-cell 16 / server-source 18]; +3 S200 each-component-helper / each-peritem-predicate browser → find-count 997 at b1f5f8bf; +3 S201 markup-value/each-variant/member-arg [markup-value-render.browser, g-markup-value-in-expression, g-nested-component-member-arg] → **find-count 1000 at `fa2edccf`** [verified]; +4 S202 .test.js [each-over-arm-payload-binding-unbound.browser, g-each-inline-prop-member.browser, e2e-render-map, detector-validation] → **find-count 1004 at `60d547e1`** [verified `find compiler/tests -name '*.test.js' | wc -l` = 1004; repo-wide all-suffix = 1019]; S202 ALSO added 11 NEW source files [4 .test.js + 6 e2e-render-map support .js (render-corpus-enumerator/render-detectors/render-harness/generate-baseline/observe-one/seed-fixtures) + NEW scripts/flograph.ts] + 3 fixtures + e2e-render-map-baseline.json, so the source-file total moves to ~1479; **+1 S204 [control-flow-in-markup-reject unit 11 cases] → find-count 1005 at cc765a5a** [verified]; **+2 S205 [emit-string-tree-precedence.test.js unit 14 / g-match-alternation-value-vs-derived.test.js unit 11] → find-count 1007 at 492b4bb9** [verified]; **+2 S206 [g-colon-shorthand-markup-misparse.test.js unit 12 / engine-autodecl-bare-variant-write.test.js unit 10] → find-count 1009 at 359a1d83** [verified]; **+2 S206-block-analysis-emit [block-analysis-footprint.test.js unit 13/40; block-analysis.test.js unit 16/69] → find-count 1011 at d12fdef7** [verified]; **+2 S207 [g-each-peritem-attr-ternary-quoted-arms.test.js unit ~38; emit-block-analysis-integration.test.js integration ~77] → find-count 1013 at c553dd84** [verified])
Version:    v0.7.0 (project-tracked; compiler/package.json reads 0.2.0 — subpackage drift, ignore)

## Map Index

| Map                  | Status  | Contents                                                      |
|----------------------|---------|---------------------------------------------------------------|
| structure.map.md     | present | directory layout, entry points; S148-S210 source changes + **"Key S212 Source Changes"** (`8569f774`): A2 W4 `emitRequestNode` api= branch + `buildApiEndpointRegistry` + `emitApiUrlExpr` + `emitParseVariantDecodeIIFE` wiring (emit-reactive-wiring.ts +201L + emit-parse-variant.ts +24L) + `W-API-RESPONSE-NOT-VARIANT` honesty lint (type-system.ts +44L) + flogence Bug A bare-ref event wiring (`bareRefHandler` flag, emit-html.ts +23L + emit-event-wiring.ts +20L + binding-registry.ts +13L) + flogence Bug B arm-body class:/attr-tpl `_scrml_effect` (emit-html.ts +41L + binding-registry.ts +35L + emit-bindings.ts +93L + emit-event-wiring.ts +7L + emit-variant-guard.ts +52L); +1 NEW Info code (W-API-RESPONSE-NOT-VARIANT); flogence Bug A/B carry ZERO new codes; PRIOR S212: collect-class-names.ts `match-block`/`each-block` body-descent (Tailwind scan) + emit-each.ts nested-each `_scrml_effect` subscription (Approach C, Tier-1+Tier-0) + scheduling.ts transitive-dep closure + reassigned-`let` gate + body-dg-builder.ts `collectLambdaBodyReads` lambda-free-read collector; ZERO new codes (prior batch); **"Key S211 Source Changes"** (lint-w-interp-in-raw-content NEW 207L ss11/A2 W3 api-decl typer +346L / ss7 emit-logic tweak +4L); **"Key S210 Source Changes"** (engine-name-dual-table-fix + codegen-interp-literal-serializer + A2 `<api>` parser + ss2 derived-engine-crash + ss3 paren-grouping/sigil + ss8 tailwind arbitrary-string/ring-offset); S148-S189 source changes + NEW "Key S190 Source Changes" (`1e17213e`); §51.0.J derived-engine expression form; E-ENGINE-004 §51.0.J steer + §52.1 cross-ref + NEW "Key S191 Source Changes" (`7f2092cf`): Tailwind composing-utility families + if=fn() condition routing + NEW "Key S192 Source Changes" (`0cafe665`): engine-varname.ts; SYM PASS 1.d; W-CONST-AT-DEPRECATED; E-STATE-UNDECLARED + NEW "Key S194 Source Changes"; §52 server-authority Tier-1 codegen + NEW "Key S195 Source Changes"; GAP-A void-element arm-closer fix + NEW "Key S196 Source Changes"; `<render of=X/>` structural element + NEW "Key S198-S199 Source Changes"; engine-hydration arc + NEW "Key S200 Source Changes"; CE helper-export hoist + nested expression-prop substitution + NEW "Key S201 Source Changes"; markup-as-value in EXPRESSION position + NEW "Key S202 Source Changes"; each-inline Class-A arc + e2e-render-map test-infra + NEW "Key S204 Source Changes" (`a6405053`): E-CONTROL-FLOW-IN-MARKUP + NEW "Key S205 Source Changes"; precedence-aware parens + g-match-alternation-value-vs-derived + NEW "Key S206 Source Changes" (`e2516298`/`105f1ee4`/`e1c20e3a`): g-colon-shorthand-markup-misparse + g-engine-autodecl-bare-variant-write + block-analysis D1+D2 + NEW "Key S207 Source Changes" (`fee6fc98`/`736bdf33`/`a6e64126`/`36e022bc`): D3 --emit-block-analysis + g-each-peritem-attr-ternary-quoted-arms tokenizer fix + D5 span.endLine collapse fix + **g-compound-field-render-by-tag-unexpanded RESOLVED** (emit-html.ts:1628/:1641 + symbol-table.ts:11783 `lookupCompoundMembersByLeafName` NEW + dependency-graph.ts:2295/:2596 `compoundMemberToParent` + render-by-tag-compound-member-non-lexical.test.js 11 cases); ONE new error code (E-CELL-AMBIGUOUS-MEMBER-RENDER). +3 test files (find-count **1014 at d931f8be**); **+NEW "Key S208 Source Changes" (`9afc746e`): Fix A g-pure-module-server-emit tree-shake (emit-server.ts sentinel+prune, +test) + Fix B W-SERVER-IMPORT-UNEMITTED (api.js:2077 cross-file invariant, +test); find-count **1016 at `9afc746e`** (verified)** |
| dependencies.map.md  | present | 9 packages (3 runtime root + 2 compiler + 4 devDeps), internal graph; **S212: emit-reactive-wiring.ts now imports codegen/emit-parse-variant.ts (`emitParseVariantDecodeIIFE` for api= response decode); emit-html.ts routes bare-ref on*= to `addEventBinding({bareRefHandler:true})`; NEW emit-html.ts / emit-event-wiring.ts / emit-bindings.ts / emit-variant-guard.ts / binding-registry.ts rows for arm-tagged directive bindings**; **S211: NEW `lint-w-interp-in-raw-content.js` standalone edge + `api.js` → lint import (Stage 2.5) + `type-system.ts` A2 W3 checkApiDeclarations note**; **S209 ss2 item 3: NEW `engine-statechild-grammar.ts` + 2 edges**; **S210 ss3: NEW `codegen/emit-expr.ts` + `codegen/rewrite.ts` rows in internal graph (paren-grouping, no new packages)** |
| schema.map.md        | present | ~47 AST node types + `acceptsType` on EngineDeclNode (S154) + `subsetVariants` on PredicatedType (S156) + MessageArmEntry + EnumSubsetParse (S155-S156) + `EachEngineCtx` + `EachReconcileCtx` (S156-S159) + `matchExpr` side-field on ReactiveDeclNode (S157 Bug 71) + `MapLitExpr`/`MapEntry` ExprNode + `MapType` ResolvedType (§59, S169) + native `makeFunctionDecl` modifiers `{canFail,errorType}` (S166); **S172 `inlineMatchArmArrows` engine-decl stamp; S173/S174 add NO new AST shapes — E-EXPORT-001/W-TYPE-FN-FIELD (S173) + W-LOG-SHADOWED/E-TYPE-ANY-FORBIDDEN (S174) are decl-site/use-site scans over EXISTING nodes; the only S174 type is the codegen-internal `LogLocSpan` interface (log-loc.ts:32, resolver input not a FileAST shape); NO `AnyType` member in ResolvedType (`any` is not a scrml type); **S175 adds NO new FileAST node shapes — typed-SQL-row scans existing nodes + raw SQL text. NEW type-system-internal `ResolvedType` members: `FunctionType` (type-system.ts:324, distinguishable fn-type kind, NOT `asIs`; via `tFunction()` :830) + the `<fn-return>` over-approximation sentinel (`FN_RETURN_TYPE_NAME` :631); sql-projection.ts exports `ProjectedColumn`/`SelectProjection` interfaces (extractor I/O, not FileAST); protect-analyzer.ts `ColumnDef` interface (F-SCHEMA-001 source)**; **S177 ONE new walkable field: `match-block.armBodyChildren` (+ companion `_matchArmBodyForm`) — walkable per-arm body markup AST re-parsed from `armsRaw` for the g-formfor markup-expansion passes; LIVE-only; no new node TYPES** |
| config.map.md        | present | 4 env vars, 3 config files (HEADER STALE — last refreshed 948d3f2f) |
| build.map.md         | present | 12 npm scripts, maintenance scripts, pre-commit hook; **S174: new "`scrml compile` Flags" table including `--production`/`--prod` (§20.6.5 log() strip)** |
| error.map.md         | present | 398+ error codes (**S212 A2 W4: +1 NEW W-API-RESPONSE-NOT-VARIANT Info §60.5/§34 [type-system.ts:18189]; flogence Bug A/B carry ZERO new codes**); (**S211: +1 NEW W-INTERP-IN-RAW-CONTENT Info §4.17 [lint-w-interp-in-raw-content.js]; +3 NEW E-API-PATH-PARAM-UNBOUND / E-API-ENDPOINT-UNKNOWN / E-API-REQ-SHAPE-MISMATCH Error §60.2/§60.4 [type-system.ts:18185/18283/18314]; all A2 W3 — ss11 item 7 + ss7 emit-logic tweak carry ZERO new codes**); (S210 A2: +4 NEW E-API-* codes — E-API-BASE-MISSING / E-API-RESPONSE-TYPE-UNDECLARED / E-API-METHOD-INVALID / E-API-ENDPOINT-MALFORMED, §60.9, all Error; engine-name-dual-table/interp-literal/comment-opacity/tokenizer carry ZERO new codes); (S207: +1 E-CELL-AMBIGUOUS-MEMBER-RENDER); S154-S160 codes (+E-COLON-SHORTHAND-ON-VOID, +W-COLON-SHORTHAND-LEGACY-PLACEMENT, +E-REFINEMENT-NO-DEFAULT, +4 engine-message codes, +E-MATCH-SUBSET-DEAD-ARM); S169 +7 §59 value-native-map codes (IMPLEMENTED, fire sites live); S170 NO new codes (Bug B mistarget + native parity resolutions); **S173: +2 (E-EXPORT-001 Error §21.2 reactive state-cell export reject [module-resolver.js:301]; W-TYPE-FN-FIELD Info §14.3 fn-typed struct field — **RETIRED/ESCALATED S175**); S174: +2 (W-LOG-SHADOWED Info §20.6.7 user-`log` shadow nudge [type-system.ts:3620]; E-TYPE-ANY-FORBIDDEN Error §14.1.1 literal `any`-token reject [type-system.ts:3720]); **S175: +3 (E-STRUCT-FUNCTION-FIELD Error §14.3/§15.11 fn-typed struct field HARD reject [type-system.ts:3497, escalated from W-TYPE-FN-FIELD]; E-SQL-ROW-CONTRACT-MISMATCH Error §14.8.8 SQL-row → `:struct` width-subtype miss [type-system.ts:9491/9629]; W-SQL-ROW-UNTYPED Info §14.8.7 untyped SQL-row long-tail [type-system.ts:5676]) + S175 Key New Codes block; **S176: +1 (E-TYPE-UNKNOWN-NAME Error §14.1.2 unrecognized type name [type-system.ts:4232], the symmetric companion to E-TYPE-ANY-FORBIDDEN — closes the S174 deferred follow-on) + W-PURE-REDUNDANT renamed to W-PURE-DEPRECATED [type-system.ts:7597] + E-FN-004 GENERALIZED to imported non-det stdlib bindings [type-system.ts:6589/6598/17511] + S176 Key New Codes block; **S177: ZERO new codes (refinements only) — E-CLOSER-001 fires on `/>`+`:`-shorthand (bug-74), E-SYNTAX-050 bare-`/` no over-fire before close tag (bug-4), bogus E-SCHEMAFOR-NO-SQL-MAPPING prevented (r28-7b); + S177 Fix Notes subsection****; **S181: +1 (W-DISPLAY-TEXT-OVERQUOTE Info §4.18.7 nested-plain-markup over-quote, the INVERSE of E-UNQUOTED-DISPLAY-TEXT [type-system.ts checkDisplayTextOverquote :3949, type pass :17006]) + deprecated-`server function` diagnostic reword (E-FN-004/E-CG-006/W-LINT-019, no code change) + S181 Key New Codes block**; **S182: +1 (E-ENGINE-EFFECT-NOT-INTERPOLATED Error §51.0.B/§51.0.H+§34 — engine opener Form-3 boot-effect AND state-child Form-1 `effect=` bare/non-`${...}` value, previously null→silently tree-shaken; fires at SYM PASS 10.A opener + PASS 17 state-child [symbol-table.ts:5463]) + E-ENGINE-VAR-DUPLICATE/E-ENGINE-003 double-fire DE-DUPED (symmetric `legacyMachineKeyword` gate, no code change) + S182 detail block**; **S183: +2 (E-FORMFOR-NOT-IMPORTED Error §41.14.1+§34 + E-TABLEFOR-NOT-IMPORTED Error §41.16.1+§34 — unimported `<formFor>`/`<tableFor>` L22 markup element previously SILENTLY forwarded as a literal tag; fire from the NEW shared helper scanForUnimportedTypeDataElement [type-system.ts:15830], else-arm of the import-locals expansion gates) + W-TAILWIND-001/W-TAILWIND-UNRECOGNIZED-CLASS dynamic-class-fragment precision fix (NO new code, §26.5.1) + S183 detail block**; **S184: ZERO new §34 codes (FP-suppressions / scope fixes on existing codes) — E-STRUCT-FUNCTION-FIELD no longer fires on a comment-bearing lifecycle field (collectBracedBody COMMENT-skip [ast-builder.js:3346]); E-TYPE-001 double-fire de-duped (statementText normalizeKey [type-system.ts:18844]); W-LINT-007/004/021 exempt the §16.6 snippet-fill `prop={ (p) => <markup> }` [lint-ghost-patterns.js:613]; E-VARIANT-AMBIGUOUS recovered on a Shape-1 variant-lifecycle initializer via inferEnumFromVariantLifecycleAnnotation [type-system.ts:19782]; payload-binding Gaps 1+2 bind `!{}` multi-field + block-form `<match>` arm payloads [type-system.ts:199/9354/10253]; W-EACH-PROMOTABLE skips `_tableForSynth` for-stmts [lint-w-each-promotable.js:211]) + S184 detail block**; **S185: +1 (E-VALIDATOR-INLINE-COLON Error §55.10/§41.12+§34 — validator inline-message COLON form `<name req:"…">` reject + paren-form recovery; fired+recovered by tryRecoverColonInlineMessage in scanStructuralDeclLookahead [ast-builder.js]; kills the misleading downstream E-SCOPE-001 on the corrupted cell) + errarm re-fail-from-arm 2-layer reuses E-ERROR-001/E-SCOPE-001 (NO new code — `fail` inside `!{}` / `<match>` arm lowers to a function-exiting error envelope via emitFailExpr; NS-1 fires E-ERROR-001 in a non-`!` fn; the spurious E-SCOPE-001 on the `fail` keyword removed) + S185 detail block**; **S186: +0 codes (channel codegen fix — Bug1 reconnect bare-int reuses an E-SCOPE-001 visitAttr EXEMPT for reconnect/channel-reconnect; Bug2 onserver/onclient wiring reuses §12.2 Trigger-7 / E-CG-002 — E-CG-002 no longer false-fires on a route-less isChannelWsHandler server fn) + S186 detail block**; **S188: +1 (E-ATTR-UNQUOTED-OPERATOR Error §5.1/§5.2/§17.1+§34 — bare binary/ternary operator in an unquoted `if=`/`show=`/`else-if=` condition; cluster-A "reject + parens"; fires ONCE per attr + recovers `absent`) + E-TYPE-045 ENFORCEMENT BROADENED to ALL expression positions + the BARE form (`not @x`) via a lowering-choke-point stamp harvested by the NEW TS-J pass harvestNotPrefixNegation [type-system.ts:~15270] — the former if/while-only paren-only checkNotPrefixNegation RETIRED; g-division-in-ternary-arm added NO new code (reuses E-CODEGEN-INVALID-JS prevention) + S188 detail block**; **S189: +2 (E-SYNTAX-045 Error §42.2.3+§34 — `given name = <expr>` rebind reject at BOTH given-guard parse sites [ast-builder.js:6682/:10899], adjacent to E-SYNTAX-044; E-CHANNEL-SERVER-CELL-READ Error §38.4/§38.6.1+§34 — server-context channel fn reading a client-held channel cell, change-id channel-cell-write-client-side-A RULING A, walker detectServerContextChannelCellRead [route-inference.ts:1525], fires :3423) + §12.2 Trigger 7a DROPPED (channel-cell-WRITE no longer escalates; Trigger 7b now broadcast()/disconnect()-only) + S189 detail block**; **S190: +1 (E-DECL-RHS-INTERP-WRAPPED §6.2/§34 — `${}`-wrapped decl RHS reject+recover) + E-ENGINE-004 message improved (plain-cell derived=@var steers to §51.0.J) + S190 section**; **S191: ZERO new codes (Tailwind composing families §26.7.x = recognition coverage so W-TAILWIND-UNRECOGNIZED-CLASS no longer fires on ring/shadow/gradient/transform/filter/backdrop tokens; if=fn() = a codegen routing fix, no diagnostic — bare-call `if=fn()`/`show=fn()`/`else-if=fn()` routes as a reactive conditional) + S191 section**; **S192: +2 (W-CONST-AT-DEPRECATED Info §6.6.1 legacy `const @name` deprecation [type-system.ts:8688]; E-STATE-UNDECLARED Error §6.1.2 undeclared-cell read [type-system.ts:6282, post-CE relocation, bug-12-vkill]) + NEW engine-varname.ts canonical §51.0.C module + ref-binding SYM registration (PASS 1.d) + S192 detail block**; **S194: +0 NEW codes (§52 server-authority disposition shift) — W-AUTH-001 now SUPPRESSED for a Tier-1 server-authority instance carrying serverAuthorityTable (compiler-generated SELECT* load) [type-system.ts:9037-9042]; W-AUTH-002 NARROWED to the §52.8 SSR-pre-render residual [type-system.ts:7972] (was the Tier-1 interim "not yet wired" warning); the deleted emitServerSyncStub/emitOptimisticUpdate carried no diagnostic**; **S195: ZERO new codes (GAP-A void-element arm-closer fix `f563bc89` REMOVES two misleading false-fires — bare §24 void in a `<match>`/`<each>` arm body no longer surfaces a spurious E-CTX-001 "Unclosed `<match>`" / E-MATCH-PARSE-001) + 8 NEW §S195 known-gaps tracked (HIGH g-failable-arm-nested-constructor-crash [emit-logic.ts ~520-565], HIGH g-each-body-bare-variant-arg [emit-each.ts], MED g-held-error-display [RATIFIED render-expression primitive, build-pending], MED g-engine-autodecl-bare-variant-write, MED g-shorthand-interp-match-arm-codegen, MED g-match-arm-apostrophe-bs, LOW g-blocksplitter-comment-span-not-opaque, LOW g-each-body-sigil-invariant-classifier) + S195 section**; **S196: +4 (E-RENDER-NO-OF / E-RENDER-NO-CLAUSE / E-RENDER-NOT-ENUM Error §19.15.3 — the `<render of=X/>` render-expression exhaustiveness fence [type-system.ts:7692/:7729/:7744 annotateNodes]; E-MATCH-ARM-MARKUP-IN-VALUE Error §18.0 — a JS-style value-`match` arm whose body is a MARKUP element, early typer steer [type-system.ts:13446 checkMatchDiagnostics]) — `471cbb34` (3 render codes) / `fcdec43c` (the steer prereq) + S196 section** **S198: +1 (E-ENGINE-INITIAL-BOTH-FORMS §51.0 — A-leg `initial=.Literal` vs `initial=@cell` mutual-exclusion; A-leg also REUSES E-ENGINE-INITIAL-CELL-UNDECLARED/-TYPE/-INVALID-VARIANT); S199 E-leg: +3 (E-ENGINE-SERVER-WITH-DERIVED, E-ENGINE-SERVER-WITH-INITIAL-CELL §51.0.E mutual-exclusions; W-ENGINE-SERVER-SOURCE-NOT-AUTHORITATIVE Info §52.4.4 — `server=@source` reactive server-authoritative; reuses A-leg E-ENGINE-INITIAL-CELL-* + -INVALID-VARIANT on bare-root, suppresses W-ENGINE-INITIAL-MISSING) + S198-S199 section** **S201/S202: ZERO new codes — both are codegen/render fixes (markup-value expression-position lowering S201; the each-inline Class-A arc + arm-payload `<each>` binding + CE markup-attr substitution S202; the S202 e2e-render-map test capability + flograph tooling carry no compiler diagnostics — error map NOT regenerated, count UNCHANGED at 387+)**; **S204: +1 (E-CONTROL-FLOW-IN-MARKUP Error §17.4/§7+§34 — bare `for`/`if`/`while` control-flow statement leading a text run in a markup body, no `${}` wrapper; `BARE_CONTROL_FLOW_IN_MARKUP_RE` + `liftBareDeclarations` detector [ast-builder.js:1517]; reject+recover — ships NEITHER `for(){}` NOR `${}`; sibling of E-UNQUOTED-DISPLAY-TEXT per S111; descriptive §34 code; SPEC §34 row after E-CTRL-011 + §17.4 normative note)**; **S206: ZERO new codes** (both MED behavioral fixes — g-colon-shorthand-markup-misparse [block-splitter.js angleDepth + COMPOUND_LIFT_EXEMPT_TAGS] + g-engine-autodecl-bare-variant-write [type-system.ts return-stmt comparison pre-pass] carry no new §34 entries; existing E-COLON-SHORTHAND-ON-VOID + E-VARIANT-AMBIGUOUS surface unchanged); **S208: +1 W-SERVER-IMPORT-UNEMITTED (api.js:2077, Info, non-fatal; Fix A zero new codes)**; **S209 ss2: +1 W-ENGINE-SERVER-DEFERRED (Warning, §51.0.A — bare `server` flag on `<engine>` non-operational, steers to `server=@source`; fires symbol-table.ts `validateEngineStateChildrenAndRules` Step 2.5b; ss2 items 1/4/5 are ZERO new codes — boot-write-validate reuses E-ENGINE-INVALID-TRANSITION, shorthand-body fix reuses existing emit paths, D6 block-analysis import-inlined-channel-fn skip has no diagnostic)** |
| test.map.md          | present | bun:test, **1034 .test.js files at `8569f774`** (+2 S212 flogence/W4: g-bare-ref-event-handler.browser [browser, 8 cases], g-match-arm-reactive-attr-effects.browser [browser, 8 cases]; api-decl-typer.test.js EXTENDED +3 W-API-RESPONSE-NOT-VARIANT cases); was **1032 at `a9c2108f`** (+3 S212 prior batch: g-tailwind-markup-block-scan [integration], g-nested-each-no-own-subscription [browser], lift-concurrent-transitive-tdz [unit]); was **1029 at `6d8a47ab`**; was **1027 at `0a605d3e`** (+2 S211: lint-w-interp-in-raw-content.test.js unit + api-decl-typer.test.js unit; ss3: +2 [g-paren-receiver-group-dropped.test.js integration + each-sigil-expr-parser.test.js unit]; A2 W2 api-decl-parser.test.js already counted in prior watermark); was **1024 at `5c68e87e`** (verified find at 0cafe665 = 984; +2 S190 [derived-engine-expression-form 16, cluster-c-decl-boundary 19]; +4 S191 [attr-if-fn-call-conditional 12, attr-if-fn-condition-followup 5, bug-1-tailwind-filter-family 28, bug-1-tailwind-gradient-family 14] + extended ring/transform/minor/unrecognized/arbitrary-emit/tailwind-classes; +8 S192 [const-at-deprecated-lint 15, v-kill-readside-undeclared 6, ref-binding-sym-registration 6, state-block-bare-write-decl-lint 10, engine-binding-b14 49, native-reactive-write-deepset-mutation, parser-conformance-canary extended]; **+1 S195 [match-arm-void-element-scanner 13 cases / 46 expects]; +5 S196 [render-expr-primitive integration 6; g-failable-arm-nested-constructor-crash 6, g-shorthand-interp-match-arm-codegen 8, g-match-arm-apostrophe-bs 8, h1-steer-markup-in-value-match 8 unit]; find-count = 992 at 471cbb34**) **+2 S198-S199 [engine-hydration-initial-cell 16 cases A-leg / engine-hydration-server-source 18 cases E-leg]; trucking-dispatch-smoke +7L HOS-showcase E-leg dog-food; find-count = 994 at `76d03aa9`** **+3 S200 [g-each-component-helper-hoist browser, g-each-component-transitive-helper browser, g-each-peritem-if-predicate browser]; find-count = 997 at `b1f5f8bf`** **+3 S201 NEW .test.js [markup-value-render.browser.test.js, g-markup-value-in-expression.test.js, g-nested-component-member-arg.test.js] + each-block.test.js EXTENDED (+48); verified `find compiler/tests -name '*.test.js' | wc -l` = 1000 at `fa2edccf`** **+4 S202 NEW .test.js [each-over-arm-payload-binding-unbound.browser 10, g-each-inline-prop-member.browser 14, e2e-render-map 4-describe, detector-validation 3-describe] + the NEW `compiler/tests/e2e-render-map/` test-infra capability (D0–D7 render detectors + baseline allowlist); verified `find compiler/tests -name '*.test.js' | wc -l` = **1004 at `60d547e1`**; test.map body REGENERATED this pass (NEW "S202 New Test Files" section + the S198-S202 carry-forward in the Total row)**; **+1 S204 [control-flow-in-markup-reject.test.js unit 11 cases (6 reject-fire / 2 recover-no-double / 3 canonical-clean)]; verified `find compiler/tests -name '*.test.js' | wc -l` = **1005 at `cc765a5a`** **+2 S205 [emit-string-tree-precedence.test.js unit 14; g-match-alternation-value-vs-derived.test.js unit 11]; verified `find compiler/tests -name '*.test.js' | wc -l` = **1007 at `492b4bb9`** **+2 S206 [g-colon-shorthand-markup-misparse.test.js unit 12; engine-autodecl-bare-variant-write.test.js unit 10]; verified `find compiler/tests -name '*.test.js' | wc -l` = **1009 at `359a1d83`**; **+2 S206-block-analysis-emit [block-analysis-footprint.test.js unit 13/40; block-analysis.test.js unit 16/69]; verified `find compiler/tests -name '*.test.js' | wc -l` = **1011 at `d12fdef7`**; **+2 S207 [g-each-peritem-attr-ternary-quoted-arms.test.js unit ~38 (tokenizer fix regression); emit-block-analysis-integration.test.js integration ~77 (D3+D5 R26 anchor)]; verified `find compiler/tests -name '*.test.js' | wc -l` = **1013 at `c553dd84`** **+2 S208 [g-pure-module-server-emit.test.js integration 145L (p1 prune / p2 no-over-prune); w-server-import-unemitted.test.js integration 130L (missing-export / client-only-no-fire / missing-file / non-fatal-partition)]; find-count = **1016 at `9afc746e`****; **+4 S209-ss2 NEW .test.js [engine-opener-effect-c1.test.js EXTENDED +79L §2; engine-server-flag-deferred.test.js NEW 223L §1+§2; engine-statechild-grammar.test.js NEW 71L; engine-shorthand-body-render.test.js NEW 288L §1-§3] + emit-block-analysis-integration.test.js EXTENDED +41L D6; verified `find compiler/tests -name '*.test.js' | wc -l` = **1020 at `b67cd6e6`****; **+4 S210 NEW .test.js [engine-name-dual-table.test.js integration; engine-statechild-comment-opacity.test.js unit; g-attr-interp-fn-name-not-renamed.test.js unit; g-literal-arg-expr-serializer-wrong-span.test.js unit]; verified `find compiler/tests -name '*.test.js' | wc -l` = **1024 at `5c68e87e`** (full suite 24659/0)**; **+3 S210 A2/ss3 NEW [api-decl-parser.test.js unit; g-paren-receiver-group-dropped.test.js integration; each-sigil-expr-parser.test.js unit] + ss2 derived-engine-rejections.test.js EXTENDED + ss8 tailwind bug-1 files EXTENDED; find-count = **1027 at `0a605d3e`**** |
| domain.map.md        | present | 12-stage pipeline + sidecar, 30+ domain concepts incl. S153-S160 colon-shorthand/each-in-dynamic-context, S162-S170 native-parser-swap orientation block (re-measured 605→~508), S168 cycles-prereq, S169 §59 value-native-map IMPLEMENTED, S170 Bug B + set-algebra; **S173 E-EXPORT-001/W-TYPE-FN-FIELD concept rows; S174 `log()` location-transparent builtin concept (compile-certain side + dev unified-view + production-strip) + `W-LOG-SHADOWED` shadowing concept + `E-TYPE-ANY-FORBIDDEN` no-any-hard-line concept + invariants; S175 typed-SQL-row flagship concept (read-site typing T2a / prop-contract width-subtyping T2b / cell-contract T3b / fn-return inference T3c) + `E-SQL-ROW-CONTRACT-MISMATCH`/`W-SQL-ROW-UNTYPED` + function-boundary `E-STRUCT-FUNCTION-FIELD` (W-TYPE-FN-FIELD escalated) + passed-vs-stored + F-SCHEMA-001 + invariants; S176 unrecognized-type-name `E-TYPE-UNKNOWN-NAME` concept + `pure`-deprecation `W-PURE-DEPRECATED`/Migration-3 + scalar-vocabulary stdlib (`scrml:math` pure / `scrml:time.now()` + `scrml:random` capability-scoped non-det, E-FN-004 generalized) concept rows + capability/non-det/pure-deprecation invariants**; codegen each/match/engine emit map (+log-loc.ts + log() lowering sites); **S177 "markup-expansion-in-arms" (g-formfor) concept row + formFor/tableFor concept rows updated (expand inside engine state-children / match arms) + emit-match codegen row (armBodyChildren consumption + bug-48 resolveOnExpr)**; **S191: NEW Tailwind class-emit pipeline row (tailwind-classes.js composing families §26.7.x — registerRing/registerGradient/registerTransform/registerFilters/registerBackdrop + *_COMPOSE consts, Approach C inline-`var()`-fallback, NO preflight block) + if=fn() condition routing on the emit-html.ts/emit-event-wiring.ts codegen rows (NOTE: domain.map watermark advanced 1e17213e→7f2092cf; body still carries S177-era concept tags otherwise)**; **S200: +2 concept rows (g-each-component-body-invalid-js — CE helper-export hoist STEP1 + transitive helper-import STEP2-A + nested expression-prop STEP2-B; `<each>` per-item §42 predicate + `if=` conditional g-each-peritem-if-predicate C1+C2) + emit-each.ts (1928L) / component-expander.ts (4037L) file-table rows updated; domain.map watermark advanced 7f2092cf→`b1f5f8bf`**; **S201: +1 concept row (markup-as-value Pillar 1 §1.4/§7.4 in EXPRESSION position — g-markup-value-ternary-fnreturn-codegen RESOLVED) + 11 file-table line-counts/content rows updated (emit-each 1960L / emit-lift 2349L / emit-expr 1906L / emit-logic 4018L / emit-event-wiring / emit-client 2550L / runtime-template 4667L / ast-builder 16080L / block-splitter 3268L / component-expander 4057L / types/ast.ts 2073L); domain.map watermark advanced `b1f5f8bf`→`fa2edccf`** **S202: +1 concept row (the each-inline Class-A arc — CE markup-attr prop substitution + LAYER-2 lift + arm-payload `<each>` binding, g-each-inline-component-prop-member-unsubstituted + g-inlined-component-root-class-interp-raw + g-each-over-arm-payload-binding-unbound) + emit-each (2149L) / emit-match / emit-engine (4647L) / component-expander (4196L) / emit-lift codegen-map rows updated; domain.map watermark advanced `fa2edccf`→`60d547e1`**; **S209 ss2: +1 W-ENGINE-SERVER-DEFERRED diagnostic code row + NEW `engine-statechild-grammar.ts` Pipeline Source Files table row (SSOT for ENGINE_STATE_CHILD_RESERVED_ATTRS + STATE_CHILD_STRUCTURAL_TAGS); ZERO new domain CONCEPTS (boot-write-validate + shorthand-body + D6 are codegen/diagnostic fixes only); domain.map watermark advanced `60d547e1`→`b67cd6e6`**; **S212 A2 W4 + flogence Bug A/B: ZERO new domain concepts / AST shapes. Codegen-internal; W-API-RESPONSE-NOT-VARIANT honesty lint added (Info, §60.5); flogence Bug A (bare-ref event wiring) + Bug B (arm-body reactive directives) are pure codegen routing fixes. domain.map watermark advanced `a9c2108f`→`8569f774`** |
| api.map.md           | absent  | no HTTP route handlers in compiler source                     |
| state.map.md         | absent  | no client state management (compiler is a pure function)      |
| events.map.md        | absent  | no EventEmitter/pubsub detected in compiler source            |
| auth.map.md          | absent  | auth is a COMPILED FEATURE (auth-graph.ts), not compiler auth |
| style.map.md         | absent  | no design tokens or CSS framework in compiler source          |
| i18n.map.md          | absent  | no i18n detected                                              |
| infra.map.md         | absent  | no Dockerfile, CI workflows, or IaC detected                  |
| migrations.map.md    | absent  | no database migrations (runtime DBs are user-app concerns)    |
| jobs.map.md          | absent  | no job scheduler in compiler source                           |

## File Routing

| Query | Map |
|-------|-----|
| types / interfaces / AST node shapes | schema.map.md |
| error codes / CGError / diagnostic stream / fix-notes | error.map.md |
| environment variables / config keys | config.map.md |
| test patterns / fixtures / conformance / happy-dom canaries | test.map.md |
| build commands / pre-commit hook / **compile flags (`--production`)** | build.map.md |
| directory layout / entry points / pipeline stages / per-session source changes / native-parser file table | structure.map.md |
| external packages (acorn, astring, MCP SDK, vscode-languageserver) | dependencies.map.md |
| domain concepts (pipeline stages, engine-graph, source-map, each/match emit, enum-subset, message-dispatch, per-item-reactivity, live-keyed, render-by-tag, match-exhaustiveness, colon-shorthand HTML content-model, **`log()` location-transparent builtin, no-`any` hard line**, **`<render of=X/>` held-variant render-expression (errors-as-states display)**) | domain.map.md |
| `log()` builtin / location-transparent logging / `W-LOG-SHADOWED` / `--production` strip | domain.map.md "log() builtin" concept + structure.map.md "Key S174 Source Changes" + error.map.md W-LOG-SHADOWED |
| `any`-reject / `E-TYPE-ANY-FORBIDDEN` / no-`any` hard line / `asIs` escape hatch | error.map.md E-TYPE-ANY-FORBIDDEN + structure.map.md "Key S174 Source Changes" + domain.map.md `any`-reject invariant |
| reactive state-cell export reject / `E-EXPORT-001` | error.map.md E-EXPORT-001 + structure.map.md "Key S173 Source Changes" |
| native-parser-swap state / flip-failure families / which native file owns which family | domain.map.md "Native-Parser Swap Orientation" + structure.map.md "Native-Parser File Table (S170)" |
| `<each>` / `<match>` / engine codegen emit modules | domain.map.md "Codegen each/match/engine Emit Map" + structure.map.md S154-S175 |
| typed SQL projection rows / `?{ SELECT }` row typing / SQL-row → `:struct` prop or cell contract / width-subtyping | domain.map.md "typed SQL projection rows" concept + error.map.md E-SQL-ROW-CONTRACT-MISMATCH/W-SQL-ROW-UNTYPED + structure.map.md "Key S175 Source Changes" |
| function-typed struct field reject / `E-STRUCT-FUNCTION-FIELD` / passed-vs-stored rule | error.map.md E-STRUCT-FUNCTION-FIELD + domain.map.md function-boundary concept/invariant + structure.map.md "Key S175 Source Changes" function-boundary block |
| unrecognized type name / `E-TYPE-UNKNOWN-NAME` / undefined type rejected (not silently `asIs`) | error.map.md E-TYPE-UNKNOWN-NAME + domain.map.md unrecognized-type-name concept/invariant + structure.map.md "Key S176 Source Changes" |
| `pure` modifier deprecation / `W-PURE-DEPRECATED` / `scrml migrate` Migration 3 (`pure`→`fn`) | error.map.md W-PURE-DEPRECATED + domain.map.md `pure`-deprecation concept + structure.map.md "Key S176 Source Changes" |
| stdlib scalar vocabulary / `scrml:math` (pure) / `scrml:random` + `scrml:time.now()` (capability-scoped non-det) / E-FN-004 in `fn` / transitive shim copy | domain.map.md scalar-vocabulary-stdlib concept + structure.map.md "Key S176 Source Changes" + error.map.md E-FN-004 |
| `<schema>` DDL / `F-SCHEMA-001` / ColumnDef sources / SELECT-projection extractor (`sql-projection.ts`) | structure.map.md "Key S175 Source Changes" Tranche 1 + domain.map.md F-SCHEMA-001 concept + PA Pipeline Source Files |
| formFor / tableFor / component inside an `<engine>` state-child or `<match>` arm renders RAW (g-formfor) / `armBodyChildren` / markup-expansion-in-arms | domain.map.md "markup-expansion-in-arms" concept + structure.map.md "Key S177 Source Changes" g-formfor block + schema.map.md S177 note (`match-block.armBodyChildren`) |
| bare `/` closer / `E-SYNTAX-050` over-fire / `/>`+`:`-shorthand body / `E-CLOSER-001` (bug-4 / bug-74) | error.map.md S177 Fix Notes subsection + structure.map.md "Key S177 Source Changes" bug-tail block (block-splitter.js) |
| inline map-method assign in a handler / `_scrml_map_insert` TypeError / s169-map-inline-insert | structure.map.md "Key S177 Source Changes" (emit-event-wiring.ts :480-506) + error.map.md S177 Fix Notes |
| client stdlib sibling-shim import not resolving / `_inlineSiblingShimImports` / Math de-leak through `scrml:math` | structure.map.md "Key S177 Source Changes" client stdlib-inliner block (runtime-template.js :116, data.js/auth.js) |
| `W-API-RESPONSE-NOT-VARIANT` / `<api>` endpoint non-variant ResponseT / §60.5 boundary-parse honesty lint / `checkApiDeclarations` / `responseEnum` annotation | error.map.md W-API-RESPONSE-NOT-VARIANT family row + S212 A2 W4 section + structure.map.md "Key S212 Source Changes" A2 W4 block (type-system.ts:18189) |
| `<request api= args=>` client fetch codegen / `emitRequestNode` api= mode / `ApiEndpointForEmit` / `buildApiEndpointRegistry` / `emitApiUrlExpr` / seq-guard / refetch / §60.4 / §60.5 / §60.6 / §60.7 | structure.map.md "Key S212 Source Changes" A2 W4 block (emit-reactive-wiring.ts + emit-parse-variant.ts) + dependencies.map.md |
| bare-ref event handler (`onclick=handler`, §5.2.2 row 5) / `bareRefHandler` / direct-listener wire / `g-bare-ref-event-handler-emits-literal-not-wired` | structure.map.md "Key S212 Source Changes" flogence Bug A block (emit-html.ts + emit-event-wiring.ts + binding-registry.ts) + test.map.md "S212 New Test Files" |
| reactive `class:` / `style="...${@x}..."` inside `<match>` arm body not wiring `_scrml_effect` / arm-tagged `class-directive` / `attr-template` / `emitArmWireFunction` directive loop / `_disposers` / `g-match-arm-drops-reactive-attr-class-effects` | structure.map.md "Key S212 Source Changes" flogence Bug B block (emit-html.ts + emit-bindings.ts + binding-registry.ts + emit-event-wiring.ts + emit-variant-guard.ts) + test.map.md "S212 New Test Files" |
| `server function` → `function` migration / `scrml migrate` Migration 4 / W-DEPRECATED-SERVER-MODIFIER / `server` keyword non-load-bearing | error.map.md S180 Key New Codes + structure.map.md "Key S180 Source Changes" D3 block (migrate.js:626) |
| route-inference Trigger 7 (channel-cell-write / broadcast / disconnect → server) / Trigger 8 (reserved-name `handle` → middleware) / `isSSE` codified | structure.map.md "Key S180 Source Changes" D2 block + error.map.md S180 Key New Codes |
| E-ROUTE-003 / E-ROUTE-004 / wire-serializability / non-serializable server-fn return or param | error.map.md E-RI-* family row + S179 Key New Codes block + structure.map.md "Key S179 Source Changes" |
| I-FN-PROMOTABLE inferred-server skip / keyword-vs-inference blind spot | error.map.md I-FN-PROMOTABLE family row + S179 Key New Codes block + structure.map.md "Key S179 Source Changes" |
| engine `effect=` not interpolated / `E-ENGINE-EFFECT-NOT-INTERPOLATED` / bare `effect=load()` silent tree-shake / E-ENGINE-VAR-DUPLICATE-vs-E-ENGINE-003 dedup | error.map.md E-ENGINE-EFFECT-NOT-INTERPOLATED row + S182 detail block + structure.map.md "Key S182 Source Changes" + test.map.md S182 New Test Files |
| lifecycle-field comment-leak (`E-STRUCT-FUNCTION-FIELD` false-positive) / `E-TYPE-001` double-fire / §16.6 snippet-fill ghost-lint exemption (W-LINT-007/004/021) / Shape-1 variant-lifecycle initializer (`E-VARIANT-AMBIGUOUS` recovery) / payload-binding Gaps 1+2 (`!{}` multi-field + block-form `<match>` arm payloads) / `_tableForSynth` W-EACH-PROMOTABLE skip | error.map.md "S184" detail block + structure.map.md "Key S184 Source Changes" + test.map.md S184 entries (line 11 / Unit row) |
| prefix-`not`-as-negation reject (bare `not @x` + paren `not (expr)`, all positions) / `E-TYPE-045` broadened / `_notPrefixNegation` stamp / `harvestNotPrefixNegation` / g-not-negation-enforce | error.map.md E-TYPE-045 row + S188 detail block + structure.map.md "Key S188 Source Changes" (expression-parser.ts choke-point + type-system.ts harvest + tokenizer/ast-builder attr holes) |
| bare binary/ternary operator in an unquoted condition attr (`if=@n >= 3`) / `E-ATTR-UNQUOTED-OPERATOR` / cluster-A reject-+-parens / `ATTR_OP_REJECT` / `>=` opener non-close / unquoted-condition-atomic-only | error.map.md E-ATTR-UNQUOTED-OPERATOR row + S188 detail block + structure.map.md "Key S188 Source Changes" cluster-A block (tokenizer.ts attrConditionOperatorAhead + block-splitter.js + ast-builder.js) |
| division/operator truncated in a ternary arm (`cond ? @cell / x : alt`) / `E-CODEGEN-INVALID-JS` from a mis-fired typed-reactive break / g-division-in-ternary-arm / `ternaryDepth` guard | structure.map.md "Key S188 Source Changes" g-division-in-ternary-arm block (ast-builder.js collectExpr ~3069) + error.map.md S188 detail block |
| `given name = <expr>` rebind reject / `E-SYNTAX-045` / given-narrows-in-place / given-guard parse sites | error.map.md E-SYNTAX-045 row + S189 detail block + structure.map.md "Key S189 Source Changes" (ast-builder.js:6682 logic + :10899 markup, adjacent to E-SYNTAX-044) |
| server-context channel fn reads a client-held channel cell (`@<cell>`) / `E-CHANNEL-SERVER-CELL-READ` / channel-cell-write client-side RULING A / `detectServerContextChannelCellRead` | error.map.md E-CHANNEL-SERVER-CELL-READ row + S189 detail block + structure.map.md "Key S189 Source Changes" (route-inference.ts:1525 walker, fires :3423; RULING A) |
| channel-cell WRITE no longer escalates to server (Trigger 7a DROPPED) / `detectChannelBroadcastReason` now broadcast()/disconnect()-only / channel cells client-held §38.4 | structure.map.md "Key S189 Source Changes" RULING A block (route-inference.ts:1433) + error.map.md S189 detail block — read BEFORE re-diagnosing a channel-cell placement question |
| `${ schemaFor(Struct) }` inside `<schema>` not recognized as a table def / protect-analyzer schemaFor CREATE TABLE source / `extractSchemaForCreateTableStatements` | structure.map.md "Key S189 Source Changes" schemaFor block (protect-analyzer.ts:605/545/559, wired runPA :911) — closes S186 g-schemafor-pa-unrecognized |
| attribute forward-ref to a later-declared reactive cell not resolving / §6.9 reactive-cell hoist / `preBindReactiveStateCells` | structure.map.md "Key S189 Source Changes" §6.9 hoist block (type-system.ts:10589, after preBindExportedNames) — no diagnostic, a resolution-ordering pre-pass |

| §51.0.J derived-engine expression form / `derivedExprNode` / 3 derivedExpr.kinds / B16 NO-RULES/INITIAL/WRITE/CIRCULAR | error.map.md E-ENGINE-004 row (now §51.0.J steer) + structure.map.md "Key S190 Source Changes" (ast-builder derivedExprNode + operator-aware opener, symbol-table kinds + B16, emit-engine C14 recompute, dependency-graph per-upstream edges) + test.map.md S190 section |
| Cluster C parser fixes / `E-DECL-RHS-INTERP-WRAPPED` / `markupRootClosed` sibling boundary / `${}` wrapped decl RHS | error.map.md E-DECL-RHS-INTERP-WRAPPED row + structure.map.md "Key S190 Source Changes" Cluster C block (ast-builder.js) + test.map.md S190 section |
| engine var-name §51.0.C / `autoDeriveEngineVarName` / acronym-run rule / 4-site canonicalization | structure.map.md "Key S192 Source Changes" engine-varname.ts block |
| `E-STATE-UNDECLARED` / undeclared-cell `@name` read / post-CE relocation / bug-12-vkill | error.map.md E-STATE-UNDECLARED row + S192 detail block + structure.map.md "Key S192 Source Changes" type-system.ts block |
| `W-CONST-AT-DEPRECATED` / legacy `const @name` form / Migration 4b / `const <name>` canonical | error.map.md W-CONST-AT-DEPRECATED row + S192 detail block + structure.map.md "Key S192 Source Changes" migrate.js block |
| `ref=@name` SYM registration / `walkRegisterRefBindings` / Class-C ref-binding scope closure | structure.map.md "Key S192 Source Changes" SYM PASS 1.d block + error.map.md S192 section (related changes) |
| markup-as-value (Pillar 1, §1.4/§7.4) in EXPRESSION position / `MarkupValueExpr` / `markup-value` ExprNode / `emitMarkupValueExpr` markup→DOM-node IIFE / `${ cond ? <span> : <span> }` ternary arm / `return <markup>` value / `const <badge> = cond ? <markup> : <markup>` derived ternary / `_scrml_render_value` node-aware display / g-markup-value-ternary-fnreturn-codegen | domain.map.md "markup-as-value in EXPRESSION position" concept row + structure.map.md "Key S201 Source Changes" markup-as-value block (types/ast.ts + ast-builder.js parseExprWithMarkupValues + block-splitter.js scanShape12DeclEnd + emit-lift.js/emit-expr.ts/emit-logic.ts/emit-event-wiring.ts/emit-client.ts + runtime-template.js) |
| bare `.Variant` enum literal as an `<each>`-body call-arg renders raw → `E-CODEGEN-INVALID-JS` / `lowerEachExpr` guard / `serializeCallArgsLowered` / g-each-body-bare-variant-arg | domain.map.md emit-each.ts row + structure.map.md "Key S201 Source Changes" g-each-body-bare-variant-arg block (emit-each.ts) |
| nested-component prop member-arg dropped/misparsed in a component body (`<Badge s=row.name/>` → `row . name`) / `normalizeTokenizedRaw` member-access space-collapse / `E-COMPONENT-011` phantom / silent member-DROP / g-nested-component-member-arg-misparse | domain.map.md component-expander.ts row + structure.map.md "Key S201 Source Changes" g-nested-component-member-arg-misparse block (component-expander.ts normalizeTokenizedRaw ~:560) |
| the each-inline Class-A arc — inline-component prop substitution + LAYER-2 lift + arm-payload `<each>` binding / `substituteInterpSegments` / class-merge post-substitution / LAYER-2 string-literal-attr `${}` lowering / `armPayloadBinding` / `stampArmPayloadEaches` / g-each-inline-component-prop-member-unsubstituted / g-inlined-component-root-class-interp-raw / g-each-over-arm-payload-binding-unbound | domain.map.md "the each-inline Class-A arc" concept row + emit-each/emit-match/emit-engine/component-expander/emit-lift codegen-map rows + structure.map.md "Key S202 Source Changes" block |
| e2e render known-failure-map test capability — `compiler/tests/e2e-render-map/` / D0–D7 oracle-free render detectors / render-corpus-enumerator (tier-tagged flagship/probe/stress/perf/sample) / render-harness (happy-dom mount + detect) / e2e-render-map-baseline.json allowlist / generate-baseline.js `--check` gate | test.map.md "S202 New Test Files" section + structure.map.md "Key S202 Source Changes" NEW test-infra block |
| flograph project-graph MVP harness — `scripts/flograph.ts` / `@node`/`@gap` tokens / typed `[[blocks/supersedes/decided-by/cites: target]]` edges / `verified` provenance bit / REPORT/`--emit`/`--check` modes / docs/graph/graph.{json,mmd} | structure.map.md "Key S202 Source Changes" NEW tooling block (scripts/flograph.ts + scripts/flograph-fixture/sample.md) |
## Task-Shape Routing (agents — read this section first)

This is a COMPILER repo. Task shapes are bug-fix / codegen / parser / new-feature / spec-amendment /
test-authoring / audit. Each shape lists maps in priority order — read in order until oriented.


**engine-hydration arc — `<engine for=T initial=@cell>` (A-leg) / `<engine for=T server=@source>` (E-leg) work (S198-S199 — §51.0 / §51.0.E / §52.4.4):**
1. `error.map.md` — "S198-S199" section + family rows: E-ENGINE-INITIAL-BOTH-FORMS (A-leg, `initial=.Literal` vs `initial=@cell` mutual-exclusion) + E-ENGINE-SERVER-WITH-DERIVED / E-ENGINE-SERVER-WITH-INITIAL-CELL (E-leg mutual-exclusions, §51.0.E) + W-ENGINE-SERVER-SOURCE-NOT-AUTHORITATIVE (Info, §52.4.4 — source-cell-not-a-read-authority nudge). BOTH legs REUSE E-ENGINE-INITIAL-CELL-UNDECLARED / -TYPE (bare-root only) + the runtime decoder-boundary E-ENGINE-INITIAL-INVALID-VARIANT; the E-leg SUPPRESSES W-ENGINE-INITIAL-MISSING.
2. `structure.map.md` — "Key S198-S199 Source Changes": ONE shared guard-free construction hook + the SHARED runtime helper `_scrml_engine_hydrate_init` (runtime-template.js ~:3817 — bare reactive set, NEVER `_scrml_engine_direct_set`; engine stays writable). A-leg: ast-builder `initialCell` + symbol-table `EngineMetadata.initialCell` + emit-engine `emitEngineCellHydrationInit`/`...ForFile` (deferred after reactiveLines) + DG credits the cell (snapshot-once). E-leg: ast-builder `serverSource` (dotted path) + symbol-table `EngineMetadata.serverSource` + emit-engine `emitEngineServerSourceHydration`/`...ForFile` [~:1746] (`_scrml_reactive_subscribe(rootCell)` → guard-free hydrate, null-safe field-walk, skip-if-absent) + emit-client wiring after `emitReactiveWiring` + DG credits the root cell + within-node-classifier STRIP_KEYS `serverSource` (LIVE-only, native-parser swap-class). §38 server-push composes for free.
3. `test.map.md` — "S198-S199 New Test Files": engine-hydration-initial-cell (unit, 16 — A-leg) + engine-hydration-server-source (unit, 18 — E-leg); HOS-showcase smoke (`trucking-dispatch-smoke-integration.test.js` +7L) dog-foods the E-leg.
4. SPEC: §51.0 (initial=@cell attr-row, construction-not-transition) + §51.0.E (NEW server-form subsection) + §51.0 attr-table row + §52.4.4 reciprocal + §34 (+4 codes). A-leg = Approach F (no anytime-restore verb, Approach B lost). E-leg supersedes g-engine-server-flag-silent-swallow for the `server=@source` form; persist-back is the dev's explicit `?{}` (§52.6.2). RATIFIED/built S198 (A-leg) + S199 (E-leg). Authority: `docs/changes/engine-hydration-initial-cell-2026-06-15/BRIEF.md` + `docs/changes/engine-server-authority-2026-06-16/BRIEF.md`.

**render-expression `<render of=X/>` / held-error display / `renders`-contract work (S196 — §19.15 / §19.2 / §18.0):**
1. `error.map.md` — S196 section + family rows: E-RENDER-NO-OF / E-RENDER-NO-CLAUSE / E-RENDER-NOT-ENUM (the exhaustiveness fence, all in type-system.ts annotateNodes [:7692/:7729/:7744]; NO-CLAUSE reuses §19.6.6 E-ERROR-005; NOT-ENUM conservative on `asIs`) + E-MATCH-ARM-MARKUP-IN-VALUE (the early typer steer in checkMatchDiagnostics [:13446], scoped to JS-style `match-arm-inline`, block-form `<match>` exempt)
2. `structure.map.md` — "Key S196 Source Changes": `<render>` is a NEW scrml STRUCTURAL element (parallel to `<errors>`/`<each>`) — html-elements.js REGISTRY.set("render") [~:799] (self-closing, `rendersToDom:false`) + attribute-registry.js `of=` supportsInterpolation:false [~:403]; emit-html.ts generateHtml expansion [~:1041-1173] (per-variant `switch(X.variant)` from `allVariantRenderExprs` + `emitBoundaryMarkupExpr` against the HELD value's `.data`, NOT `_eb_result.data`; SIDESTEPS the `__scrml_error` gate; `<errorBoundary>` codegen UNCHANGED); binding-registry.ts/emit-event-wiring.ts `render-element` kind + renderHeldAccessor/renderHeldSubscribe (top-level `<render of=@cell/>` reactive); emit-variant-guard.ts emitArmWireFunction (arm-payload `<render of=err/>` dispatch)
3. `test.map.md` — "S196 New Test Files": render-expr-primitive (integration, 6 — both loci dispatch on `(X).data`, fence fires, errorBoundary unperturbed) + 4 prereq-bug unit (g-failable-arm-nested-constructor-crash 6, g-shorthand-interp-match-arm-codegen 8, g-match-arm-apostrophe-bs 8, h1-steer-markup-in-value-match 8)
4. SPEC: NEW §19.15 (.1 Syntax, .2 Semantics, .3 Exhaustiveness fence, .4 What it does NOT do, .5 Codegen) + §19.2 `renders`-amend + §4.15/§24.4 register `<render>`. RATIFIED S195 (deep-dive → debate → design-insight a/c); surface `<render of=X/>` per S196 user ruling (cohesive with `<errors of=expr/>`). Closes g-held-error-display. Follow-on g-render-not-enum-asis-miss (LOW). Authority: `docs/changes/render-expr-primitive-2026-06-15/BRIEF.md` + `BRIEF-recovery.md`.

**engine var-name / `E-STATE-UNDECLARED` / `W-CONST-AT-DEPRECATED` work (S192 — §51.0.C / §6.1.2 / §6.6.1):**
1. `error.map.md` — E-STATE-UNDECLARED (read-side fire, post-CE at type-system.ts:6282; exemptions list; scopeChain resolution order) + W-CONST-AT-DEPRECATED (Info, structuralForm:false gate, type-system.ts:8688; Migration 4b)
2. `structure.map.md` — "Key S192 Source Changes": engine-varname.ts (41L, ONE canonical §51.0.C rule) + SYM PASS 1.d walkRegisterRefBindings (`ref=@name` scope closure) + type-system.ts read-side fire relocation + migrate.js Migration 4b
3. `test.map.md` — S192 New Test Files: engine-binding-b14 (49), const-at-deprecated-lint (15), v-kill-readside-undeclared (6), ref-binding-sym-registration (6), state-block-bare-write-decl-lint (10)

**GAP-A void-element arm-closer fix + the §S195 corpus-rewrite gap board (S195 — §24 / §18.0.1; arm-scanner / errors-as-states):**
1. `structure.map.md` — "Key S195 Source Changes": the two-stage scanner fix. block-splitter.js `findStructuralBodyEnd` push-gate gains `&& !VOID_ELEMENTS.has(openerName)` [~:691] so a bare §24 void opener (`<input>`/`<br>`/`<img>`) inside a `<match>`/`<each>` body is a self-terminating leaf, NOT a tag-stack push; match-statechild-parser.ts NEW local `VOID_ELEMENTS` set [~:54] + `findArmCloser` `if (!isVoidOpener) depth++` [~:397] + the LATENT flush-closer fix (was: `p===q` guard never fired → dropped the byte after an opener `>`, skipping a flush closer like `<input></Editing>`; now resumes at `q` with a `foundOpenerEnd` flag). The self-closed `<input/>` form already short-circuited.
2. `error.map.md` — S195 section: ZERO new codes — the fix REMOVES two misleading false-fires (a bare void direct child of a `<match>`/`<each>` arm body no longer surfaces a spurious E-CTX-001 "Unclosed `<match>`" / E-MATCH-PARSE-001). NO SPEC change (§24 + §4.14 + §18.0.1 already imply void = self-terminating leaf)
3. `test.map.md` — "S195 New Test File": match-arm-void-element-scanner (13 cases / 46 expects, 3 describe blocks) — §1 parseMatchArms void-leaf, §2 unclosed-non-void still fires E-MATCH-PARSE-001 (detector not weakened), §3 end-to-end compile clean
4. `docs/known-gaps.md §S195` — 8 NEW open gaps from the corpus-rewrite wave-1a. Read BEFORE touching the named sites: **g-failable-arm-nested-constructor-crash (HIGH)** — a payload-bearing variant constructor nested as an arg in an `!{}` arm lowers to a string-invoked-as-function → runtime crash [emit-logic.ts ~520-565]; **g-each-body-bare-variant-arg (HIGH, non-blocking)** — a bare `.Variant` enum literal as an event-handler call-arg in an `<each>` per-item body emits invalid JS [emit-each.ts]; **g-held-error-display (MED, RATIFIED-fix-pending-build)** — a held error variant (errors-as-states `.Failed(err)`) cannot fire its own `renders` clause; the RATIFIED fix (S195 a/c) is a NEW narrow render-expression primitive (keyword NOT `show` — §17.2 collision; exhaustiveness-fenced reuse §19.6.6 E-ERROR-005; reuses allVariantRenderExprs + emitBoundaryMarkupExpr), gated on g-failable-arm-nested-constructor-crash; **g-engine-autodecl-bare-variant-write (MED)**, **g-shorthand-interp-match-arm-codegen (MED)**, **g-match-arm-apostrophe-bs (MED)** [block-splitter.js string-delimiter scan — contraction in arm prose breaks the BS], **g-blocksplitter-comment-span-not-opaque (LOW)**, **g-each-body-sigil-invariant-classifier (LOW, test-infra)**

**§52 server-authority / sync / Tier-1 read-authority codegen work (S194 — §52.3.5 / §52.6 / §52.8):**
1. `structure.map.md` — "Key S194 Source Changes": the codegen ownership map. §52 is now READ-authority ONLY (load + SSR residual); the WRITE is the dev's own `?{}` server fn (auto-persist/optimistic RETRACTED, Q1=C/Q2=WF). emit-sync.ts `emitServerAuthorityLoad(varName, table)` [:104] (SELECT* client-load IIFE → `/__serverLoad/<var>`; `emitServerSyncStub`/`emitOptimisticUpdate` DELETED) · collect.ts `collectServerAuthorityTypes` [:588] (Tier-1 instances) / `collectServerVarDecls` [:546] (Tier-2, now disjoint) · emit-server.ts `/__serverLoad/<var>` `_scrml_sql\`SELECT * FROM <table>\`` route [:1591] + server-file emission gate [:531] · emit-reactive-wiring.ts per-instance wiring [:618]
2. `error.map.md` — W-AUTH-* row: W-AUTH-001 SUPPRESSED for a Tier-1 instance (compiler-generated SELECT* load) [type-system.ts:9037-9042]; W-AUTH-002 NARROWED to the §52.8 SSR-pre-render residual [type-system.ts:7972]
3. parser side: `structure.map.md` ast-builder.js `tryParseServerAuthorityDecl(startTok, nameTok)` [:4765] — the §52.3.5 server-authority TYPE-decl + INSTANCE recogniser, GATED on `authority="server"` (the discriminator; was previously swallowed as `html-fragment`). Sub-shape T registers the type in `_serverAuthorityTypes`; sub-shape I builds a `state-decl` carrying `isServer:true`/`stateType`/`serverAuthorityTable`. §54.2 substates / §35.2 constructors / local states are UNTOUCHED (gate is `authority="server"`-only)
4. SPEC: §52.6.2/.3/.4 retracted auto-persist; NEW §52.6.6 (dev write-fn convention) + §52.6.7 (§52↔§38 channel fan-out = explicit composition, NO `broadcast=` attr) + SPEC-ISSUE-026 RESOLVED. SPEC-INDEX §52 row regenerated

**compiler-source bug fix (the dominant shape — most dispatches):**
1. `error.map.md` — find the offending code, its family, AND the "Fix Notes" for any prior fix on the same class (the S153-S160 colon-shorthand / each-in-dynamic-context / Bug 60-73 notes are pattern templates — read them before re-diagnosing)
2. `domain.map.md` — locate the responsible pipeline stage + its primary source file (Pipeline Source Files table) and any relevant invariant
3. `structure.map.md` — the "Key S154-S174 Source Changes" sections give exact file + function + line for recently-touched code; the codegen directory ownership line names the emit-* module
4. `test.map.md` — find the existing canary for the feature; a behavior fix WITHOUT a happy-dom test is the S140/S152 blind-spot trap — always add one

**codegen (`<each>` / `<match>` / engine / emit-* work — highest-churn area):**
1. `domain.map.md` — the **"Codegen `<each>` / `<match>` / engine Emit Map"** table (names every emit module + role + the S153-S159 runtime helpers and Bug 62/64/65/72/73 patterns; S174 adds `log-loc.ts` + the `log()` lowering sites; **S177 adds the "markup-expansion-in-arms" g-formfor concept + the emit-match `buildMatchArms` armBodyChildren-wrapper consumption + bug-48 `resolveOnExpr` complex-on= lowering**)
2. `structure.map.md` — S154-S177 source-changes sections: exact functions + line numbers in emit-each.ts / emit-lift.js / emit-control-flow.ts / emit-engine.ts / emit-html.ts / **log-loc.ts / emit-expr.ts (log lowering ~L1630)** / **emit-match.ts (S177 g-formfor armBodyChildren :676-694 + bug-48 resolveOnExpr :272) / emit-event-wiring.ts (S177 s169 inline-map-assign :480-506) / runtime-template.js (S177 `_inlineSiblingShimImports` :116)** / dependency-graph.ts / ast-builder.js (S177 `buildMatchArmBodyChildren` :12128) / block-splitter.js (S177 bug-74/bug-4) / type-system.ts (S177 walkAndSplice recursion + r28-7b)
3. `error.map.md` — E-CODEGEN-INVALID-JS fix notes + chunk-survival / dep-first-read / engine-ctx-threading / per-item-live-keyed invariants
4. `test.map.md` — happy-dom canary list; emit-string-only tests mask runtime miscompiles

**Tailwind / CSS class-emit work — composing-utility families (S191 — §26.7/§26.7.1/§26.7.2/§26.7.3, the biggest prior map gap; dev agents flagged its absence 3×):**
1. `domain.map.md` — the **"Tailwind class emit"** Pipeline Source Files row: `tailwind-classes.js` is the class-token → CSS rule registry + the W-TAILWIND-* lints. The composing-utility families follow **Approach C** — each utility emits a shared `var()`-based composing shorthand AND sets one `--tw-*` custom property; **every `var()` carries an inline fallback so NO global `*, ::before, ::after` preflight defaults block is emitted** (preserves the §26.1/§26.2 "only what's used" minimalism axiom; mirrors the `space-x-reverse` self-fallback precedent)
2. `structure.map.md` — **"Key S191 Source Changes"** Tailwind block: the five `register*()` builders (invoked in registry build [tailwind-classes.js:3008-:3012]) + the four `*_COMPOSE` consts — `registerRing()`/`ringShadowSetter()`/`BOX_SHADOW_COMPOSE` (ring/ring-offset/shadow §26.7 Phase 1; `ring-color` defaults `currentColor`, scrml divergence from Tailwind v3 blue-500/50) · `registerGradient()` (gradient §26.7.1 Phase 2; `bg-gradient-to-{dir}` + from/via/to stops with transparent-twin defaults) · `registerTransform()`/`TRANSFORM_COMPOSE` (translate/scale/rotate/skew §26.7.2 Phase 3) · `registerFilters()`/`registerBackdrop()` + `FILTER_COMPOSE`/`BACKDROP_COMPOSE` (filter + backdrop-filter §26.7.3 Phase 4; EMPTY `var(--tw-blur,)` fallbacks + `-webkit-backdrop-filter:` prefix). Arbitrary-value forms at :1726 (transform) / :1753 (ring) / :1794 (gradient) / :1806 (filter)
3. `error.map.md` — S191 section: ZERO new codes — adding these registry families makes their class tokens RECOGNIZED, so `W-TAILWIND-UNRECOGNIZED-CLASS` no longer false-fires on them (recognition coverage, not a new diagnostic). The S183 `findInterpolationRanges`/`tokenTouchesInterpolation` dynamic-class-prefix precision still applies
4. `test.map.md` — "S191 New Test Files": bug-1-tailwind-filter-family (28), bug-1-tailwind-gradient-family (14) + extended bug-1-tailwind-ring-family (34) / transform-shorthand (37) / minor-families (26) / unrecognized-class (39) / arbitrary-value-emit (66) / tailwind-classes (160)

**if=fn() condition routing — bare-call conditionals (S191 — §5.1, a codegen routing fix, ZERO new codes):**
1. `error.map.md` — S191 section: a bare-call `if=fn()` / `show=fn()` / `else-if=fn()` condition (a `call-ref` attr value) was mis-routed (treated as a nonexistent `if`/`show` DOM event binding, or in a chain head read as a cell — `_scrml_reactive_get("isHigh")` → branch never activated). NOW routed as a reactive conditional, parity with the paren form `if=(fn())`. NO new diagnostic
2. `structure.map.md` — **"Key S191 Source Changes"** if=fn block + `domain.map.md` emit-html.ts / emit-event-wiring.ts codegen rows: THREE seams — (a) emit-html.ts call-ref attr-value branch [:1773] `g-attr-if-fn-call-misroute` (emits `data-scrml-bind-if`/`-show` + `addLogicBinding`); (b) emit-html.ts clean-subtree if= handler [:1413] `g-attr-if-fn-display-not-mount` (same mount/unmount controller as `if=(fn())`/`if=@var`; gate at :1394 now accepts `call-ref`); (c) emit-event-wiring.ts `_update_chain_<id>` [:1282] `g-attr-if-fn-chain-head-call-misroute` (build call string → `parseExprToNode` → `emitExprField` so the fn is CALLED). `@`-args dynamic-track via `_scrml_effect`; fn name auto-mangled by the whole-buffer post-pass
3. `test.map.md` — "S191 New Test Files": attr-if-fn-call-conditional (12), attr-if-fn-condition-followup (5)

**errarm re-fail-from-arm / fail-expr lowering (S185 — §19.5.2, the 2-layer parser+typer+codegen shape):**
1. `structure.map.md` — **"Key S185 Source Changes"** errarm blocks: `_parseFailExprString` + `arm.failExpr` attach in `parseErrorTokens`/`parseOneMatchAsExpr` + the `?`-propagate const-decl hook (ast-builder.js); the guarded-arm `failExpr.argsExpr`-only scope-check + NS-1 `visitStmt` recurse-keys (`failExpr`/`matchExpr`) (type-system.ts); shared `emitFailExpr` (emit-logic.ts) + `MatchArm.failExpr` field + `emitMatchExpr` envelope (emit-control-flow.ts)
2. `error.map.md` — S185 section: re-fail-from-arm reuses E-ERROR-001 (NS-1 fires it in a non-`!` fn) + E-SCOPE-001 (the `fail`-keyword ident-walker skip removes the SPURIOUS fire) — NO new code; a `fail` inside an `!{}` / `<match>` arm lowers to a function-exiting `return { __scrml_error, … }` envelope, escaping like statement-position `fail`
3. `schema.map.md` — `failExpr` is a per-arm side-field (`fail-expr` node carrying `argsExpr`) on both `!{}` error-arms and match-expr arms; `MatchArm.failExpr` (emit-control-flow.ts) threaded through all arm-kind constructors
4. `test.map.md` — `errarm-refail-lowering.test.js` (11 cases; repro fixtures under docs/changes/errarm-refail-lowering-2026-06-11/repro/)

**validator inline-message colon-form reject (S185 — §55.10/§41.12):**
1. `error.map.md` — E-VALIDATOR-INLINE-COLON row + S185 section: the colon form `<name req:"…">` is NOT scrml (the §55.10 form is the PAREN form `<name req("…")>`); the colon collided with the decl scanner `:`-handling and corrupted `@`-cell registration → a MISLEADING downstream E-SCOPE-001
2. `structure.map.md` — "Key S185 Source Changes" → `tryRecoverColonInlineMessage(afterValidatorIdx)` in `scanStructuralDeclLookahead` (ast-builder.js): detects validator-name + `:`-string, pushes the code, and RECOVERS by registering the cell with the paren-form override (two call sites: `recoveredCall` post-call-arg + `recoveredBare`)
3. `test.map.md` — `validator-inline-colon.test.js` (10 cases; control paren + typed-cell colon cases still WORK)

**diagnostic-authoring (a new lint / error code, the S173-S174 shape):**
1. `error.map.md` — the code-family conventions + the partition rule (W-/I- prefix OR severity:warning/info → result.warnings; everything else → result.errors/exit 1) + the S173 (E-EXPORT-001/W-TYPE-FN-FIELD), S174 (W-LOG-SHADOWED/E-TYPE-ANY-FORBIDDEN), S175 (E-STRUCT-FUNCTION-FIELD [escalated from W-TYPE-FN-FIELD] / E-SQL-ROW-CONTRACT-MISMATCH / W-SQL-ROW-UNTYPED), and S176 (E-TYPE-UNKNOWN-NAME — the SYMMETRIC-COMPANION precedent: a registry-dependent leaf-classifier check sharing `forEachTypeAnnotationLocus` with E-TYPE-ANY-FORBIDDEN, wired AFTER the imported-types seed) "Key New Codes" blocks as templates — all are decl-site/read-site scans wired in the type pass (type-system.ts ~15917-15924) or MOD stage (module-resolver.js); they add NO new AST shape and NO codegen. NOTE the W-TYPE-FN-FIELD → E-STRUCT-FUNCTION-FIELD escalation precedent (Info-nudge promoted to a hard reject once the design ratified)
2. `structure.map.md` — "Key S173/S174 Source Changes": the exact `check*` function + line + wiring site
3. `schema.map.md` — confirm whether the diagnostic needs a new node shape (S173/S174 did NOT — they scan existing nodes / raw strings)
4. `test.map.md` — cross-stream W-/I- helper requirement (a `result.errors.filter(...)` on a W-/I- code silently passes)

**`log()` / location-transparent logging work (S174 — §20.6):**
1. `domain.map.md` — the `log()` builtin concept row (compile-certain `side` from `ctx.mode`/§12.4 placement; `loc` from byte offset via log-loc.ts; dev unified-view via dev.js `POST /_scrml/log`; production-strip via `--production`; the `'log'` runtime chunk gated on actual `_scrml_log(` usage) + the `W-LOG-SHADOWED` shadowing concept
2. `structure.map.md` — "Key S174 Source Changes" → log() block: `log-loc.ts` (224L NEW: `resolveLogLoc`/`registerFileSource`/`resetLogLoc`/`fileDeclaresLog`/`SERVER_LOG_HELPER`) + emit-expr.ts lowering (~L1630, three branches: shadowed→plain call / `_logProductionStrip`→`(void 0)` / builtin→`_scrml_log(side, loc, ...)`) + emit-server.ts/emit-client.ts/runtime-chunks.ts chunk gating + index.ts toggles (455/587/760) + dev.js endpoint (~L459) + compile.js/api.js `production` flag
3. `error.map.md` — W-LOG-SHADOWED fix note (fires at the DECLARATION in the type pass, NOT codegen; `log` is not reserved; reserved for `E-LOG-SHADOWED` promotion) + the log-loc resolution fix note (byte-offset, not `span.line`)
4. `build.map.md` — the `--production`/`--prod` compile flag (strips ALL `log()` to 0 bytes, §20.6.5 F4=A)

**`any`-reject / type-token policy work (S174 — §14.1.1):**
1. `error.map.md` — E-TYPE-ANY-FORBIDDEN family row + the S174 Key New Codes block: `any`-token-SPECIFIC, fires BEFORE the silent `asIs` collapse; an arbitrary undefined type name resolving to `asIs` was a SEPARATE follow-on — **CLOSED S176 by `E-TYPE-UNKNOWN-NAME` (§14.1.2), the symmetric companion check over identical loci (registry-dependent, wired after the imported-types seed)**
2. `structure.map.md` — "Key S174 Source Changes" → `any`-reject block: `typeTextMentionsAnyToken` (type-system.ts:3678, splits on non-identifier chars + tests for bare `any` atom) + `checkAnyTypeForbidden` (type-system.ts:3720, scans struct/cell/fn-param/return-type positions) + wiring (type-system.ts:15924)
3. `domain.map.md` — the `any`-reject invariant (there is no `any`; `asIs` is the named greppable escape hatch, analogous to TS `unknown`)
4. `schema.map.md` — NO `AnyType` member in `ResolvedType` (the `any` token currently falls through to `AsIsType`/`UnknownType`; the check catches it first)

**typed-data / typed-SQL-row work (S175 flagship — §14.8.7/§14.8.8):**
1. `domain.map.md` — the **"typed SQL projection rows"** concept row (the read-site T2a / prop-contract T2b / cell-contract T3b / fn-return T3c halves + the boundedness invariant: width-subtyping applies ONLY projection-row → declared `:struct`; general struct assignment stays NOMINAL §14.8.1) + the F-SCHEMA-001 concept
2. `structure.map.md` — **"Key S175 Source Changes"**: the four tranches with exact functions + line numbers in type-system.ts (`resolveSqlRowType` :5676, `checkSqlRowWidthSubtype` :704, `checkPropContract` :9491, `checkSqlRowAgainstCellContract` :9629, `resolveIterableRowElement` :5812, `checkRowFieldAccessInExpr` :9420, `inferReturnTypeFromBody` :5917), sql-projection.ts (`extractSelectProjection`), protect-analyzer.ts (`extractSchemaCreateTableStatements` F-SCHEMA-001), component-expander.ts (`__propContractChecks`), schema-differ.js (`generateCreateTable` export)
3. `error.map.md` — E-SQL-ROW-CONTRACT-MISMATCH (the width-subtype miss; one per unsatisfied field) + W-SQL-ROW-UNTYPED (the deferred long-tail degradation: `*`/CTE/UNION/subquery/unknown-col) + E-TYPE-004 reuse (T2a unknown-column field access)
4. `schema.map.md` — NO new FileAST node shapes; the new type-system-internal `ResolvedType` members are `FunctionType` (:324) + the `<fn-return>` sentinel; sql-projection.ts `ProjectedColumn`/`SelectProjection`; protect-analyzer `ColumnDef`

**function-boundary / struct-field type work (S175 — §14.3/§15.11):**
1. `error.map.md` — E-STRUCT-FUNCTION-FIELD (hard reject; escalated from the retired S173 W-TYPE-FN-FIELD Info-nudge) + the passed-vs-stored framing
2. `domain.map.md` — the function-boundary concept row + the passed-vs-stored invariant (a function is not value data: PASSED/CALLED legal, STORED rejected; the limit-the-primitive axiom §14.1.1)
3. `structure.map.md` — "Key S175 Source Changes" function-boundary block: `checkFunctionTypedStructFields` (:3497), `isFunctionTypeAnnotation` (:2087, conservative so lifecycle fields never mis-fire), `tFunction()`/`FunctionType` wired through `resolveTypeExpr` (:2400)
4. `schema.map.md` — the distinguishable `FunctionType` member (not opaque `asIs`) is what makes the reject precise

**parser / grammar fix — NATIVE-PARSER swap-grind (the active strategic line — drives `--parser=scrml-native` to default):**
1. `domain.map.md` — **"Native-Parser Swap Orientation"** block: the family flip-failure table re-measured 605 on `df08f282` → ~508 after the two S170 waves (default BS+Acorn 0-fail / fully green; native is opt-in). CLOSED: each/match promotion (S162), F1 engine-substrate (S163), B1 reset-expr (S163), §51.0.S message-arm + exprNode (S164), lift close-tag/F2a/table-for (S164), F2-match/promote-each/R1/server-fn-star (S165), bare-`function` failable + cross-file `${...}`-export (S166), S170 fix-wave-1 (`on mount`/`on dismount`, `const @name` F5-CLOSED, deepset/array-mutation node-synth, exprtext-backfill walker) + fix-wave-2 (BlockStub `verbatim` Mario fix). OPEN next-pick: F4 formFor · F2 server-fn `?{}` body-drop · F6/F9 · `effect=` opener · mario PowerUp payload-enum · F7. Read FIRST + re-run the flip harness before picking.
2. `structure.map.md` — **"Native-Parser File Table (S170)"** + the S162-S170 source-changes sections: every native file + role + CURRENT line counts + locus per open family
3. `api.js` routing — `--parser=scrml-native` is STRICTLY OPT-IN (default `null`); native `_buildAST` branch calls `nativeParseFile` then `populateNativeAttrValueExprNodes` (945) + `backfillNativeExprText` (960). **The default flip has NOT happened — PA ships parity-closers, never the flip (standing user decision).**
4. `error.map.md` — native parity resolutions (E-SCOPE-001 destructured params, E-VARIANT-AMBIGUOUS→E-CONTRACT-001) + the §4.18.7 E-UNQUOTED-DISPLAY-TEXT SPEC-correct-not-spurious ruling
5. `test.map.md` — parser-conformance within-node allowlist (1005/0); the flip-harness is the family-level gate — re-run it to re-rank before picking a family

**parser / grammar fix — LIVE pipeline (block-splitter / ast-builder / engine-statechild-parser):**
1. `domain.map.md` — pipeline stage (BS/TAB) + engine-arm-parsing row; S159 `:`-shorthand HTML content-model; S160 ruling (b) inside-opener canonical; S167 deep-set write-loss boundary recognizer; S168 cycles-prereq COW bracket-write; **S177 markup-expansion-in-arms (g-formfor): the `armBodyChildren` re-parse + walkAndSplice/walkAndExpand recursion**
2. `structure.map.md` — S153-S177 LIVE-pipeline source changes (engine-statechild-parser.ts, match-statechild-parser.ts, ast-builder.js, block-splitter.js, type-system.ts); **S177 "Key S177 Source Changes": bug-74/bug-4 block-splitter closer refinements (`isGenuineShorthandBodyNotDirective` :1085, `looksLikeCloser` :2973), bug-48 opener-finder depth, g-formfor `buildMatchArmBodyChildren` :12128**
3. `error.map.md` — E-ENGINE-* / E-CTX-* / E-EXPR-* / E-STMT-* / E-SYNTAX-064 / E-SYNTAX-050 (bug-4 narrowed) / E-CLOSER-001 (bug-74) / E-COLON-SHORTHAND-ON-VOID / W-COLON-SHORTHAND-LEGACY-PLACEMENT / E-REFINEMENT-NO-DEFAULT families + the S177 Fix Notes subsection
4. `test.map.md` — parser-conformance within-node allowlist (live-pipeline vs native parity)

**bare-variant inference fix (R28-8 target — type-system.ts):**
1. `structure.map.md` — "Bare-variant inference helpers" table: `inferBareVariantsInExpr` @7925, `inferBareVariantsForStructConstructor` @8153, `inferBareVariantsWithStructNav` @8199 + let/const-decl call site @~5820
2. `error.map.md` — E-VARIANT-AMBIGUOUS / E-CONTRACT-001 / E-TYPE-063
3. `domain.map.md` — bare-variant inference domain concept (§14.10 + §14.10.7)
4. `schema.map.md` — `PredicatedType.subsetVariants` + ExprNode shapes the walker descends

**value-native map work (§59 — IMPLEMENTED S169; bug-fix / extension on the map type):**
1. `error.map.md` — the 7 §59 codes + fire sites (key-comparability type-system.ts ~2069/~2086, bracket-write ~7705, literal scanners, iteration-order lint)
2. `structure.map.md` — "Key S169 Source Changes (value-native maps §59)": exact file + function + line per phase (D0-D4)
3. `schema.map.md` — `MapType` (ResolvedType) + `MapLitExpr`/`MapEntry` (ExprNode) shapes
4. `domain.map.md` — value-native-map row + the cycles-prereq groundwork row
5. `test.map.md` — "S169 New Test Files"

**set-algebra / `scrml:data` work (S170 — type DEFERRED, helpers shipped):**
1. `domain.map.md` — "set-algebra helpers" concept row (`union`/`intersection`/`difference`/`member` + `_data_value_canonical` value-correctness keystone + `unique` struct-unsafety fix; NO `set` type)
2. `structure.map.md` — "Key S170 Source Changes" → set-algebra block (data.js + `.scrml` mirror)
3. `test.map.md` — `data-set-algebra.test.js` (16, value-correct over structs)

**new feature / spec-amendment:**
1. `domain.map.md` — invariants + concept lexicon (check language cohesion before proposing syntax)
2. `structure.map.md` — where the feature's stage lives
3. `schema.map.md` — AST node shapes (a new construct needs a node type; note S173/S174 diagnostics did NOT)
4. `error.map.md` — code-family conventions for any new diagnostic

**test-authoring:**
1. `test.map.md` — runner, categories, patterns, cross-stream W-/I- helper requirement
2. `error.map.md` — the code under assertion (and which stream it lands in)

**channel codegen / onserver-onclient handler wiring / reconnect attr (S186 — §38):**
1. `structure.map.md` — **"Key S186 Source Changes"**: Bug1 reconnect bare-int E-SCOPE-001 exempt (type-system.ts:10399-10410) + Bug2 wiring — `channelAttrToCall`/`channelAttrParam`/`collectChannelAttrHandlerNames` (emit-channel.ts), `channel-ws-handler`/`isChannelWsHandler` (route-inference.ts), onserver WS divert (emit-server.ts), E-CG-002 skip (emit-functions.ts:505)
2. `error.map.md` — **"S186"** fix-notes block: NO new codes (Bug1 reuses an E-SCOPE-001 visitAttr EXEMPT; Bug2 reuses §12.2 Trigger-7 / E-CG-002, the latter no longer false-firing on a route-less `isChannelWsHandler` server fn) + the E-SCOPE-001 family-row reconnect annotation
3. `domain.map.md` — channel/§38 + the S180 Trigger-7 boundary rows (onclient §38.10 wins over Trigger-7; onserver force-escalates server with dead route)
4. NOTE 5 NEW open dog-food gaps filed (g-channel-onserver-cell-read MED is the SPEC-silent server-cell-read residual of Bug 2) — SCOPE at docs/changes/s186-dogfood-followups-2026-06-12/

**prefix-`not`-as-negation enforcement + unquoted-condition-operator reject + ternary-arm codegen (S188 — §5.2/§17.1/§42.10):**
1. `error.map.md` — **"S188"** section + the E-TYPE-045 / E-ATTR-UNQUOTED-OPERATOR detail rows: E-TYPE-045 is now enforced in ALL expression positions + the BARE form (`not @x`), driven by a SINGLE lowering-choke-point stamp (`_notPrefixNegation`) harvested by the NEW TS-J pass; cluster-A E-ATTR-UNQUOTED-OPERATOR rejects a bare binary/ternary operator in an unquoted `if=`/`show=`/`else-if=` and recovers `absent`; g-division-in-ternary-arm is a codegen-correctness fix with NO new code. NOTE the choke-point-stamp + harvest pattern (one detector at the lowering site → one structural-walk emitter pass) is the template for any "enforce a syntactic rule across every expression position" task.
2. `structure.map.md` — **"Key S188 Source Changes"**: the four arcs with exact functions + lines — expression-parser.ts (`preprocessForAcorn` `detector` out-param + `parseExprToNode`→`_parseExprToNodeInner` `_notPrefixNegation` stamp), type-system.ts (`harvestNotPrefixNegation` ~15233/~15270 wired ~17880; `checkNotPrefixNegation` RETIRED), tokenizer.ts (`isPrefixNotOperandAhead` ~289, `isConditionAttrName` ~319, `attrConditionOperatorAhead` ~354, ATTR_OP_REJECT capture), block-splitter.js (`>=` non-close guard ~951 + ternary `?`-depth ~902/1001/1042 + Shape-1 `=`-peek `>=` skip ~1196), ast-builder.js (ATTR_OP_REJECT branch ~2050, stray-`not`-pair detector ~2192, `ternaryDepth` guard ~3069), within-node-classifier.ts STRIP_KEYS += `_notPrefixNegation`
3. `domain.map.md` — §42 `not` = unified absence VALUE (NOT a negation operator; the `!` rule); §5.2 unquoted-condition-atomic-only; the §17.1 `if=` condition-expr rules
4. `schema.map.md` — `_notPrefixNegation` is a LIVE-only diagnostic-support flag on an ExprNode (no new node SHAPE); ATTR_OP_REJECT is a tokenizer token kind (payload `{name,value,op}`), not an AST node
5. NOTE the attr-bare hole (`<p if=not @y>`) + compound-shred hole (`<p if=@x && not @y>`) are closed at the TOKENIZER/AST-BUILDER (not the choke-point), because the unquoted-value reader stops at the `not`-then-space boundary before any expression reaches parseExprToNode — read the S188 structure section before re-diagnosing an attr-position `not`/operator miss

**`server function` keyword eliminate / route-inference trigger / Migration 4 (the S180 arc):**
1. `error.map.md` — S180 Key New Codes block: T7/T8 trigger descriptions, W-DEPRECATED-SERVER-MODIFIER (Info, §34), Migration 4 diagnostic-driven strip logic
2. `structure.map.md` — "Key S180 Source Changes": D1 (emit-client.ts/mcp-descriptors.ts), D2 (route-inference.ts T7/T8 + W-DEPRECATED), D3 (migrate.js Migration 4), D4a (example migration)
3. `error.map.md` — S179 Key New Codes block: E-ROUTE-003 (enforced) / E-ROUTE-004 (new) / I-FN-PROMOTABLE inferred-server skip; the I-FN-PROMOTABLE skip is the keyword-vs-inference blind spot — lint-i-fn-promotable.js + api.js `inferredServerKeys`
4. `test.map.md` — S179/S180 New Test Files section

**audit / non-compliance:**
1. `non-compliance.report.md` — current findings + dispositions (Bug 69 NON-GAP tension flagged as uncertain)
2. `structure.map.md` — what's in-scope vs out-of-scope (archive/, handOffs/, samples/)

**Don't know which** (e.g., open-ended task brief from user):
1. Read `primary.map.md` (this file) in full
2. Read the Task-Shape Routing section above and self-classify
3. If genuinely unclear, surface to PA before consuming further context

## Use feedback loop

When this map's content was load-bearing for a dispatch outcome, the agent's final report should
note **"map content consulted: [list of map files]; load-bearing finding: [one sentence]"**. When
the map content was NOT useful, report **"maps consulted but not load-bearing"** so PA can diagnose
whether the wrong maps were named in the brief OR the map content is at the wrong granularity.
3-5 consecutive "not load-bearing" reports on the same task shape trigger a map-design review.

## Key Facts
- Entry point: `compiler/src/cli.js` → subcommand router; public API in `compiler/src/api.js` → `compileScrml()`; `--emit-engine-graph` flag (S149) writes `<base>.engine-graph.json` sidecar; **`--production`/`--prod` flag (S174) strips `log()` to 0 bytes (§20.6.5)**
- Pipeline: 12 ordered stages BS → TAB → NR → MOD → CE → PA → RI → TS → META → VSS → DG → CG; stage contracts at `compiler/PIPELINE.md`; engine-graph sidecar runs after CG via lazy getter in compile result
- Spec: `compiler/SPEC.md` (**~32,647 lines, 59 numbered sections** (count current to S194; §6.2 + §51.0.J + §52.1 amended/added) + appendices; net +171 lines S175); normative per pa.md Rule 4; §4.14/§51.0.I/§18.0.1/§6.2 amended S159-S160 (colon-shorthand + Shape 4); §4.15/§24.4 amended S162; §59 Value-Native Maps IMPLEMENTED S169; §59.12 set bullet reconciled S170; §21.2 E-EXPORT-001 + §14.3 W-TYPE-FN-FIELD S173; NEW §20.6 (`log()` Location-Transparent Logging) + §34 `W-LOG-SHADOWED` + NEW §14.1.1 no-`any` + §34 `E-TYPE-ANY-FORBIDDEN` S174; **NEW §14.8.8 (cross-file projection-row contracts via structural width-subtyping) + NEW §15.11.5.1 (the passed-vs-stored rule, named) + §14.3 fn-field reject (E-STRUCT-FUNCTION-FIELD) + §15.11.2 typed read-only data-prop note + §15.11.2 Fork-3 identity/value note + §34 +2 rows (E-SQL-ROW-CONTRACT-MISMATCH, W-SQL-ROW-UNTYPED) S175; **NEW §14.1.2 (unrecognized type names — E-TYPE-UNKNOWN-NAME) + NEW §41.18 (scrml:math) + §41.19 (scrml:time.now()) + NEW §41.20 (scrml:random) + §33 DEPRECATED banner (the `pure` modifier; canonical pure = `fn`) + §34 +2 rows (E-TYPE-UNKNOWN-NAME, W-PURE-DEPRECATED) S176**; **S179: §12.5.3 E-ROUTE-004 row added + E-ROUTE-003 now ENFORCED; S180: §12.2 +T7/T8 + §38.4/§38.6 relaxed to any-channel-scope-fn + §39.3.2 `handle` inferred-server + §34 +W-DEPRECATED-SERVER-MODIFIER row**; **S189: §12.2 Trigger 7 amendment (channel-cell-write client-side, RULING A) + §34 +2 rows (E-SYNTAX-045, E-CHANNEL-SERVER-CELL-READ) + §38.6.1/§38.9/§38.3.1 channel amendments + §38.9 v0.3 placement-code reconciliation**; **S194: §52.6.2/.6.3/.6.4 retracted auto-persist (Q1=C/Q2=WF) + NEW §52.6.6 (dev write-fn convention) + NEW §52.6.7 (§52↔§38 channel server-fan-out = explicit composition) + SPEC-ISSUE-026 RESOLVED + SPEC-INDEX §52 row regenerated**; **S196: NEW §19.15 (`<render of=X/>` Render-Expression; .1 Syntax / .2 Semantics / .3 Exhaustiveness fence / .4 What it does NOT do / .5 Codegen) + §19.2 `renders`-clause amend + §4.15/§24.4 register `<render>` + §34/§19.13 ×3 E-RENDER codes + §18.0 E-MATCH-ARM-MARKUP-IN-VALUE row; SPEC.md now ~32,698 lines (top-level numbered-section count unchanged at 59 — §19.15 is a subsection)**
- Error surface: CGError with `severity: 'error'|'warning'|'info'`; W-*/I-* → result.warnings (non-fatal); all else → result.errors (CLI exit 1); emitted-JS parse-gate (E-CODEGEN-INVALID-JS) is default-ON BUT suppressed when compilation already has a prior fatal error (Bug 70, api.js)
- `<each>` codegen is the highest-churn area: Bug 62 (S156) engine-ctx threading in emit-each.ts; Bug 65 (S157) SAME gap in emit-lift.js; Bug 64/R28-1c (S158) per-item content reactivity on reconcile (both tiers); Bug 72 (S158) nested `<each>` inside `${for…lift}`; Bug 73 (S159) per-item EVENT HANDLER live-keying (both tiers); **S200 (`39bd061f`) g-each-peritem-if-predicate C1+C2 in emit-each.ts (`lowerEachExpr` §42 per-item predicate lowering + per-item `if=` conditional) + S200 (`60ace8b4`/`ecba9fee`) g-each-component-body-invalid-js in component-expander.ts (helper-export hoist + transitive import synthesis + nested expression-prop substitution — fixes component-with-helper inlining on BOTH the each per-item AND Tier-0 lift paths)**. Read domain.map.md "Codegen Emit Map" before touching any of these files.
- **S202 the each-inline Class-A arc — completed the S193 trucking-board flagship, board HIGH 1→0 (`d830ec59` CE+lift / `60d547e1` arm-payload)**: the codegen arc that made the board `<each>` / for-lift columns render. **(1) CE markup-attr prop substitution (`d830ec59`, component-expander.ts):** NEW `substituteInterpSegments` (~:1163) substitutes prop refs as LEADING identifiers INSIDE a string-literal markup-attr's `${...}` segments (`${load.id}` member-base, `${cls(status)}` call-arg) via `substitutePropsInRawExpr` (literal class text untouched); the inlined-component root `class=` merge runs AFTER substitution so an interpolated root class emits the substituted expr not raw `${...}` (g-each-inline-component-prop-member-unsubstituted + g-inlined-component-root-class-interp-raw RESOLVED). **(2) LAYER-2 lift (`d830ec59`, emit-lift.js ~:1081):** the `val.kind==="string-literal"` markup-attr branch lowers a string-literal attr value carrying `${expr}` interpolation segments (distinct from the LAYER-1 bare-expression attr value). **(3) arm-payload `<each>` binding (`60d547e1`, g-each-over-arm-payload-binding-unbound RESOLVED):** a TOP-LEVEL `<each in=BARE>` whose iterable is an enclosing match-/engine-arm PAYLOAD binding (`.Loaded(rows)` → `<each in=rows>`) lost its arm association after arm-body flattening → the top-level no-arg render fn emitted `const _items = rows;` UNBOUND → ReferenceError at mount. NEW `armPayloadBinding {cellName, variantTag, fieldName}` field on EachBlockAstNode (emit-each.ts ~:138) + exported `stampArmPayloadEaches` (:242) walk-stamps only a top-level bare-binding each; emitEachBodyRenderForFile (:2022) resolves the iterable from `_scrml_reactive_get(cell).data[field]` (gated on variant===variantTag, else `[]`). ONE shared mechanism for both `buildMatchArms` (emit-match.ts:920) + `buildEngineArms` (emit-engine.ts:2459) arms. **Flagship (`a0f93c92`):** examples/23-trucking-dispatch/pages/dispatch/board.scrml converted from Tier-0 `for/lift` filter blocks to Tier-1 `<each>` over NEW derived filtered cells (`const <incomingLoads>/<activeLoads>/<closedLoads> = @loadRows.filter(...)`).
- **S202 NEW e2e render known-failure-map test capability — `compiler/tests/e2e-render-map/` (`0a0e0391` MVP / `04ad76e3` tier-tags)**: the standing whole-corpus render harness from the e2e-known-failure-map DD MVP. compileScrml({write:true}) → mount in happy-dom → run the D0–D7 ORACLE-FREE render-invariant detectors (compile-fail / mount-throw / console.error / `[object ` in DOM / raw `${` / nullish text / empty-with-seeded-data / `is not defined`) → record per-app/per-seed state + smells. render-corpus-enumerator.js inventories `<program`-rooted apps (examples/ + samples/ + benchmarks/; EXCLUDES stdlib/self-host) tier-tagged flagship/probe/stress/perf/sample. e2e-render-map-baseline.json is the known-failure ALLOWLIST (434 apps / 438 cells) — gaps existing is NOT a failure; the delta-gate fails ONLY on a closed cell re-opening (green→red); the hard gate is `bun generate-baseline.js --check` on CI/pre-push. NO error-class suppression — classifies, never hides (the reverse of the SERVER_EXAMPLES suppression in examples/test-examples.js). 2 test files (e2e-render-map.test.js delta-gate WARN-on-slice + detector-validation.test.js proves D0–D7 fire on the 3 S202 acceptance-bug shapes) + 6 support .js + 3 fixtures.
- **S202 flograph project-graph MVP harness — `scripts/flograph.ts` (`b0346f28`)**: a THROWAWAY validation harness for the flogeance typed-edge + provenance VOCABULARY, run over scrml's own durable `.md` corpus (docs/known-gaps.md + master-list.md by default; `--with-support` opts the scrml-support design corpus in). Parses `<!-- @node id= kind= status= [sev=] -->` (+ `@gap` alias) tokens + typed `[[blocks/supersedes/decided-by/cites: target]]` edges (+ untyped relates + a `verified` provenance bit). Modes: bare REPORT, `--emit` (docs/graph/graph.json + graph.mmd), `--check` (dangling WARN / dup-id+drift ERROR / sweep INFO). House style mirrors scripts/state.ts. The product is flogeance-in-scrml; this proves the vocabulary cheaply against scrml's corpus first (spec scrml-support/docs/flogeance-graph-mvp-spec-2026-06-17.md). Fixture at scripts/flograph-fixture/sample.md.
- **S201 markup-as-value (Pillar 1, §1.4/§7.4) in EXPRESSION position — RESOLVED (`268a27c5` form-c → `2b4ea4d8` forms-a/b → `fa2edccf` render-wiring)**: markup as a VALUE in expression position — a ternary arm (`${ cond ? <a/> : <b/> }`), a derived-cell ternary (`const <badge> = cond ? <a/> : <b/>`), a `return <markup>` value — all now RENDER in happy-dom (was E-CODEGEN-INVALID-JS / raw `< span >`). NEW `MarkupValueExpr` ExprNode + `emitMarkupValueExpr` markup→DOM-node IIFE primitive + `_scrml_render_value(el,v)` node-aware display helper (`v instanceof Node`→`replaceChildren`). Also closed an in-scope Bug-57-class tree-shake gap (emit-client pulls the `derived` chunk for markup-typed derived cells). See domain.map.md "markup-as-value in EXPRESSION position" + structure.map.md "Key S201 Source Changes". Co-landed: g-each-body-bare-variant-arg (`17d2711a`, emit-each `lowerEachExpr` + `serializeCallArgsLowered`) + g-nested-component-member-arg-misparse (`7d3855a6`, component-expander `normalizeTokenizedRaw`).
- **S173 two new diagnostics — additive, zero-codegen (`642950a2`)**: **E-EXPORT-001** (Error, §21.2) rejects exporting a plain (Shape-1) OR derived reactive STATE CELL — previously swallowed SILENTLY (no export in emitted JS; cross-file import → garbage). SHARED MOD-stage check, keyed on `kind:"state-decl"` so `export const Greeting` / `export <channel>` / exported engines stay legal. `collectStateCellNames`+`exportedLocalNames`+`buildImportGraph` [module-resolver.js:97/136/295-313]. **W-TYPE-FN-FIELD** (Info, §14.3) nudged a function-typed struct field — **ESCALATED S175 to the hard `E-STRUCT-FUNCTION-FIELD` (Error); see the S175 facts above**. `checkFunctionTypedStructFields` [type-system.ts:3497] wired at the type pass [type-system.ts:15917], now resolving the field to a distinguishable `FunctionType` (not `asIs`) and firing the reject.
- **S174 location-transparent `log()` builtin — NEW §20.6 (`916b8bb3`)**: `log(...args)` lowers to `_scrml_log(side, loc, ...args)` where `side` ("server"/"client") is COMPILER-CERTAIN per call site (from `ctx.mode`, per-statement-accurate incl. inside CPS-split fns — the §12.4 placement) and `loc` ("basename:line") is resolved at COMPILE time from the call node's BYTE OFFSET (NOT `span.line`, which is unreliable post-re-parse) via NEW `codegen/log-loc.ts` `resolveLogLoc` (224L; + `registerFileSource`/`resetLogLoc`/`fileDeclaresLog`/`SERVER_LOG_HELPER`). Args render value-faithfully (`_scrml_log_render`, §20.6.4 — NOT `JSON.stringify`; value cycles forbidden by §59.5 so it terminates). **Dev unified view (§20.6.3 F2=B):** server `log()` → `scrml dev` terminal; client `log()` → `POST /_scrml/log` (dev.js ~L459) + browser console. **Production strip (§20.6.5 F4=A):** `--production`/`--prod` (compile.js:170) → `log(...)` lowers to `(void 0)` (0 bytes, no `_scrml_log` reference). The `'log'` runtime chunk (`SERVER_LOG_HELPER` prepended in emit-server.ts ~L1766; client chunk in runtime-chunks.ts:138; gated on actual `_scrml_log(` usage) is omitted from shadowed/stripped builds. Wiring: emit-expr.ts (~L1630 three branches) + index.ts (455/587/760 toggles via `setLogProductionStrip`/`setLogShadowedInFile(fileDeclaresLog(...))`) + api.js (`production` opt, 586/1931).
- **S192 engine-varname.ts + E-STATE-UNDECLARED (Error, §6.1.2, bug-12-vkill)**: `engine-varname.ts` (NEW, 41L) is the ONE canonical §51.0.C `autoDeriveEngineVarName` rule, imported by 4 former divergent sites (symbol-table.ts / type-system.ts `engineNameToProjectedVar` / ast-builder.js / codegen/emit-machines.ts). The SYM register/read mismatch (4 divergent implementations disagreed on acronym-leading names like `UIState`→`uiState` vs `uRL`) silently blocked the E-STATE-UNDECLARED fire. Now CLOSED: `E-STATE-UNDECLARED` fires at type-system.ts:6282 POST-CE (where the complete post-CE scopeChain is visible — CE-inlined channel cells / `<each>` row locals / engine boot cells all resolve). SYM PASS 1.d (`walkRegisterRefBindings`) registers `ref=@name` DOM-ref cells so `@myEl` reads no longer false-fire. `W-CONST-AT-DEPRECATED` (Info, §6.6.1): the legacy `const @x = expr` derived-cell form fires at type-system.ts:8688; auto-fixed by `scrml migrate` Migration 4b (`const @x` → `const <x>`).

- **S196 render-expression primitive `<render of=X/>` — held-variant display (`471cbb34` primitive / `fcdec43c` prereq steer; RATIFIED S195 a/c)**: closes g-held-error-display — a variant's `renders` clause (§19.2) previously fired ONLY when an `<errorBoundary>` (§19.6) caught a LIVE `!`-call, never for a variant HELD by the canonical errors-as-states lift (`@phase = .Failed(err)`). `<render of=X/>` is a NEW scrml STRUCTURAL element (parallel to `<errors>`/`<each>`; html-elements.js REGISTRY.set("render") self-closing `rendersToDom:false`, attribute-registry.js `of=` supportsInterpolation:false) that fires a HELD enum value's per-variant `renders` markup at the markup position where the value is held — the held-value counterpart to the boundary catch. **Two loci:** a `<match>` arm-payload binding `<render of=err/>` (wired via emit-variant-guard.ts emitArmWireFunction, the payload is a wire-fn param) AND a top-level `<render of=@cell/>` (reactive — subscribes the cell, re-renders on change; emit-event-wiring.ts). **Codegen (emit-html.ts ~:1041-1173):** reuses `allVariantRenderExprs` + `emitBoundaryMarkupExpr` against the HELD value's `.data` (NOT `_eb_result.data`), emits a per-variant `switch(X.variant)` filling a `<span data-scrml-render-anchor>` innerHTML; SIDESTEPS the `__scrml_error` gate (a new fire site, never pretends held=thrown). `<errorBoundary>` codegen UNCHANGED. **Exhaustiveness fence (the limit-primitives guarantee, §19.15.3):** type-system.ts annotateNodes — E-RENDER-NO-OF [:7692], E-RENDER-NO-CLAUSE [:7729] (REUSES §19.6.6 E-ERROR-005 per-variant logic; every reachable variant MUST declare `renders` or it's a compile error), E-RENDER-NOT-ENUM [:7744] (conservative on `asIs` — no false fire; follow-on g-render-not-enum-asis-miss LOW). Does NOT widen `<errorBoundary>` and does NOT generalize `renders` to non-error enums (option d rejected). **Prereq (`fcdec43c`):** E-MATCH-ARM-MARKUP-IN-VALUE (§18.0, type-system.ts:13446 checkMatchDiagnostics) — an early typer steer for a JS-style value-`match` arm whose body is MARKUP (replaces downstream E-CODEGEN-INVALID-JS / E-SCOPE-001); + 4 error-display seam fixes (rewrite.ts, block-splitter.js, match-statechild-parser.ts, emit-match.ts). **SPEC:** NEW §19.15 (5 subsections) + §19.2 amend + §4.15/§24.4 register. Full suite 24,321/0. Build crashed environmentally 2× (agents aae6f659, a0c27a50), salvaged Layers 1-3 + PA-direct finish. Authority: `docs/changes/render-expr-primitive-2026-06-15/BRIEF.md` + `BRIEF-recovery.md`.

- **S194 §52 server-authority — auto-persist RETRACTED + Tier-1 read-authority codegen (3 commits: `fdcd7fcc` G1 retract / `fff841ca` §52↔§38 bridge / `a78272e5` Tier-1 codegen)**: §52 is now a READ-authority + reactive-wiring layer ONLY (load + SSR residual + E-AUTH diagnostics); the WRITE is the developer's own `?{}` server fn at BOTH tiers. **G1 retract (`fdcd7fcc`):** the §52.6 auto-persist route + optimistic-update machinery is GONE — `emitServerSyncStub` AND `emitOptimisticUpdate` DELETED from `emit-sync.ts` (were 5 refs, now 0 — only an explanatory comment [:124-125] names their removal). An assignment to a `<var server>` cell is the ordinary reactive set (Q1=C/Q2=WF deep-dive ruling). **Tier-1 read-authority codegen (`a78272e5`):** a §52.3.5 `< Name authority="server" table="…">` TYPE-decl + instance (`< Name> @var`) gets a compiler-generated `SELECT * FROM <table>` read-authority load on mount. `emit-sync.ts emitServerAuthorityLoad(varName, table)` [:104] emits the client load IIFE (`fetch("/__serverLoad/<var>", POST)` → assign rows into `@<var>`); `emit-server.ts` emits the synthetic POST route `/__serverLoad/<var>` running `_scrml_sql\`SELECT * FROM <table>\`` server-side [:1591-1615] + now FIRES the server-file emission gate on any server-authority cell [:531-532]; `collect.ts collectServerAuthorityTypes` [:588] collects the Tier-1 instances (DISJOINT from `collectServerVarDecls` [:546] which now excludes them); `emit-reactive-wiring.ts` wires the per-instance load [:618-628]. **Recogniser (`a78272e5`):** `ast-builder.js tryParseServerAuthorityDecl` [:4765] — the §52.3.5 shape lives inside a `${…}` logic block + was PREVIOUSLY swallowed as `html-fragment`; THE GATE is `authority="server"` in the opener [:4847] (sub-shape T registers the type in `_serverAuthorityTypes`, sub-shape I builds a `state-decl` carrying `isServer`/`stateType`/`serverAuthorityTable`). §54.2 substates / §35.2 constructors / local states are UNTOUCHED (gate is `authority="server"`-only). **Diagnostics:** `W-AUTH-001` SUPPRESSED for a Tier-1 instance (its initial load is compiler-generated) [type-system.ts:9037-9042]; `W-AUTH-002` NARROWED to the §52.8 SSR-pre-render residual ("loads client-side after first paint, a brief placeholder flash; SSR pre-render is a tracked follow-on") [type-system.ts:7972]. **SPEC:** §52.6.2/.3/.4 retracted auto-persist; NEW §52.6.6 (dev write-fn convention) + §52.6.7 (§52↔§38 server fan-out = explicit composition of a `?{}` persist + a `broadcast()` in the same fn; NO `broadcast=` attr, NO auto-fan-out); SPEC-ISSUE-026 RESOLVED; SPEC-INDEX §52 row regenerated.
- **S174 `W-LOG-SHADOWED` (Info, §20.6.7)**: a user-declared in-scope `log` (the canonical no-op debugging stub `function log(...)`, or any local/import named `log`) WINS — the builtin steps aside (`log(...)` emitted as a plain call, no origin tag, no forwarding). `log` is NOT a reserved identifier (declaring `function log` is legal — this is a lint, not E-RESERVED-IDENTIFIER). Fired at the SHADOWING DECLARATION by `checkLogShadowing` [type-system.ts:3620] (the type pass has the wired diagnostic stream; codegen's `EmitExprContext.errors` is not reliably populated — emit-expr.ts only suppresses the builtin lowering via `_logShadowedInFile`). Reserved for promotion to `E-LOG-SHADOWED` end-of-window.
- **S174 `any`-reject hard line — `E-TYPE-ANY-FORBIDDEN` (Error, §14.1.1, `f0b3cb04`)**: the literal type-token `any` in ANY type-annotation position (struct/error field, state-cell `typeAnnotation`, fn/function param + return-type). `any` is not a scrml type — "there is no any" (S174 user hard line; TS's type-check opt-out has no scrml equivalent). The sanctioned untyped escape hatch is `asIs` (named, greppable, analogous to TS `unknown`). Today `any` falls through `resolveTypeExpr` to `asIs`/`unknown` SILENTLY; `typeTextMentionsAnyToken` [type-system.ts:3678] (splits the raw type string on non-identifier chars, tests for a bare `any` atom — catches `any`/`any[]`/`any | not`/`[string: any]`/`{ payload: any }`, NOT a NAME containing the substring like `Company`/`manyThings`) catches it BEFORE that collapse. `checkAnyTypeForbidden` [type-system.ts:3720] wired at the type pass [type-system.ts:15924]. `any`-token-SPECIFIC — an arbitrary undefined type name (`Frobnicate`) ALSO resolving to `asIs` was a SEPARATE broader leak, now **CLOSED S176 by `E-TYPE-UNKNOWN-NAME` (§14.1.2)** — the symmetric companion check over the identical `forEachTypeAnnotationLocus` loci (registry-dependent, wired after the imported-types seed). Corpus migration: ~23 `any`→`asIs` sites in examples/23-trucking-dispatch + samples + stdlib/http rode the wrap.

- **S175 typed-SQL-row arc — the flagship typed-data delivery (`45bea7c5` T1 / `1dbf67b4` T2 / `95c25b67` T3)**: end-to-end SQL-row type-flow. A `?{ SELECT ... }` query resolves to a typed projection-row struct (`resolveSqlRowType` joins NEW `sql-projection.ts extractSelectProjection` against the generated table types). **Read-site (T2a):** a projection-row binding (loop var over `Row[]`, or unwrapped `Row | not`) is typed as the row struct — `r.<projected-col>` → the column type, `r.<unknown>` → E-TYPE-004 (`resolveIterableRowElement` :5812 / `checkRowFieldAccessInExpr` :9420). **Prop-contract (T2b):** a row passed to a component's `:struct` prop STRUCTURALLY width-subtypes INTO the contract — every contract field present + assignable, EXTRA columns OK, one-directional; a miss is `E-SQL-ROW-CONTRACT-MISMATCH` (`checkPropContract` :9491, fed by `__propContractChecks` stamped by component-expander.ts; delegates to `checkSqlRowWidthSubtype` :704). **Cell-contract (T3b):** same width-check INTO a `:struct`-typed state cell (`checkSqlRowAgainstCellContract` :9629). **Fn-return (T3c):** a server-fn returning a row over-approximates via `inferReturnTypeFromBody` :5917 stamping the `<fn-return>` sentinel — EXEMPT from the reject. Untyped long-tail (`*`/CTE/UNION/subquery/unknown-col) degrades to `asIs` + `W-SQL-ROW-UNTYPED`. Boundedness IS the design — width-subtyping applies ONLY projection-row → declared `:struct` (general struct assignment stays NOMINAL §14.8.1; generated table types stay WALLED inside `<db>` — §14.8.8 is a single bounded exception). F-SCHEMA-001 (protect-analyzer.ts `extractSchemaCreateTableStatements` :471) adds `<schema>` DDL as a 3rd ColumnDef source; schema-differ.js `generateCreateTable` now exported. type-system.ts grew +990 lines (~19,200L); NO new FileAST node shapes.
- **S175 function-boundary rule — `E-STRUCT-FUNCTION-FIELD` (Error, §14.3/§15.11, `9e6156c4`; ESCALATED from the retired S173 `W-TYPE-FN-FIELD` Info-nudge)**: a function-typed struct field (`onClick: () -> void`, `cb: fn()`) is HARD-REJECTED at declaration. A function is NOT value data — no structural equality (§45.2), not serializable, not a map key (§59.4) — so it must never be STORED on a value-shaped collection (the limit-the-primitive axiom §14.1.1; the STORED face of the NAMED passed-vs-stored rule §15.11.5.1: PASSED [a prop, `W-COMPONENT-001`] / CALLED [handler] legal, STORED [struct field / state cell] rejected). The field type now resolves to a distinguishable `FunctionType` via `tFunction()` (NOT opaque `asIs`) so the reject is precise; lifecycle annotations `(A to B)`/`(A -> B)` (arrow WRAPPED in outer parens) are NOT function types and never reject. `checkFunctionTypedStructFields` :3497 via `isFunctionTypeAnnotation` :2087, wired at the type pass :15917. The S173 `W-TYPE-FN-FIELD` Info code no longer fires anywhere (grep-confirmed absent).

- **S176 unrecognized-type-name reject — `E-TYPE-UNKNOWN-NAME` (Error, §14.1.2, `46cffc83`)**: closes the BROAD unknown-type-name leak the S174 §14.1.1 `any`-reject explicitly deferred. Every type-NAME leaf in EVERY type-annotation position must resolve to a built-in / a same-file decl (incl. forward ref) / an import / the `asIs` escape hatch; a typo'd / undefined PascalCase name (`Frobnicate`, `LaodCardRow`) now RED-fires instead of silently collapsing to `asIs`. SYMMETRIC to `checkAnyTypeForbidden` by construction — both drive `forEachTypeAnnotationLocus` :3997 → identical loci; the difference is the per-leaf predicate (position-aware `forEachTypeNameLeaf` :3759, `emitMapKeys:false`, NOT the flat `any`-token atomize) → `isUnrecognizedTypeNameAtom` :3726. REGISTRY-DEPENDENT: `checkUnknownTypeNames` :4232 is wired AFTER the imported-types seed at :16464 (vs the registry-free any-check at :16425). `exemptTypeNames` = import-specifier names (single-file-mode guard so `<loadRows>: LoadCardRow[]` doesn't RED-fire on a single-file compile) + machine names (`@state: M`, lives in the machineRegistry). Out of v1 scope: db-block generated DB types, native `.scrml` mirrors, type-as-argument idents.
- **S176 `pure`-modifier deprecation — `W-PURE-DEPRECATED` (Info, §33, `4a19a047`)**: the `pure` modifier is DEPRECATED language-wide; the canonical pure form is `fn`. Fires on ANY `pure`-modified declaration (SUPERSEDES the narrower `W-PURE-REDUNDANT` `pure fn`-redundancy nudge), gated on `isPure` at the fn-decl walk :7597. Auto-fixed by `scrml migrate` **Migration 3** [commands/migrate.js:197]: `[server ]pure[ server ](function|fn) NAME(` → `[server ]fn NAME(` (declaration-shape regex so prose is untouched; idempotent; applied after Migrations 1+2). Not yet a hard reject. `W-PURE-REDUNDANT` no longer fires.
- **S176 scalar-vocabulary stdlib + capability classes (DD1 Fork 1, `beb8a115` math+time / `35172d78` random)**: closes the scalar-vocabulary gap. **`scrml:math`** (§41.18, PURE) — `round`/`floor`/`ceil`/`abs`/`min`/`max`/`clamp`/`parseInt`/`parseFloat`/`toNumber`/`isNaN`; the centralized sanctioned `Math.*` touch (deterministic only — `Math.random` is NOT here); usable in a pure `fn`. **`scrml:time.now()`** (§41.19) + **`scrml:random` `random()`/`randomInt()`** (§41.20) are CAPABILITY-SCOPED NON-DETERMINISTIC (class-C IO) — each is the ONE sanctioned host touch (`Date.now()` / `Math.random()`); calling any in a pure `fn` body is **E-FN-004** (`function`/`server function` are not pure → legal). **E-FN-004 GENERALIZED**: the non-det gate now fires on imported non-det stdlib BINDINGS (bare `now()`/`random()`/`randomInt()`), not just hard-coded host member-expressions — registry-driven (`NONDET_STDLIB` :6589, `collectNonDetStdlibBindings` :6598, fire :17511) so the next non-det primitive extends with one registry row. http's retry-jitter de-leaked through `scrml:random`; api.js `bundleStdlibForRun` (:302) now recursively copies a shim's TRANSITIVE same-directory sibling-shim deps into `_scrml/` (:328-356) so the runtime resolves them. NEW files: `stdlib/math/index.scrml` (214L) + `compiler/runtime/stdlib/math.js` (66L); `stdlib/random/index.scrml` (88L) + `random.js` (36L); `stdlib/time/index.scrml` + `time.js` gained `now()`. stdlib catalog now 21 module dirs.
- **S177 g-formfor-in-arms arc + bug-tail batch + client stdlib-inliner (3 landings: `b1931f02` bug-tail / `75f724af` g-formfor / `c48c4f71` client-inliner)** — ALL additive; default pipeline output UNCHANGED except where a formFor/component lives inside an engine state-child or match arm (was RAW = silent non-render), an inline map-method assign sits in a handler (was TypeError), or a stdlib chunk imports a sibling shim (now inlined). NO new error codes; ONE new walkable AST field. **g-formfor (`75f724af`):** the markup-EXPANSION passes (component-expander `walkAndExpand`, type-system formFor/tableFor `walkAndSplice`) descended into `.children`/`.body` but NOT an engine-decl's `bodyChildren` (state-children) nor a match-block's per-arm body — a `<formFor>`/`<tableFor>`/`<Component>` inside an `<engine>` state-child (r27-c6) or a `<match>` arm (g-formfor-in-match-arm) was emitted RAW. ast-builder builds a NEW walkable `match-block.armBodyChildren` (`buildMatchArmBodyChildren` :12128, re-parsed from `armsRaw`, `_matchArmBodyForm` tag) parallel to the raw `armsRaw` codegen path; `walkAndSplice` (formFor :15159 / tableFor :16395) + `walkAndExpand` (:2710) recurse into BOTH `bodyChildren` + `armBodyChildren`; emit-match `buildMatchArms` (:522) consumes the expanded wrapper (lookup :676-694) for formFor/component arms. LIVE-only (within-node STRIP_KEYS += `armBodyChildren`/`_matchArmBodyForm`). **bug-tail (`b1931f02`):** bug-74 E-CLOSER-001 on `/>`+`:`-shorthand body (`isGenuineShorthandBodyNotDirective` block-splitter.js:1085) · bug-4 E-SYNTAX-050 bare-`/` no longer over-fires before a close tag (refined `looksLikeCloser` :2973, EOF/before-new-opener only) · bug-48 opener `>`-finder paren/bracket depth (`on=@nums.filter(c => c == 1)` not truncated; ast-builder + emit-match `resolveOnExpr` :272 complex-on= Shape-B lowering) · r28-7b schemaFor predicated-primitive-in-union recovery (:15596, no bogus E-SCHEMAFOR-NO-SQL-MAPPING) · s169-map-inline-insert handler routes through `emitExprField → emitAssign` (emit-event-wiring.ts:480-506, `_scrml_map_insert` resolves; was TypeError). **client stdlib-inliner (`c48c4f71`):** runtime-template.js `_loadStdlibChunk` (:47) runs NEW `_inlineSiblingShimImports(source, shimDir, emitted)` (:116) — a relative `./x.js`/`../x.js` sibling-shim import is INLINED transitively, an external `bun`/`bun:sqlite`/`node:*`/bare import is STRIPPED (loud-fail); the CLIENT-side mirror of api.js:302's server `bundleStdlibForRun` (S176). data.js (:48) + auth.js (:38) Math de-leaked through `scrml:math` (`./math.js` sibling import) — these are the transitive sibling shims the new inliner resolves.
- **S186 channel codegen fix — reconnect bare-int (Bug 1) + onserver/onclient handler wiring (Bug 2) (`658cb1a9`, change-id channel-codegen-fixes-2026-06-12, FINAL_SHA `2dda0642`)** — Channels §38 dog-food. NO new codes. **Bug 1 (LOW):** type-system.ts `visitAttr` [10399-10410] EXEMPTS `reconnect` (§38.3) / `channel-reconnect` (§38.3.1) from the E-SCOPE-001 scope-check (mirrors the `ref` guard) — the §38.2 bare-int form (`reconnect=2000`, parses to a `variable-ref` named "2000") was false-firing; `<input value=42>` still errors. **Bug 2 (MED):** channel `onserver:*`/`onclient:*` handler attrs wire end-to-end from real source. emit-channel.ts `channelAttrToCall` [:306] + `channelAttrParam` [:327] recognize `call-ref` (+ §38.6.1 `messageParam`): `onserver:message` → server `message()` JSON.parse → handler(msg); `onclient:open/close/error` → client `ws.on{...}`. NEW exported `collectChannelAttrHandlerNames` [emit-channel.ts:546] feeds route-inference, which adds the `channel-ws-handler` reason kind [:110] + `FunctionRoute.isChannelWsHandler` [:272]: `onclient` stays CLIENT (§38.10 wins over §12.2 Trigger-7, normative §38.10.2); `onserver` force-escalates as a plain callable server fn with a DEAD HTTP route + suppressed client fetch stub; normal channel publishers keep route+fetch. emit-server.ts diverts `onserver:*` to the plain-function path invoked from `_scrml_ws_handlers` [:388/:1582]; emit-functions.ts:505 `continue`s past an `isChannelWsHandler` route so it emits as an ordinary `function` BEFORE the no-route E-CG-002 check (was false-firing). +11 tests (incl. 6 real-source canaries closing the S138 synthesized-AST-masks-real-source canary), full suite 23,946 → 23,957 / 0 fail / 221 skip. 5 NEW open dog-food gaps (g-channel-onserver-cell-read MED · g-channel-spec-38-9-stale LOW · g-channel-topic-forward-ref LOW · g-schemafor-pa-unrecognized MED · g-markup-const-consumes-cell-decl LOW; SCOPE at docs/changes/s186-dogfood-followups-2026-06-12/).
- **S189 — given-rebind reject + channel-cell-write client-side RULING A + schemaFor PA source + §6.9 reactive-cell hoist (`a00624f5`, 3 arcs)** — +2 NEW §34 codes. **E-SYNTAX-045** (Error, §42.2.3): `given <name> = <expr> :> { ... }` rebind shape rejected at BOTH given-guard parse sites (logic [ast-builder.js:6682] + markup [ast-builder.js:10899], adjacent to E-SYNTAX-044); `given` narrows in place, never rebinds — detector keys on `peek()` PUNCT `=` (`==`/`=>`/`:>` are OPERATOR so equality/separators never false-fire); recovers past `= <rhs>`. **E-CHANNEL-SERVER-CELL-READ** (Error, §38.4/§38.6.1, change-id `channel-cell-write-client-side-A-2026-06-12` RULING A): a SERVER-context channel fn (escalated via `broadcast()`/`disconnect()`, `onserver:*`, or any §12.2 trigger) READING a client-held channel cell (`@<cell>`) — the server has no value for it (client-held, §38.4) so the read is `undefined` → silent crash. NEW `detectServerContextChannelCellRead` walker [route-inference.ts:1525] structurally scans body ExprNode fields via `forEachIdentInExprNode` (RHS-read aware, string-content + nested-fn exempt), fires once [route-inference.ts:3423]. **RULING A Part 1 — §12.2 Trigger 7a DROPPED**: `detectChannelBroadcastReason` [route-inference.ts:1433] no longer escalates a channel-cell WRITE to the server; Trigger 7b is now `broadcast()`/`disconnect()`-only — a channel-cell WRITE stays CLIENT. (Closes the S186 g-channel-onserver-cell-read MED follow-up.) **schemaFor CREATE TABLE PA source (no code):** NEW `extractSchemaForCreateTableStatements` [protect-analyzer.ts:605] + `paPluralizeStructName` [:545] + `splitStructFieldsTopLevel` [:559], wired as a 4th lowest-precedence ColumnDef source in `runPA` [:911] — `${ schemaFor(Struct) }` inside `<schema>` is recognized as a table def (closes S186 g-schemafor-pa-unrecognized MED). **§6.9 reactive-cell hoist (no code):** NEW `preBindReactiveStateCells` [type-system.ts:10589, called :10609] in `annotateNodes` after `preBindExportedNames` — seeds file-scope reactive cells before the walk so attribute forward-refs resolve. SPEC 32,256 lines (§12.2 Trigger 7 amendment + §34 +2 + §38.6.1/§38.9/§38.3.1 channel amendments + §38.9 v0.3 placement-code reconciliation); SPEC-INDEX updated.
- **S170 Bug B — structural-compound deep-set retarget (`72aa6836`, LIVE/codegen, CLOSED — last open HIGH)**: `@a.ref = v` on a Variant-C structural compound emitted a write to the COMPOSITE key `a` that the derived recompute clobbered (lost mutation, NO diagnostic, failed even for ONE write). Fix: `reactive-deps.ts:stampCompoundDeepSetTargets` (~L739) stamps `_deepSetLeafKey`/`_deepSetResidualPath`; `emit-logic.ts` (~L3025) retargets to the leaf cell. SPEC §6.3.2. known-gaps HIGH 1→0.
- **S169 §59 Value-Native Maps — IMPLEMENTED end-to-end**: a `[KeyT:ValT]` map compiles on the default pipeline + has native literal parity. Reads bracket (`@m[k]` → `V | not`), writes method (`E-MAP-BRACKET-WRITE` on `@m[k]=v`), keys structural-`==`-identified. 7 §34/§59 codes have fire sites. The cycles-prereq (`8d9db4e1`, S168) is its enabling groundwork.
- **S168 cycles-prereq COW-all bracket-write (`8d9db4e1`, LIVE)**: a bracket-index WRITE `@arr[i] = x` routes through copy-on-write (`reactive-nested-assign` → `_scrml_deep_set` with a HETEROGENEOUS path); bracket READS stay verbatim. `_scrml_structural_eq` gained a WeakMap/WeakSet cycle guard. The build prerequisite for value-native maps.
- **S167 deep-set write-loss fix (HIGH Bug A, LIVE)**: consecutive dotted-path deep-set / array-mutation writes inside a `function` body were SILENTLY DROPPED at codegen unless they were the FIRST statement. Fixed in ast-builder.js `parseLogicBody`/`collectExpr` (~L2747) via a forward-scan statement-boundary recognizer. Codegen UNCHANGED.
- **S172 derived=match arm-arrow deprecation (`d7de8a60`, lint+migrate, LIVE/lint-only)**: the §51.0.J `derived=match` engine arms joined the §18.2 `:>` deprecation; raw-text body → ast-builder.js `scanInlineMatchArmArrows()` stamps `engine-decl.inlineMatchArmArrows[]`; type-system.ts fires W-MATCH-ARROW-LEGACY; migrate.js `--fix` rewrites.
- **S172 `scripts/state.ts` DD3 project-state tool**: `bun scripts/state.ts` prints state-at-HEAD; `--write` regenerates `@generated:*` sections; `--check` is the currency gate (maps-staleness is WARN-only, so this 6c refresh does not gate the wrap). Step 6d generator.

### Native-Parser Swap — current state (S161 ratified / S163 engine-substrate fix / S164-S170 parity-closer grind)
- **The active strategic line**: drive `--parser=scrml-native` to DEFAULT, then DELETE legacy block-splitter (BS) + Acorn at M6 (direction-a, ratified S161; realistic v0.8 target). **The Phase-A default-flip is a STANDING USER DECISION — PA ships parity-closers, never "the flip."**
- **CLOSED — each/match structural promotion (S162)**, **F1 engine-substrate silent-miscompile (S163, `a41df176`, machineDecls two-instance object-identity defect)**, **B1 reset-expr (S163)**, **§51.0.S message-arm + exprNode (S164)**, **F2-match / promote-each / R1 typed-`@cell` / server-fn-star (S165)**, **bare-`function` failable + cross-file `${...}`-export (S166)**.
- **Flip re-measure: 605 native-only flip-failures on `df08f282` (S170 baseline) → ~508 after the two S170 waves** (was 451 S165-close; the 605 is a FRESH full re-measure on the new baseline counting the current corpus / opt-in `--parser=scrml-native` against the default 0-fail / fully-green pipeline). Default output UNCHANGED across S162-S174 (every closer is shadow-only). Full suite at S170 fix-wave-2: 23405 pass / 0 fail. Remaining families (full table in domain.map.md "Native-Parser Swap Orientation"):
  - **LANDED S164:** §51.0.S message-arm (B2) `7cbad5dd` + native attr-value `exprNode`/`argExprNodes` `c1566faa` (family FULLY closed) · lift `<markup>` close-tag `649f4ef8` · F2a chained `?{}.method()` `7e54f321` · table-for struct-field-drop `66301357`.
  - **LANDED S165:** F2-match string-literal arms `2c2e5bb2` · promote-each 3 §17.4 for-stmt gaps `785f24d1` · R1 typed-`@cell` decl `89912bb9` · server-fn-star `function*`+yield `26a24b71`.
  - **LANDED S166:** bare-`function` failable `76059024` — native `parseFunctionDecl` (parse-stmt.js:1695) consumes the trailing `!` failable marker + error-type annotation (`! -> Err` arrow form §19.4.1 + bare `! Err` form §41.14 with R25-Bug-36 continuation disambiguation), ported verbatim from `parseScrmlFunctionDecl`, and threads `{canFail,errorType}` as `makeFunctionDecl`'s 7th arg (was 6 → metadata silently dropped). fnKind stays `"function"`. Closes the bare-`function` failable parse gap (`fn`/`server`/`pure` already worked; ~31 failable-via-`function` test files). New native error `E-STMT-FN-ERROR-TYPE` on a malformed `! ->`. · cross-file `${...}`-export raw-slice `9d12d980` — native `synthExportDecl` (collect-hoisted.js) anchors the raw slice to `block.bodyStart` (the LogicEscape inner-body host-start = `frame.openSpan.end`, one byte past `${`) instead of `block.span.start` (the opener `$`); over-shifting LEFT by the opener length made `hi` overshoot `blockText.length` → `raw=""` → cross-file CE registry empty → spurious E-COMPONENT-020/035. parse-markup.js stamps `block.bodyStart` at 4 body-attach sites (the `${...}`/`^{...}` branches in `emitContextBlock` + `synthLiftedLogicBlock`/`synthPairedLogicBlock` where `bodyStart === span.start`); `walkStmts`→`synthExportDecl`/`collectComponentDefs` thread `bodyStart` (fallback `blockSpan.start` preserves the working file-top path); within-node-classifier.ts adds `bodyStart` to STRIP_KEYS (native-internal raw-slice coordinate, no live analogue — resolved WITHOUT an allowlist rebump). Same off-by-opener class as M6.7-C1 `synthComponentDef`.
  - **LANDED S170 fix-wave-1 `5a346faa`:** GROUP P — `on mount`/`on dismount` lifecycle desugar (§6.7.1, parse-stmt.js; ~52 incl. cross-file cascade) + `const @name` → `parseConstAtStateDecl` derived-state-decl (F5 CLOSED; the brief's `parseBindingIdent` was empirically wrong — live treats `const @name` as a DERIVED state-decl). GROUP T — deepset/array-mutation node-synth (native emits live `reactive-nested-assign`/`reactive-array-mutation` → COW + trigger, routing through the S170 Bug-B-fixed emit-logic) + destructured-param structuring (false E-SCOPE-001 closed) + var-decl `typeAnnotation` thread (E-VARIANT-AMBIGUOUS → E-CONTRACT-001). GROUP W — NEW `src/native-walker/exprtext-backfill-walker.ts` `backfillNativeExprText` stamps `.expr`/`.init`/`.condition` from structured `exprNode`/`initExpr`/`condExpr` siblings (INERTNESS VERIFIED 295/297 byte-identical; 2 deltas a NET-POSITIVE latent `const token;` fix). FIX 3 (arrow-shorthand fn body) correctly NOT implemented (invalid scrml — live REJECTS); FIX 4 (`export <cell>`) deferred pending SPEC ruling. +24 native-parser tests; within-node allowlist surgically reconciled (34 over-budget → current; PARSE-FAILURE:0/NESTED-SHAPE:0).
  - **LANDED S170 fix-wave-2 `cc69c62d` (the Mario fix):** native `reconstructArmBody` returned literal `"{}"` for block-bodied match arms (dropping their statements — the dominant ~1463-file corpus form; Mario's MUSHROOM click fired but never transitioned Small→Big) + same gap dropped lambda callback block bodies. Fix: `parse-expr.js parseBlockStub` stamps `stub.verbatim` (balanced `{...}` slice; render-body scope-guard skips lift/markup arm bodies) + `translate-expr.js reconstructArmBody` returns `body.verbatim`, `translateArrow`/`translateFunctionExpr` emit a full-lambda `EscapeHatchExpr` (mirrors live expression-parser.ts:2152-2192). +17 closures, ZERO new failures, full suite 23405/0, +5 statement-survival canary tests. DEFERRED (pre-existing, NOT introduced): code-position match-BLOCK promotion for render arm bodies · engine-write-guard routing for match-as-statement over an engine cell · single-word bare-display-text silent-drop tokenizer bug (filed).
  - **NEXT DISPATCH (re-run flip harness to re-rank):** F4 formFor expansion (~32, was largest open) / F2 top-level server-fn `?{}` body-drop + assign-RHS `@x = ?{}.all()` / F6/F9 fn-param/export-fn-body / `effect=` opener (small) / mario PowerUp payload-enum / F7 missing-diagnostics (lower-leverage). **F5 `const @name` CLOSED S170.**
  - **S166 BANKED (STOP-IF-DIVERGENT, not landed):** native empty `fail X::V(arg)` envelope (function + fn forms) · native `renders ${id}` interpolation break · ROOT-1 exported-inner-decl reaching codegen (emit-fix worked but surfaced a 58-fixture within-node divergence; 2 prereqs — deep-shift promoted spans by `bodyStart` + the native FunctionDecl trailing-match-as-return + return-type-annotation drop, which reproduces on NON-exported fns too, a separate native gap).
  - **mario PowerUp payload-enum (NEW S163)** — native captures only `["Mushroom"]` (drops payload variants), mis-emits `PowerUp.Flower(3)` as `"Flower"(3)`; payload-bearing-enum native gap (mario residual 133 diff-lines).
  - **`effect=` opener (§51.0.H Form 3, small)** — native `synthEngineDecl` has no openerEffect read.
  - F2 SQL `?{}` in server-fn (~58) — `parse-sql-body.js` drops SQL body in top-level server fns.
  - F3 if-as-expr residual (small) — same-line match arms DONE S162 + string-literal arms DONE S165 (F2-match); if-as-expr residual LOW.
  - F4 formFor expansion (~32) — native parse→bridge→form pass drops field-markup expansion.
  - ~~F5 `const @name` derived-decl~~ — **CLOSED S170** (`parseConstAtStateDecl`, parse-stmt.js).
  - F6/F9 fn param / export-fn-body (~16) — `parse-stmt.js` / `parse-expr.js`.
  - F7 missing diagnostics (~15) — body-parser gates swallow `E-STRUCTURAL-ELEMENT-MISPLACED` etc.
  - F8 stdlib `await import()` (13) — RULED a stdlib-migration task (native stays strict no-`await` enforcer); NOT a native-parser change.
  - Other large signatures spread across the remaining ~508 (re-measure for current counts): `E-CODEGEN-INVALID-JS`, `E-TYPE-063` + `E-VARIANT-AMBIGUOUS` (native bare-variant resolution), `E-TYPE-001/-020`, `E-MATCH-NOT-EXHAUSTIVE` + `E-MATCH-SUBSET-DEAD-ARM`.
- **`.js`/`.scrml` mirror staleness (S162)**: the `.scrml` self-host mirrors are FEATURE-stale, not merely predicate-drifted — whole machinery is missing vs the `.js`. S115 `.js`/`.scrml` lockstep is MOOT for native-parser fixes until a deliberate re-sync. Native fixes land in the `.js`; do not block on the `.scrml`.
- null/undefined: BOTH do not exist in scrml (`W-ABSENCE-IN-SCRML-SOURCE`); `""` / `0` / `false` ARE defined values; `async`/`await`/`switch`/`try`/`throw` are forbidden vocabulary. **`any` does NOT exist either (S174 hard line, `E-TYPE-ANY-FORBIDDEN`) — `asIs` is the only named untyped escape hatch.**
- type-system.ts is **~20,232 lines** (largest single source file; +990 S175, +~980 S176, +51 S177 r28-7b/r27-c6/g-formfor); type-checking, linear types, validity-surface synthesis, enum-subset resolution, match-as-expr exhaustiveness, markup-attr `.advance` two-plane check, E-COLON-SHORTHAND-ON-VOID guard (S159), S160 ruling (c) implicit-lifecycle synthesis + `runRefinementNoRhsDefaultCheck`, §59 map key-comparability (S169), **`checkFunctionTypedStructFields` (S173/S175 — now fires `E-STRUCT-FUNCTION-FIELD` hard reject, :3497), `checkLogShadowing` (S174, :3620), `checkAnyTypeForbidden`/`typeTextMentionsAnyToken` (S174, :3720/:3678) — wired in the type pass at :15917/:15920/:15924; S175 typed-SQL-row flow: `resolveSqlRowType` :5676 / `checkSqlRowWidthSubtype` :704 / `checkPropContract` :9491 / `checkSqlRowAgainstCellContract` :9629 / `resolveIterableRowElement` :5812 / `checkRowFieldAccessInExpr` :9420 / `inferReturnTypeFromBody` :5917 + the distinguishable `FunctionType` (:324) / `tFunction()` (:830) / `<fn-return>` sentinel (:631); **S176: `checkUnknownTypeNames` :4232 / `isUnrecognizedTypeNameAtom` :3726 / `forEachTypeNameLeaf` :3759 (E-TYPE-UNKNOWN-NAME, symmetric to checkAnyTypeForbidden, REGISTRY-DEPENDENT so wired at :16464 AFTER the imported-types seed vs the any-check at :16425) + `W-PURE-DEPRECATED` :7597 + the E-FN-004 generalization `NONDET_STDLIB` :6589 / `collectNonDetStdlibBindings` :6598 / fire :17511**; **S177: the formFor `walkAndSplice` :15159 + tableFor `walkAndSplice` :16395 now recurse into engine `bodyChildren` (r27-c6) + match `armBodyChildren` (g-formfor-in-match-arm); r28-7b schemaFor predicated-primitive-in-union recovery :15596**
- symbol-table.ts is **11341 lines**; engine state-child walkers, PASS 20 match exhaustiveness, message-arm exhaustiveness (S155), W-COLON-SHORTHAND-LEGACY-PLACEMENT emission (S160) at two sites
- ast-builder.js is **~15,268 lines approx** (+250 S190 Cluster C + derived= classification); S160 ruling (c) Shape 4 no-RHS synthesis; S167 deep-set write-loss boundary recognizer; S168 cycles-prereq `collectAtPathSegments`; S177 g-formfor: NEW `match-block.armBodyChildren` (`buildMatchArmBodyChildren` ~12128, walkable per-arm body AST re-parsed from `armsRaw`) + bug-48 paren/bracket-depth opener-finders / `on=`-capture loops; **S185 errarm re-fail-from-arm: `_parseFailExprString` (builds a `fail-expr` node from an arm-result string) + `arm.failExpr` attach in `parseErrorTokens`/`parseOneMatchAsExpr` + `?`-propagate const-decl hook; S185 validator-colon: `tryRecoverColonInlineMessage(afterValidatorIdx)` in `scanStructuralDeclLookahead` (E-VALIDATOR-INLINE-COLON reject + paren-form recovery, two call sites)**
- module-resolver.js — **S173 E-EXPORT-001** reactive state-cell export reject (`collectStateCellNames`+`exportedLocalNames`+`buildImportGraph`, :97/136/295-313)
- **codegen/log-loc.ts (NEW S174, 224L)** — location-transparent `log()` resolver: `resolveLogLoc(span)`→"basename:line" from byte offset, `registerFileSource`/`resetLogLoc` per-compile cache, `fileDeclaresLog(fileAST)` shadow detector, `SERVER_LOG_HELPER` runtime chunk (`_scrml_log`/`_scrml_log_render`). Consumed by emit-expr.ts (lowering), emit-server.ts (helper prepend), index.ts (toggles)
- Native parser files (largest, S170 line counts): `parse-stmt.js` (4482L), `parse-expr.js` (4368L), `parse-markup.js` (3109L), `tag-frame.js` (2402L), `translate-stmt.js` (2442L), `parse-file.js` (1671L), `translate-expr.js` (1251L), `collect-hoisted.js` (900L), `ast-stmt.js` (605L), `ast-expr.js` (493L). NEW S170 walker `src/native-walker/exprtext-backfill-walker.ts` (200L, run from api.js:960); native attr-value `exprNode`/`argExprNodes` stamped by `src/native-walker/attrvalue-exprnode-walker.ts` (api.js:945). See structure.map.md "Native-Parser File Table (S170)".

- **S191 Tailwind composing-utility families §26.7.x + if=fn() condition routing (`7f2092cf`)**: TWO arcs, ZERO new §34 codes. **Tailwind (THE biggest prior map gap — dev agents flagged its absence 3×; ALL composing families now COMPLETE):** `tailwind-classes.js` Approach-C inline-`var()`-fallback composing model — `registerRing()`+`ringShadowSetter()`+`BOX_SHADOW_COMPOSE` (ring/ring-offset/shadow §26.7 Phase 1), `registerGradient()` (gradient §26.7.1 Phase 2), `registerTransform()`+`TRANSFORM_COMPOSE` (transform §26.7.2 Phase 3), `registerFilters()`/`registerBackdrop()`+`FILTER_COMPOSE`/`BACKDROP_COMPOSE` (filter/backdrop §26.7.3 Phase 4); all invoked in the registry build [:3008-:3012]. Each utility emits a shared `var()`-based composing shorthand + sets one `--tw-*` prop; **every `var()` carries an inline fallback so NO global `*, ::before, ::after` preflight block is emitted** (§26.1/§26.2 "only what's used" minimalism). Net: these class tokens are now RECOGNIZED → `W-TAILWIND-UNRECOGNIZED-CLASS` no longer fires on them. **if=fn() condition routing:** a bare-call `if=fn()`/`show=fn()`/`else-if=fn()` (`call-ref` attr value) now routes as a reactive conditional (parity with the paren form `if=(fn())`), NOT a nonexistent DOM event binding / NOT a cell read — emit-html.ts call-ref attr-value branch [:1773] (`g-attr-if-fn-call-misroute`) + clean-subtree if= handler [:1413] (`g-attr-if-fn-display-not-mount`) + emit-event-wiring.ts `_update_chain` call-ref head [:1282] (`g-attr-if-fn-chain-head-call-misroute`). +4 test files (filter-family 28, gradient-family 14, attr-if-fn-call-conditional 12, attr-if-fn-condition-followup 5). SPEC §26.7/§26.7.1/§26.7.2/§26.7.3 + §5.1 updated.
- **S190 §51.0.J derived-engine expression form (full suite 24100/0 at `f0030049`)**: `derived=match @x {}` (inline-match) + `derived=@a > 0 ? .On : .Off` (arbitrary expr) + `derived=classify(@x)` (call) — three `derivedExpr.kind` discriminants in symbol-table.ts; B16 NO-RULES/NO-INITIAL/NO-WRITE/CIRCULAR fire for modern kinds; emit-engine.ts C14 `_scrml_derived_*` substrate recompute + dependency-graph per-upstream DG edges; type-system.ts modern forms skip `validateDerivedMachines`; E-ENGINE-004 message steers plain-cell `derived=@var` to the `derived=match @var {}` form. **Cluster C parser fixes**: `E-DECL-RHS-INTERP-WRAPPED` (ast-builder.js) rejects a `${}` wrapped decl RHS + recovers; `markupRootClosed` boundary stops a markup-const from consuming sibling decls. SPEC §6.2/§34/§51.0.J/§52.1 + PRIMER §13.7 updated.
- **Bug 69 / NON-GAP tension:** hand-off.md records a conflict: user said "fold Bug 69 in" (tableFor §41.16.6 subset reach) but the S156 CLOSE block called it NON-GAP. Confirm with user before scheduling (d)-A batch 5. See `non-compliance.report.md` Uncertain section.

## Tags
#scrmlts #map #primary #compiler #bun #v0.7.0 #each-in-dynamic-context #codegen #enum-subset #message-dispatch #per-item-reactivity #live-keyed #r28-1c #r28-8 #bug60 #bug62 #bug63 #bug64 #bug65 #bug70 #bug71 #bug72 #bug73 #colon-shorthand-html #colon-shorthand-canonical #shape4-no-rhs #native-parser #native-parser-swap #each-promotion #match-promotion #flip-failure-families #f1-engine-substrate-closed #b2-message-arm-closed #machinedecls-instance-share #native-exprnode-walker #f2-match #promote-each #typed-atcell #server-fn-star #bare-function-failable #cross-file-export-bodystart #deepset-write-loss #reactive-nested-assign #reactive-array-mutation #cycles-prereq #cow-all-bracket-write #structural-eq-cycle-guard #value-native-maps #value-native-maps-implemented #spec-section-59 #e-map-bracket-write #map-type #map-lit #each-tuple-destructure #union-not-normalization #within-node-1005 #bug-a-deepset-write-loss #bug-b-structural-compound-deepset #stamp-compound-deepset-targets #set-algebra #scrml-data #value-canonical #unique-struct-unsafety-fix #on-mount-dismount #const-at-derived-decl #f5-closed #deepset-node-synth #exprtext-backfill-walker #blockstub-verbatim-body #mario-match-arm-fix #flip-605 #flip-508 #s148 #s149 #s150 #s151 #s152 #s153 #s154 #s155 #s156 #s157 #s158 #s159 #s160 #s161 #s162 #s163 #s164 #s165 #s166 #s167 #s168 #s169 #s170 #s172 #s173 #e-export-001 #w-type-fn-field #s174 #log-builtin #log-loc #location-transparent-logging #w-log-shadowed #e-type-any-forbidden #no-any-hard-line #production-strip #production-flag #s175 #typed-sql-row #sql-projection #width-subtyping #e-sql-row-contract-mismatch #w-sql-row-untyped #e-struct-function-field #function-boundary #passed-vs-stored #f-schema-001 #fn-return-inference #flagship-typed-data #function-type #limit-the-primitive #s176 #e-type-unknown-name #unrecognized-type-name #w-pure-deprecated #pure-deprecation #migration-3 #scrml-math #scrml-random #scrml-time-now #capability-scoped #non-deterministic #e-fn-004 #nondet-stdlib-registry #transitive-shim-copy #scalar-vocabulary #dd1-fork1 #s177 #g-formfor #markup-expansion-in-arms #arm-body-children #match-arm-body #r27-c6 #r28-7b #bug-4 #bug-48 #bug-74 #e-closer-001 #bare-slash-closer #schemafor-predicated-base #inline-map-assign #s169-map-inline-insert #inline-sibling-shim-imports #client-stdlib-inliner #math-de-leak #s179 #e-route-003 #e-route-004 #wire-serializability #i-fn-promotable #inferred-server #e-fn-001 #s180 #server-keyword-eliminate #w-deprecated-server-modifier #migration-4 #trigger-7 #trigger-8 #channel-broadcast #handle-middleware #inferred-boundary #sse-route #emit-client-inferred #mcp-descriptors-inferred #trucking-dispatch-smoke #s181 #w-display-text-overquote #display-text-overquote #inverse-footgun #e-unquoted-display-text #server-keyword-reword #e-cg-006 #w-lint-019 #s182 #e-engine-effect-not-interpolated #engine-effect #engine-var-dedup #e-engine-003 #s183 #e-formfor-not-imported #e-tablefor-not-imported #scan-for-unimported-type-data-element #formfor-unimported #tablefor-unimported #w-tailwind-001 #w-tailwind-unrecognized-class #dynamic-class-prefix #g-tailwind-dynamic-class-prefix #fn-pure-canonicity #i-fn-promotable-reword #s184 #lifecycle-field-comment-leak #collect-braced-body-comment-skip #e-struct-function-field-fp #e-type-001-double-fire #statement-text-normalize #w-lint-007 #w-lint-004 #w-lint-021 #snippet-fill-exemption #braced-body-opens-paren-arrow-lambda #shape-1-variant-lifecycle-initializer #infer-enum-from-variant-lifecycle-annotation #e-variant-ambiguous-fp #payload-binding-gaps #extract-match-arm-payload-bindings-by-variant #match-block-arm-payload-scope #error-arm-multi-field #table-for-synth-skip #w-each-promotable #s185 #errarm-refail #re-fail-from-arm #fail-from-arm #parse-fail-expr-string #emit-fail-expr #match-arm-fail-expr #ns1-recurse-failexpr #s19-5-2 #e-validator-inline-colon #validator-inline-msg-paren #try-recover-colon-inline-message #colon-form-reject #s186 #channel-codegen #g-channel-reconnect-bare-int #g-channel-handler-wiring #onserver #onclient #channel-ws-handler #is-channel-ws-handler #collect-channel-attr-handler-names #channel-attr-to-call #reconnect-bare-int #e-scope-001-exempt #e-cg-002-skip #s188 #g-not-negation-enforce #e-type-045 #prefix-not-negation #not-prefix-detector #not-prefix-negation-stamp #harvest-not-prefix-negation #lowering-choke-point #preprocess-for-acorn-detector #cluster-A #e-attr-unquoted-operator #attr-condition-operator-ahead #is-condition-attr-name #is-prefix-not-operand-ahead #attr-op-reject #unquoted-condition-atomic-only #reject-plus-parens #g-division-in-ternary-arm #ternary-depth-guard #typed-reactive-boundary #e-codegen-invalid-js #stray-not-attr-pair #s189 #e-syntax-045 #given-rebind-reject #given-narrows-in-place #e-channel-server-cell-read #channel-cell-write-client-side #ruling-a #detect-server-context-channel-cell-read #trigger-7a-drop #channel-cell-client-held #detect-channel-broadcast-reason #schemafor-create-table-pa #extract-schemafor-create-table-statements #pa-pluralize-struct-name #pre-bind-reactive-state-cells #reactive-cell-hoist #section-6-9 #annotation-ordering #s190 #cluster-c #e-decl-rhs-interp-wrapped #markup-root-closed #markup-const-sibling-swallow #g-derived-rhs-interp-wrapped #g-markup-const-consumes-cell-decl #derived-engine-expression-form #derived-expr-node #derived-expr-kind #inline-match-derived #expr-derived #legacy-source-var #b16-no-rules #b16-no-initial #b16-no-write #b16-circular #derived-upstream-enum #e-engine-004-steer #c14-derived-substrate #e-derived-engine-initial-undefined-rt #dependency-graph-derived-edges #section-51-0-j #section-51-9 #section-52-1 #s191 #tailwind-composing-families #approach-c #inline-var-fallback #no-preflight-block #w-tailwind-unrecognized-class #register-ring #register-gradient #register-transform #register-filters #register-backdrop #box-shadow-compose #transform-compose #filter-compose #backdrop-compose #ring-shadow-setter #section-26-7 #section-26-7-1 #section-26-7-2 #section-26-7-3 #if-fn-condition #call-ref-conditional #g-attr-if-fn-call-misroute #g-attr-if-fn-display-not-mount #g-attr-if-fn-chain-head-call-misroute #update-chain-call-ref #reactive-conditional-not-event #section-5-1 #zero-new-codes #s192 #engine-varname #auto-derive-engine-var-name #section-51-0-c #e-state-undeclared #w-const-at-deprecated #const-at-deprecated #migration-4b #ref-binding-sym-registration #walk-register-ref-bindings #bug-12-vkill #post-ce-relocation #s194 #server-authority #tier-1-read-authority #tier-2-server-var #auto-persist-retracted #q1-c #q2-wf #read-authority-only #emit-server-authority-load #server-load-route #select-star-load #collect-server-authority-types #server-authority-instance #server-authority-table #try-parse-server-authority-decl #authority-server-gate #emit-sync-stub-deleted #optimistic-update-deleted #w-auth-001-suppressed #w-auth-002-ssr-residual #ssr-pre-render-residual #section-52-3-5 #section-52-6 #section-52-6-2 #section-52-6-6 #section-52-6-7 #section-52-8 #section-52-11 #spec-issue-026 #server-write-fn-convention #channel-fan-out-composition #broadcast-explicit-composition #s195 #gap-a #void-element-self-terminating #find-structural-body-end #find-arm-closer #void-elements-set #match-arm-void #each-arm-void #flush-closer-fix #e-ctx-001-unclosed-match #e-match-parse-001 #section-24-void #section-18-0-1 #match-arm-void-element-scanner #corpus-rewrite-wave-1a #errors-as-states #g-failable-arm-nested-constructor-crash #g-each-body-bare-variant-arg #g-held-error-display #render-expression-primitive #g-engine-autodecl-bare-variant-write #g-shorthand-interp-match-arm-codegen #g-match-arm-apostrophe-bs #g-blocksplitter-comment-span-not-opaque #g-each-body-sigil-invariant-classifier #s196 #render-expression #render-of #render-element #render-held-accessor #all-variant-render-exprs #emit-boundary-markup-expr #e-render-no-of #e-render-no-clause #e-render-not-enum #e-match-arm-markup-in-value #h1-steer-markup-in-value-match #held-error-display #g-held-error-display-closed #section-19-15 #section-19-2 #section-4-15 #section-24-4 #section-18-0 #structural-element #data-scrml-render-anchor #errors-as-states #limit-the-primitive #g-render-not-enum-asis-miss #s198 #s199 #engine-hydration #initial-cell #server-source #scrml-engine-hydrate-init #e-engine-initial-both-forms #e-engine-server-with-derived #s200 #g-each-component-body-invalid-js #g-each-peritem-if-predicate #helper-export-hoist #transitive-helper-import #nested-expression-prop #lower-each-expr #serialize-call-args-lowered #export-is-user-component #s201 #markup-as-value #markup-value-expr #pillar-1 #section-1-4 #section-7-4 #emit-markup-value-expr #markup-to-dom-node-iife #scrml-render-value #node-aware-display #ternary-markup-arm #return-markup #parse-expr-with-markup-values #saw-ternary-at-root #scan-shape12-decl-end #value-terminator-discriminator #markup-typed-derived #derived-chunk-treeshake #g-markup-value-ternary-fnreturn-codegen #bare-variant-call-arg #g-nested-component-member-arg-misparse #normalize-tokenized-raw #member-access-collapse #s202 #each-inline-class-a #g-each-over-arm-payload-binding-unbound #arm-payload-binding #stamp-arm-payload-eaches #g-each-inline-component-prop-member-unsubstituted #g-inlined-component-root-class-interp-raw #substitute-interp-segments #class-merge-post-substitution #layer-2-string-literal-attr #build-match-arms #build-engine-arms #trucking-board-flagship #board-each-conversion #derived-filtered-cells #board-high-0 #e2e-render-map #known-failure-map #render-detectors #d0-d7 #oracle-free #render-harness #tier-tag-corpus #delta-gate #regression-sentinel #r26-industrialized #flograph #project-graph #typed-edge #provenance-sweep #flogeance-graph-mvp

## Links
- [structure.map.md](./structure.map.md)
- [dependencies.map.md](./dependencies.map.md)
- [schema.map.md](./schema.map.md)
- [config.map.md](./config.map.md)
- [build.map.md](./build.map.md)
- [error.map.md](./error.map.md)
- [test.map.md](./test.map.md)
- [domain.map.md](./domain.map.md)
- [non-compliance.report.md](./non-compliance.report.md)
- [master-list.md](../../master-list.md)
- [pa.md](../../pa.md)
