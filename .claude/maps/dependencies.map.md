# dependencies.map.md
# project: scrmlts
# updated: 2026-06-23  commit: 062165a5

## Runtime Dependencies (root package.json — v0.7.0)
@modelcontextprotocol/sdk@1.29.0 — MCP server SDK for scrml MCP integration
vscode-languageserver@^9.0.1 — LSP server protocol implementation
vscode-languageserver-textdocument@^1.0.11 — LSP text document utilities

## Dev / Build Dependencies (root package.json)
@happy-dom/global-registrator@^20.8.9 — DOM environment for browser tests
happy-dom@^20.8.9 — fast in-process DOM for Bun unit tests
marked@^14.1.3 — Markdown parser used by docs/build.ts
puppeteer@^24.40.0 — headless browser for e2e / Playwright support
@playwright/test@^1.49.0 — Playwright test framework for e2e tests

## Runtime Dependencies (compiler/package.json — compiler workspace)
acorn@^8.16.0 — JS parser used for escape-hatch expression parsing in ast-builder
astring@^1.9.0 — JS AST-to-source printer; used with acorn for re-serializing escape-hatch nodes

## Dev Dependencies (compiler/package.json)
@happy-dom/global-registrator@^20.8.9 — DOM environment for compiler browser tests

## Runtime Engine
bun>=1.3.13 — required runtime; no Node support (Bun-specific APIs used throughout)

## Internal Module Graph (major imports, compiler/src/)

| Module | Imports from |
|--------|-------------|
| cli.js | commands/compile.js, commands/dev.js, commands/build.js, commands/migrate.js, commands/promote.js |
| api.js | block-splitter.js, ast-builder.js, code-generator.js, module-resolver.js, component-expander.ts, type-system.ts, engine-graph.ts (S149 — buildEngineGraphJson); **S211: + lint-w-interp-in-raw-content.js (Stage 2.5 wiring, line 52)** |
| code-generator.js (codegen/index.ts) | codegen/emit-*.ts, codegen/srcmap-provenance.ts, codegen/build-source-map.ts, codegen/source-map.ts, dependency-graph.ts, auth-graph.ts, route-inference.ts |
| codegen/emit-client.ts | codegen/emit-*.ts, codegen/runtime-chunks.ts, codegen/context.ts; derives _scrml_modules key via moduleRegistryKey() |
| codegen/emit-engine.ts | codegen/emit-*.ts; emitEngineOpenerEffect() for §51.0.H Form 3 (S148); **S198-S199 engine-hydration arc emitEngineCellHydrationInit/emitEngineServerSourceHydration** |
| codegen/emit-reactive-wiring.ts | codegen/collect.ts, codegen/reactive-deps.ts, codegen/emit-engine.ts (require), codegen/emit-sync.ts, codegen/emit-channel.ts, codegen/emit-parse-variant.ts (`emitParseVariantDecodeIIFE` — S212 A2 W4); **S210 dual-table-fix: isModernEngine skip (empty machine.rules) for both buildMachineBindingsMap §51.3 write-guard and emitReactiveWiring §51.3 table emission — modern engines route exclusively via §51.0 engineBindings/emitEngineWriteGuard**; **S212 A2 W4: `buildApiEndpointRegistry` flattens `api-decl` nodes into `ApiEndpointForEmit` {base, method, path, reqShape, responseEnum}; `emitApiUrlExpr` lowers `${param}` path templates to `encodeURIComponent` substitutions; `emitRequestNode` api= branch emits the full §6.7.7 reactive surface (loading/data/error/stale, seq-guard, `refetch`, args-cell `_scrml_effect`, mounted cleanup); ENUM ResponseT drives `emitParseVariantDecodeIIFE` → automatic parseVariant decode (→ .data/::ParseError → .error); NO .server.js (§60.6 pure-client)**; **S213 render-bridge: imports `collectRequestIds` from reactive-deps.ts; threads `requestIds: Set<string>` into `emitOpts`; HOISTS `var _scrml_request_<id> = _scrml_deep_reactive(...)` declarations BEFORE top-level logic (ordering fix); `classifyMarkupNodes` gains `apiDeclNodes: any[]` (deep-walked api-decl set) for A2 W5 fix; `extractRequestId(node)` helper extracted; both `emitRequestNode` branches upgraded from plain `{}` to `_scrml_deep_reactive({...})`; dead `_scrml_notify(...)` removed** |
| codegen/collect.ts | types/ast.ts (FileAST shapes); **S216 §52-Pattern-C: NEW export `serverVarDeclLoadKind(decl): ServerVarLoadKind` — classifies a Tier-2 `<var server>` decl RHS as `"sql-load"` (param-free inline `?{}`, Pattern C disposition A), `"param-bearing"` (interpolates `${@cell}`, W-AUTH-004), or `"none"` (literal/Pattern-A/B placeholder); `ServerVarLoadKind` type exported; used by emit-server.ts + emit-reactive-wiring.ts + emit-logic.ts** |
| codegen/emit-each.ts | codegen/context.ts; emitEachBodyRenderForFile() guards undefined cell pre-init (S152); **S217 GITI-030: `RAW_CONTENT_ELEMENT_NAMES = Set(["pre","code"])` — guards the per-item text emit path from running `rewriteContextualSigil` on raw-content element bodies; `renderTemplateChildToJs` text-node branch gates `rewriteContextualSigil` behind `!insideRawContent`; `insideRawContent` flag propagated through the recursive child walk (mirrors the `RAW_CONTENT_ELEMENTS` guard in block-splitter.js)** |
| codegen/emit-server.ts | codegen/emit-*.ts, codegen/emit-channel.ts, codegen/emit-logic.ts (emitLogicNode for Pattern-C sqlNode lowering); **S216 Bug-51: imports `emitEnumVariantObjects` from emit-client.js for server-bundle enum injection + `topLevelBindingExists`/`localServerImportNameUsed` collision guards (module-scope); NEW `generateValueOnlyServerJs` export (const-only-module server emit for `g-const-only-module-no-server-emit`); NEW `injectAfterHeader` shared helper (hoists structural-eq/wire/log/enum helpers above route handlers); S216 ss1 Pattern C: `_patternCLoadDecls` = `collectServerVarDecls(fileAST).filter(d => serverVarDeclLoadKind(d) === "sql-load")` drives `/__serverLoad/<var>` route emission (lines 2086-2123) via `emitLogicNode(sqlNode, {boundary:"server"})` lowering** |
| codegen/emit-error-boundary.ts | block-splitter.js, ast-builder.js (re-parse pipeline) |
| codegen/emit-control-flow.ts | emit-lift.js (`hasFragmentedLiftBody`/`emitConsolidatedLift`/etc.), emit-expr.ts, emit-logic.js, emit-machines.ts, emit-html.ts, reactive-deps.ts; **S214 ss15 items 3+4: imports `pushLiftRequestIds`/`popLiftRequestIds` from emit-lift.js; `emitIfStmt` and `emitForStmt` each wrap their inner body in `pushLiftRequestIds(opts.requestIds ?? null)` + `popLiftRequestIds()` (via try/finally); `IfOpts` and the for-stmt opts gain `requestIds?: Set<string> | null`; `_ifExprCtx` gains `requestIds`; nested else-if bodies thread `requestIds` so a `<#feed>.loading` condition at ANY nesting depth routes to `_scrml_request_feed` not the §36 input-state registry** |
| codegen/emit-variant-guard.ts | **engine-statechild-grammar.ts** (ENGINE_STATE_CHILD_RESERVED_ATTRS + STATE_CHILD_STRUCTURAL_TAGS — ss2 item 3 SSOT dedup; replaces inline literal sets); codegen/emit-control-flow.ts (`rewriteBlockBody` — lazy require); codegen/emit-engine.ts (buildEngineBindingsMap/collectEngineVarNames — lazy require); **S216 Family-A: `emitArmWireFunction` now imports `emitBindDirectiveBody`, `buildEnumVarMap`, `buildReactiveTypeMap` from emit-bindings.ts (dynamic require, lines ~612-614) and calls `emitBindDirectiveBody(binding.bindAttr, binding.bindNode, {rootGetter:..., disposerPush:..., contextId:...})` for each arm-tagged `bind:*` directive — replaces a previously duplicated inline emission path.** **S212 Bug B: `emitArmWireFunction` now collects `wireableDirectives` (logic-bindings with `kind==="class-directive"` OR `kind==="attr-template"` tagged with the current arm's `armContextId`) and emits per-element `classList.toggle`/`setAttribute` + `_scrml_effect` pushed onto `_disposers` — torn down on next variant change**; **S214 ss15 item-2: `emitArmRenderFunction` and `emitInitialArmHtmlForMount` both pass `nestedMarkupContext: true` (6th arg) to the `generateHtml` lazy-require call; this seeds `markupParentStack = ["__nested-markup__"]` inside `generateHtml` so the arm body is treated as a NESTED markup-render subtree (its `${}` interpolations render) rather than a §40.8 default-logic root (where a bare-expr would be classified as an effect and suppress the render slot — wrong for arm bodies which are always nested markup)**; **S217 GITI-031: `subscribeSubPath?: string` added to `EmitVariantGuardOpts` interface — when `on=@cell.subfield`, Shape-A subscribe fires with the WHOLE cell value, so dispatcher + DOMContentLoaded init-fire both apply the sub-path suffix (e.g. `".state"`) to reach the enum-variant discriminant; `emitVariantGuardedRender` reads `opts.subscribeSubPath ?? ""` and emits the `.subPath` suffix on both the subscribe callback (`_cv)${subPath}`) and the init-fire read (`_scrml_reactive_get(name)${subPath}`)** |
| codegen/emit-html.ts | (generates HTML; self-contained with registry callbacks); **S212 Bug A: on*=<bare-non-@-identifier> attr-value routes to `addEventBinding({bareRefHandler:true})` instead of literal attribute emission [emit-html.ts:1910-1931]**; **S212 Bug B: `class:` directive and interpolated string-literal attr inside a `registry.currentArmContext != null` context calls `registry.addLogicBinding({kind:"class-directive"/"attr-template", directiveSelector, directiveJsExpr, directiveRefs})` [emit-html.ts:1831-1882] so emitArmWireFunction can wire them per-mount**; **S213 render-bridge: imports `collectRequestIds`; NEW `exprHasRequestRef(expr): boolean` (+30L); stamps `hasRequestRef:true` on `addLogicBinding` for bindings whose expr reads a request state object**; **S214 ss15 item-2: NEW `nestedMarkupContext?: boolean` 6th param to `generateHtml`; NEW `DEFAULT_LOGIC_MODE_TAGS = Set(["program","page","channel"])`; NEW `markupParentStack: string[]` seeded with `["__nested-markup__"]` when `nestedMarkupContext=true` (arm-body callers); bare-expr in a default-logic body (`program`/`page`/`channel` top-of-stack) is classified as a mount EFFECT — no `<span data-scrml-logic>` render slot emitted (closes g-on-mount-bare-call-render-slot); a bare-expr INSIDE a real markup element still renders (that element pushed its tag first)**; **S217 g-onmount: `_onMountEffect` bare-expr marker added to the `logic`-node binding loop skip gate — a bare-expr stamped `_onMountEffect:true` is SKIPPED in the addLogicBinding loop (falls to the file-scope/mount-effect emit path) in ALL enclosing contexts, not just default-logic-mode; this covers the `on mount { val() }` inside a `<db>`/`<program db=>` body where `inDefaultLogicMode` would be false but the directive is still a fire-and-forget lifecycle effect** |
| codegen/emit-event-wiring.ts | (event wiring pass); **S212 Bug A: `binding.bareRefHandler` branch resolves `handlerName` through `fnNameMap` to `_scrml_<name>_N` and uses that reference DIRECTLY as the listener value (no `function(event){ fn(); }` wrap) [emit-event-wiring.ts:567-572]**; **S212 Bug B: arm-tagged `class-directive`/`attr-template` bindings are filtered OUT of global init emission [emit-event-wiring.ts:320] — a module-init `document.querySelector` on a stale/absent arm element would cache nothing**; **S213 render-bridge: imports `collectRequestIds`; `requestIds` in `engineExprCtxExtras`; `hasRequestRef?: boolean` on `LogicBinding` (+7L); `if=<#id>.member` condition-code routes to `(_scrml_request_<id>.tail)` not `_scrml_reactive_get(...)`; `varRefs.length > 0 || binding.hasRequestRef` guard forces `_scrml_effect`-wrapped path** |
| codegen/emit-bindings.ts | expression-parser.ts, emit-expr.ts, collect.ts (getNodes), rewrite.js, emit-predicates.ts; **S216 Family-A: NEW exported `emitBindDirectiveBody(bAttr, mkNode, opts: BindDirectiveBodyOpts): string[]` — root-agnostic bind-directive body lowering extracted from the inline `emitBindings` loop; `BindDirectiveBodyOpts` interface exported (element-acquisition getter, disposer push, `contextId`). Both the top-level `emitBindings` path AND the new arm-body path in emit-variant-guard.ts now call this shared helper — identical JS output shape guaranteed.** **S212 Bug B: lowering helpers `lowerClassDirectiveCondition` + `lowerAttrTemplateValue` are now used by BOTH the top-level emit-bindings.ts pass AND the new arm-tagged path in emit-html.ts (same helpers → identical JS output shape)** |
| codegen/reactive-deps.ts | (reactive dependency analysis); **S213 render-bridge: NEW `collectRequestIds(fileAST): Set<string>` export (+73L) — collects every `<request>` `id` value via deep descent; imported by emit-reactive-wiring.ts, emit-event-wiring.ts, emit-html.ts, emit-match.ts** |
| codegen/binding-registry.ts | (pure data; no imports); **S212 Bug A: `EventBinding.bareRefHandler?: boolean` field (line ~99)**; **S212 Bug B: `LogicBinding.kind` extended with `"class-directive"` and `"attr-template"` variants; `directiveSelector`/`directiveJsExpr`/`directiveRefs`/`className`/`attrName` carry the lowered directive data; arm-tagged logic-binding filter in `emitArmWireFunction` keyed on these kinds**; **S213 render-bridge: `LogicBinding.hasRequestRef?: boolean` field (+6L)** |
| codegen/emit-expr.ts | codegen/rewrite.js (rewriteExpr/rewriteServerExpr chain), codegen/emit-parse-variant.ts, codegen/emit-control-flow.ts, symbol-table.ts (SYNTH_PROPERTY_NAMES), codegen/srcmap-provenance.ts, codegen/log-loc.ts (resolveLogLoc), emit-lift.js (emitMarkupValueExpr); **S210 ss3: paren-grouping preservation case added (BinaryExpr/TernaryExpr/AssignExpr wrapped in paren-receiver position — no new external deps)**; **S213 render-bridge: NEW `requestIds?: Set<string>` on `EmitExprContext` (+18L); `emitInputStateRef(node, ctx)` checks `requestIds?.has(node.name)` to return `_scrml_request_<id>`; `emitIdent` bare-recovery TAB-form `_scrml_input_<id>_` also routes via `requestIds`**; **S214 ss16 C3: NEW `_renderShadowedInFile` module-level flag + `setRenderShadowedInFile(on)` export (+16L); `emitCall` `render()` hijack now YIELDS when `_renderShadowedInFile || ctx.declaredNames?.has("render")` so the call falls through to the generic path and the `fnNameMap` post-pass repairs it to the user fn encoded name** |
| codegen/rewrite.ts | codegen/var-counter.ts (genVar), codegen/compat/parser-workarounds.js (splitBareExprStatements), expression-parser.ts (rewriteReactiveRefsAST/rewriteServerReactiveRefsAST), codegen/errors.ts (CGError), codegen/code-segments.ts (rewriteCodeSegments/regexAllowedAfter); **S210 ss3: parenthesized receiver preservation — paren-group before .method()/(args)/[idx] chains (no new external deps)** |
| codegen/emit-match.ts | (match arm emission); **S213 render-bridge: `resolveOnExpr` imports `collectRequestIds` + threads `requestIds` into `emitExpr` call for the `on=` expr**; **S217 GITI-031: `OnExprResolution` interface gains `subscribeSubPath: string` field; bare `@ident` ref returns `subscribeSubPath: ""`; member-access `@ident.path` branch returns `subscribeSubPath: path` (e.g. `".state"`); all other shapes return `subscribeSubPath: ""`; `resolveOnExpr` threaded into `emitVariantGuardedRender` call with `subscribeSubPath` populated** |
| codegen/emit-lift.js | (Tier-0 `${for...lift}`); **S214 ss15 items 3+4: NEW `pushLiftRequestIds(set)` + `popLiftRequestIds()` + `currentLiftRequestIds()` exports — a file-global stack that tracks the active `<request>` id set during lift emission; `liftExprCtx(extra)` reads the top of stack and adds `requestIds` to every `EmitExprContext` created during lift; NEW `liftExprReadsRequestState(rewritten)` helper — when the rewritten lift expression references a `_scrml_request_<id>` pattern, wraps the `textContent` write in `_scrml_effect(...)` so the DOM re-renders on fetch-resolve (mirrors the bare-expr interpolation path; closes g-request-lift-nested-path)** |
| codegen/emit-logic.ts | (logic emission); **S213 render-bridge: NEW `requestIds?: Set<string>` on `EmitLogicOpts` (+9L); threaded into `_makeExprCtx(opts)` + forwarded in for-stmt/do-while-stmt/nested-if-expr-alt-chain** |
| codegen/log-loc.ts | (standalone; no external imports); **S214 ss16 C3: `fileDeclaresLog` replaced by generalised `fileDeclaresFn(fileAST, name)` that checks for any function-decl kind with the given name; `fileDeclaresLog` now delegates to `fileDeclaresFn(ast, "log")`; NEW `fileDeclaresRender(fileAST)` export delegates to `fileDeclaresFn(ast, "render")` — same WeakSet-guarded recursive walk** |
| codegen/emit-sync.ts | (standalone — no codegen/ imports); **S216 §52-Pattern-C: NEW `emitDeclRhsSqlLoad(varName: string): string[]` export — the client-side IIFE that POSTs to `/__serverLoad/<varName>` on mount and calls `_scrml_reactive_set(varName, await res.json())` (Pattern C param-free load); sibling of `emitInitialLoad` (Pattern A/B) and `emitServerAuthorityLoad` (Tier-1 SELECT*).** |
| codegen/build-source-map.ts | codegen/srcmap-provenance.ts, codegen/source-map.ts |
| engine-statechild-grammar.ts | (standalone — NO imports; pure constant exports: ENGINE_STATE_CHILD_RESERVED_ATTRS + STATE_CHILD_STRUCTURAL_TAGS; placed at compiler/src/ NOT codegen/ to be importable by both type-system and codegen layers without cycle) |
| engine-graph.ts | types/ast.ts (FileAST shapes via unknown); standalone — no codegen/ imports |
| auth-graph.ts | types/ast.ts, symbol-table.ts |
| type-system.ts | types/ast.ts, dependency-graph.ts, protect-analyzer.ts, **engine-statechild-grammar.ts** (ENGINE_STATE_CHILD_RESERVED_ATTRS + STATE_CHILD_STRUCTURAL_TAGS — ss2 item 3; type-system.ts:81); **S211 A2 W3: `checkApiDeclarations` (+346L; line 18053) fires E-API-PATH-PARAM-UNBOUND / E-API-ENDPOINT-UNKNOWN / E-API-REQ-SHAPE-MISMATCH; no new external imports — uses the existing typeRegistry + apiExemptTypeNames**; **S213 A2 W5: `checkApiDeclarations` shallow top-level scan replaced by deep-walk `collectApiDecls(node)` (+24L) — descends body/children/bodyChildren/branches; S213 C1: `"animationFrame"` added to `LOGIC_SCOPE_GLOBAL_ALLOWLIST`; S213 render-bridge: E-SCOPE-001 exemption for `_scrml_input_<id>_` TAB-lowered refs in `annotateNodes` (+14L)**; **S216 §52-Pattern-C: W-AUTH-004 fires at state-decl walk [type-system.ts:9344] for param-bearing inline `?{}` on `<var server>` decls; W-AUTH-001 suppressed when `_isPatternCParamFree` (the param-free Pattern C path loads via `/__serverLoad/<var>`, so no steer needed); W-AUTH-001 suppressed when `_isTier1AuthInstance` (SELECT* compiler-generated).** **S214 ss16 C3: NEW `checkRenderShadowing(fileAST, errors)` walker (mirrors `checkLogShadowing`) fires W-RENDER-SHADOWED at every file-level `function render`/`fn render` decl; wired at the type pass; S214 ss16 C4: NEW `checkEqPayloadVariantOperands(exprNode, typeRegistry, span, errors)` fires W-EQ-PAYLOAD-VARIANT when a `==`/`!=` operand is a call-expr whose callee is a `.Variant` member-access on a declared enum type with a payload-variant at that name; wired at const/reactive init, bare-expr, if-condition, return sites; S214 ss16 C5: NEW `inferBareVariantsAtVariantCtorArgs(exprNode, ctxType, typeRegistry, span, errors)` — infers enum context from a payload-variant ctor callee (`.Variant(arg)`) so bare args are typed against the payload shape (suppresses spurious E-TYPE-063 on typed args)** |
| reachability/*.ts | types/reachability.ts, types/ast.ts |
| expression-parser.ts | acorn, astring, codegen/code-segments.ts (GITI-017 rewriteCodeSegments fence); **S210: acornNodeToExprNode regex-literal branch uses node.raw (not outer rawSource) — prevents wrong-span serializer bug in call-arg position**; **S210 ss3: @. sigil structuring — each-sigil `IdentExpr` leaf production (no new external imports; internal AST shape change only)** |
| lint-w-interp-in-raw-content.js | (standalone — no compiler/src imports; `runWInterpInRawContent` walks bsResults BS-output objects directly) |
| tailwind-classes.js | (standalone; `getTailwindCSS`, `findUnrecognizedClasses`, `findUnsupportedTailwindShapes`); **S214 ss15 item-1: NEW `collectAuthorDefinedClasses(source)` helper (+85L) — brace-balanced scan of inline `#{}` CSS blocks + `<style>...</style>` blocks extracts all `.identifier` class selectors the author DEFINES; `findUnrecognizedClasses` now calls this and excludes the returned set from W-TAILWIND-UNRECOGNIZED-CLASS (author-defined classes emit real scoped CSS via the `@scope`/`collectCssBlocks` path; they are not Tailwind classes and should not be flagged)** |
| native-parser/*.js | (self-contained; no compiler/src imports) |
| native-parser-canary/within-node-classifier.ts | (standalone classifier; no compiler/src imports beyond own types); **S217 g-onmount / GITI-029: `STRIP_KEYS` set gains `"_onMountEffect"` (LIVE-only lifecycle marker on bare-expr desugared from `on mount {}`/`on dismount {}`) + `"_onLifecycleLift"` (LIVE-only diagnostic marker on synthetic `${...}` built by `liftBareDeclarations` for a comment-flushed `on mount {}` text run) — both are pipeline-internal codegen-support fields with no native-parser analogue; withinNode classifier SKIPS them as non-semantic divergences** |
| ast-builder.js | (stage TAB; produces FileAST); **S217 GITI-029: NEW `TOPLEVEL_ON_LIFECYCLE_RE = /^\s*on\s+(?:mount|dismount)\s*\{/` module constant — a `//` comment preceding `on mount {}` previously split the text block so the lifecycle directive appeared in a NEW text block whose leading token was `on` (a non-keyword), defeating the existing shared-block lift gate; `preprocessBSBlocksForAst` now detects a top-level text block matching this regex in default-logic mode and wraps it in a synthetic `${...}` logic block (same `_bareDeclLift: true` / `_onLifecycleLift: true` marker class as other lift gates; gated `isDefaultLogicBody` — the §40.8 default-logic surface only)**; **S217 g-onmount: `_onMountEffect: true` stamped on bare-expr nodes desugared from `on mount {}` / `on dismount {}` at BOTH parser sites — `parseOneStatement` (logic-body, line ~8739/~8753) AND the markup-child desugar (line ~12146/~12160); 4 total stamp sites; emit-html.ts keys on this flag to unconditionally skip render-slot + addLogicBinding for the node** |
| route-inference.ts | types/ast.ts, protect-analyzer.ts; **S217 escalation-2 fix: `generateRouteName()` — author-declared `route="/path"` is NEVER used as the JS binding identifier; a `__ri_route_*` name is ALWAYS synthesized (the author path is the HTTP request path, carried separately in `explicitRoute`); in APP mode, `route=` on a `server function*` SSE or `handle()` is honored as the stable foreign-facing contract URL mounted at the author path, but the JS binding always stays `__ri_route_*`; pre-fix, conflating the two emitted `export const /path = ...` — an ESM SyntaxError on any route containing `/`** |
| commands/compile.js | api.js (compileScrml), engine-graph sidecar write site (--emit-engine-graph); **S213 C2: `fileURLToPath(import.meta.url)` for `compilerSrcDir` (Windows double-drive path fix)** |
| validators/lint-async-user-source.ts | (standalone validator); **S213 C2: `fileURLToPath(import.meta.url)` for `STDLIB_ROOT` (Windows double-drive path fix)** |
| module-resolver.js | (module resolution); **S213 C2: `fileURLToPath(import.meta.url)` for `STDLIB_ROOT` (Windows double-drive path fix)** |
| commands/dev.js | api.js (compileScrml); Bun.serve + per-file fs.watch (rewritten S152 — no recursive-dir) |
| commands/migrate.js | api.js (compileScrml), block-splitter.js, ast-builder.js (buildAST — for rewriteMatchArmArrows AST-driven walk) |

## Tags
#scrmlts #map #dependencies #s217 #giti-029 #giti-030 #giti-031 #g-onmount #toplevel-on-lifecycle-re #_onmounteffect #_onlifecyclelift #on-mount-lift-gate #comment-flushed-on-mount #subscribe-sub-path #subpath-dispatch #on-cell-subfield #raw-content-verbatim #pre-code-verbatim #escalation-2 #route-inference-app-mode #generate-route-name #author-route-path #esm-syntax-error-fix #within-node-classifier-rebaseline #s216 #bug-51 #e-cg-016 #enum-server-bundle #generate-value-only-server-js #inject-after-header #top-level-binding-exists #local-server-import-name-used #ss1 #pattern-c-load #server-cell-load #server-var-decl-load-kind #emit-decl-rhs-sql-load #sql-load #param-bearing #section-52-6-5 #w-auth-004 #family-a #emit-bind-directive-body #bind-directive-body-opts #arm-body-bind-directive #bun #acorn #lsp #mcp #engine-graph #source-map #s149 #s152 #s209 #ss2 #engine-statechild-grammar #ssot-dedup #s210 #engine-name-dual-table #dual-table-fix #symbol-table-governed-cell #emit-reactive-wiring-modern-engine-skip #s211 #ss11 #w-interp-in-raw-content #a2-w3 #check-api-declarations #s212 #a2-w4 #w-api-response-not-variant #s213 #e-match-block-in-lift #render-bridge #collect-request-ids #s214 #default-logic-mode-tags #nested-markup-context

## Links
- [primary.map.md](./primary.map.md)
- [master-list.md](../../master-list.md)
- [pa.md](../../pa.md)
