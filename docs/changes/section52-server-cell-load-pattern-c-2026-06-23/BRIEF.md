# Dispatch BRIEF (S216) — §52 server-cell LOAD (Pattern C, param-free core)
Agent: scrml-js-codegen-engineer · isolation:worktree · opus · bg · base 96745d34 · change-id section52-server-cell-load-pattern-c-2026-06-23
Ruling (S216, user "rule LOAD, ship param-free first"): `<var server> = ?{select…}` (+ `server @var = ?{}`) IS the cell's mount LOAD → build `/__serverLoad/<var>` route + client fetch-on-mount, MIRRORING the BUILT Tier-1 emitServerAuthorityLoad (emit-server.ts ~:1996) + generateValueOnlyServerJs (~:600, ss1 item-2); the sqlNode is now attached (ss1 item-3 leak-stop). SPEC (Rule 4, same landing): +§52.6.5 Pattern C + reword §52.4.3 (client-placeholder = in-flight VALUE not the query; §52.4.4 already mandates fetch-on-mount). PARAM-FREE only — param-bearing SELECT (POST-body param-passing on /__serverLoad) = bounded follow-on → graceful diagnostic not crash. Engine-rides free: `<engine server=@source>` (dpa-005 E-leg) hydrates via emitEngineVariantCellInit (emit-engine.ts ~:1578) — verify compiles+hydrates (primary success crit). Verify: sPA repros (bare-cell/engine-rides/server-fn-call) load + E-leg hydrates + giti-F1 + flux-G1-read + S215 adversarial (multi-cell/cell+engine/@-form/param-bearing-graceful) + R26 + full suite. Authority: DD section52-server-cell-load-rhs-2026-06-23.md + design-insights [S216/§52-load].
