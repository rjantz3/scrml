# Dispatch BRIEF (S216) — Family-A convergence HALF 1 (match-arm + engine bind: — the HIGH)
Agent: scrml-js-codegen-engineer · isolation:worktree · opus · bg · base 83afdcdb · change-id family-a-converge-half1-2026-06-23
Task: extract root-agnostic emitBindDirectiveBody from emitBindings (emit-bindings.ts:493+; preserve enum-select/numeric/predicated-§53.7.2/_flatBindKey special cases; default = byte-identical top-level) + route arm bind: through emitArmWireFunction (emit-variant-guard.ts:380, _root-rooted, the deferred bind: at :63-72) → fixes g-bindvalue-wiring-dropped-in-match-arm (HIGH) for BOTH match arms AND engine state-child bodies (shared emitVariantGuardedRender, variant-source-agnostic). SCOPE: Half-1 only — NOT emit-each (Half-2), NOT buildHandlerExpr dedup (Half-2). Byte-identity gate = behavioral/browser suite + manual diff of reactive-016-bind-value/reactive-014-form-state/combined-003-form-validation (NOT within-node parser canary). Verify: match+engine bind: repros now wire + adversarial (bind:checked/group/enum-select/predicated/nested-match/top-level-byte-identical) + R26 + full suite. Authority: family-a-converge-wiring SCOPING (S216 survey).
