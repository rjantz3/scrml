bug-12-vkill READ-SIDE FIRE at TS — PHASE 1 BUILD (Phase 0 passed). Wires the fire + R26, closes bug-12-vkill + g-readside-undeclared-postce. Change-id sym-cell-registration-completeness-2026-06-13. Agent a27d0f5d.

READ FIRST (from MAIN's working tree — untracked, reads permitted): POSTCE-REDUX-SCOPE.md (authority + the S192 Phase-0 surface-5 correction) + progress-postce.md (Phase-0 verify). MAPS: primary.map.md Task-Shape Routing.

PHASE 0 DONE — DO NOT RE-VERIFY. Result (S192, agent a6ddcb97): 4/5 surfaces + the idiomatic markup-derived-cell resolve at TS; the one "miss" is ${@ComponentName} (component-def read by @), which CORRECTLY fires (misuse: 0 corpus; bare ${ComponentName} already hard-errors E-SCOPE-001; components instantiate via <Name/>). USER RULED proceed — that fire is correct/symmetric.

STARTUP (F4): pwd under .claude/worktrees/agent-; `git reset --hard 46ce09b75a6b280b16647ea52f5d3c23c3381ce2` (Phase-0 branch — descends from main 77c80fcf, adds only progress-postce.md; type-system.ts byte-identical to main); bun install; bun run pretest. Path discipline: Bash-edits on worktree-absolute paths, never cd into main, no --no-verify, commit per phase, append progress-postce.md.

PHASE 1 (commit) — wire the fire at type-system.ts:6240: replace `if (raw.startsWith("@")) return;` with a resolution — strip @ → base; scopeChain.lookup(base); reactive/variable entry → resolved (mirror the bare-name path ~6271-6282); both sigil+bare miss + no import → fire E-STATE-UNDECLARED. PORT the message from `git show 22205aba:compiler/src/symbol-table.ts` (read-side fire emission ~2331-2400). Native-parser exemption: TS likely does NOT process native-parser /*.scrml mirrors (type-system.ts:4787, S162) — verify; add defensively only if one reaches the walker. DROP entirely: getCrossFileChannelCellNames/collectChannelCellNamesInFileAST scan + fileASTMap + ReadSideCtx (TS reaches the inlined channel decl directly). TESTS: ${@typo} fires; the 5 surfaces resolve (channel @boardEvents, <each> outer-cell, engine effect=, state-block sibling-fn, markup-derived const <badge> ${@badge}); component-def const Name=<markup props=> read via ${@Name} FIRES (locks the S192 ruling).

PHASE 2 — R26: full-corpus REAL-COMPILE census (fire at TS, not flagged) ZERO false-positives (all idiomatic surfaces resolve) + synthetic ${@typo} fires; only new errors = genuine undeclared reads or component-def ${@Name} misuse (if an idiomatic construct newly errors → STOP). Full suite 0 NEW fail (baseline 8 pre-existing: 6 within-node over-budget + 2 TodoMVC missing-dist). SPEC §34 read-side → "wired S192 at TS (post-CE; Class-B scan retired)"; §6.1 read-side SHALL (Rule 4). known-gaps: bug-12-vkill → resolved; g-readside-undeclared-postce → resolved; g-export-channel-body-text stays open.

REPORT: WORKTREE_PATH, FINAL_SHA, per-phase SHAs, FILES_TOUCHED, Phase-2 census (0-FP + typo-fires + component-def-fires), suite (vs 8-fail baseline), maps, deferred, any STOP. [Verbatim dispatch prompt; full F4/maps/path-discipline blocks included.]
