bug-12-vkill READ-SIDE FIRE — POST-CE redux, landing the fire at the TS (type-system) stage. Stage-2 SYM-stage prototype was the wrong layer (salvaged); the fire belongs at TS where all post-SYM surfaces already resolve. Closes bug-12-vkill + g-readside-undeclared-postce. Change-id sym-cell-registration-completeness-2026-06-13.

READ FIRST: docs/changes/sym-cell-registration-completeness-2026-06-13/POSTCE-REDUX-SCOPE.md IN FULL (materialization map, line-anchored TS resolution evidence, drop-vs-port, the Phase-0 safeguard). MAPS: primary.map.md Task-Shape Routing.

STARTUP (F4): branches from main (77c80fcf). pwd under .claude/worktrees/agent-; clean; bun install; bun run pretest. Path discipline: Bash-edits on worktree-absolute paths, never cd into main, no --no-verify, commit per phase, progress-postce.md.

BACKGROUND: fire site type-system.ts:6240 `if (raw.startsWith("@")) return;` is the only thing suppressing a read-side @name check at TS. TS runs POST-CE + owns a full @name resolution table (scopeChain reactive double-bind ~8928-8929; each/tableFor as-locals ~10180-10203; engine auto-decls from machineRegistry §51.0.C-canonicalised ~10723-10733 [solves UI/ui]; preBindReactiveStateCells hoist ~10663-10683 [incl. inlined channel bodies]). PA-verified: board.scrml compiles 0 errors + boardEvents wires as a registered cell (5 set/9 get) — channel cell DOES resolve post-CE (the "raw text" belief was the S139-Bug-51 stale-scope artifact). Existing bare-name E-SCOPE-001 path ~6271-6282 already does scopeChain.lookup(base)+detects reactive.

PHASE 0 — VERIFY THE RESOLUTION SURFACE (HARD STOP-GATE, do NOT build until it passes): via runTS probes confirm TS's scopeChain resolves ALL 5 surfaces post-CE — (1) cross-file channel @boardEvents (board.scrml + 4 trucking channels + 08-chat + 15-channel-chat); (2) <each>/<tableFor> @row/@. (each-as-row + 27-type-derived-table/07-admin-dashboard); (3) engine boot-effect= implicit cell; (4) <state>-block cell read from a sibling fn body; (5) markup-const @A top-level + component-def variant. If ANY does NOT resolve (would false-fire), STOP + report which (the P1 dissent) — re-scope before building.

PHASE 1 (commit) — wire the fire at type-system.ts:6240: replace the blanket @-skip with a resolution — strip @, scopeChain.lookup(base); reactive/variable entry → resolved; both sigil+bare miss + no import → fire E-STATE-UNDECLARED (mirror the base-strip ~6271-6282). PORT from branch worktree-agent-af9b984e883af80a2 @ 22205aba: the E-STATE-UNDECLARED message + the native-parser self-host exemption (if mirrors traverse TS). DROP entirely: the Class-B getCrossFileChannelCellNames/collectChannelCellNamesInFileAST scan + fileASTMap + ReadSideCtx (TS reaches the inlined channel decl directly). Tests: ${@typo} fires; the 5 surfaces resolve (no fire).

PHASE 2 — R26: full-corpus TS-stage census ZERO false-positives (now a real-compile census — the fire is at TS, not behind a flag) + synthetic ${@typo} fires; full suite 0 new fail; trucking + channel examples clean. SPEC §34 read-side → "wired S192 at TS (post-CE; Class-B scan retired)"; §6.1 read-side SHALL. known-gaps: bug-12-vkill → resolved; g-readside-undeclared-postce → resolved; g-export-channel-body-text stays open.

REPORT: WORKTREE_PATH, FINAL_SHA, per-phase SHAs, FILES_TOUCHED, the Phase-0 5-surface result (resolved/false-fire per surface — load-bearing gate), Phase-2 census (0-FP + typo-fires), suite, maps, deferred, any STOP. [Verbatim dispatch prompt; full F4/maps/path-discipline blocks included.]
