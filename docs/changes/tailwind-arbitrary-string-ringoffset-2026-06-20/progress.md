# progress — tailwind-arbitrary-string-ringoffset-2026-06-20

Change-id: tailwind-arbitrary-string-ringoffset-2026-06-20
Worktree: /home/bryan-maclee/scrmlMaster/scrml/.claude/worktrees/agent-aa1ba07f7987579fa
Base HEAD: a3b08cbb (origin/main S210)

## Scope
- Sub-arc 1: string-shaped arbitrary values (content-['text'] / font-[Inter])
- Sub-arc 3: lone arbitrary ring-offset-[<len>]
- (Sub-arc 2 safelist/@apply is OUT — design-deferred)

## Log (append-only, timestamped)
- 2026-06-21T02:42:19Z — startup verified: pwd=/home/bryan-maclee/scrmlMaster/scrml/.claude/worktrees/agent-aa1ba07f7987579fa, toplevel==WORKTREE_ROOT, base a3b08cbb, status clean, bun install ok, baseline bug-1-tailwind-arbitrary-value-emit GREEN (66 pass). Read SPEC §26.4 / §26.4.1 / §26.7 ring-offset; read source loci (validateArbitraryCss, ARBITRARY_PREFIX_MAP, ARBITRARY_DECL_TRANSFORM, ARBITRARY_OVERLOADED_PREFIXES, registerRing). FINDING: arbitrary-value-emit §12 has a stale test asserting ring-offset-[2px] STILL triggers the lint — must invert it (coupled to sub-arc 3); will reuse skew-[45deg] as the still-unsupported sanity control.
- 2026-06-21T02:46:33Z — SUB-ARC 1 DONE (string-shaped + content + font). Added: validateArbitraryCss string-detection branch (after backtick check, before underscore-split) -> kind "string", same-quote-embedded reject, underscore->space in interior; ARBITRARY_PREFIX_MAP += "content"; ARBITRARY_OVERLOADED_PREFIXES += "font" (number->font-weight, else font-family). Tests: arbitrary-value-emit §16-§20 (content strings, font overload, variant integ, rejections, lint regression). 84 pass / 0 fail (was 66). DISCREPANCY SURFACED: brief rejection-table lists font-['a"b'] as reject, but brief's RULE rejects only SAME-quote interior; 'a"b' is a valid single-quoted CSS string (different quote inside). Implemented per the RULE (CSS-correct); §19 documents 'a"b' ACCEPTED + same-quote ' and " rejects. Flag for sPA.
- 2026-06-21T02:48:48Z — pre-commit gate caught a coupled stale test: bug-1-tailwind-transform-shorthand.test.js §12 asserted font-[Inter] STILL fires. Inverted it to RECOGNIZED + updated the file-header deferred-list. Re-running gate.
- 2026-06-21T02:56:26Z — SUB-ARC 3 DONE (arbitrary ring-offset-[<len>] / [<color>]). Added ARBITRARY_DECL_TRANSFORM["ring-offset"] kind-dispatched: color/var/keyword -> --tw-ring-offset-color only (mirrors named ring-offset-{color}); length/number -> width + offset-shadow var + BOX_SHADOW_COMPOSE (mirrors named ring-offset-{w}). Emit verified byte-exact vs the brief table. Composition ring-[3px]+ring-offset-[2px] no collision. List value rejected upstream (single-token). Tests: ring-family §13 (full-string + composition + lint-regression + variant + list-reject). INVERTED 4 stale STILL-fires tests (now RECOGNIZED) coupled to this change: unrecognized-class §2, transform-shorthand §12, minor-families §10, arbitrary-value-emit §12 (swapped sanity control to skew-[45deg]); corrected the misleading "needs preflight machinery" comments (Approach C never needed preflight). All 5 touched tailwind test files green: 230 pass / 0 fail. Running full gate.
- 2026-06-21T03:03:21Z — R26 VERIFY DONE. Step1: 3 named test files 166 pass/0 fail. Step2: empirical re-emit probe — all 8 cases match the brief tables byte-exact. Step3: full suite `bun run test` (incl browser) 24760 pass / 0 fail / 215 skip across 1042 files (pre-commit-gate baseline at startup was 17457 pass/0 fail/68 skip for the unit+integration+conformance scope; full-suite scope is broader). 0 failures. FINAL_SHA 44fb8711; sub-arc 1 = 81d1d3c2, sub-arc 3 = 44fb8711. Tree clean. DONE.
