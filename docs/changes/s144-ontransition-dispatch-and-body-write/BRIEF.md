# BRIEF — S144 Cluster E / Bug-AB (6nz Q-AB → codegen bug + folded-in E-ENGINE-VAR-DUPLICATE finding)
agent: a4b19b299a1f6b932 · scrml-dev-pipeline · isolation:worktree · model:opus · dispatched S144 2026-05-30 on HEAD 93d8cab4 (all 5 S144 clusters landed).
discipline: standard S126/S99/S88/S83/S90/R26 verbatim per pa.md. Folded per user "fold it in" (2026-05-30).

DEFECT 1 (core Bug-AB): <onTransition> never fires from a program-scope `function` trigger. Codegen GENERATES the rule table + `__scrml_engine_<var>_fire_hooks(from,to)` (handler body inside) but NEVER INVOKES it: direct write → bare `_scrml_reactive_set` (no dispatch); `.advance()` → `_scrml_reactive_get("mode").advance("Edit")` (method on STRING → TypeError). Root: C13 dispatch arm is engine-context-threaded (engineBindingsMap/engineVarNames/engineCtx) — threaded for state-child event-handlers (emit-event-wiring.ts ~319-445) + state-child bodies (emit-control-flow.ts ~158/1515/1713) but NOT into free `function` bodies (emit-functions.ts ~405-415). SAME family as A+B. Repro = the question-ab toggle() shape. R26: toggle() routes @mode=.Edit via _scrml_engine_direct_set → @transitions 0→1 (happy-dom).

DEFECT 2 (folded-in): @engineVar=.X written INSIDE the engine's own <onTransition> body → phantom hard error E-ENGINE-VAR-DUPLICATE (mis-classified as a <engineVar> decl) + false E-DG-002 (write not dep-graph-wired). Distinct from program-scope writes (toggle() didn't trip it). Repro = scratch/probe1-controlB-no-fn.scrml (11-line minimal). Hits README Stage-3 flagship (only passes via // gate: skip). Leads: symbol-table.ts (E-ENGINE-VAR-DUPLICATE / PASS 16/12.B), dependency-graph.ts (E-DG-002). Root: onTransition `${}` body logic not walked like other logic bodies (decl-vs-write classifier + dep-graph). R26: /tmp/ab2 exit-0, no phantom errors.

May share a root (onTransition-body logic not integrated into engine analysis/codegen). Working path (state-child triggers) must NOT regress (c13-advance-write-hook / engine-self-write-option-d / machine-codegen guards). §51.0.F.1 self-write semantics + W-ENGINE-SELF-WRITE lint unchanged. Incremental commits + progress.md mandatory (crash-recovery — a sibling's report was just lost to a 529). 3-way patch likely at landing (emit-control-flow/emit-functions/type-system carry A+B + D).
