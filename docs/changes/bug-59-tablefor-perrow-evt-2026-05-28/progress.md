# progress — bug-59-tablefor-perrow-evt-2026-05-28

(append-only, timestamped. Agent updates after each step. WIP commits expected.)

- DISPATCH (S140, baseline c4d5ef96): brief at BRIEF.md. Agent has not yet started.

- 2026-05-28 START at /home/bryan-maclee/scrmlMaster/scrmlTS/.claude/worktrees/agent-a647b42cd1adb179b — startup verification PASSED (pwd worktree-prefixed; toplevel match; ff-merge main 73162ef9 clean; bun install; pretest ok).
- 2026-05-28 REPRODUCED: compiled examples/27-type-derived-table.scrml; client.js line 37 = `_scrml_lift_el_15.addEventListener("change", function(event) { if (evt !== null && evt !== undefined) {...}; });` — free `evt` confirmed. Master/header handler line 78 (`evt => {...}`) is correct (delegated path).
- 2026-05-28 ROOT CAUSE (refined): per-row checkbox synth (emit-table-for.ts:buildRowCheckboxCell ~L797) builds onchange attr value `{kind:"expr", raw:"(evt) => {...}"}` with NO exprNode. In emit-lift.js the AST `val.kind==="expr"` onevent path (L739-761) calls emitExprField(undefined, raw) → rewriteExprWithDerived → Pass 1 rewritePresenceGuard matches `(evt) => {body}` → `if (evt !== null && evt !== undefined){body}` → not callable → wrapped `function(event){...}` → free `evt`. Brief named L531 (string-form sibling path, same shape); actual per-row flow is the AST `val.kind==="expr"` path L745/760. Both get the mirrored Bug-50 fix.

- 2026-05-28 TEST WRITTEN: compiler/tests/browser/tablefor-perrow-onchange-evt-bug-59.test.js (6 tests: §1 emit-regression no free evt, §2 master handler unaffected + new Function syntax check, §3+§4 happy-dom runtime drive). Fails-before CONFIRMED: 4 fail / 2 pass against buggy code — §3 threw `ReferenceError: evt is not defined`, selectedIds stayed [].
- 2026-05-28 FIX APPLIED: compiler/src/codegen/emit-lift.js. Mirrored Bug-50 fix at BOTH uncovered onevent paths: (a) AST `val.kind==="expr"` path (~L760) — when no exprNode AND raw source is arrow/fn-expr, route through rewriteExprArrowBody (skips presence-guard) instead of emitExprField; (b) string-form emitSetAttrs path (~L514, brief's named L531 site) — same mirror on the non-`${}` branch. Imported rewriteExprArrowBody from ./rewrite.js. rewrite helpers + emit-event-wiring delegated path UNTOUCHED.
- 2026-05-28 PASSES-AFTER: new test 6 pass / 0 fail. R26 example 27 recompile: per-row handler now `evt => {...}` (arrow emitted directly), bug pattern GONE, node --check PASS, master handler L78 still `evt => {...}` (delegated, unaffected). R26 sweep examples/07-admin-dashboard + samples/tableFor-basic: clean compile, node --check PASS, no per-row select (never affected).
- 2026-05-28 SUITES: unit+integration+conformance 15076 pass / 0 fail (= baseline, no regressions). Browser dir 228 pass / 0 fail (222 baseline + 6 new), stable across 3 runs (one-time todomvc dist-flake unrelated, did not reproduce).
