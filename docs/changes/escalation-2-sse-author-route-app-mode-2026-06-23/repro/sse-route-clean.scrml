<program db="./flogence.db">

${
  server function* fspDeltas() route="/fsp/deltas" {
    for (let row of ?{`SELECT seq, kind FROM delta_log ORDER BY seq ASC`}.all()) {
      yield { event: "delta", id: row.seq, data: row }
    }
  }
}

<h1>FSP host</h1>
</program>
