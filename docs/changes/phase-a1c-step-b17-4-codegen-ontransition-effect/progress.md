# B17.4 Progress Log

- 2026-05-09 10:18 — Worktree forked from `532966f` (S73 wrap). Required preconditions (B17.2 `fd70150`, C15 `43c8747`, B17.3 `40813f4`) NOT in fork. Merged main into worktree (fast-forward to `40813f4`). `bun install` clean. Baseline confirmed: **10,512 / 65 / 1 / 0** ✓ matches BRIEF.
- 2026-05-09 10:23 — Read BRIEF, anti-patterns, kickstarter (Mario), B17.2 + B17.3 SURVEYs, SPEC §51.0.H + §51.0.J, current `emit-engine.ts` (1,190 LOC), runtime-template.js engine helpers, derived-engine substrate, `emitExprField` raw-text path. SURVEY.md written documenting Decisions 5-7 (initial-state firing = NO; derived-engine = wrap inside closure body; file-locus = extend emit-engine.ts).
- 2026-05-09 10:30 — Wrote SURVEY.md + progress.md, committed `3d2ce34`.
- 2026-05-09 10:50 — Implemented hook-firing emission in `emit-engine.ts` (+~520 LOC):
  - `OnTransitionEntryShape` type mirror
  - `EngineStateChildEntry` extended with `effectRaw` + `onTransitionElements`
  - `EngineHookArm` discriminated union (effect/to/from kinds)
  - `collectEngineHooks` walker (per state-child, source order, defensive skips for B17.3-rejected shapes)
  - `engineHookFiringFunctionName` + `engineOnceFlagName` naming helpers
  - `unwrapIfExprRaw` + `unwrapBodyRawDollarBraces` (strip parser wrappers)
  - `rewriteHookExprText` (lazy-require rewriteExpr to avoid circular import)
  - `emitHookArm` (per-arm if-block w/ once + if= compound gate)
  - `emitEngineHookFiringFunction` (per-engine fn + once-flag declarations)
  - `engineHasHooks` predicate + `collectEnginesWithHooks` (non-derived + derived)
  - `emitEngineHookFiringFunctionsForFile` (top-level orchestrator)
  - `wrapDerivedEngineClosureBodyWithHooks` (Decision 6 — old-vs-new comparison via `_scrml_derived_cache`)
  - Extended `EngineBindingInfo.hasHooks` field
  - Extended `emitEngineWriteGuard` (block-scope wrap with capture-pre + fire-post)
  - Extended `emitEngineAdvanceCall` (IIFE wrap when hasHooks=true)
  - Extended `emitDerivedEngineSubstrate` (wraps closure body when engine has hooks)
- 2026-05-09 10:55 — Wired threading through `emit-expr.ts` (`enginesWithHooks` ctx field), `emit-logic.ts` (`enginesWithHooks` opts field + `_makeExprCtx` propagation), `emit-functions.ts` (collects + threads), `scheduling.ts` (signature extension), `emit-reactive-wiring.ts` (collects + threads), `emit-client.ts` (calls `emitEngineHookFiringFunctionsForFile` after engine substrate).
- 2026-05-09 11:00 — Verified C12/C13/C14/B17.2/B17.3 still pass: 95/0 across 3 files; full suite 10,512/0. Committed `b11a403`.
- 2026-05-09 11:20 — Wrote `b17-4-codegen-ontransition-effect.test.js` with 38 tests across §B17.4.0 - §B17.4.24. Initial run: 5 fails — all related to `<onTransition>` body wrapper (parser preserves `${...}` for body but strips for `effect=`). Added `unwrapBodyRawDollarBraces` to normalise; updated 4 test assertions to match the function-name mangler post-pass output.
- 2026-05-09 11:35 — All 38 B17.4 tests pass. Full suite: 10,550/0. Committed `6937b3a`.
- 2026-05-09 11:45 — Added 3 more runtime-shape tests (§B17.4.25 sub-tests for direct-write effect, once flag layout, co-existence source order). 41 B17.4 tests total. Final suite: 10,553 pass / 65 skip / 1 todo / 0 fail. **VERDICT: SHIP.**
