# D4c BRIEF (archived per S136) — dispatched S180 2026-06-10, base e1d4f88c · agentId ad93e40f2f5511e24
agent: scrml-js-codegen-engineer · isolation: worktree · run_in_background: true (parallel to D4a)
TASK: Migrate `server function` out of worked-examples in SPEC.md + kickstarter + PRIMER per D4-INVENTORY
allow/deny. Per-site classify (NOT sed): SQL-body/import → function; handle → function (T8); channel
publisher → function (T7); pure-server-pin (computeDouble, redeem) → server fn; LEAVE prose mentions +
server fn + ALL server function* SSE + deprecation-TEACHING passages (§52.10, §20.5, §34 rows, PRIMER
186-196, kickstarter 319/326) + session-only (getProfile/checkAuth). Spot-compile 4-6 migrated worked
examples (incl. handle/channel/server-fn) — confirm compile + stay server (no client-flip). regen-spec-index
if ranges shift. Grep gate: residual `server function` = only SSE/prose/teaching (report with reason).
Full F4+S99/S126+MAPS+commit-discipline. Merge main e1d4f88c. Scope: the 3 docs ONLY.
