# Phase A1a Step 6 — progress (append-only timestamped)

## Step 6 — parser: `default=` attribute + `pinned` bareword on state-decl

- [06:00 step-6 startup] WORKTREE_ROOT = `/home/bryan-maclee/scrmlMaster/scrmlTS/.claude/worktrees/agent-a46a9b41839b6cf69`. HEAD `ce30247` (matches expected). Branch `phase-a1a-step-6-default-pinned` created from clean tree. `bun install` ok (113 packages). `bun run pretest` ok (12 samples). Baseline run-1: 8783 / 43 / 2 fail (ECONNREFUSED tail-shape — same flake pattern as prior baselines). Run-2: 8784 / 43 / 0 fail / 8827 — exactly the documented baseline. Per flake protocol: ≤3 fails on run-1 + clean run-2 = BASELINE STABLE. Proceed.
- [06:01 step-6 survey — touchpoint] Confirmed BRIEF's named touchpoint is correct: `tryParseStructuralDecl` at `compiler/src/ast-builder.js:2883` calls helper `scanStructuralDeclLookahead` at `:3030`. The helper's main loop (lines 3039-3123) is where bareword validators are matched. Step 6 extends this same loop. No divergence from BRIEF's locus.
- [06:02 step-6 survey — `default` keyword status] `default` is in KEYWORDS at `tokenizer.ts:58`. Therefore `<x default=null>` would currently DECLINE the entire scan (KEYWORD hits the "Anything else: decline" line 3122). NEW BRANCH needed: detect KEYWORD with text "default" + next token PUNCT `=`, then collect an EXPR-form RHS. Cannot be matched by existing IDENT branch.
- [06:03 step-6 survey — `pinned` keyword status] `pinned` is NOT in KEYWORDS (correct per AST-CONTRACTS §2.1 — contextual). It tokenizes as IDENT. Therefore `<x pinned>` would currently be CAPTURED AS A VALIDATOR by the existing IDENT bareword branch (line 3068). MUST add a guard BEFORE the generic validator branch: when IDENT.text === "pinned" AND next is NOT `(`, treat as the `pinned` modifier (set flag, do NOT add to validators[]).
- [06:04 step-6 survey — `default=expr` RHS collector] Reviewed `_parseLiftAttrValue` at ast-builder.js:2459+. It is markup-attribute-context tailored — handles BLOCK_REF, string-literals, call-form `name=fn(args)`, ident references — and lives inside `parseLiftTag`. It is NOT a general expression collector. For `default=expr` we need to collect an expression bounded by whitespace (next attr) or `>` (closer). Approach: token-walk with bracket/paren depth tracking similar to the call-form arg collector at lines 3079-3094. Stop conditions: top-level (depth 0) PUNCT `>`, top-level PUNCT `)` (defensive — should not occur), top-level whitespace-bounded next IDENT/KEYWORD attr (peeked via lookahead). Simpler approach: collect tokens until next top-level `>` is seen, then post-process by trimming any trailing IDENT tokens that look like the next attribute. Even simpler: collect raw token text until we hit a token that COULD start a new attr (PUNCT `>`, or IDENT/KEYWORD where next is `=` or `(` — but that is hard to disambiguate from expr). **Final design choice:** the EXPR for `default=` is bounded by the NEXT TOKEN that is one of: PUNCT `>` (closer, stop), PUNCT `(` at depth 0 attached to a preceding IDENT (next call-form attr), or a sequence of IDENT IDENT (current ends, next starts) — but that interferes with binary ops. Cleanest rule: collect tokens with bracket-depth tracking; stop at top-level `>` or `=` (would mean "attr=val attr2=val2" — next attr). For Step 6 we keep it minimal: collect tokens while depth > 0 OR (next is not `>` AND not a bareword-ident-followed-by-`(`-or-end). Per-token simple approach: **collect tokens until top-level `>` is reached, OR top-level whitespace-separator followed by IDENT/KEYWORD where the IDENT is `pinned` or another known attribute name**. Reuse: not viable; write fresh helper.
- [06:05 step-6 survey — simpler collector approach] Re-examining: in practice, the RHS expressions for `default=` are simple literals (`null`, `0`, `""`, `false`, identifiers). Per SPEC §6.8, `default=` RHS must be a constant expression at parse-time. **Final design choice — minimal token-walker:** collect tokens at bracket depth tracking. At depth 0, stop on: PUNCT `>` (closer), or any token sequence "IDENT" (where the IDENT has text "pinned" — known modifier) at NEXT position, or "IDENT (" "next call-form attr" (= sign or open-paren following IDENT). Rather than overengineer, adopt the rule: **at depth 0, stop on PUNCT `>` or any IDENT/KEYWORD that has lookahead pattern matching another attr (`pinned` bareword, ident-followed-by-`(`, ident-followed-by-`=`)**. Args text is joined raw and stored as `args[0]`-style raw text per Step 5 convention; the field shape will be `defaultExpr: { raw: "...", span }` — but BRIEF says ExprNode|null. **Decision: store as ExprNode if `safeParseExprToNode` succeeds, else null with a `defaultExprRaw` fallback.** Survey existing `safeParseExprToNode`: lives in ast-builder.js, parses a string via acorn. Yes, can use it. Final shape: `defaultExpr: ExprNode | null` where ExprNode is `safeParseExprToNode(rawText, startSpan)`.
- [06:06 step-6 survey — existing tests/samples using default= or pinned] `grep` confirmed: zero existing tests/samples reference `default=` or `pinned` on state-decls. Only mention is the pre-existing TODO comment in parse-shapes-v0next.test.js header (line 105): `* default=` / `pinned` modifiers (Step 6)`. No regression risk from prior fixtures.
- [06:07 step-6 survey — self-host parity] `compiler/self-host/ast.scrml` has NO `tryParseStructuralDecl` or `scanStructuralDeclLookahead` counterpart (Step 5 didn't add one). Therefore Step 6 ALSO does not need self-host parity — the structural-decl scan logic is JS-builder-only. Confirmed via `grep`.
- [06:08 step-6 design summary]
  - Extend `scanStructuralDeclLookahead`:
    - Track new locals `defaultExpr` (string|null), `defaultExprStart` (span|null), `pinnedFlag` (boolean).
    - Add new branch for KEYWORD `default` followed by PUNCT `=` → collect raw RHS text via depth-tracked walker; bound on top-level `>` or next-attr pattern.
    - Add new branch for IDENT `pinned` + NOT followed by `(` → set pinnedFlag = true, advance, continue (DO NOT add to validators[]).
    - Existing IDENT branch continues to handle other barewords/call-forms.
  - Return value: extend the result object with `defaultExpr` (raw text or null) + `pinned` (boolean).
  - In `tryParseStructuralDecl`: parse rawText to ExprNode via `safeParseExprToNode`; populate `defaultExpr` + `pinned` on all three return shapes (Shape 1, Shape 2, Shape 3).
- [06:09 step-6 implementation in progress] Beginning code edits.
