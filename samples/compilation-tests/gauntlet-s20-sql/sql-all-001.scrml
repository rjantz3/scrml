// Phase 6: ?{}.all() — multiple rows SELECT (§8.3)
// Should compile clean.

< db src="./test.db" tables="users">
${
  let users = ?{`SELECT id, name FROM users`}.all()
}
<ul>
  <each in=users as user>
    <li>${user.name}</>
  </each>
</ul>
</>
