// arithmetic on the per-item value inside a canonical `<each>` iteration (§17.7)
${
    <items>: number[] = [1, 2, 3]
}
<program>
    <ul>
        <each in=@items as item>
            <li>${item * 2}</li>
        </each>
    </>
</>
