// meta-003-function.scrml
// Meta block with function. Tests: ^{} with function declaration.

${
    import { random } from 'scrml:random'
}

<div>
    ^{
        function generateId() {
            return "meta-" + random()
        }
    }
    <p>Meta function</>
</div>
