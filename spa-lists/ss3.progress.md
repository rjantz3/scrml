# sPA ss3 — codegen-expr-attr · progress log

Append-only. Branch `spa/ss3` off `origin/main` a99246e2. Boot S209.

- BOOT — worktree `../scrml-spa-ss3` created off origin/main a99246e2; node_modules symlinked; progress file initialized. Starting item 1.
- item2 `s169-ordered-unordered-build` — **LANDED on spa/ss3** (selective file-delta from agent tree bfd7c2d2: 7 codegen src + 2 tests + progress; NOT the co-resident item3 type-system.ts on the shared branch). Agent: pre-commit gate 17282 pass/0 fail + browser 442/0; +24 tests (18 unit + 6 e2e). sPA R26 re-verify on branch: `@ordered` decl-init literal + `[:]`-empty → `_scrml_map_from_entries(..., true)`; non-ordered control → `false` (no false positive). Fix = `collectOrderedMapVarNames` (reactive-deps) + ctx flag (`emitMapLitOrdered`) consumed by emitMapLit (nested map-VALUE literals cleared→stay unordered) at state-decl init/C5-sidecar (emit-logic, keyed on decl `@ordered` typeAnnotation OR the name-set for reassignment) + emitAssign + threaded everywhere `mapVarNames` is. KEY: `isInit` is true for ANY non-engine state-decl so reassignment `@m=[...]` reaches emit-logic not emitAssign — both paths covered. Base: agent merged main a99246e2 at startup (S112); item2 files byte-identical a99246e2↔spa/ss3 → clean delta. Nested-VALUE `@ordered` (`[K:[K:V]@ordered]`) explicitly OUT-OF-SCOPE (no per-value annotation at codegen). **Confirmed the shared-worktree race from item2 side too** (held for index.lock ~39s, committed with pathspec).
- item2 `s169-ordered-unordered-build` — **DISPATCHED** (agent ab878043, bg). R26-confirmed bug: `@ordered` map cell init emits `_scrml_map_from_entries([...], false)` (decl-init literal + `[:]`-then-insert + reassign-literal all drop the affix); normative §59.8 violation in shipped (S169) behavior; runtime already honors `ordered=true`. Fix: ordered-cell name-set (reactive-deps) + ctx-threaded flag consumed by emitMapLit (nested map-VALUE literals stay unordered) at decl-init (emit-logic) + reassign (emitAssign). BRIEF archived. Awaiting agent.
- item3 `g-bare-literal-attr-value` — **LANDED on spa/ss3** (file-delta from agent commit 0030ba5f: type-system.ts + type-system.test.js + progress.md). Agent: 238 unit pass (8 new §43), full suite 24537 pass incl browser. sPA R26 re-verify on branch: interval/running/delay → 0 E-SCOPE-001; `running=false` W-LIFECYCLE-007 intact; negatives `<input value=42>` + `interval=@bogus` STILL error. Fix = value-aware `TS_SPEC_BARE_LITERAL_ATTRS` {reconnect,channel-reconnect,interval,running,delay} skip in visitAttr (bare numeric/dur/bool only); reconnect made value-aware too (S186 improvement, no locking test). `after=` confirmed NOT needed (dedicated walker handles it). **SHARED-WORKTREE HAZARD:** agent ran in pre-existing stale worktree agent-a6eb2c2fd9ba6086b CONCURRENTLY with item2's agent (s169 commit 2309d5fd landed on same branch); landed ONLY 0030ba5f's files (NOT wholesale). Base-check: type-system.ts/test.js byte-identical between agent-base 254346e0 and spa/ss3-base a99246e2 → clean file-delta. **RESIDUAL surfaced (file to PA):** derived-engine `<engine for=@phase>` crashes "autoDeriveEngineVarName is not defined" — pre-existing on clean main, NOT item3, NOT introduced.
- item3 `g-bare-literal-attr-value` — **DISPATCHED** (agent a4f36cd0, bg). R26-confirmed: bare numeric/bool on spec-typed structural attrs false-fire E-SCOPE-001 (`interval=`/`running=false`/`delay=` parse as variable-ref named by the literal text). Fix: value-aware exemption in type-system.ts visitAttr extending the S186 reconnect allowlist to {interval,running,delay} (bare-literal shape only — `running=@var` still scope-checks; `<input value=42>` still errors). debounced/throttled NOT affected (decl-scanner path). after= to investigate. BRIEF archived. Awaiting agent.
- item5 `r28-2b` — **PARKED → escalate PA.** Already a triaged/deferred known-gap (known-gaps.md:336 `R28-2b` LOW/open; changelog.md:341 re-confirmed-and-deferred). Leading-`:` on `:let` stripped by tokenizer START class (`tokenizer.ts:451` `/[A-Za-z_@]/` excludes `:`; mid-name `:` survives via the continuation class). R26-repro: `:let` triggers an E-CTX-001 cascade (the `:` misread as the §4.14 `:`-shorthand body introducer). Admitting leading `:` ripples across ALL leading-colon attrs (`bind:`/`class:`/`on:`) AND into block-splitter §4.14 `:`-shorthand recognition (ss4 territory) → broad blast radius + a language-cohesion ruling. `:let` works via the `let` alias today. NO code change. Escalate class: design-ruling + blast-radius-exceeds-shared-ingestion.
- item8 `dq12-phase-b-bare-compound-is-op` — **PARKED → escalate PA (design ruling + a real silent-wrong bug).** R26 findings: (1) bare-compound is-op in a LOGIC body (`doMatch(@s) is not`) ALREADY works — emits identical correct codegen to the paren form (AST-level is-op lowering). (2) The rewrite.ts string-pass (`_rewriteParenthesizedIsOp` + DOTTED_LHS regex) is a FALLBACK for non-parsing contexts (attribute `if=` / for-lift markup). (3) **SILENT-WRONG BUG:** bare-compound is-op in an attribute condition (`<p if=doMatch(@s) is not>`) silently DROPS the `is not` → emits `if ((doMatch(s)))` (plain truthiness — semantics inverted vs the absence check); the paren form `if=(doMatch(@s) is not)` correctly emits `=== null || === undefined`. `is`/`is not`/`is some` are NOT in the cluster-A operator set, so `E-ATTR-UNQUOTED-OPERATOR` does NOT fire and the bare form slips to silent-wrong codegen. **FIX DIRECTION is a ruling the sPA can't make:** (a) SUPPORT bare-compound in the fallback rewrite (the filed dq12-phase-b feature framing) vs (b) REJECT-with-parens-steering, consistent with the cluster-A "compound conditions SHALL be parenthesized" direction (§5.2/§17.1, S188). No ratification (no `dq12` provenance in docs/compiler; no SPEC §42.2.4 Phase-B). Escalate class: design-deliberation + amendment-direction-must-be-explicit. The silent-wrong must be fixed EITHER way; the direction is the open question. NO ss3 code change pending the ruling.
- item6 `emit-sql-ref-placeholder` — **PARKED → escalate PA (prereq-blocked experiment).** `emitSqlRef` (emit-expr.ts:1850) is an explicit `TODO(Phase 3 Slice 4): structured SQL ref emission`; returns `/* sql-ref:N */` placeholder. The comment states SQL blocks are handled at the STATEMENT level, not expression level — i.e. statement-level SQL emission is the PREREQ (a separate subsystem, not present; the existing sql-ref code in emit-validators/constant-folder/collect only CLASSIFIES nodes for server-only suppression). Classified an experiment; future-phase + design-open. Blast-radius exceeds ss3's emit-expr surface. The nodeId<0 defense-in-depth guard is intentional. NO ss3 code change.
- item7 `giti-006-async-reactive-module-top-read` — **ANALYZED → DISPATCH after item2 lands** (file overlap on emit-logic.ts/emit-expr.ts). R26-confirmed bug: markup `${@var.path}` emits a SPURIOUS module-top bare statement read (e.g. `_scrml_reactive_get("data").name;`) in the module-top logic section, IN ADDITION to the correct render-effect. Dead/no-op for sync data; for an async-init reactive (`<data> = serverFn()`, null placeholder at init) it's `null.name` → THROWS at module init before the fetch resolves (workaround `@data default`). Root: a MARKUP-interpolation bare-expr is double-emitted — once as a module-top logic statement (emit-logic.ts) AND once as render wiring (emit-html/emit-bindings). Fix = distinguish markup-interpolation bare-exprs (render-only, suppress module-top emission) from genuine logic bare-exprs (keep module-top, side effects). Bounded codegen fix; NOT a design question (the spurious statement is dead code regardless of async-null semantics). Touches emit-logic.ts (+ emit-html/emit-bindings, maybe emit-expr) → serialize after item2. BRIEF pending.
- item4 `g-render-not-enum-asis-miss` — **LANDED on spa/ss3** (file-delta from agent ece13d5c, branch ss3-item4-render-fresh based off spa/ss3 7eb75dca via `checkout -B` — base-setup worked, NO collision this time despite reusing worktree agent-a6eb2c2fd9ba6086b). Agent suite 17291 pass/0 + browser 442/0; render-expr-primitive.test.js 15 pass (+9). sPA R26: `<render of=@s/>`+`<render of=@n/>` → E-RENDER-NOT-ENUM ×2 (was silent empty-switch no-op). Fix = `classifyRenderInitShape` concretizes an asIs-erased of= target from the cell's literal init (string/number/bool/array/object/map-lit/negated-num → fence; everything else incl. call-init/bare-variant/absence-init → STAY SILENT, strict guard preserves the deliberate asIs-silence for enum-erased cells); ScopeEntry gains lazy `declNode` read ONLY by the fence. type-system.ts only (item3/item4 in disjoint regions). **2 PA residuals surfaced:** (1) render-expr-primitive.test.js:65-76 misnamed ("string cell inert no-op" but asserts on an enum cell; assertions correct, left intact); (2) the untyped reactive `state-decl` arm doesn't infer a primitive resolvedType from a literal init the way let/const does (the upstream reason cells stay asIs) — fix sidesteps at the fence rather than changing the bind type; worth a future look.
- item7 `giti-006-async-reactive-module-top-read` — **LANDED on spa/ss3** (file-delta from agent 4a1b43fe, branch ss3-item7-asyncread-fresh based off spa/ss3 631ea548; agent created its OWN worktree agent-ss3-item7 — NO collision). Agent suite 17297 pass/0 + browser 442/0; bug-5-const-interpolation.test.js 32/32 (+6). sPA R26: spurious module-top `_scrml_reactive_get("data").name;` GONE; render-effect intact (×2). **ROOT differed from brief hypothesis** (R26 win): NOT a double-emit in emit-logic — the existing S107/S144 pure-read-orphan SUPPRESSION regex in emit-reactive-wiring.ts already suppressed bare-cell `${@data}` but had a GAP for the dotted-path `${@data.name}` chain; the `_scrml_(reactive|derived)_get(...)` alternative lacked the trailing `(?:\.IDENT|\[k\])*` member chain the sibling `_scrml_input_state_registry` alt already carried (S144, same throw-on-init reason). 1-regex-class fix. Structural-role guard satisfied (suppression gated on `pid`=markup-interp; top-level `${fn()}` has no pid → still emits; method-call `${@x.join()}` NOT over-suppressed — deliberately excluded the `(...)` call alt). emit-reactive-wiring.ts only (disjoint from item2/3/4). **PA residual surfaced:** typed-object-literal return in a `server function` body (`return {name: string...}`) trips E-SCOPE-001 on the field name — separate type-scope quirk (matches the E-SCOPE-001-on-`name` I hit in my own async reproducer); out of scope, NOT investigated.
- item1 `g-component-001-coverage` — **NOT-REPRODUCED.** Reproduced fn-typed-prop component on real source (fnprop2/fnprop3): W-COMPONENT-001 fires for `() => void`, `(e) => T`, optional `() => bool`, single+multi-line `props={}`. Root: block-splitter `scanAttributes` already tracks bare `{` depth (block-splitter.js:1233-1241) — the footprint's "can't fire until BS updated" premise is stale. `isFunctionType` covers the canonical arrow form fully; no real coverage gap. Residual: corrected stale "will not fire" comment at component-expander.ts:1066-1072 (comment-only, sPA-authored, no behavior change). → landing.

---

## S210-rebuild run (3-item list) — fresh boot

- BOOT (S210 rebuild) — prior `spa/ss3` branch GONE/merged post-S210; the list above (items 1-8) is the OLD S209 shape, superseded by the rebuilt 3-item list (`ss3-codegen-expr-attr.md`: item1 bare-compound is-op silent-drop · item2 bug-18 is-op ternary computed-LHS · item3 each-body @. expr-parser). Re-created worktree `../scrml-spa-ss3` off local `main` db906e40 (= merge-base; origin/main was stale at 135c8a78, local main carries the S210 work incl. item1's verified-HEAD base 956460af). node_modules symlinked. Running sPA-direct (CWD-reset in this session makes isolation:worktree routing fragile; items are surgical + disjoint).
- item3 `g-each-body-sigil-root-expr-parser` — **LANDED on spa/ss3 @544e5c42** (sPA-direct). R26 (direct `parseExprToNode`): `@.`/`@.field`/`@.a.b`/`@.items[0]` all escape-hatched (acorn `scrmlAtPlugin` only consumed `@`+ident) — the ExprNode layer couldn't STRUCTURE the §17.7.3 sigil; ROOT of the ss14 expr-node-corpus-invariant each-sigil false-positive. **Scope clarified by R26:** the each-body MARKUP path (`${@.name}`) already lowered fine via the emit-lift string-rewrite, and `@.`-outside-each is E-SYNTAX-064 via a type-system token scan — both independent of the acorn parse; the gap was purely ExprNode structuring (the classifier root). Fix: `scrmlAtPlugin.readToken` recognises `@.` via a non-destructive inline-ws-tolerant lookahead (handles the BS `@ . name` join form), emitting one name token; `@name`/`@x.y` untouched (lookahead only fires when `@` is followed past-ws by `.`); no string-corruption (readToken never fires in string interiors). classifier each-sigil **2→0** (total escapes 20→18); +9 unit tests (`each-sigil-expr-parser.test.js`); full suite 24692/0. **PA RESIDUALS:** (1) the each-sigil classifier band-aid (`expr-node-corpus-invariant.test.js` ~134-143/285/388-396) is now always-0 — remove in a hygiene pass else it masks future `@.`-parse regressions; (2) native-parser (M2.x) `@.` structuring NOT verified (separate pipeline). NOTES: `docs/changes/ss3-item3-each-sigil-expr-parser/`.
- item2 `bug-18` / GITI-015 — **LANDED on spa/ss3 @7ed9ff86** (sPA-direct). R26 reproduced (`--mode library`): is-op (`is some`/`is not`/`is not not`) with a bracket-index LHS (`args[i + 1]`) not lowered → keyword literal in output → E-CODEGEN-INVALID-JS. **Empirical signature corrected the stale repro comment** — on HEAD ALL bracket-index LHS fail (ternary AND if-pred), not just ternary; ident/dotted/call-tail work. Root: LIBRARY-mode line-by-line emit path (`rewriteIsOperator(rewriteNotKeyword(line))`, emit-library.ts:195/375) uses `_rewriteNotSegment` whose `DOTTED_LHS` lacked a bracket-index segment; `rewriteIsOperator` has no `is some`/`is not not` fallback. Client/AST path already fine (rewriteIsPredicates balanced-bracket scanLhsLeft). Fix: extend `DOTTED_LHS` (rewrite.ts) with a one-nesting-level bracket-index tail; mirrors the dotted-path double-eval form. **Value-asserted at runtime** (ran the emitted ES module — v1/v2/v3/control/controlIfPred all correct). +6 `rewriteNotKeyword` unit tests; full suite 24683/0. **PA RESIDUAL surfaced:** call-tail LHS `re.exec(s) is some` silently MIScompiles to `re.exec((s) != null)` (`_rewriteParenthesizedIsOp` grabs the call's arg-parens) — separate root cause, silent-WRONG, out of bug-18 scope; worth a dedicated item. NOTES: `docs/changes/ss3-item2-isop-ternary-computed-lhs/`.
- item1 `g-attr-bare-compound-is-op-silent-drop` — **LANDED on spa/ss3 @7f3bd4ca** (sPA-direct, base db906e40). R26 reproduced: bare is-op in `if=`/`show=`/`else-if=` silently dropped the keyword run → plain truthiness, no diagnostic; `is not` also INVERTED (`if=fn() is not` → `if((fn()))`). **Fix locus differed from footprint** — NOT `rewrite.ts` `_rewriteParenthesizedIsOp` (that's the non-parsing fallback the bare form never reached); the real locus is the TOKENIZER `attrConditionOperatorAhead` op-set (`is`/`is not`/`is some`/`is not not` were absent) + the call path which committed to ATTR_CALL before the reject check. Fix: add keyword is-ops to the op-set + extract a shared `pushConditionOpReject` helper called from BOTH ident and call paths. The call path was a WIDER latent gap — also silently dropped bare binary ops after a call (`if=fn() && @m`). +20 tests (attr-unquoted-operator-reject 31→51). **Full suite 24677 pass / 0 fail** — the initial 140 fails were the S209-ss9 fresh-worktree ENV-GAP (missing gitignored `samples/compilation-tests/dist/`); built via `bun run pretest` (all 13 browser samples compiled clean incl. the if=-heavy ones → not a regression). NOTES: `docs/changes/ss3-item1-bare-is-op-attr/`.
