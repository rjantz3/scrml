# sPA ss2 — progress (engine-codegen-statechild)

Branch `spa/ss2` · worktree `../scrml-spa-ss2` · base c734ec35 (origin/main, S209).
Append-only. Each line: what was done / next / blockers.

- [boot] worktree from origin/main c734ec35; node_modules symlinked; branch clean. Read coreFiles (engine-graph.ts full; symbol-table/emit-engine targeted). Foundation warmed.
- [item-1 scope] `engine-boot-effect-invalid-transition`: reproduced. §3 test (engine-opener-effect-c1.test.js:242) is `test.skip` documenting the deferred `.<initial>.rule` write-validation. SPEC §51.0.H Form 3 (25741-25745) + engine attr table (24871) NORMATIVE: opener `effect=` writes to the engine var are checked against `.<initial>.rule`. **Landing = SYM (symbol-table.ts), NOT type-system.ts** — the footprint's guess is contradicted by the test harness (`symErrorCodes` runs `runSYM` only). Reuse: `validateEngineA5Extensions` (9259) + `scanDirectWritesInStateChildBody` (9206) + the `switch(r.kind)` membership pattern (fire-site #9, 9661-9719). New once-per-engine fire-site against the initial state-child's rule. Next: brief + dispatch.
- [BATCH-A dispatched] 3 file-disjoint agents in parallel (all branch off c734ec35 under main, land via file-delta onto spa/ss2):
  - item 1 (a1125279) symbol-table.ts — boot-effect §51.0.H write-validation (new SYM fire-site in validateEngineA5Extensions).
  - item 4 (a91d0c9f) emit-engine.ts (+emit-html.ts/emit-match.ts export) — engine state-child :-shorthand body dropped (§51.0.I); mirror emit-match.ts:817-834 displayTextLiteralInner→nativeParseFile. R26-reproduced: sc.bodyRaw+sc.isColonShorthand captured but buildEngineArms renders from empty match.children.
  - item 3 (a58c1007) type-system.ts + NEW engine-statechild-grammar.ts — dedup the reserved-attr/structural-tag sets (zero behavior change).
  BRIEFs archived under docs/changes/ss2-*. Next while they run: scope items 2 + 5 (Batch B — overlap symbol-table/emit-engine, land after Batch A).
- [item-2 scope] `g-engine-server-flag-silent-swallow` (MED): R26-reproduced — bare `<engine ... server>` → 0 diagnostics, flag parsed-and-dropped (known-gaps.md:196). FOOTPRINT CORRECTION (R4): bare flag is NOT captured at parse — ast-builder.js:14263 serverSource regex only matches `server=@source`. So needs FOUR touch points: ast-builder.js (capture `serverFlagBare`), symbol-table.ts (fire W-ENGINE-SERVER-DEFERRED, warning stream), SPEC.md §34 (new catalog row — SPEC-touch, FLAG to PA at re-integration), emit-engine.ts (confirm inert). Design settled per known-gaps prescription (S198 engine-hydration DD). BRIEF authored, ready. Overlaps items 1+4 (Batch A: symbol-table/emit-engine) + item 5 (ast-builder) → lands after Batch A via cherry-pick.
- [item-5 scope] `engine-component-scope-b17-e2e-deferred` (LOW · tier HIGH): b17 test has 8 test.skip cases (259-324). R4/R26 FINDING — the blocker COMMENTS ARE STALE: they claim `<onTransition>` un-tokenized, §51.0.F state-children unparsed, block-`<match>` unparsed — but ALL THREE have since landed (validateEngineA5Extensions fire-site #4 validates <onTransition>; engine-statechild-parser parses §51.0.F sc.rule/sc.bodyRaw; etc.). So ~5 of 8 cases (onTransition placement, effect-ambiguous, match-arm forbidden ×3) are likely CHEAPLY un-skippable now. The 3 genuinely-blocked cases (engine-decl in component-def.defChildren / raw body / EngineName mount) need a COMPONENT-BODY MARKUP PARSER (component-def stores `raw: string`) = tier-HIGH from-scratch subsystem → candidate PARK (escalate to PA). Plan: dispatch a SURVEY-FIRST brief that re-verifies each skip's current blocker, activates the now-unblockable cases, and PARKS the parser-build for the PA. Sequenced AFTER item 2 (both touch ast-builder.js + symbol-table.ts).
- [PLAN] Batch B (items 2 then 5) lands sequentially after Batch A (items 1,3,4) integrates onto spa/ss2 — cherry-pick used where regions overlap (item1 sym~9259, item2 sym~5204; item4 emit-engine~2148, item2 emit-engine~1791; item2 ast-builder~14263, item5 ast-builder~9149). Awaiting Batch A completions.
- [item-1 LANDED] agent a1125279 SHA 100447ff verified: diff scope clean (symbol-table.ts +125 fire-site #11, test +79/-1), no main leak, agent full-suite 24583 pass/0 fail, no corpus illegal-boot-writes (4 opener-effect .scrml audited). sPA independent re-verify: targeted test 20 pass/0 fail on spa/ss2 tree. file-delta'd → committing on spa/ss2.
- [item-1 COMMITTED] spa/ss2 tip = bd4c1b34 (hook-gated, exit 0). Coherence: HEAD = base+1.
- [COHERENCE NOTE for PA] origin/main advanced c734ec35 → 256c81b6 (+3: deputy ticks 96/97 + `4e7fa0f0 despace Part B`) since ss2 branched. NONE of the +3 touch my source files (symbol-table/emit-engine/type-system/ast-builder/emit-html/emit-match all untouched) → clean source merge at re-integration. HEADS-UP: despace 4e7fa0f0 touches SPEC.md; item-2's planned §34 W-ENGINE-SERVER-DEFERRED row lands on a stale SPEC base → PA must reconcile item-2's SPEC.md edit vs despace at re-integration. ss2 stays on c734ec35 base (do NOT rebase mid-flight — agents 3/4 still based on c734ec35).
- [item-4 LANDED] agent a91d0c9f SHA 6bd1d352 verified: emit-engine.ts +85 (buildEngineArms §51.0.I :-shorthand body from sc.bodyRaw, mirrors emit-match.ts S196 pattern), emit-match.ts +2/-2 (export displayTextLiteralInner), new test +288 (12 cases). No main leak, clean file-delta (emit-match/emit-engine untouched on spa/ss2). §4.14 emit-html.ts second face ALREADY-CORRECT — no change. sPA re-verify: targeted 12/0; R26 codegen repro now renders interp+literal (was both dropped); agent full-suite incl browser 24589/0. No re-baselines.
- [item-2 dispatched] agent a93aa91a (off c734ec35). 4 touch points: ast-builder.js (capture serverFlagBare), symbol-table.ts (W-ENGINE-SERVER-DEFERRED warning), SPEC.md §34 row (SPEC-touch → flag to PA), emit-engine.ts (confirm inert). Running concurrently with item 3 (file-disjoint). Lands after via cherry-pick (sym/emit-engine carry items 1/4). Item 5 sequenced AFTER item 2 (both touch ast-builder.js + symbol-table.ts).
- [item-3 LANDED] agent a58c1007 SHA 902fb6da verified: NEW engine-statechild-grammar.ts SSOT (2 ReadonlySet exports) at compiler/src/ (not codegen/ — TS-importable); type-system.ts + emit-variant-guard.ts import it, local literals deleted; NEW 5-test guard. No main leak, clean file-delta (both files untouched on spa/ss2). Agent verified codegen set member-identical (no divergence). Full suite 24582/0, delta +5 = new test only (zero behavior change). sPA re-verify: SSOT test 5/0.
  RESIDUALS for PA: (a) payloadBindings-swap at type-system.ts:170 deferred (semantic inversion, not a literal swap — no byte-identical proof); (b) reserved-attr set is actually 5-wide not 2-wide — 3 more member-identical copies NOT migrated (scope discipline S88): engine-statechild-parser.ts:102 RESERVED_STATE_CHILD_ATTRS, native-walker/engine-statechild-walker.ts:273 RESERVED_PAYLOAD_ATTRS, symbol-table.ts:6672 inline. Clean follow-on dedup (noted in new module header).
- [item-3 COMMITTED] spa/ss2 tip = ff196ce8 (hook exit 0). Batch A fully landed: items 1,4,3 on spa/ss2 (base+3).
- [item-5 dispatched] agent a53930ca (off c734ec35), running concurrently with item 2. Survey-first b17: PARK cases 1-3 (component-body markup parser, from-scratch → PA escalation), ACTIVATE-candidate cases 4-8 (stale blockers — onTransition/effect-ambiguous/match-arm validations likely already fire). Expected mostly test-only (b17 test) + small symbol-table wiring if any → cherry-pick land. BRIEF authored.
- [BASE-DIVERGENCE caught] items 2+5 agents dispatched AFTER local main advanced c734ec35→256c81b6 → they branched off 256c81b6 (despaced SPEC). Verified: base divergence c734ec35..256c81b6 touched ONLY SPEC.md (+deputy chore files). ALL item-2/5 SOURCE files base-identical → file-delta safe; SPEC.md needs special handling (item 2's §34 row is on despaced base).
- [item-5 LANDED/PARTIAL] agent a53930ca SHA 075f0d89: b17 test ONLY (+189/-47), base-identical clean delta. Survey: 6 ACTIVATE (cases 4-8, validations already fire end-to-end — NO source change) + 3 PARK (cases 1-3 engine-in-component-body, need from-scratch component-body markup parser). sPA re-verify: b17 15 pass/3 skip/0 fail on spa/ss2. PARK ESCALATION → PA (see re-integration msg): cases 1-3 need component-body markup parser (component-def.raw:string → walkable AST); ast-builder.js:~9149-9151 + ~10370 (raw storage) + ~15007 (defChildren) + ~15607 (§51.3 collector). Out of sPA scope (from-scratch subsystem).
