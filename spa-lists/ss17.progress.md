# ss17 — each-codegen · progress

Branch `spa/ss17` · worktree `../scrml-spa-ss17` · base `2a4bf8af` (== origin/main at boot).

## S219 sPA run (2026-06-25)

- **Boot:** worktree created from origin/main; node_modules + compiler/node_modules + samples/compilation-tests/dist symlinked from main (S209 dist-gap mitigation).
- **Scoping (R26 reproduce-first):** all 3 items confirmed against real source.
  - Item 1 `g-expr-event-handler-dead-in-each`: REPRODUCED. `onclick=${()=>mark(@.id)}` → dead arrow-expr statement (silent no-op); `onclick=${()=>@x=@.id}` → E-CODEGEN-INVALID-JS ("assigning to rvalue"). Locus emit-each.ts:971-985 (`expr` branch). Fix: route through structured emitter (mirror S216 Half-1 `buildHandlerExpr`, f4bef40f).
  - Item 2 `g-each-peritem-markup-value-ternary`: REPRODUCED. `${@.active ? <span>ON</span> : ""}` emits skip-comment, non-render. Locus emit-each.ts:549-552. Fix: thread `@.` iter-scope into emitMarkupValueExpr/emitCreateElementFromMarkup.
  - Item 3 `g-nested-each-outer-key-reuse-inner-frozen`: code-path gap confirmed. Inner-each `_scrml_effect` (emit-each.ts:730-735) bypasses maybeWrapEachPerItemEffect → reads create-time outer closure param; frozen on outer-row REUSE. Fix: inject outer-item re-resolution prelude into the inner effect.
- **Dispatch:** ONE combined `scrml-js-codegen-engineer` (isolation:worktree, opus), agent `a70743c2d10d2eb1a`, change-id `ss17-each-peritem-emitter-2026-06-25`. All 3 items (same file emit-each.ts, different functions → no collision; per-item incremental commits mandated). BRIEF archived `docs/changes/ss17-each-peritem-emitter-2026-06-25/BRIEF.md`.

### Status
- [in-flight] item 1 — g-expr-event-handler-dead-in-each (agent a70743c2)
- [in-flight] item 2 — g-each-peritem-markup-value-ternary (agent a70743c2)
- [in-flight] item 3 — g-nested-each-outer-key-reuse-inner-frozen (agent a70743c2)

## Landing (2026-06-25) — agent a8761ec73e6d4aca4 → spa/ss17 (S67 file-delta)

Agent branch `worktree-agent-a8761ec73e6d4aca4`, 3 per-item commits (provenance):
- item 1 `920fb3f7` — buildEachExprHandlerBody (parallel impl of buildHandlerExpr's shape-dispatch; arrow/fn→invoke, bare-ref→invoke, assign→_scrml_reactive_set). emit-each.ts only.
- item 2 `a336f6f9` — emitEachPerItemMarkupValue (deep-clone exprNode, @.-iter-scope rewrite, markup-text ${} split into interp children, live-keyed data-scrml-mv wrapper). emit-each.ts only; emit-lift reused unchanged.
- item 3 `18f4c1a2` — outer-item re-resolution prelude injected into inner-each effect (capture OUTER ctx before inner push; _scrml_resolve_item). NO runtime-template.js change.

5 files file-delta'd onto spa/ss17 (emit-each.ts + 3 new browser tests + giti-032 unit coupled-update S113).

**sPA independent verification (R26 + adversarial, S215):**
- R26 reproducers (my own, not the agent's): arrow-call now `(()=>...)(event)` (was dead stmt); arrow-assign compiles + `_scrml_reactive_set` (was E-CODEGEN-INVALID-JS); markup-ternary builds DOM (was skip-comment). ALL CONFIRMED.
- New + each-regression browser tests + giti-032 unit: 35 pass / 0 fail.
- TodoMVC: 57 pass / 0 fail (the 2 transient full-suite fails were the S209 startup dist-gap on benchmarks/todomvc/dist, compiled mid-run — NOT a regression).
- Full `bun run test`: 25068 pass / 213 skip / 1 todo / 2 fail-then-0 (the 2 = todomvc dist-gap; 0 on re-run).
- giti-032 unit coupled-update reviewed: STRENGTHENED (asserts lowered DOM), not weakened.

**NEW RESIDUAL to file at re-integration (agent-surfaced, out of ss17 scope):**
`g-emit-lift-markup-text-interp-not-lowered` — emit-lift's emitCreateElementFromMarkup renders a markup TEXT child's `${...}` LITERALLY for ALL callers (match/engine arm markup-value path too, not just each). Item 2 works around it INSIDE the each machinery (splits markup-text into interpolation children before emitMarkupValueExpr); the shared emit-lift gap remains for match/engine arms. Candidate follow-on if markup-value interpolation matters outside `<each>`. (Also noted: each per-item factory mounts ONE root node per item — multiple sibling roots in an each body silently drop all but the first; pre-existing, not in scope.)

### Status — ALL 3 LANDED on spa/ss17
- [landed-on-branch] item 1 — g-expr-event-handler-dead-in-each (agent 920fb3f7)
- [landed-on-branch] item 2 — g-each-peritem-markup-value-ternary (agent a336f6f9)
- [landed-on-branch] item 3 — g-nested-each-outer-key-reuse-inner-frozen (agent 18f4c1a2)
