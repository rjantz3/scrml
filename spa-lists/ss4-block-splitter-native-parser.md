# sPA ss4 — block-splitter-native-parser

**Launch:** `read spa.md ss4` · **Branch:** `spa/ss4` · **Worktree:** `../scrml-spa-ss4`
**Merged from:** block-splitter-statechild-scanners · native-parser-frontend-conformance

## Shared ingestion
The structural front-end. `block-splitter.js` scanners + the two sibling state-child parsers
(`match-statechild-parser.ts`, `engine-statechild-parser.ts`): comment/string-span opacity
(`computeCommentRegions`/`skipHtmlComment`), the E-STRUCTURAL-ELEMENT-MISPLACED engine-never-reaches-
parser gate, the `legacyColonPlacement:true` match path that works e2e (why match works but engine
doesn't), and §4.14/§18.0.1/§51.0.I placement rules. PLUS the Charter-B native parser
(`compiler/native-parser/`) + the dual-pipeline conformance harness: M1-M6 ladder,
native-as-canonical-enforcer, byte-identical-vs-Acorn gate, GAP-*/DIFF-* ledger.

## Core files
`compiler/src/block-splitter.js` · `compiler/src/match-statechild-parser.ts` · `compiler/src/engine-statechild-parser.ts` · `compiler/native-parser/parse-file.js` · `compiler/tests/parser-conformance/dual-pipeline-canary.js` · `docs/changes/native-parser-front-end/IMPLEMENTATION-ROADMAP.md`

## Items (least-ingestion-first)
1. **`bug-75`** `[dropped — RESOLVED pre-list, verified NOT-REPRODUCED S209]` bug LOW · tier med — after-`>` ENGINE `:`-shorthand hard-fails e2e at block-splitter (E-STRUCTURAL-ELEMENT-MISPLACED); the deprecated form should compile-with-warning (`W-COLON-SHORTHAND-LEGACY-PLACEMENT`) — match locus already works (`legacyColonPlacement:true`). Entry: block-splitter.js ~175-180. **VERIFIED RESOLVED:** S208 `tryConsumeAfterCloseColonShorthand` (block-splitter.js:1320, sites 2948/3254) + symbol-table.ts emit; e2e repro compiles EXIT 0 w/ info-warning, no hard-fail.
2. **`g-blocksplitter-comment-span-not-opaque`** `[landed-on-branch]` bug LOW · tier med — BS + arm/state-child scanners don't treat `<!-- -->` as opaque (tag-mentions/odd-quotes inside comments skew balance). Uniformly treat comments opaque, extending `computeCommentRegions`. Entry: match-arm scanner (E-MATCH-PARSE-001 :580) + engine state-child scanner (:1316/:1504). **FIXED (engine locus only; match + BS NOT affected — R4 catch):** two loci in engine-statechild-parser.ts — (1) `skipCommentOrString` missing `<!-- -->` branch; (2) `parseEngineStateChildren` stepping into a comment beginning at `lt`. New test engine-statechild-comment-opacity.test.js (7 cases). 201 engine-suite pass.
3. **`native-parser-corpus-gap-ledger`** `[dropped — VERIFIED healthy by-design S209]` bug LOW · tier med — unexplained native-vs-live FileAST divergences opt-in-skipped as GAP-LEDGER entries; skip→strict flips automatically as a class closes. Entry: `parser-conformance-corpus.test.js:275` + dual-pipeline-canary.js + parse-file.js/parse-stmt.js. **VERIFIED:** `classifyDivergence` re-partitions every run (no stale skips by construction). Current: 991/1008 strict-pass (98.3%), 17 gap-ledger skips, 0 fail. Closing the 17 residual gap classes = item-6 native-parser work; no isolated sPA fix.
4. **`native-parser-byte-identical-lexer-gap`** `[landed-on-branch]` experiment LOW · tier med — byte-identical native-vs-Acorn token-stream parity skipped pending M1.3(comments)/M1.4(regex) + template-token-shape normalizer. Entry: `parser-conformance-lexer.test.js:581` + native-parser/lex.js + token.js. **PARTIALLY CLOSED:** empirical probe — 5 of 8 `M1.2-*` bench files now PASS the byte-identical gate (M1.3/M1.5 normalizers landed); FLIPPED to `full` (decl-destructure, expr-async-await, expr-yield-generator, stmt-import-export, stmt-try-catch). 3 genuine residuals stay skipped (decl-class, expr-optional-chain, expr-template-literal) → item-6 native-lexer work. Lexer test 113 pass / 0 fail.
5. **`phase-a2-structural-elements`** `[verified — PA currency edit queued]` feature n-a · tier high — STALE legacy S58 row; substance shipped (A1c waves + A7). Currency-correct then close; live front-end is native-parser Charter B. Entry: master-list.md §0.1 row A2 + roadmap. **VERIFIED SHIPPED:** all 5 elements compile (`<engine>`+A7 · `<match>` block · `<channel>` · `<errors>` [17 test files] · `<onTransition>` [33 test files]); conformance corpus 991/1008 strict. **NOT edited on-branch** — master-list.md is PA-owned durable state (stale-base clobber hazard); exact row-A2 replacement handed to PA in re-integration message + progress.md.
6. **`native-parser-front-end-m2-m6`** `[PARKED — escalate to PA: design-gated arc, footprint unbounded]` feature n-a · tier high — **BIG multi-milestone arc** (M2.4 + MK2 + M3-M6 incl. block-splitter deletion + Acorn removal). Dominant unit: each/match/colon-shorthand structural-promotion ~70% (M6.6). Dispatch per-milestone, not as one. Entry: IMPLEMENTATION-ROADMAP.md (top banner L22-L35) + native-parser/*. **PARK REASON (S209 ss4):** (1) the actual Phase-A default-flip is a **STANDING USER DECISION (~v0.8 target)** per the roadmap banner — design-gated, not sPA-rulable; (2) the footprint (`native-parser/*` = the whole parser) is unbounded for one sPA session — exceeds ss4's shared ingestion (contract: park + flag PA). Current state (roadmap S170): native parser BUILT (M1-M4 + MK1-MK4); flip-failures 1,150 (S161) → ~508 (S170 W2), 0 true regressions; #2f each/match/colon-shorthand DONE (S162). Remaining flip-failure buckets: MISSING-FIELD emit-shape ~296 (dominant) · engine-statechild ~116 · FIELD-SHAPE-other ~21 · each-match residual ~11 · legacy-stage-probe ~14-18. Each bucket = a substantial per-milestone native-parser dispatch the PA should sequence (not an sPA bulk-run). Authority chain: `scrml-support/docs/deep-dives/scrml-native-parser-front-end-charter-2026-05-20.md` + `m6-joint-retirement-cutover-plan-2026-05-23.md`.
7. **`derived-value-compound-mutate`** `[(a) landed-on-branch · (b) dispatched]` bug MED · tier med — **re-clustered from ss6 (S209 flag A — mis-ingested into type-system).** The diagnostic walker is CORRECT (`derived-mutation-ops.ts`, 12 ops pass); the blockers are FRONT-END: (a) the tokenizer splits compound-assign ops `<<=` / `>>=` / `>>>=` at markup `<`/`>` boundaries inside `${…}`; (b) no parser support for an in-compound `const <derived>` + multi-segment receivers. Entry: `tokenizer.ts` (compound-op lexing inside `${}`) + block-splitter/parser (in-compound derived-decl). Both are ss4 surface, NOT type-system. **BLOCKER (a) FIXED:** TWO loci — (1) tokenizer.ts MULTI_OPS gained `<<=`/`>>=`/`>>>=` (was lexing `<<`+`=`→`<< =`); (2) ast-builder.js COMPOUND_OPS completed to the full 15-op set (the prior 7-op set merged + SILENTLY DROPPED consecutive `@x <op>= n` for the other 10 ops — a silent-data-loss class beyond the named shift scope, same root). §B8.2b un-skipped (3 shift tests now active, 15 ops total). **BLOCKER (b) LANDED:** compound `const`-derived child registration (§B8.3/§B8.6, §6.6.16). Fix in `ast-builder.js` parseLogicBody compound child-loop — a `const`+`<` opener recurses `tryParseStructuralDecl(constTok, true, {inCompoundBody:true})` → child state-decl `isConst:true`/`shape:"derived"` (walker UNCHANGED — it already descends compound scope). §S11A.8 invariant (no const PARENT) preserved. §B8.3 (4) + §B8.6 (1) un-skipped + pass. Dispatched to scrml-js-codegen-engineer (agent 65a52043, merged spa/ss4 so blocker (a) in history); file-delta landed (PA-wrap didn't touch the files; agent diff = blocker-b-only). R26-verified on spa/ss4: §B8.3 repro fires E-DERIVED-VALUE-MUTATE; derived-value-mutate+parse-shapes 161 pass/0 fail.

## Progress
`ss4.progress.md`. Land on `spa/ss4`; ping PA inbox when ready. Do not advance main / do not push.
