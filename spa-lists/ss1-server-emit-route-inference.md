# sPA ss1 — server-emit-route-inference

**Launch:** `read spa.md ss1` · **Branch:** `spa/ss1` · **Worktree:** `../scrml-spa-ss1`

**Fill:** ~60% · `healthy` (REFRESHED S215 — +3 server-codegen items from the dPA batch: §52 server-cell-load, dpa-002 `route=`-for-`server function*`, targeted E-RI-002)

## Shared ingestion
Server emit + route inference + selfhost server-emit triangle: `route-inference.ts` (Step 5b/5d
`__ri_route_` inference + the E-RI-002 fire site 3534-3542), `emit-server.ts`, `codegen/index.ts`, `api.js`,
`route-splitter.ts`. The const-only-module no-server-emit dangling-import bug and the route-001 over-fire are
both reachable from the emit-server/route-inference understanding (same triangle as the RESOLVED
`g-route-mis-inference` + `g-pure-module-server-emit-missing` siblings). **S215 additions ride the SAME
triangle:** the §52 `<var server> = ?{}.get()` server-cell-LOAD codegen (the `?{}`-placeholder leak; emit-server
+ reactive-wiring), the `route=` author-path for `server function*` (the explicitRoute/explicitMethod plumbing
already in emit-server L1099/L1205), and the E-RI-002 diagnostic text (one site, route-inference.ts:3534-3542).

## Core files
`compiler/src/route-inference.ts` · `compiler/src/codegen/emit-server.ts` · `compiler/src/codegen/index.ts` · `compiler/src/api.js` · `compiler/src/codegen/emit-engine.ts` (§52-load hydration hook) · `compiler/src/codegen/emit-reactive-wiring.ts`

## Items (least-ingestion-first)
1. **`g-route-001-local-computed-write`** `[status=open]` LOW · tier med — `E-ROUTE-001` over-fires on a pure-fn LOCAL computed-index array write. A pure fn doing `result[idx]=result[idx]+1` on a freshly `slice()`'d LOCAL array emits warning `E-ROUTE-001` (computed member access cannot statically determine property); route inference flags ALL computed member-assigns, not just ones reachable to protected DB/wire fields. Benign (warning only) but erodes diagnostic surface. Flux dog-food S193. route-inference.ts. status=open.
   > **Brief seed:** Scope `E-ROUTE-001` to computed writes whose receiver can reach a protected/route-relevant binding, excluding pure-fn-local arrays (a freshly `slice()`'d local can't reach a protected field). Grep actual fire sites first; R26 the false-positive.
2. **`g-const-only-module-no-server-emit`** `[status=open]` MED · tier med — const-only module (no server content) emits no `.server.js` → server-used const import dangles at runtime. A module whose exports are CONSTANTS/pure-types only emits NO `.server.js`; a server-side consumer still emits `import {CONST} from './mod.server.js'` → file doesn't exist → 'Cannot find module' at runtime. WARNED (`W-SERVER-IMPORT-UNEMITTED` MISSING-FILE branch) not silent. ss1 (795704c1) closed only the MISSING-EXPORT branch. Sibling of the RESOLVED route-mis-inference + pure-module-server-emit gaps (same emit-server/api.js triangle). Option-1 force-emit rejected (link-errors on erased TYPE imports). emit-server.ts + index.ts + route-inference.ts. status=open.
   > **Brief seed:** When a const-only module is server-imported by-name, EITHER emit a minimal value-only `.server.js` (value bindings, NOT erased types) OR tree-shake the consumer's server import to the used value names (drop the line when all dangle). Mirror the sibling RESOLVED pure-module fix; verify the `W-SERVER-IMPORT-UNEMITTED` warning drops to 0.
3. **`g-section52-server-cell-load-codegen`** `[status=open]` **HIGH · tier med** — the §52 `<var server> = ?{}.get()` server-cell INITIAL-LOAD leaks an UNLOWERED `?{}` SQL placeholder into the CLIENT bundle → `E-CODEGEN-INVALID-JS`. **dpa-005-VERIFIED (2026-06-23, df6f747b):** compiling the SPEC's own §51.0.E canonical example fails; the failing byte is `reactive_set("driver", await (?{ /* sql */ }.get()))`. Isolation test `<driver server> = ?{...}.get()` with NO engine reproduces it → the gap is the §52 server-cell LOAD codegen, NOT the engine wiring (engine-hydration `emitEngineVariantCellInit` emit-engine.ts:1578-1590 is BUILT and rides this). **Unblocks giti F1 (server-auth pattern) + dpa-005-B `<engine server=@source>` + flux G1 READ-path + the §51.0.E example.** files: emit-server.ts · codegen/index.ts · emit-reactive-wiring.ts · emit-engine.ts. specSections: §52 (state-authority — READ-authority + reactive-wiring; the `<var server>` load is a server fetch-on-mount, NOT a client `?{}`) · §51.0.E (engine `server=@source` hydration). priorArt: dpa-005 artifact `scrml-support/docs/deep-dives/server-authoritative-engine-2026-06-23.md` + `engine-hydration-from-persisted-state-2026-06-15.md`. **OUT OF SCOPE (do NOT pull in):** the §52 WRITE-BACK path (flux G1 write — `emit-sync.ts emitServerSyncStub` no-op; Tier-1 server-route gen unstarted) is a SEPARATE bigger arc; this item is the READ/LOAD half ONLY. R4: read §52 + §51.0.E before briefing — the load must lower to the server fetch-on-mount form, not a client-side `?{}`. status=open.
   > **Brief seed:** Lower the `<var server> = ?{}.get()` §52 server-cell initial-load so the `?{}` is emitted as a SERVER fetch-on-mount (the §52 read-authority + reactive-wiring path) instead of leaking the raw `?{}` placeholder into the client bundle. The engine-hydration subscription already rides the cell — fixing the cell-load fixes both the bare `<driver server>=?{}.get()` AND `<engine server=@source>`. R26: recompile the §51.0.E canonical example + the bare-cell isolation repro → `node --check` clean, no `?{ /* sql */ }` in the client bundle; full `bun run test`.
4. **`g-route-attr-for-server-generator-app-mode`** `[status=open]` MED · tier low — the `route=` author-path (explicit route + method) is plumbed parser→RI→emit-server for `server function` (the `explicitRoute`/`explicitMethod` fields, emit-server L1099/L1205) but NOT wired for `server function*` (SSE generators) in APPLICATION mode — the one genuine net-new dpa-002 (serve-side raw-route) needs. **dpa-002-VERIFIED:** codegen ALREADY plumbs `explicitRoute`/`explicitMethod`; `handle()` (§39.3/§40) already serves a raw Request→Response; the gap is only `route=` on a `server function*` in app mode. files: route-inference.ts · emit-server.ts. specSections: §37 (SSE `server function*`) · §40 (handle/route author-path). priorArt: dpa-002 artifact `scrml-support/docs/deep-dives/serve-side-raw-route-2026-06-23.md`. status=open.
   > **Brief seed:** Extend the existing `explicitRoute`/`explicitMethod` plumbing (works for `server function`) to `server function*` in application mode, so an SSE generator can declare its own route. Confirm the plumbing exists first (emit-server L1099/L1205); R26 a `server function*` with `route=` → emits at the declared path; full suite.
5. **`g-e-ri-002-targeted-diagnostic`** `[status=open]` LOW · tier low — E-RI-002 ("server fn assigns to a `@` reactive variable") fires one blunt message at `route-inference.ts:3534-3542`; ship a TARGETED message steering a server-fn-writing-an-engine-cell to the channel-synced-cell + derived-engine / `<engine server=@source>` pattern (dpa-005 Approach A/B). One site; one carve-out already exists at 3497-3532 (channel-scoped write → `broadcast()`). **⚠ DEPENDENCY: land item 3 (§52 load) FIRST/together** — the dpa-005 caveat is that the diagnostic must NOT steer to a form that still codegen-fails; `<engine server=@source>` only compiles once item 3 lands. files: route-inference.ts:3534-3542. specSections: §12.2 (RI invariant — the rule is CORRECT, this is message-only) · §38.4 (channel-sync) · §51.0.E. priorArt: dpa-005 artifact §"targeted E-RI-002 diagnostic". status=open.
   > **Brief seed:** Improve the E-RI-002 message at route-inference.ts:3534-3542 to name the canonical server-authoritative pattern (channel-synced cell + derived engine / `<engine server=@source>`). Message-only — do NOT relax the RI rule (it is correct per §12.2). SEQUENCE AFTER item 3. R26: E-RI-002 still fires on giti repro-24 with the improved text; full suite.

## Progress
`ss1.progress.md`. Land on `spa/ss1`; ping PA inbox when ready. Do not advance main / do not push.
