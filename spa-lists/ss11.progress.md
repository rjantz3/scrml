# ss11 — doc-currency-corpus · progress

Append-only. Landing SHAs on branch `spa/ss11`. PA re-integrates → main.

- **2026-06-19 (S208)** — sPA ss11 boot: contract + list + coreFiles read; worktree `../scrml-spa-ss11` provisioned on `spa/ss11` from `origin/main` @72dc4fdb; node_modules symlinked; pre-commit hook verified live (unset hooksPath → common .git/hooks).
- **2026-06-19** — **item 1 `bug-19-cite` LANDED** `b2f4d093`. R4-verified the §11 fold against SPEC (§11 @6968 "Content Folded" → §6.12 declarations + §52 authority). Rerouted 8 stale §11 citations → §52 across 4 dev.to articles (server-boundary-disappears, lsp-and-giti-advantages, orm-trap, npm-myth). Pre-commit gate green (17248 pass / 0 fail).
  - **R4/R5 FLAG to PA:** brief-seed says "5 dev.to articles" — genuine count is **4**. `components-are-states-devto` §11.3/§11.1 are **kickstarter-v2-internal** section refs (verified: kickstarter §11.1 "Engine recipe" L1181, §11.3 "Real-time recipe `<channel>`" L1337), NOT SPEC §11 — false positive from a raw `§11` grep. (That article is item 2's target for a different reason.)
  - **Out-of-scope currency noted (separate class, NOT fixed here):** `scrmlTS` repo-name + `/home/bryan/` path drift in the article bibliography lines (`/home/bryan/scrmlMaster/scrmlTS/compiler/SPEC.md`). Repo is `scrml`, home is `bryan-maclee`. Affects many lines across many docs — a distinct currency sweep, not bug-19-cite.
  - index.html mirrors: `docs/index.html` does NOT contain these articles (grep=0); no per-article HTML mirrors in-repo — nothing to regenerate.
- **2026-06-19** — **item 2 `g-s52-retraction` — HALF landed, HALF escalated.**
  - **R4 SPEC verification:** retraction is REAL + current. §52.1 L28887 + §52.6.2 L29244 ("auto-persist route is RETRACTED") + §52.6.3 L29288 (no auto-rollback; `_scrml_server_sync_<var>` stub + auto-rollback subscriber NOT emitted). Current model = read-authority only (initial-load-on-mount + SSR pre-render + E-AUTH + immediate-local-landing); persist/optimistic/rollback = developer's explicit `?{}` + `!{}`/`on error`. **Date is 2026-06-14** (server-state-persist-semantics DD, Q1=C/Q2=WF), not "S194" as the brief-seed labeled it — minor label drift, substance confirmed.
  - **LANDED — `docs/heads-up/spec-consolidation-2026-05-25.md`** (internal S129 decisions-record): added 2 additive supersession notes at L297 (server cell-modifier clarification) + A4 L332 (§52.4.1 enumeration). Record-preserving (did NOT rewrite the historical S129 text — falsifying a decisions-chain record is wrong; pointed forward to §52.6.2/§52.6.3 instead).
  - **LANDED — `components-are-states-devto-2026-04-29.md` + `docs/website/pages/articles/components-are-states.scrml`** (public voice article + live website page): reworked the §52 value-prop to the current read-authority model. **User-direction shift (S208):** initially chose "I scaffold, you reword," then ("i don't have time to hand work old articles. do you actually need me to rework the paragraphs?") delegated the reword to the sPA for this old/deprioritized article — tight currency-correction in the article's existing voice (declarative, first-person, no em-dashes per its own style note), user reviews not writes. Framing = "plumbing generated, verb yours" (the write is the one decision a compiler shouldn't fake; co-location axiom). Edits: body L139/162/175/185 + L14 status-banner correction note (user-approved dated shape) + L236/239 audit block rewrite (mine). Same applied to the .scrml (body + banner; no audit block there). React-baseline mentions preserved (accurate). .scrml recompiles clean. **index.html** = gitignored build artifact (regenerates), not hand-edited. **NOTE: the live dev.to post is still stale — only the user can edit it on dev.to; sPA can't reach it.**
- **2026-06-19** — **item 3 `r28-c2` — PARKED, ESCALATED to PA (NOT executed).** Brief-seed framed it as "despace-in-fences + backtick-wrap + print() residual" doc-currency cleanup. Reality is bigger:
  - **R26 empirical probe (CLI compile):** spaced `< db>` fires **W-WHITESPACE-001** today ("canonical form is no-space `<db>`; with-space form deprecated → E-WHITESPACE-001 in P3"); despaced `<db>` is clean (no W-WHITESPACE-001, no E-STATE-001 — routes to the db state block). So the brief's **despace DIRECTION is empirically correct** (no-space is canonical, per §15.15.5 + NR-authoritative §15.15.6, P3-FOLLOW 2026-05-02).
  - **BUT — SPEC self-contradiction (blocks execution):** SPEC §4 / §4.3 still teaches the OPPOSITE — L367 "the space after `<` signals a state block", L31031 "the leading space inside `< SubstateName>` is REQUIRED … `<SubstateName>` (no space) parses as an HTML element", L599 E-STATE-001 hint, EBNF L8990 `state-type-decl ::= '< ' identifier`. The live compiler contradicts §4 (empirically `<db>` does NOT parse as HTML). Which model is canonical (§4 space-required vs §15.15.5 no-space-canonical) is a **DESIGN RULING + SPEC amendment** — sPA must NOT decide or amend SPEC. Possible the compiler's W-WHITESPACE-001 is itself the bug vs intended §4 model; can't tell without the ruling.
  - **Blast radius corpus-wide:** SPEC.md = **194** spaced openers; examples/ = 27; docs/articles = 36 (kickstarter = 13). Despacing ONLY the kickstarter while SPEC §4 + the rest of the corpus stay spaced = new incoherence. Migration must be coherent + ruled-on (cf. [[feedback_no_batch_ratify_foundational_axioms]] + [[feedback_amendment_direction_and_target_explicit]]).
  - **Kickstarter internal inconsistency (real, but direction = the contested ruling):** mixes spaced `< schema>` (L1462/69/93/96) and despaced `<schema>` (L782/832/1084).
  - **`print()` residual: 0 hits** in the kickstarter (R4 — brief-seed claim moot; already fixed or never present).
  - **Disposition:** PARK; do NOT despace. PA decides canonical-form direction + SPEC §4 amendment, then a coherent corpus-wide migration (sPA can execute the kickstarter slice once direction is ruled).
- **2026-06-19 — USER RULING (S208):** *"the space is stale. fix."* → **no-space `<db>` is canonical; the spaced `< db>` form + the SPEC §4 text that teaches it are stale.** Item 3 unblocked.
  - **LANDED (kickstarter = ss11's scope):** despaced all 13 `< db>`/`< schema>` → `<db>`/`<schema>` in `docs/articles/llm-kickstarter-v2-2026-05-04.md`. Verified: 0 spaced openers remain; recompiled the L40 fence → no W-WHITESPACE-001. All prose sites were already backticked (display safe); no less-than false positives (`db`/`schema` can't be numeric RHS).
  - **ESCALATED to PA (beyond ss11 scope — normative SPEC + other surfaces):** (1) **SPEC §4/§4.3 amendment** — L367, L599, L27198, L31031 + EBNF L8990 `'< '` still teach space-required + no-space=HTML-element; must flip to no-space-canonical to match the ruling + the live compiler (removes the self-contradiction, ends the ouroboros). (2) **Corpus-wide despace migration** — SPEC.md 194 openers, examples/ 27, docs/articles/ 36 (other articles beyond the kickstarter). The W-WHITESPACE-001 message names planned `scrml-migrate` tooling — PA may prefer tooling over hand-migration at that scale. Ruling recorded; sPA can take additional bounded slices if the PA assigns them.
