# sPA ss16 — progress

*(append per item: `[order] item-name → scoped / dispatched <agent> / verified / landed <SHA> | parked <reason> | NOT-REPRODUCED`. The PA reads this at re-integration.)*

## Boot (2026-06-22)
- Worktree `../scrml-spa-ss16` created from `origin/main` @ `1ce8de34`; branch `spa/ss16`; node_modules symlinked. `git status` clean.
- Read coreFiles: `emit-expr.ts` (render hijack 1726 + log-shadow precedent 1747-1777), `type-system.ts` (checkLogShadowing 4022 + inferBareVariantsInExpr 11657 + E-TYPE-063), `log-loc.ts` (fileDeclaresLog/setLogShadowedInFile precedent), PongAI bug report.

## R26 reproduce-first — ALL THREE REPRODUCED on HEAD `1ce8de34` (repros in `.repro-ss16/`)
- **[1] pongai-c5-ctor-arg-contextual-typing → REPRODUCED.** `type Mode:enum = { OnePlayer(difficulty: Difficulty), TwoPlayer }; <mode>: Mode = .OnePlayer(.Easy)` → `E-TYPE-063: .Easy is not a declared variant of enum Mode`. **Root:** `inferBareVariantsInExpr` (type-system.ts:11657) is a FLAT `forEachIdentInExprNode` walk — it resolves EVERY bare-variant against ONE `contextType` (the LHS enum `Mode`), including the ctor ARG `.Easy`, which should type against the ctor param type `Difficulty`. The code comment at 11636-11637 explicitly DEFERS positions 3 (param) + 4 (return) to "B20.b" — C5 is exactly position-3.
- **[2] pongai-c4-eq-vs-payload-variant-ctor → REPRODUCED.** `<phase>: Phase = .Idle; <stuck> = @phase == Phase.Serving` (Serving = payload variant) → compiles CLEAN (only unrelated E-DG-002), emits `_scrml_structural_eq(_scrml_reactive_get("phase"), Phase.Serving)` where `Phase.Serving` is the ctor FUNCTION → always-false; NO equality lint. **Root:** `emit-expr.ts:1047` lowers `==` to `structural_eq(left,right)` (correct per §45.4); the MISSING piece is a type-system/lint surface that detects `==`/`!=` against a PAYLOAD-variant constructor and nudges to `is .Variant`/`match`.
- **[3] pongai-c3-render-builtin-shadowing → REPRODUCED.** `function render() {...}` called from `loop()` → def emits `function _scrml_render_1()`, call emits `_scrml_render()` (undefined) → ReferenceError; compiles clean. **Root:** `emit-expr.ts:1726` UNCONDITIONALLY hijacks `render(...)` → `_scrml_render(${args})`. emitIdent("render") would otherwise return `"render"`, which the fnNameMap post-pass (emit-client.ts:1849-1866, `\b(name)\b` word-boundary regex) rewrites → `_scrml_render_1` (matching the def). The hijack short-circuits this; `_scrml_render` has no word-boundary before `render` so the post-pass can't repair it. **Fix = mirror `log`-shadowing** (the §20.6.7 precedent): flag + setRenderShadowedInFile + fileDeclaresRender + guard the hijack + checkRenderShadowing(W-RENDER-SHADOWED) + §34 row.

## Dispatch plan
- ONE `scrml-js-codegen-engineer` (isolation:worktree, opus), all three items SEQUENTIALLY (shared ingestion; all 3 touch type-system.ts → parallel would collide). Incremental commit PER ITEM (crash-recovery). Order: C5 → C4 → C3 (list order; C3 is the largest, a new lint across 5 files).

## Dispatch (2026-06-22)
- **[1] C5 / [2] C4 / [3] C3 → dispatched** to `scrml-js-codegen-engineer` agent `a58f1b208605f9779` (isolation:worktree, opus, base `1ce8de34`). BRIEF archived `docs/changes/ss16-pongai-c3c4c5-2026-06-22/BRIEF.md`. All three in ONE dispatch (shared type-system.ts ingestion). Awaiting completion → R26 verify (recompile the 3 repros + full pre-commit gate + grep c3 def/call match) → file-delta to `spa/ss16` → commit (one commit; bundles this progress + the BRIEF + disposition) → ping PA inbox.
- Verify-on-land checklist: (C5) `.repro-ss16/c5.scrml` clean + `.OnePlayer(.Nope)` still errors against Difficulty; (C4) c4 emits W-EQ lint to result.warnings, unit-variant `==` + `is .Serving` clean; (C3) c3.client.js call == `_scrml_render_1()` matching def, `node --check` clean, no-user-render still hijacks to builtin.

## VERIFIED + LANDED (2026-06-22)
Agent `a58f1b208605f9779` returned 3 clean per-item commits on `worktree-agent-a58f1b208605f9779` (base `1ce8de34`, divergence `0 3`, base IS ancestor). Provenance SHAs: C5 `75bcf670` · C4 `0cf70072` · C3 `3f6fa0c8`. Files (9): SPEC.md, emit-expr.ts, codegen/index.ts, log-loc.ts, type-system.ts (+449), 3 new test files, agent progress.md.

**R26 independent recompile (sPA, against the file-delta'd working tree — NOT the agent narrative):**
- **[1] C5 → LANDED.** `.OnePlayer(.Easy)` compiles CLEAN; typo `.OnePlayer(.Nope)` → `E-TYPE-063 ... not a declared variant of enum Difficulty` (correctly the PAYLOAD enum, not Mode). Fix: `inferBareVariantsAtVariantCtorArgs` recognizes bare + qualified ctor callees, sources param types from `VariantDef.payload`, stamps the dedup flag. §14.10 pos-3.
- **[2] C4 → LANDED.** `@phase == Phase.Serving` (payload ctor) → fires `W-EQ-PAYLOAD-VARIANT` (warning → result.warnings) with `is`/`match` steer; NO false positive on unit-variant `==` (`Phase.Idle`) or `@phase is .Serving`. emit (`structural_eq`, §45.4) unchanged. §45.7/§45.8/§34.
- **[3] C3 → LANDED.** `c3.client.js` def `_scrml_render_1()` AND call `_scrml_render_1()` MATCH; `node --check` CLEAN; `W-RENDER-SHADOWED` (info) fires; no-user-render path still lowers to the `_scrml_render` builtin. Mirror of log-shadowing. §20.3a/§34.

Landed to `spa/ss16` via S67 file-delta as ONE sPA-authored commit (this commit). Pre-commit full gate (unit+integration+conformance) green. Agent self-reported 17588 pass / 0 fail / 68 skip (+14 new tests).

**Surfaced to PA (out-of-scope, NOT touched):** (a) bare `render()` with no user `function render` fires `E-SCOPE-001` in the typer (call-form `render` builtin not registered as a typer global, though codegen lowers it fine) — pre-existing diagnostic-coverage gap, orthogonal to C3. (b) C4's lint is homed in type-system.ts; sibling `E-EQ-001/003`/`W-EQ-001` live in `gauntlet-phase3-eq-checks.js` (map-key only) — if PA wants the equality checks consolidated, that's the alternative home (out of this dispatch's scope).
