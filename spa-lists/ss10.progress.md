# sPA ss10 — e2e-render-map-test-hygiene · progress

Branch `spa/ss10` (worktree `../scrml-spa-ss10`). Base origin/main `85d9e958` (S209 merge).
Append-only. Each line: item · disposition · SHA · note.

## S210 (2026-06-20)
- BOOT — worktree created from origin/main `85d9e958`; node_modules symlinked; coreFiles + enumerator read.
- item 1 `e2e-render-map-tier-rebaseline` — **LANDED `d77b58e5`**. Full corpus regen (438 cells / 196s). Bakes per-cell `tier` (was 0/438 → now 438/438: flagship 35 / probe 375 / sample 13 / stress 12 / perf 3). State diff vs 2026-06-17 baseline: **0 green→red regressions**; 2 improvements folded DOWN (`03-contact-book#populated` smell→clean, `match-pipe-alternation#empty` fails-compile→clean); 1 red→red shift (`gauntlet-r10-svelte-dashboard#empty` fails-compile→throws); 0 new/removed cells. Histogram clean 257→259, fails-compile 128→126, smell 2→1, throws 30→31.
- item 2 `examples-verified-md-human-seed` — **LANDED `9856a741`**. Synced the 5 missing rows 23-27 (the ledger's own "pending VERIFIED sync" gap); header 28→31; all unchecked (only the USER flips `[x]`).
- item 3 `test-examples-server-suppression-antipattern` — **LANDED `9856a741`**. Removed `_scrml_fetch_`/`SyntaxError` class-suppression (acceptance bug-2's codegen class); kept only the "Not found" network artifact. Disposition = **rely on render-map** (the 3 server examples mount server-less CLEAN per the regenerated baseline; render-map is the no-suppression classifier). RESIDUAL: `examples/dist` not built → puppeteer static-serve behavior of the 3 server examples under the narrowed filter is unverified here (not in the gated suite); render-map says clean.
- item 4 `todomvc-ac7-test-conflation` — **LANDED `9856a741`**. AC7 → `test.fixme` (records the edit-UI source gap; de-conflates found-a-gap vs test-broken; flip back to `test` when source renders `<input class="edit">`).
- NEXT: items 5+6 (render-map needs-server / server-class classification — coupled, tier med), 7 (gap-ingestion → flograph @gap, tier med), 8 (L2/L3 oracle fork, tier high — possible escalate).
- items 5+6 `g-rendermap-{needs-server,server}-classification` — **LANDED `c09af7f1`** (one fix, both facets). Added a `needs-server` non-gap cell-state: server-dependent app (emits serverJs OR `?{}` SQL) + null/undefined-access throw → needs-server; ReferenceError/TDZ stay red (codegen). Re-regen: EXACTLY 9 cells flipped throws→needs-server, 0 unexpected; 22 codegen throws stay red; tier preserved; e2e-render-map suite 7/7. Detection compiler-grounded (serverJs) — correctly excludes 22-multifile (multi-file-mount, not server-absence). DEFERRED→PA: mock-server SEEDING (S203 "b" realism path; not required for the non-gap goal). seed-fixtures.js untouched.
- item 7 `e2e-render-map-gap-ingestion` — **PARKED (escalate-to-PA).** Design mostly specified (DD §379-388: `@gap id=render-<app>-<smell>` token shape) BUT execution writes into `docs/known-gaps.md` (PA-owned board driving the session HIGH/MED/LOW counts) + `scripts/state.ts` (deputy/flograph checker) — blast radius beyond the harness shared-ingestion. Open placement decision: auto-inject into the board vs a separate render-gap registry; dedup vs hand-triaged gaps; needs-server must be EXCLUDED from gap-emission (non-gap). Deliberately DEFERRED at L1. Needs PA ruling on registry placement + board semantics.
- item 8 `e2e-render-map-L2-L3` — **PARKED (escalate-to-PA).** The DD itself names L2/L3 "the only place a real debate-fork lives" (§267-280). L2 (snapshot gated on VERIFIED.md provenance) carries the rubber-stamp design question; L3 (legacy-vs-native render differential) is ranked LAST/skip — it couples to the native parser which is FEATURE-stale (S162). tier high, post-MVP oracle-strategy fork → PA/dPA, not sPA.
- **END-STATE (S210): all 8 items dispositioned — 6 LANDED, 2 PARKED.** Branch `spa/ss10` tip `c09af7f1`, base `85d9e958`, 4 ahead / 0 behind origin/main (clean linear). Re-integration message sent to `scrml/handOffs/incoming/`. PA owns re-integration + durable bookkeeping. Instance closeable.
- OPERATIONAL NOTE (corroborates ss9's flag): the non-blocking post-commit hook runs the FULL browser suite; `browser-conditionals.test.js` fails in a fresh worktree because gitignored `samples/compilation-tests/dist/` isn't checked out by `git worktree add`. NOT a regression (blocking pre-commit excludes browser + passed clean). Same worktree-recipe gap ss9 flagged — recommend `spa-scrml.md` §Worktree symlink that dir alongside node_modules.
