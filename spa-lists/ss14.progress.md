# sPA ss14 — progress log (flograph-residuals-orphans)

Branch `spa/ss14` · worktree `../scrml-spa-ss14` · base `c734ec35` (origin/main).
Append-only. Land per-item; PA re-integrates.

---

## Boot (S209)
- Worktree created from origin/main @ c734ec35; node_modules symlinked; `git status` clean.
- Read: spa.md → spa-scrml.md (contract) → INDEX → ss14 list → core file `scripts/flograph.ts`.
- 6 items, largely independent (catch-all). Working top-to-bottom, least-ingestion-first.

## Item 1 — flogence-superseded-doc-deref-residual  [PARKED → PA ruling]
Disposition: **NO MOVE — recommend MARK-only (already satisfied).** Parked for PA ratification of the
partially-superseded-stays-live policy (1-line spec §2.1 clause).

Evidence:
- The 4 live partially-superseded docs: `living-compiler-recoverability-and-comp-time-shape-2026-04-26`,
  `living-compiler-mid-compile-config-swap-2026-04-26`, `living-compiler-bridge-architecture-2026-04-26`,
  `m5-m6-scope-revision-2026-05-21`. All carry `status: partially-superseded` + a resolvable live `superseded-by:`.
- Archive (`archive/deep-dives/`) holds 48 docs, ALL `status: superseded` (FULLY). ZERO partially-superseded
  in archive — the deref operation only ever moved fully-superseded docs. `partially-superseded` is a
  deliberately distinct status (spec §2.1 enum, line 36) meaning "retains current value, keep visible + flagged."
- These 4 are textually referenced by **9 / 5 / 5 / 1** OTHER live docs (20 sites). A physical move is NOT
  "doc-move only" — it relocates 20 live references into the opt-in `--with-archive` tier, hiding still-current
  content from default dev-agent navigation. The footprint's lean-MOVE brief-seed under-scopes the blast radius (R4).
- `flograph --with-support` currency sweep = **0** edges live→superseded: NO provenance-hygiene pressure forcing a move.
- Spec §2.1 (line 94) calls superseded-in-live-corpus a "deref-to-archive signal" but `SUPERSEDED` lumps both
  statuses and the spec never NORMATIVELY states the policy for *partially*-superseded → genuine gap.

Recommendation to PA: keep all 4 live (already correctly marked); add a 1-line §2.1 clause:
"deref-to-archive applies to FULLY `superseded` docs only; `partially-superseded` stays in the live dir
(retains current value) and is surfaced by the currency/sweep INFO, not relocated." No code/doc change landed.

## Item 2 — g-each-body-sigil-invariant-classifier  [landed-on-branch]
File: `compiler/tests/integration/expr-node-corpus-invariant.test.js` (test-hygiene only).
Reproduced (R26): 2 `@.name` body exprs (29-engine-vs-flags.scrml `<li>${@.name}</li>`) surface as
`parse-error` escape-hatch. Root: `expression-parser.ts scrmlAtPlugin` only consumes `@` + ident-start, so
`@.` (followed by `.`) → acorn ParseError. Symptom REAL but minor — escape rate 0.85%, gate (>50%) far from
tripping; the footprint's "tripping the gate" is a GROWTH concern, not current.
Fix (test-side whitelist, per footprint): added an `each-sigil` category in `classifyEscapeHatch` (before the
ParseError branch) for `nativeKind==="ParseError" && /@\s*\./` raws; added the key to escapeCounts +
grandCounts; EXCLUDED `each-sigil` from the >50% GATE (`gateEscapes = total − sigil`) while keeping it
visible in the catalog + per-file log. `@.` is valid scrml (§17.7.3), not a genuine escape hatch.
Verify: `bun test expr-node-corpus-invariant.test.js` → 69 pass / 0 fail; parse-error 2→0, each-sigil=2.
**PA FINDING (out of ss14 scope):** the ROOT is the expr-parser @-plugin lacking `@.` support — the new
ExprNode layer cannot structure the sigil (it escape-hatches). Phase-2 expr-parser work; touches
`expression-parser.ts` (ss3 codegen-expr-attr surface, not this list). The test fix stops the false-positive;
the underlying `@.` modeling gap remains open.

## Item 3 — dock-malformed-sweep-residual  [landed-on-branch (dock); remainder PARKED]
Footprint counts STALE (R4): claimed "dock 1 + flograph 7"; current HEAD:
- `dock --check`: 1 unverified INFO — the self-cite `#dock[ cites=agentic-code-provenance-dock-2026-06-17 ]`
  at `scripts/flograph.ts:391` (the `import.meta.main` guard that exists so dock.ts can import flograph's
  `build`/`defaultCorpus` — S205 thin-build refactor).
- `flograph --check` (DEFAULT corpus = scrml's durable docs): **0 unverified** already. Plus 2 dangling WARN
  (`decided-by: each-inline-component-architecture-2026-06-17` known-gaps:226/231 — DD lives in support, dangles
  in default; by-design non-gating) + 2 ERROR (stale `docs/graph/graph.json`+`.mmd`).
- `flograph --with-support`: 40 unverified (NOT 7) — all in the OPT-IN design corpus (insights + 183 deep-dives),
  explicitly "noise that drowns scrml's own graph" per flograph.ts:45.

LANDED (dock side, bounded + verifiable): marked the flograph.ts:391 self-cite `· verified` after reading the DD
(`agentic-code-provenance-dock-2026-06-17.md`, status:current, ADOPTED S202) — it prescribes "build the thin
version riding flograph," exactly what the import-guard serves. `dock --check` → PASS, 0 unverified.

PARKED (out of ss14 bounds — flag to PA):
- **flograph-40 unverified**: full design-corpus provenance curation (read each cited decision, confirm grounding,
  mark verified) — broad ingestion across 183 deep-dives + decision-history judgment = curator/PA territory, NOT
  mechanical sPA work; canonical (default-corpus) sweep is already 0. Mechanically stamping `verified` without real
  verification would falsify the grounding bit (R2/R3) — declined.
- **stale `docs/graph/` artifacts** (2 ERROR): `graph.json`+`.mmd` are UNTRACKED, NEVER committed, NOT gitignored,
  and `flograph --check` is wired into NO gate (no husky/CI). Local-only cruft from a Jun-17 `--emit`. Whether to
  TRACK + drift-gate them (commit + obligate re-emit on every corpus change) vs gitignore them is a PA tracking-policy
  call — not landed by the sPA.

## Item 4 — g-block-analysis-phantom-block (D6)  [landed-on-branch]
File: `compiler/src/block-analysis.ts` + `compiler/tests/integration/emit-block-analysis-integration.test.js`.
Root-caused empirically (R26, full pipeline — the phantom only exists post-inlining, NOT in raw buildAST):
a CHANNEL import (`import { "driver-events" as … } from "…/channels/driver-events.scrml"`) inlines the channel's
`function publishDriverEvent` into the importing page's AST so the page can CALL it (line 163). The inlined node
carries `span.file = driver-events.scrml`. `collectFunctionDecls` walked the post-inlining tree and counted it as a
LOCAL block → phantom (span line 27 / char 1203-1590 = driver-events.scrml coords, indexed against messages.scrml's
source) overlapping the real `getCurrentUser` → block-lease two-holders. (Instrumented collectFunctionDecls to
confirm span.file; reverted.)
Fix: `collectBlocks` computes `ownerFile = relativeFilePath(fileAST.filePath)`; `collectFunctionDecls` skips
function-decls whose `span.file` (normalized) ≠ ownerFile (absent span.file → kept, prior behavior preserved).
Added `file?` to `SpanShape`. Inlining itself is correct (codegen needs it) — block-analysis is the consumer that
must report only LOCAL decls.
Verify: messages.scrml block-analysis 12→11, no `publishDriverEvent`; `dock --units` (the block-lease consumer)
→ 11 leasable blocks, phantom gone; driver-events.scrml's OWN sidecar still declares it. New real-pipeline regression
test `(f)` + unit/integration block-analysis suites: 13 + 28 pass, 0 fail.
**PA FINDING (separate bug, OUT of ss14 scope — ast-builder/parser, not block-analysis):** every LOCAL function
block's byte `end`/`endLine` OVERSHOOTS into the next function (e.g. getCurrentUser `}` at line 38 but end=1721 =
line 40, inside `function fetchMessages`). All 11 adjacent pairs share a boundary line (`endLine_N == line_{N+1}`).
The hand-off-212 claim "the other 11 blocks correct" is WRONG on the end-overshoot. Root is function-decl span
computation in the parser/ast-builder (upstream of block-analysis); fixing it is a different subsystem + blast radius
beyond ss14. Filed for PA (a smaller second two-holders source; `.find`-returns-first currently masks it in dock).

## Item 5 — bug-14 (MCP V0.D deferred runtime)  [PARKED — blocked on §58 + out of bounds]
known-gaps:1234 (`@gap id=bug-14 sev=MED status=open`). V0.A-E shipped S125-S131; the 3 V0.D RUNTIME items:
(1) runtime-helper registration on `globalThis` (boot reads `globalThis._scrml_reactive_get`, never set — runtime is
module-scoped per generated `.server.js`); (2) `scrml dev` in-process `Bun.serve` MCP wiring (boot lives only in
build-time `_server.js`); (3) dev-only NODE_ENV gate.
GATE VERIFIED (R4): §58 Build Story is **SPEC'd (§58.1-58.9, E-STORY-UNKNOWN/W-STORY-ON-TOP-LEVEL) but NOT
implemented** — 0 impl symbols (`buildStory`/`parseStory`/`storyTable`/`E-STORY-*`) in `compiler/src`. Bug 14 item (3)
states verbatim "no §58 Build Story hook exists yet; revisit when §58 implementation lands." Gate holds.
PARK rationale: item (3) is HARD-gated on un-landed §58; items (1)+(2) are substantial runtime/dev-server codegen
feature builds (the generated boot path + in-process Bun.serve), whose blast radius far exceeds ss14's
flograph/dock/test/block-analysis surface — a deferred FEATURE arc mis-clustered into the "actionable orphans" catch-all,
NOT a bounded sPA fix. Documented workaround exists (`<program mcp="always">` + `scrml build`, not `scrml dev`).
No change landed. PA: revisit Bug 14 when §58 implementation lands; consider re-bucketing it to a design/feature track.
