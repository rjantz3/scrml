# ss8 — promotion-tailwind — progress

Branch `spa/ss8` · worktree `../scrml-spa-ss8` · base `cf950bab` (origin/main S210).

Append-only. Timestamped-by-step lines: what was done, what's next, blockers.

- BOOT — sPA booted on ss8 (promotion-tailwind, n=2, at-ceiling). Read spa-scrml.md + INDEX + ss8 list. Worktree `../scrml-spa-ss8` created from origin/main @ cf950bab; node_modules + compiler/node_modules + dist symlinked from main; dist symlink added to common `info/exclude` (gitignore `dist/` trailing-slash misses the symlink). git status clean.
- BOOT — core-files mapped. `collect-class-names.ts` is under `codegen/` (footprint path was stale). tailwind-classes.js structural map taken. P2-gradient/P3-transform/P4-filter composing families CONFIRMED LANDED (registerGradient/registerTransform/registerFilters/registerBackdrop all present) → matches "P1-P4 LANDED S191"; not re-opening them.
- bug-1 SCOPE — reproduced all 3 open sub-arcs (R26, /tmp/repro-bug1.mjs against live engine):
  - (1) string-shaped: `content-['hello']`→E-TAILWIND-001; `font-[Inter]`→null(silent drop, `font` not in prefix map); `font-['Helvetica_Neue']`→E-TAILWIND-001 (underscore list-split breaks the quoted token); `content-[attr(...)]`→E-TAILWIND-001 (attr not whitelisted). CONFIRMED GAP.
  - (3) ring-offset: `ring-offset-[2px]`/`[#ff0000]`/`[3px]` all → null silent drop (not in any arbitrary map). CONFIRMED GAP. (Named `ring-offset-{w}` shipped P1; ring shadow already references `--tw-ring-offset-width`.)
  - (2) safelist/@apply: only a deferred-fix COMMENT at tailwind-classes.js:2875; SPEC §26.5 (line 16147) explicitly "remains deferred." NO ruled direction (safelist config vs @apply vs `#{}`-scan suppression). → PARK / escalate to PA (design item).
- bug-1 R4 — §26.4 (SPEC 16089-16101) NORMATIVELY enumerates supported arbitrary prefixes; §26.4.1 (16120) enumerates allowed CSS functions. Sub-arcs 1&3 are mechanical extensions of the ALREADY-RULED Bug-1 arc (approach C, S191; remainder explicitly listed in known-gaps 1193-1198 as "to build"). SPEC §26.4/§26.7 change = CURRENCY NOTE (like the S109 grid/flex extension), NOT a design ruling → buildable; sPA proposes the §26.4/§26.7 + known-gaps note text for the PA at re-integration (SPEC single-writer = PA). Pipeline contained to tailwind-classes.js (collect-class-names + emit-css are pure pass-through, no prefix allowlist).
- REBASE — main advanced cf950bab→a3b08cbb mid-boot (PA: A2 W2 `<api>` parser + deputy ticks 134/134b). Diff touches ast-builder.js/SPEC/api-tests/deputy-bookkeeping — NONE of my files (tailwind-classes.js + bug-1 tailwind tests UNCHANGED). FF'd spa/ss8 cf950bab→a3b08cbb for base coherence with the agent worktree. Brief base SHA updated.
- DISPATCH — ONE scrml-js-codegen-engineer (isolation:worktree, opus, bg) for sub-arcs 1+3 (same file `tailwind-classes.js`, ingestion-disjoint within file, no contention); change-id `tailwind-arbitrary-string-ringoffset-2026-06-20`; base a3b08cbb. Agent `aa1ba07f7987579fa`.
- bug-20 SCOPE → **PARKED / ESCALATE TO PA** (design + SPEC §34/§28 amendments; LOW pri; the `--engine` stub safely exits code 2 today, no breakage). Findings (R4 cross-checked against SPEC §56.6 + §3 + source greps):
  - The `--engine` REWRITE itself is mostly specced + mechanical: §56.6 (input `<match for=Phase on=@phase>` w/ rule= arms → output `<engine for=Phase initial=.X>`) + §3 line 161 ("promotion is mechanical and additive; state-children carry forward verbatim; the wrapper swap is the commitment moment"). Same span-rewrite shape as shipped `--match`/`--each` (promote.js). `initial=` inference = first arm's variant (specced). BUILDABLE.
  - **BLOCKER (design): `W-MATCH-TRANSITIONS-ACCRUING` is undefined.** Exists ONLY as a name (SPEC §56.6 + promote.js:1542 stub) — NO §34 row, NO §28 config, NO fire-conditions, NO impl. Unlike the fully-specced sibling `I-FN-PROMOTABLE` (§56.9.1/.2/.3), its fire-conditions are unwritten = a design decision.
  - **CRITICAL OVERLAP:** `W-MATCH-RULE-INERT` (§18.0.2, §34 line 17111, `lint.match-rule-inert` config line 16408) is ALREADY SHIPPED (5 source files) and ALREADY fires on `rule=` on a `<match>` arm AND ALREADY recommends "promote to `<engine>` (Tier 2)". So `W-MATCH-TRANSITIONS-ACCRUING` may be REDUNDANT. PA/user must rule: (a) is the new lint needed at all vs W-MATCH-RULE-INERT already surfacing the opportunity? (b) if distinct, what differentiates it (threshold ≥N accruing rules? non-double-fire with W-MATCH-RULE-INERT?).
  - REUSE: `W-ENGINE-INITIAL-MISSING` is ALREADY SHIPPED (symbol-table.ts:6361 + emit-engine.ts) — the rewrite's default-initial path reuses it; NO new initial-missing diagnostic needed (§56.6's "may emit W-ENGINE-INITIAL-MISSING" is already-existing behavior).
  - RECOMMENDATION to PA: rule the lint question first (likely: W-MATCH-RULE-INERT suffices → drop the new lint, OR define a distinct accruing-threshold). THEN the `--engine` rewrite + (optional) lint + §34/§28 rows = one dispatch. The four-pieces-together coupling in the stub may collapse to 2 (rewrite + reuse existing lints) if the new lint is dropped.
- bug-1 AGENT DONE — agent `aa1ba07f` src SHA `2ed6cf42` (branch worktree-agent-aa1ba07f...). Full suite 24760/0 (incl browser); R26 byte-exact. Agent NOTE 1 (CSS-correct judgment call): `content-['a"b']` ACCEPTED (different-quote inside `'` is valid CSS), only SAME-quote-embedded rejected — followed the brief RULE over the example table; sPA AGREES (kept). Agent NOTE 2: inverted 4 coupled stale "STILL-fires" assertions in sibling test files (unrecognized-class §2, transform-shorthand §12, minor-families §10) — legit consequences of the recognition change; gate caught 2 (working as designed).
- bug-1 VERIFY — main moved a3b08cbb→6eb31fb2 (PA ss2 re-integration + deputy ticks) but touched NONE of my 6 files → file-delta clean (not cherry-pick). sPA-independent R26 on LANDED ss8 source: emit probe byte-exact (incl. NOTE-1 cases) + 230/230 across all 5 tailwind test files.
- bug-1 LAND — file-delta'd 6 src/test files + agent progress.md into spa/ss8; list-file status marked landed(1+3)/parked(2); committing (pre-commit gate = full unit+integration+conformance).
- **LIST DISPOSITIONED:** bug-1 sub-arcs 1+3 = landed-on-branch; bug-1 sub-arc 2 = parked→PA; bug-20 = parked→PA. ss8 done. → re-integration message to PA inbox (branch tip SHA + parked items + OWED SPEC/known-gaps currency-note proposal).
